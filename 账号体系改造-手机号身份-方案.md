# 账号体系改造方案 — 手机号身份 / 流程打通 / 自助管理 / 安全加固

> 状态:**待审**。本方案只描述设计与影响面,等你拍板再动代码。
> 背景一句话:把登录注册从「邮箱 / 用户名」切到「手机号」为唯一身份;同时补上目前线上根本跑不通的发信、忘记密码;加上登录后改密 / 注销;做必要的安全加固。
> 前提:按你的习惯**不做老数据迁移 / 兼容**,旧库可直接重建或清掉。

---

## 1. 新的身份模型(核心)

- **唯一身份 = 手机号。** 一个手机号一个账号。
- **注册**只填三样:手机号 + 密码 + 昵称 → 收一次短信验证码 → 完成即**自动登录**(不再让用户重输账号密码)。
- **登录两种方式:**
  1. 手机号 + 短信验证码
  2. 手机号 + 密码
- **砍掉**:邮箱、用户名作为登录 / 注册字段(后端列可保留但不再写入,见决策点 A)。

**Schema 变更(SQLite,`lib/db.ts`):**

| 表 | 改动 |
|---|---|
| `users` | 新增 `phone TEXT UNIQUE NOT NULL`;`display_name` 继续存昵称;`username` / `email` 改为可空、不再强制 |
| `otp_codes` | `email` 列改为 `phone`(语义对齐);`purpose` 枚举加 `login` |
| `UserRow` / `userToPublic` | 返回字段把 `username/email` 换成 `phone`(+ `displayName`) |

---

## 2. 注册流程(改造后)

1. 前端表单:手机号、密码、昵称、验证码 + 「获取验证码」按钮(去掉邮箱、用户名两个输入)。
2. `POST /api/auth/register/send-code`:校手机号格式 → 查重(已注册早返回)→ 限流 → 发短信验证码。
3. `POST /api/auth/register/verify`:校验码 → `createUser({ phone, password, displayName })` → 签 JWT 返回 → 前端存 token 跳工作台。
4. **自动登录**:现状 verify 成功就已经签发 token 并跳转,本来就是自动登录,这块保留不变。

---

## 3. 登录流程(两种)

- **手机号 + 密码**:沿用现有 `POST /api/auth/login`,把入参 `username` 换成 `phone`,内部按手机号查库。bcrypt 恒定耗时防枚举的逻辑保留。
- **手机号 + 验证码**:新增 `POST /api/auth/login/otp`(或合并进 login 路由按 mode 区分)。发码复用短信通道,`purpose = 'login'`。
  - **未注册手机号怎么办** → 见决策点 B,推荐「提示去注册」(因为注册还要设密码 + 昵称,验证码登录里补不齐)。
- 前端登录区:加一个「密码 / 验证码」切换(tab 或小链接)。

---

## 4. 打通基础流程(当前两个硬缺口)

**4.1 真实短信发送 🔴**
现状 `lib/otp.ts` 的 `sendEmail()` 是 TODO,线上发不出码。改造:抽成 `sendSms(phone, code, purpose)`,接真实短信服务商(决策点 C)。注意短信需要**签名 + 模板报备**,有审核周期(通常 1–3 个工作日),建议尽早申请。密钥走 `.env.local` / 外部 env,不硬编码。

**4.2 忘记 / 重置密码 🔴**
现状是假按钮(点了弹「即将开放」)。后端 OTP 框架已支持 `reset_password` 类型,只差接口:
- `POST /api/auth/reset/send-code`:手机号 → 发码(`purpose=reset_password`)。
- `POST /api/auth/reset/verify`:手机号 + 验证码 + 新密码 → 改库 + 让旧 token 失效。
- 前端把假按钮换成真表单。

---

## 5. 账户自助管理(登录后,目前完全没有)

一期最小集:
- **改密码**:需「旧密码」或「短信验证码」二选一验证 → 写新哈希。
- **改昵称**:直接改 `display_name`。

二级(单列,可二期):
- **改手机号**:旧号 + 新号都要验证码,流程较重。
- **注销账号**:软删(置 `disabled_at`)或硬删,按产品定。
- **登出真失效**:现状登出只清前端 localStorage,服务端 token 到期前仍有效。建议登出时走 `token_revoked_at`,让当前 token 立刻失效(粒度是「全端下线」,够用)。

---

## 6. 安全加固

- **短信防刷(最重要)🟡**:手机号体系最怕被刷短信 / 短信轰炸。复用现有 OTP 限流(同号冷却 + 单 IP 窗口),再加**全局频控**;强烈建议注册 / 验证码登录环节加**图形或滑块验证码**挡机器人(决策点 D)。
- **防账号枚举**:发码 / 登录时「该手机号已注册 / 未注册」的提示要权衡——提示清楚=好用但泄漏号码是否注册。建议登录失败统一文案,注册查重可适度提示。
- **token 有效期**:现状 30 天 JWT 存 localStorage(被 XSS 能偷)。建议缩短有效期。改成 httpOnly cookie 更安全,但会牵动前端所有 Bearer 调用,**blast radius 大,单列为二期待批大改**。
- **/workspace 服务端拦截**:现状只前端跳转(数据接口都有 Bearer 拦,风险有限)。可选小项。

---

## 7. 改动清单(扣真实代码,估影响面)

**后端**
- `lib/db.ts`:`users` 加 `phone`;`otp_codes` `email→phone`;`UserRow` / `userToPublic` 调整。
- `lib/auth.ts`:JWT payload `username→phone`、`createUser` 入参、`findUserByLogin → findUserByPhone`。
- `lib/otp.ts`:`email→phone`、加 `login` purpose、`sendEmail → sendSms` 接真实通道。
- `app/api/auth/*`:`login` / `register/send-code` / `register/verify` 重写入参;新增 `login/otp`、`reset/*`、`account/change-password`、`account/profile`、`logout`。
- `lib/llm.ts:1338`:`usernameSnapshot` 的 `user.email` fallback 改 `user.phone`(唯一一处 auth 外引用)。

**前端**
- `public/index.html`:注册 / 登录表单重做(去邮箱 / 用户名,加手机号 + 验证码登录切换 + 忘记密码真接口)。
- `public/modules/utils.js`:token 读写基本不变。
- 改完前端记得 bump `workspace.html` 的 `?v=`(避免浏览器跑旧缓存)。

**基本不受影响**:其余业务表都 FK 到 `users(id)`,id 不变,不动;`sessions` 表本来就是死代码,顺手删。

---

## 8. 决策点(需要你拍)

| # | 决策 | 是什么 / 影响 | 建议 |
|---|---|---|---|
| A | 旧 `username`/`email` 列 | 保留列(不写不展示)改动最小;彻底删列更干净但要改更多类型 | **保留列、phone 为准**(最小影响面) |
| B | 验证码登录遇到未注册号 | 提示去注册 vs 自动注册(但缺密码 + 昵称) | **提示去注册** |
| C | 短信服务商 | ~~阿里云 / 腾讯云 / 火山~~ | **已定:腾讯云短信**(Vasily 已有,签名+模板已审核通过)。接入=云 API 3.0 `SendSms`(2021-01-11)。需备:① 云 API 密钥 **SecretId + SecretKey**(访问管理 cam/capi,成对;**不是 App Key**——App Key 仅老版 2.0 接口用,新版不需要)② **SmsSdkAppId**(=SDKAppID)③ 签名内容(填字符串本身,非签名ID)④ 模板ID。注:SmsSdkAppId 须与密钥同一腾讯云账号 |
| D | 图形 / 滑块验证码 | 防短信轰炸,加在注册 / 验证码登录前 | **建议上**,至少一期就接 |
| E | token 改 httpOnly cookie | 更安全但前端全量改 Bearer,大改 | **放二期** |

---

## 9. 建议落地顺序

- **一期(能用)**:`users` 加 phone + 注册/登录改造 + 真实短信 + 忘记密码。→ 让流程端到端真正跑通。
- **二期(自助 + 安全)**:改密 / 改昵称 / 注销 + 登出真失效 + 短信防刷加固。
- **三期(可选)**:改手机号 + cookie 化 token + /workspace 拦截。

---

**下一步**:你把第 8 节的 A–E 拍一下(尤其 C 短信服务商,它有报备周期、最该先定),我再按一期范围出可落地的代码改动清单 / 开动。
