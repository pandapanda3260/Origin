# 镜头+视频合并为画板模式 —— 方向评估（不改代码）

> 范围：只做方向判断与拍板，基于真实代码扫描 + 全网行业资料，**不写文件级落地方案**（方向确认后再出落地方案，遵循"设计型升级先出方案"约定）。
> 一句话结论：**你的想法 OK，方向对。但"画板"要收窄定义——做"卡片板"不是"节点图"，做"前端面统一"不是"后端图引擎"。这个收窄是这次评估最关键的一条，否则会从一个能落地的前端升级滑成维度1的战略级大改，得不偿失。**

---

## 1. 先对你的三条判断逐条打分

| 你的想法 | 判定 | 依据（已扣代码/行业） |
|---|---|---|
| 剧本/风格/资产 **保留流水线** | ✅ 对 | 这三段是"前期准备"，天然线性、有强一致性闸门（世界观单源、角色锁定）。行业标杆 LTX Studio 的前期（脚本→分场→资产）也是线性的，画板化无收益。竞品报告也明确反对"全盘画布化"。 |
| 剪辑 **独立成页** | ✅ 对，且已经是 | `edit.js` 已是 223KB 的独立模块，自带 EDL 决策图（langgraph）+ VevDemo SDK + 时间线拖拽，和前面完全解耦。无需动它。 |
| 镜头页 + 视频页 **合并为一页、改画板** | ✅ 方向对，但要重定义"画板" | 见第 2、3 节。你的直觉刚好命中行业主流做法（LTX Studio 的"分镜板"），但落地形态和工作量需要我替你拍清楚。 |

**总判定：想法成立，可以做。** 但有一个事实纠正 + 四个拍板，决定它是"能上线的升级"还是"填不完的坑"。

---

## 2. 事实纠正：当前 UI 已经比你以为的更合并了

你说"镜头页和视频页感觉可以合并"——扫代码发现，**中间区原本是 4 个页面，现在已经退役/折叠成 2 个了**，你的合并其实是把最后两面收口：

| 原页面 | 现状（代码实锤） |
|---|---|
| 镜头设计 `shots` | 活着。`workspace.html:878` `#pageShots`，标题已是"**分镜生成 / 镜头表、素材参考、首尾帧统一工作区**"。它**已经吞掉了"分镜图"页**。 |
| 分镜图 `images` | **已空壳**。`workspace.html:1019` 只剩 `<div id="pageImages" hidden></div>`，分镜图工作区被搬进了 shots 页内（`workspace.html:974` "分镜图工作区"）。 |
| 视频创作 `prompts` | 活着。`workspace.html:1022` `#pagePrompts`，"视频创作 / 优化提示词并生成视频"，左栏分镜缩略图竖列 + 右栏提示词卡片 + 视频预览。 |
| 片段 `batch` | **已退役**。导航 `navBatch` 带 `hidden data-retired-surface="batch"`（`workspace.html:224`）。 |

**结论**：你要合并的就剩 `镜头设计(shots)` 和 `视频创作(prompts)` 这两面——也就是把"一个镜头/片段从 镜头参数 → 首帧/尾帧 → 视频提示词 → 视频片段"的整条竖切，从"分两页接力"变成"一张卡片上一站式"。这个方向和团队过去半年"把 images/batch 往主线里收"的轨迹完全一致，不是另起炉灶。

驱动这两面的是全仓**最大的 4 个前端模块**：`storyboard.js 406KB`、`videoTasks.js 201KB`、`videoPrompts.js 114KB`、`shots.js 65KB`（合计 ~786KB）。这个体量直接决定了第 5 节的工作量真相。

---

## 3. 我替你拍的板（不留选择）

### 拍板①：做"卡片板"，不做"节点图"

**画板模式有两种，必须二选一，我选卡片板：**

- ❌ **节点图（ComfyUI / LibTV 那种自由连线 DAG）**：节点+连线，用户自己接"参考图→生图→生视频"。适合"通用、探索性创作"，但对短剧这种**固定竖切流程**毫无增益——镜头天然有先后顺序，自由连线反而破坏顺序语义、稀释你的质量闸门，还要用户学习成本。
- ✅ **卡片板（LTX Studio 式分镜板）**：一块**有序卡片网格**，每张卡 = 一个片段的整条竖切（镜头参数 / 首帧·尾帧缩略图 / 视频提示词 / 视频片段预览 + 该卡的生成·重试·编辑按钮）。可框选任意子集→批量生成。这就是你说的"画板"，也是行业里跟 Origin 定位最像的产品的做法。

**行业佐证**：LTX Studio（AI 短片生产工具，比 ComfyUI 更贴 Origin）的范式正是 脚本→自动拆镜→分镜板卡片→逐卡精修（换机位/换模型/改角色）→卡片上点"生成动态"→时间线成片，全程**卡片板 + 时间线，不是连线节点图**。Graphite/Krea 的行业共识也是"**传统卡片/图层为主 + 节点图为可选高级层**才是甜点，纯节点图对非探索性任务是 overkill"。

### 拍板②：板的最小单元 = "片段(storyboard group)"，不是单镜头

数据层已经定死了这个口径：`storyboards[groupIdx]` 带 `shotIndices[]`，一个片段可以合并多个镜头（你们的"镜头合并"功能）。视频也是按 `groupIdx` 生成（`videoTasks[groupIdx]`）。所以**一张卡 = 一个 group**（可能含多镜头），而不是一镜一卡。这样和现有"镜头合并→一个首帧→一段视频"的事实绑定一致，不用动数据模型。

### 拍板③：这是"前端面统一"，不是"后端上图引擎"——后端基本不动

这条最省钱，也最反直觉，必须讲清楚：

竞品报告说"Origin 已有图内核可复用"——**扫代码发现这句被夸大了**。实情：
- `edit-edl-graph.ts` 的 langgraph 是 **7 个写死节点、只服务剪辑 EDL**，不是通用编排框架，扩不出去。
- `project-dependency-state.ts` **不是真图**，是 6 个哈希字段 + `_staleFlags` 字典 + 写死的下游失效传播（shot_plan→storyboard→video_prompt→video_segment）。
- `artifact-usage-guard.ts` 是个大 if-else 决策树，7 种产物类型预定义。

所以**不要去建后端图引擎**——既没有现成内核可白嫖，短剧固定流程也不需要它。而你真正要的"画板"，后端**已经齐了**：按 `groupIdx` 切分的执行器（`shots / storyboard_* / video_prompts / video_segments`，见 `batch-executors.ts`）+ 失效/修复动作模型（`repairActions`）+ 批次防重/恢复（`batches.ts` / `durable-tasks.ts`）。

> **画板 = 把"同一批按片段切分的后端操作"在前端换一种一站式的呈现 + 子集批量。后端最多补一层"对某 group 重跑某阶段"的干净入口，不需要新引擎。** 这是"能上线的升级"和"半年战略级重构"的分水岭。

### 拍板④：板是"可缩放的有序网格"，不是"无限自由画布"；且必须把 Origin 的强项搬上板

- **不做** Miro 式无限画布 + 绝对坐标自由摆放（镜头有固定顺序，自由 2D 定位零收益、还破坏顺序）。**做** 有序网格 + 缩放/密度切换（缩小看全片节奏 ↔ 放大看单卡细节），对应 LTX 的"project/board/frame 三级精修"。
- **必须上板**（否则就成了竞品报告警告的"用通用画布换掉垂直强项"）：每卡显示 stale 角标（上游改了→此卡下游标脏，沿用现有 staleness）、闸门拦截角标（角色一致性/帧一致性没过）、逐卡"按需重修"按钮（接 `repairActions`）。闸门和世界观单源**保留在原处**，板只是把它们的状态可视化到卡上。

---

## 4. 范围边界（一并拍死，免得做飞）

| 这次做 | 这次**不**做（属于别的维度，单独立项） |
|---|---|
| 镜头设计 + 视频创作 → 一块有序卡片板 | 节点自由连线 / 工作流存模板（维度1 战略级，长线） |
| 每卡整条竖切 + 逐卡生成·重试·编辑 | 每节点挑模型市场（维度3，model-routing 多供应商，独立） |
| 框选子集→批量生成首帧/视频 | 无头 API / Agent 化（维度2，独立大改） |
| stale/闸门角标 + 逐卡按需重修 | 3D 导演台/运镜库等可控性工具（维度7，独立） |
| 缩放/密度切换、全片总览↔单卡详情 | 改后端为通用图引擎（明确否决，见拍板③） |

---

## 5. 工作量真相 + 分期路线（避免"补丁上打补丁"）

**真相**：要合并的两面正好压在全仓最大的 4 个模块上（storyboard.js 406KB + videoTasks.js 201KB + videoPrompts.js 114KB + shots.js 65KB）。**在这堆巨石上硬叠一层画板 = 补丁上打补丁，必崩。** 你明确说了不要这么干，我同意。

**正解**：**新建一个独立的板模块去"接管"这两面**，复用底层成熟子件（`material_image_panel`、`frameRecommendations`、各卡的生成调用），但**替换掉 shots.js + videoPrompts.js 的页面级编排**；等板达到对等后，像当年退役 images/batch 一样把旧页摘掉。这样每期都能独立上线、旧路径随时兜底，全程不在巨石上动刀。

| 期 | 内容 | 后端改动 | 可独立上线 |
|---|---|---|---|
| P0 | **本评估**（方向）→ 你拍板 → 我出文件级落地方案 | 无 | — |
| P1 | 新板模块：把 `project.shots/storyboards/videoTasks` **只读投影**成卡片网格，逐字段对齐当前两页所见 | 无 | ✅ 只读板先与旧页并存 |
| P2 | 逐卡内联动作（重生首帧/尾帧、改提示词、重生视频）接**现有**按 group 的 API；stale/闸门角标接 `artifact-usage-guard` | 至多补"按 group 重跑某阶段"干净入口 | ✅ |
| P3 | 框选子集→批量（接 `batch-preflight` + 现有 batch/start 的 group 定位） | 复用现有批次 | ✅ |
| P4 | 板 UX 收尾（缩放/密度、总览↔详情、顺序），达对等后**退役 shots/prompts 旧页壳** | 无 | ✅ |

每期都遵守你们的前端版本制度（import map 单源、bump 条目、`verify:frontend`）。分工照旧：**我出可转发指令 + 审查方案 + 验收，Codex 执行。**

---

## 6. 一句话给你拍板用

> 合并镜头页+视频页、改卡片板——**做，方向对，且已是团队既定轨迹的最后一步**。但定义钉死为：**卡片板（非节点图）/ 前端面统一（非后端图引擎）/ 有序可缩放网格（非自由画布）/ 闸门与 staleness 原样搬上卡**。后端基本不动，新建板模块渐进接管旧页、不在 4 块巨石上叠补丁。**这是一个分 4 期、每期可上线的前端升级，不是维度1 那种战略级重构。**

确认方向后，我按这个骨架出文件级落地方案（P1 起每期的改动清单、新模块边界、版本号、测试锁）。

---

## Sources

代码（本地，关键路径已在正文给行号）：`public/workspace.html`、`public/modules/{shots,storyboard,videoPrompts,videoTasks,edit}.js`、`lib/batch-executors.ts`、`lib/batches.ts`、`lib/durable-tasks.ts`、`lib/model-routing.ts`、`lib/project-dependency-state.ts`、`lib/sentinel/artifact-usage-guard.ts`、`lib/edit-edl-graph.ts`

行业资料：
- LTX Studio（分镜板+时间线，逐卡生成动态）: https://ltx.io/studio ，https://ltx.studio/blog/best-storyboard-software-tools-2025
- 2025 AI 分镜画布 UX 趋势（卡片网格/拖拽/叙事感知）: https://storyflow.so/blog/best-storyboarding-software-2026 ，https://www.adobe.com/products/firefly/features/storyboard.html
- 节点图 vs 卡片/图层 取舍（混合为甜点、纯节点图易 overkill）: https://fuser.studio/blog/the-graph-will-set-you-free-why-every ，https://www.krea.ai/index/top-node-based-ai-workflow-apps
- ComfyUI/LiteGraph 节点引擎（重型自由连线参照系）: https://deepwiki.com/Comfy-Org/ComfyUI_frontend ，https://github.com/Comfy-Org/litegraph.js/
