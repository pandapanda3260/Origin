# 方案五 · 视频候选 + 进剪辑 + 素材库 + 旧页退役 — 落地方案

> 《画板模式 PRD v2》落地拆分第 5 个方案。**只出方案不改代码**。本方案把视频区做完整：片段级视频候选 + Radio 单选最终视频 + 视频卡模式选择器（定稿）+ 确认进剪辑（选中进时间线/未选进素材库）+ 旧页退役与死代码清理。
> 全程扣真实代码（路径+行号）。执行：Codex；审查/验收：本人。无外部依赖可直接落（"智能多帧"的**完整多关键帧行为**依赖方案四 Seedance 2.5，本方案先把选择器与透传铺好）。

---

## 0. 范围与依赖

**做**：
- 视频候选：board 视频区呈现每片段的多候选（源 = DB `video_tasks` 多行历史，已有），Radio 单选最终视频。
- 视频卡模式选择器（定稿）：**[全能参考(默认) | 智能多帧 | 首尾帧 | …]**，透传 `submitMode`，无需改后端。
- 生成：每片段"生成新视频"（新行）/"重新生成"（同行右卡）。
- 确认进剪辑：复用 `confirmSegmentsAndEnterEdit`，选中视频进 `edl.timeline`。
- 未选进素材库：未选中视频作为可选素材进剪辑页素材库（VevDemo 池，懒注册）。
- 旧页退役 + 死代码清理（达对等后）。

**不做**：智能多帧的真正多关键帧生成（方案四 + Seedance 2.5）；首帧候选（方案三）；新建视频选中字段（**复用现有 is_current，不造 selectedVideoCandidateId**，见 §1）。

**依赖**：方案一（board 壳）、方案三（首帧候选喂视频）。智能多帧完整能力依赖方案四。

---

## 0.5 关键现状（扣码，决定方案形态）

| # | 事实（file:line） | 影响 |
|---|---|---|
| ① | **视频"候选+选中"基础设施已 100% 落地**：DB `video_tasks` 每 group_idx 多行历史（history.ts:52，字段 id/status/filename/cover_image_id/prompt/duration_sec/created_at，**无 url 列**，url 由 `buildSignedVideoUrl` 签）；`getVideoCreationHistoryForGroup`(history.ts:28-97) 返回整组历史 + `is_current`；**历史弹窗 UI 已实现**（videoTasks.js:1839 `open-video-history`、:2521 `_openVideoHistoryModal`、替换走 `POST /api/video-creation/videos/current`）。 | 视频候选**不从零造**，是把已有能力搬上 board。 |
| ② | **已有"当前/选中"权威**：`storyboards[g].videoTaskId`/`videoIsCurrent` + `videoTasks[g].isCurrent`，写入唯一权威 `setCurrentVideoForGroup`(current-video.ts:45，事务写 + 同步 edl)；**无 `selectedVideoCandidateId`**（grep 零命中）。 | **复用 is_current/videoTaskId 当选中权威**，不造第二选中字段（避免双真相源）；与首帧（无任何选中、需新建 selectedImageCandidateId）是**有意不对称**。 |
| ③ | **模式后端透传链已通**：`videos/start/route.ts:36` 原样透传 options → `start-runs.ts:345` 读 `options.submitMode` → `resolveVideoPayloadDecision`(video-payload-decision.ts:193)。submitMode 4 值 `auto/strict_first_frame/first_last_frame/reference_images`(:61)。主线工作流**现不暴露模式**（createWorkflowVideoTask options 仅 quality/videoModel/ratio/genAudio/watermark，videoTasks.js:3016）。 | 模式选择器 = 给 options 加 `submitMode`（videoTasks.js:3016），**零后端改动**。映射：全能参考=auto / 智能多帧=reference_images / 首尾帧=first_last_frame。 |
| ④ | **首尾帧双条件**：`submitMode='first_last_frame'` **且** `sb.tailFrameIntent==='requested'`（后端从 storyboard 读，start-runs.ts:378；前端置位 storyboard.js:1255/1411），否则 `resolveVideoPayloadDecision` 以 no_tail_intent 降级(:247)。 | 选首尾帧时要同步置 tailFrameIntent + 提供结束帧。 |
| ⑤ | **进剪辑链路清晰**：`confirmSegmentsAndEnterEdit`(videoTasks.js:829)→`importAllGeneratedSegments`(:777)→`importVideoForGroup`(:666)→`POST /videos/import`→`importVideoGroupToEdit`(import-to-edit.ts:114)→push `edl.timeline`+version+1。set-current 与 import 两条独立 API（前者"哪条是当前"、后者"当前进时间线"）。 | 确认进剪辑**直接复用**，不新写。 |
| ⑥ | **素材库注册能力已存在**：`ensureVevDemoBindingForVideoTask`(vevdemo-material-registration.ts:768)→VOD 上传→`createOrReuseEditMaterial`→`vid://`+binding；现成调用点 `app/api/volcengine/import`(route.ts:312, autoRegister)；**任意 completed video_task 可注册、与 is_current 无关**，binding 幂等去重。 | 未选进素材库 = **新增编排**（遍历组取 is_current=false 的 task → 懒注册），底层能力够。**边界**：素材库=VevDemo `vid://`(在线剪辑播放)，与 `edl.timeline`(服务端烧录) 是两套；未选只进前者。 |
| ⑦ | **视频卡两面**：pagePrompts 单卡 `renderVideoResultCard`(videoPrompts.js:1333,#videoResultCard) + pageBatch 列表/活跃任务卡 `renderBatchClipList`(videoTasks.js:3364)/`updateTaskCard`(:2293)。 | board 视频卡 = 这两面的合并投影。 |
| ⑧ | **board_state 视频投影是单值无 shim**：`buildSegments` 的 `video:{coverUrl,status,taskId}`(board_state.js:179-183)；首帧候选 shim 范本在同文件 `shotFrameCandidatesForRow`(:78-99,返回 candidates+selectedCandidateId)。 | board 视频区候选化 = 把 `video:` 扩成 {candidates[], selectedTaskId}，仿首帧 shim。 |
| ⑨ | **退役边界**：4 个 `refreshXxxPage`(shots.js:705/storyboard.js:6433/videoPrompts.js:1069/videoTasks.js:3245) 是**页面级空态闸+orchestration（可退役）**；内部 `renderShotList/renderImageGrid/renderVideoPromptList/renderBatchClipList`+生成入口+编辑能力**要保留**（board 复用）。 | 退役只摘壳，留能力层（PRD §11 P4 原则）。 |

**行业范式（NLE 多 take）**：素材按"**setup 行 × take 列**"组织，**选用的进时间线/单独轨、备选留素材箱（media bin）**（DaVinci/Avid 助理剪辑流）。→ 直接背书"每行一条生成谱系、行内多候选、选中进 edl.timeline、未选留 VevDemo 素材池"。

---

## 1. 视频候选模型（复用现有，不造新选中字段）

- **候选源 = DB `video_tasks` 多行历史**（已有），经 `getVideoCreationHistoryForGroup` 取整组（含 `is_current`）。
- **选中权威 = `is_current` / `storyboards[g].videoTaskId`**（已有单值"当前"），**不引入 `selectedVideoCandidateId`**——视频侧本就有成熟的"当前视频"系统（含 edl 同步），再加一个选中字段=双真相源、补丁化。board 的"选中 = videoTaskId 那条候选"。
- **board 投影 shim**（board_state.js:179-183 的 `video:` 对象）扩为：
  ```
  video: {
    coverUrl, status,
    candidates: [ { taskId, coverUrl, status, durationSec, prompt, createdAt, isCurrent } ],
    selectedTaskId   // = is_current 那条（storyboards[g].videoTaskId）
  }
  ```
  candidates 数据到前端两选一（拍板取 **a**）：**a) board 进场对每组拉一次 `GET /api/tasks/video-by-project?...&groupIdx=N`（history 模式，已有）**；b) 后端 project 序列化时 attach（仿 asset-library `attachAssetLibraryCurrentToProject`）。a 改动小、与现有历史弹窗同源，作 V1。

---

## 2. Radio 单选（= setCurrentVideoForGroup，复用窄动作）

- 每片段视频区右上角 Radio：一个片段**有且仅一个**选中。
- 点 Radio → `POST /api/video-creation/videos/current`（`setCurrentVideoForGroup`，current-video.ts:45）：单值覆写 `storyboards[g]`+`videoTasks[g]` 指向该 task + **同步改 edl.timeline 已有条目 url + version+1**。这是现成窄动作（事务、非整包 PUT），board 直接调。
- board 选中态从 `selectedTaskId`（=videoTaskId）派生，刷新/增删后稳定（非 DOM 位置），与方案三选中权威一致的"显式 id"原则。
- 注意：`/videos/current` 与 `video-by-project` POST 两条 set-current 路由并存、底层同函数；**board 统一用 `/videos/current`**（与历史弹窗一致），不碰另一条。

---

## 3. 生成入口（生成新视频=新行 / 重新生成=同行右卡）

三入口全汇聚 `createWorkflowVideoTask`(videoTasks.js:2948)；映射 Vasily 设计：
- **生成新视频（新行）** = 全新 `createWorkflowVideoTask(gIdx, opts)`（unshift 新 task :2982 → 新 video_task 行）。
- **重新生成（同行右卡）** = `regenSingleClip(gIdx)`(:3933)（同 group 重生 → 新 video_task 行）；pagePrompts 单卡"重新生成"按钮也走这条。
- **谱系（行 vs 右卡）**：两者都产新 `video_task` 行。为区分"新行 vs 某候选的修改右卡"，给 `video_tasks` 加**可空 `parent_task_id`**（additive、老行 null=各自成行；"在某候选上改提示词重生"时 parent=该候选 taskId=同行右卡）。board 按 `parent_task_id` 把谱系归行（root + 后代同行），"生成新视频"= parent null = 新行。这是把 NLE"setup 行×take 列"落地的最小数据。
- 失败不计费、连续性预检沿用现状（regenSingleClip:3941）。

---

## 4. 视频卡模式选择器（定稿，零后端改动）

- 视频卡（或左键生视频面板）显式提供 **[全能参考(默认) | 智能多帧 | 首尾帧]**（"…"等扩展模式按需，但 V1 收敛到这三个；toolbox 的 image_to_video/first_last_frame_video 词汇**不照搬**）。
- **映射后端 submitMode**（video-payload-decision.ts:61）：
  - **全能参考** = `auto`（默认）：后端按用户输入自动选最合适，casual 无需选。
  - **智能多帧** = `reference_images`：当前=首帧+多参；**真正"片段内每镜头首帧作多关键帧"语义随方案四 + Seedance 2.5**（本方案先接通选择，行为升级在方案四）。
  - **首尾帧** = `first_last_frame`：同时置 `sb.tailFrameIntent='requested'`（复用 storyboard.js:1255/1411）+ 在视频卡提供/生成结束帧；否则后端降级。
- **接线**：`createWorkflowVideoTask` 的 options 块(videoTasks.js:3016) 加 `submitMode: opts.submitMode`，board 卡把选择器值塞进 opts；后端透传链已通（§0.5③），**不改后端**。
- 默认 `auto`，与现网行为一致（现网就是 auto），故**未选模式的老路径零变化**。

---

## 5. 确认进剪辑（复用，不新写）

- board 右上角"确认视频，进入下一步" → `confirmSegmentsAndEnterEdit`(videoTasks.js:829)（已实现）：批量 `importVideoForGroup` 把各组**选中(current)**视频 push `edl.timeline`+version+1 → `switchPage('edit')`。
- 拍板（时机）：Radio 选中只标 current（§2），**不即时导入**；显式"确认下一步"才批量导入（避免每次选都写时间线）。未选片段提示"片段 N 未选定视频"，不硬拦其余（沿用现 importAllGeneratedSegments 的 skipped 计数语义）。
- 选中视频→`edl.timeline`（服务端烧录 + 在线剪辑都认）。

---

## 6. 未选视频进剪辑页素材库（新增编排，懒注册）

- **目标**（Vasily）：未选中视频不丢，作为可选素材出现在剪辑页素材库，剪辑时可替换。
- **边界（关键）**：剪辑页素材库 = VevDemo `vid://` 池；**未选视频只进素材池、不进 `edl.timeline`**（选中才进时间线）。两套体系（§0.5⑥）。
- **落地（懒注册，省 VOD 上传成本）**：
  1. 进剪辑/打开素材面板时，对每组 `getVideoCreationHistoryForGroup` 取 `is_current===false` 的 task，**列进素材面板**（元数据：封面/时长/片段名，不预上传）。
  2. 用户把某未选素材拖入时间线时**才注册**：复用 `ensureVevDemoBindingForVideoTask({videoTaskId})`(vevdemo-material-registration.ts:768) → VOD → `vid://`（即 `volcengine/import` autoRegister 同款路径），幂等去重。
  - 不做"确认时全量预注册"（未选可能很多、VOD 上传重）。
- **缺口**：现有 `volcengine/import` 按显式 `resourceIds` 注册；要把"某组未选 video_task 列进素材面板 + 拖入时注册"接进在线剪辑器素材面板，需一处**编排 + 面板接线**（底层注册能力已具备）。
- ⚠️ **风险标注**：剪辑页素材面板是 VevDemo（火山）集成，历史踩坑多（素材隔离/绑定/保活，见 MEMORY 多条）。本节**单列为方案五里风险最高、最可能需要独立探针**的一块；建议作为 Phase 5-2、先探针后接线，不阻塞 5-1。

---

## 7. 旧页退役与死代码清理（达对等后，gate 在 board flag）

> 顺序：board 视频区（5-1）达到对等、确认进剪辑跑通后，才退役旧页；全程 board flag 关时旧页照常。

### 7.1 退役页面级壳、保留能力层
- 退役：4 个 `refreshXxxPage` 的**空态闸 + orchestration + 对应 #pageXxx 空态 DOM**（shots.js:705 / storyboard.js:6433 / videoPrompts.js:1069 / videoTasks.js:3245）；导航 `navPrompts` 隐藏 + `shots` 入口归 board（方案一已铺）。
- **保留**（board 复用，勿删）：`renderShotList`/`renderImageGrid`/`renderVideoPromptList`/`renderBatchClipList`/`updateTaskCard` + `createWorkflowVideoTask`/`regenSingleClip`/`startBatchGeneration` + 镜头编辑/首帧/提示词编辑能力（PRD §11 P4 "旧模块降为能力层"）。

### 7.2 死代码清单（真实行号，PRD §10 行号已 stale）
| 项 | 真实位置（agent 核实） | 处理 |
|---|---|---|
| `legacyStoryboardArchive` 旧多镜头归档 | project.js:89-199（modal/读写）+ shots.js:337（init 数组） | 移除 |
| batch 退役页 | `#pageBatch` workspace.html:1131 / `navBatch` :242（已 hidden retired）/ boot CSS :62 | 移除 |
| 全局单按钮 `btnGenAllImages` | workspace.html:923 + 绑定 main.js:8785 | 拆每片段后移除 |
| 字幕孤儿 `subtitle_format` | public/modules/subtitle_format.js + edit.js/workspace.html 残留引用 | 清孤儿 |
| `legacy_pencil` 旧铅笔稿 | frameRecommendations.js:32 + :149 | 清分支 |
| `addShot` 塞非 schema 字段 | main.js:7671-7684（imagePrompt/imageUrl/videoStatus 等） | 清为 schema 字段 |
| 首帧页死 no-op | `renderPromptPreviewList`/`updatePromptCard` storyboard.js:6453-6487（已注释为合并后不 mount） | 移除 |
| 按位置重排 shot.id | **大部分已完成**（shotUid 已采用 main.js:7675/8432/8444/8456）；仅剩 order 重排 main.js:7684-7686 | 复核，按方案二 shotUid 身份制收口 |
| `reference-site/modules/*` | 仅存于 `_server_snapshot`（快照），活仓无 | 勿当活代码 |
| `syncEditProjectClips` 空转 | asset-library.ts:715-761（timeline 无 clipId 实际插不进） | 记录；本方案不依赖，后续议是否清 |

---

## 8. board 视频区 UI（复用现有卡片语汇）

- 每片段视频区：候选卡（`.bc-card`/`.bc-thumb` 语汇，方案一§6A）按谱系排行（NLE setup×take）；选中卡 Radio 高亮（`.task-card.is-active` 语汇）；空态"+ 生成新视频"（图13）。
- 卡内：封面 + 播放（复用 `renderVideoResultCard`/`updateTaskCard` 的 `<video>`/封面取流与 `hydrateVideoResultPlayback`）；状态徽章 `.bc-status--active/done/fail`；右上角 Radio。
- 模式选择器（§4）置于"生成新视频"附近或左键面板。
- 进度监控复用现有（方案一§9.5 视频进度 `_batchTaskProgress`/`_batchRemainingText`）。
- 鉴权：封面 `<img>` 过 `hydrateProtectedImageElements`；播放走 `hydrateVideoResultPlayback`（签名/protected）。

---

## 9. 模块 / 版本 / 测试

- 前端：board 视频区落 `board_state.js`(视频候选投影)/`board_candidates.js`(选中)/`board.js`；复用 videoTasks.js/videoPrompts.js 能力层。import map 单源 bump，`verify:frontend`。
- 后端（小）：`video_tasks` 加可空 `parent_task_id`（§3，additive）；"未选进素材库"编排（§6，复用注册能力）。无新生视频引擎。
- 测试：
  - `test:video-candidates`：board 投影 candidates 来自 history、selectedTaskId=is_current；Radio→setCurrentVideoForGroup 单值覆写 + edl 同步；刷新后选中稳定。
  - `test:video-mode-passthrough`：submitMode 透传到 resolveVideoPayloadDecision（auto/reference_images/first_last_frame）；首尾帧需 tailFrameIntent；默认 auto 时行为与现网一致。
  - `test:video-lineage-rows`：parent_task_id 谱系归行；生成新视频=新行、改提示词重生=同行右卡。
  - `test:confirm-enter-edit`：选中→edl.timeline（复用现有 import 契约 `test-video-batch-import-contract`）；未选不进 timeline。
  - `test:unselected-to-material`（5-2）：未选 task 列进素材面板；拖入才注册 vid://；幂等。
  - 退役回归：退役后 `verify:frontend` 绿、旧能力函数仍可被 board 调；death-code 移除不破现有测试。

---

## 10. 分期

- **Phase 5-1（视频区主体，可独立上线）**：board 视频候选投影 + Radio 单选（复用 setCurrentVideoForGroup）+ 模式选择器（submitMode 透传）+ 生成新视频/重生 + parent_task_id 谱系 + 确认进剪辑（复用 confirm）。flag 后。
- **Phase 5-2（未选进素材库）**：先探针 VevDemo 素材面板接入点，再编排"未选列入 + 懒注册"。**风险最高、单列**，不阻塞 5-1。
- **Phase 5-3（旧页退役 + 死代码）**：board 达对等后退役 4 个 refreshXxxPage 壳 + 清 §7.2 死代码。最后做。
- 顺序：5-1 → 5-2 → 5-3。

---

## 11. 验收清单
- [ ] board 每片段视频区显示多候选（源 DB 历史）、Radio 单选、刷新后选中稳定（=videoTaskId，非 DOM）。
- [ ] 模式选择器 [全能参考(默认)|智能多帧|首尾帧]；submitMode 正确透传；默认 auto 时与现网逐字节一致；首尾帧置 tailFrameIntent + 结束帧。
- [ ] 生成新视频=新行、改提示词重生=同行右卡（parent_task_id 谱系）。
- [ ] 确认进剪辑：选中视频进 edl.timeline、`test-video-batch-import-contract` 绿；未选不进 timeline。
- [ ] 未选视频列入剪辑页素材库、拖入才注册 vid://、幂等去重（5-2）。
- [ ] 退役后旧 4 页壳隐藏、能力函数仍被 board 复用、死代码清除；`verify:frontend` 绿。
- [ ] 无 console 报错 / 单例违例；board flag 关时线上零变化。

---

## 12. 风险与规避
| 风险 | 规避 |
|---|---|
| 造第二选中字段致双真相源 | **复用 is_current/videoTaskId + setCurrentVideoForGroup**，不引入 selectedVideoCandidateId（§1）。 |
| VevDemo 素材面板集成（最高风险） | 5-2 先探针后接线、单列不阻塞 5-1；懒注册省成本；复用现成 ensureVevDemoBindingForVideoTask + autoRegister。 |
| 未选误进时间线 | 明确边界：未选只进 VevDemo 池(vid://)，选中才进 edl.timeline（§5/§6）。 |
| 模式透传打架现网 auto | 默认 auto=现网行为；submitMode 仅在用户显式选时改；契约测试锁。 |
| 智能多帧当前≠真多关键帧 | 明确：本方案智能多帧=reference_images（首帧+多参），真多关键帧=方案四+Seedance2.5；UI 不误导。 |
| 退役误删能力层 | 只退 refreshXxxPage 壳 + §7.2 死代码；renderXxx/生成入口保留；达对等后才退、gate flag、可回退。 |
| parent_task_id 迁移 | 可空 additive，老行 null 各自成行；不迁移老数据。 |

---

## 13. 给我审 / 待你点头
- **A. 视频选中复用 is_current**：确认不另造 `selectedVideoCandidateId`、直接复用现有"当前视频"系统（含 edl 同步）。
- **B. 未选进素材库 = 懒注册**：确认"未选列入素材面板、拖入时才注册 vid://"（省 VOD 上传），而非确认时全量预注册。
- **C. 模式 V1 收敛三个**：确认视频卡 V1 只放 [全能参考/智能多帧/首尾帧]（文生/图生/图片参考等暂不放主线工作流卡）。
- **D. parent_task_id**：确认加这一可空列做"新行 vs 右卡"谱系（否则只能全部平铺为一行候选）。

---

## Sources
- 代码（本地，行号见正文）：`lib/video-creation/{history,current-video,import-to-edit,start-runs}.ts`、`lib/video-payload-decision.ts`、`lib/vevdemo-material-registration.ts`、`lib/feature-flags.ts`、`app/api/{video-creation/videos/{current,import},tasks/video-by-project,videos/start,volcengine/import}/route.ts`、`public/modules/{videoTasks,videoPrompts,toolbox,board_state,storyboard,shots,project}.js`、`public/workspace.html`、`public/main.js`；先例 `SEGMENT_VIDEO_HISTORY_PLAN.md`（已落地）。
- 行业资料（NLE 多 take：setup 行×take 列、选用进时间线、备选留素材箱）：https://fsufilmhandbook.com/assistant-editing-workflow/ ，https://www.cined.com/video-editing-workflows-organizing-footage/ ，https://workflow.frame.io/guide/
