# "导入第二章却得到第一集" · 根因排查报告

日期：2026-06-10。性质：**只排查不动码**。方法：代码追踪 + 数据库取证（qd.sqlite 只读拷贝到 /tmp 分析，未碰原库）。

## 0. 一段话结论

**没有发生剧本回滚，也没有数据损坏。**数据库取证显示：你今天上午从 06:50 到 09:20 在 7 个项目里反复尝试"导入原文→生成草稿"（其中 5 个已被你删除，剩下"新项目"和"新项目2"），每一次 AI 改编产出的草稿**本来就是第一章剧情**——因为送进模型的原文里包含第一章内容，而"原文改编"功能只会**从头压缩改编**，没有任何"从第二章开始/上一集拍到哪"的概念。你后来在剧本页看到的"第一集剧本"不是被回滚的旧剧本，而是刚生成的新草稿（它内容就是第一章）；资产页的"第一集人物场景"是对这份剧本的正确提取 + 世界观注入复用了同名角色卡。整条链路行为自洽，但产品上缺了"连载原著按章改编"的能力，这是真正要补的洞。

## 1. 数据库里你的真实操作轨迹（token_usage_events + projects 表）

今天上午的 LLM 调用日志（节选）：

| 时间 | 项目 | 动作 |
|---|---|---|
| 06:50–07:53 | …204146 / …727651 / …516561 / …654301 / …603827（5个，已删除） | 各跑过一次 `script_adapt`（原文改编） |
| 09:14:30 | 新项目(…850694) | `script_adapt` 失败（fetch failed 网络抖动） |
| 09:15:04 | 新项目 | `script_adapt` 成功 → 09:15:34 选世界观触发风格分类器 → 09:16–09:17 风格圣经 → 09:17:54 资产提取 |
| 09:20:32 | 新项目2(…200091) | `script_adapt` 成功 → 09:21:10 选世界观 → 09:25–09:28 风格圣经（中途两次失败重试）→ 09:28:41 资产提取 → 09:37 起生成图片、09:43 镜头 |

关键事实：

1. **"第二章 九聲響，神子立"的字样在整个数据库里不存在**——projects（含 scriptTimeline 历史、所有字段）、script_library_items 全部搜不到"九聲響/九声响/神子立/第二章"。第二章文本从未进入任何项目字段。
2. 新项目2 的 scriptTimeline 只有两条事件：`draft(source=generate, v1)` + `system(已确认剧本)`。**没有任何覆盖/回滚事件**。它的剧本从生成那一刻起就是现在这份"第一章剧情"。
3. "第一章 混沌聖地收徒"项目今天**没有任何 LLM 调用**，09:34:51 的 updated_at 只是你切回去浏览时的常规保存。它没被改坏，也没把内容"吐"给别的项目。
4. 选世界观发生在 adapt **之后**（09:15:04 改编 → 09:15:34 才有世界观触发的风格分类器），所以世界观上下文没有进改编提示词——排除"世界观把剧情带偏"的可能。

## 2. 为什么断定"送进模型的原文包含第一章"

把新项目2 的剧本和第一章项目的剧本并排看，**微观细节完全平行**：

- 光幕文案一字不差："任务：进入混沌圣地（已完成）"；
- 同样的"叮，最新任务：加入混沌圣地，奖励三次顿悟"；
- 接引长老同款自我介绍 + "考核只有一关——敲响混沌钟"；
- 同样的《混沌引》顿悟桥段。

两次独立的 AI 生成（09:15 和 09:20）不可能凭空发明出与第一章项目剧本相同的任务文案和台词——这些细节只能来自**输入的原文本身**。也就是说，改编接口（`/api/script/workflow/full-create`，adapt 模式）收到的 sourceText 是从第一章内容开头的（大概率是 OCR 出来的多章合并文本——第一章项目的 oneSentence 里"湿沌大斋飞6升成仙"这种 OCR 错字可以佐证来源是扫描/截图识别）。

改编提示词（`lib/prompts.ts:574 buildAdaptSourceMessages`）的指令是"把原文改写为短视频剧本，保留核心人物、关键场景、关键事件"——面对一份从第一章开头起的长文 + 1-2 分钟的目标时长，模型必然**从头压缩**，产出永远是第一章的剧情弧，后面的章节根本轮不到。你重试 7 次结果一致，正是因为这是确定性的行为，不是随机 bug。

## 3. 两个症状的逐一解释

**"剧本变成第一集的剧本"**：你看到的是新项目里刚生成的草稿，它内容上就是第一章剧情（原因见上），不是哪个旧剧本被写回来了。时间线无覆盖事件、第一章项目无今日写入，双重排除回滚。

**"资产页全是第一集的人物和场景"**：资产提取（09:17/09:28）读的就是这份"第一章剧情"的剧本，提出来自然是萧云/接引长老/考核少年群像；又因为项目关联了从第一章沉淀的世界观 `world_proj_1780898501082`，世界观注入把这些同名角色的描述和图片复用了过来——卡片看起来和第一章项目一模一样。这一步**行为正确**（同名角色跨集复用本来就是世界观的设计目标），错觉来自上游剧本本身就是第一章。

## 4. 你可以一分钟自证

把当时那份 .txt 重新点"导入剧本"上传（别点转换），看导入卡片预览的**开头**是不是"九霄大陆，混沌圣地…"这类第一章文字。如果是——文件本身从第一章开头；如果开头确实是第二章——再喊我，那就推翻本报告结论，需要查上传解析链路（`/api/script/parse-upload` 不落盘，本次无法直接取证文件内容）。

## 5. 顺带发现的真问题（与本症状无关，但值得修）

| # | 问题 | 证据/位置 | 严重度 |
|---|---|---|---|
| 1 | **产品缺口：连载原著没有"按章改编为第 N 集"的入口**。续写弹窗（episodes.js `_openNewEpisodeDialog`）只支持 AI 自由续写（方向提示），不接受原文；导入剧本永远在"当前集"上从头改编。单剧多集 v1 设计假设了"AI 续写"，没覆盖"原著连载改编"这个你实际在用的工作流 | episodes.js:129-224、full-create adapt 无章节/续集参数 | 高（你的核心工作流走不通） |
| 2 | **服务端窄写字段被前端整包 PUT 抹掉**：full-create 服务端写入 `oneSentence="原文改编：…"`（route:225），但前端 done 处理只把 brief 镜像进 `idea`（script.js:2084），没写 `oneSentence` → 下一次整项目 PUT 带着 `oneSentence:''` 把服务端的值冲掉。证据：今天两个新项目 oneSentence 全空，而第一章项目的还在（行为不一致，新项目连 name 也没按 brief 改，疑似还有 done 处理未生效的成分，需浏览器复现确认） | full-create:225,270 ↔ script.js:2084-2087 | 中（机制级：任何"服务端写、前端不镜像"的字段都会被悄悄抹掉） |
| 3 | **409 整包替换的丢改动窗口**：`_serverSave` 撞 409 时无条件用服务器整包替换本地内存（project.js:617-637），上次成功保存之后的本地改动直接丢，只 toast 一句"已同步到服务器最新版本"。本次没踩中，但多标签/后台任务写盘场景下就是"我刚改的东西没了" | project.js:617-637 | 中（设计取舍，建议至少把丢弃行为显性化） |

## 6. 建议方向（待你拍板，未动码）

1. **补"按原文续写第 N 集"**（修 #1，建议优先）：续写弹窗加一个"粘贴/上传本集原文"模式——episode-create 路由接收 `sourceText`，提示词带上前集剧本摘要 + 世界观上下文，明确指令"前集已拍到 X，本集只改编以下原文"。这同时把你现在"开新项目硬试"的 workaround 收编回单剧多集的正轨。
2. adapt 模式加防呆：sourceText 超过一定长度（比如 >8k 字）时提示"原文较长，将从头改编；如需指定起点请截取本集内容"，避免静默从头压缩。
3. 小修 #2：前端 done 处理同步镜像 `oneSentence`（一行），并排查 name 未生效的原因。
4. #3 单独议：409 时改为"提示用户有未同步改动+选择"或字段级合并，属于保存架构调整，影响面大，不混在本批。

---

## 7. 结案与落地记录（2026-06-10）

**结案**：Vasily 确认是选错了上传文件（文件含第一章内容），与本报告结论一致——改编链路本身无 bug。

**已修（本批，#1 产品缺口按 Vasily 决定延后统一规划）**：

- **#2 oneSentence 镜像**：`public/modules/script.js` generateScript done 处理新增——adapt 路径把 `resp.oneSentenceBrief` 同步写进 `proj.oneSentence`，create 路径写 `proj.oneSentence = idea`，与服务端 full-create 落库口径对齐，整包 PUT 不再冲掉。（name 未生效的疑点随版本号 bump 一并观察：若是旧缓存 ESM 所致会自愈，复现再单查。）
- **建议2 长文防呆**：generateScript 在 `fromSource && 原文 > 6000 字符` 时弹被动 warn toast"改编会从开头压缩成单集剧本，靠后章节不会进入本集"，不拦截流程（遵守"不硬拦"原则）。
- 版本号：workspace.html `script.js ?v=113→114`。
- 顺手修一处**存量红**契约：`scripts/test-script-consult-abort-wiring.js` 锁的 apiPostStream 旧字符串在超时改造时已重构（externalSignal→内部 ctl 联动），按原意改锁新接线；与本次改动无关。

**验证**：`node --check` script.js/utils.js 通过；test-script-consult-abort-wiring / test-world-pending-contract / test-frontend-api-timeouts 全绿。浏览器手测建议：新建项目→导入超长原文→应见长文提示；改编完成后刷新页面，oneSentence（剧本页创意栏/项目名逻辑）应保留"原文改编：…"。
