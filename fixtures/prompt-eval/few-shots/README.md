# Prompt few-shot fixtures

Store only synthetic or fully redacted examples here.

P0 default policy:

- B-class JSON extractor modules use exactly 3 fixed examples.
- Examples cover hard/soft failures, not pure preference issues.
- Dynamic example selection is intentionally out of scope for P0.
