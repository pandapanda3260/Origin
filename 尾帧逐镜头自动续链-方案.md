# 尾帧逐镜头自动续链 - 方案

日期：2026-06-10　状态：待审批
方向由 Vasily 拍板：**某镜头首帧生成完，它的尾帧就自动跟着生成；不用等整批首帧（含失败重试的镜头）全部结束。**

## 1. 现状与病根

现在的自动续链是"批末一次性"：整个 `storyboard_images` 批次落终态 → 一次性把所有该生成的尾帧打成一个 `tail_frame_images` 批次。三个病根（今天"混沌圣地"实锤）：

1. **等最慢的人**：12 张首帧最早 12:58 就好了，全体尾帧要陪镜头 11（审核拦截失败）等到 13:15 批次落 partial，白等 17 分钟。
2. **一次性 flag 静默消费**：`_storyboardTailAutoStartedBySourceBatch` 在条件不满足的瞬间被置位后永不重试；中途三处只弹 toast 的静默退出（素材面板 invalid / 尾帧提示词 flush 失败 / 面板加载异常），断了就永远断。
3. **页面必须在场**：完成时刻不在本项目页（今天 12:53–13:04 切去第2集跑 shots），续链时机错过即永久丢失，无任何兜底。

## 2. 新语义（不变量，状态驱动）

> 对任一分镜组 g：**solo 且尾帧为"建议生成"或"已请求" && 首帧 ready && 尾帧缺失 && 无在途尾帧任务 && 无失败记录 → 自动开尾帧。**

不再关心"哪个批次结束了"——只看状态。任何路径让首帧变 ready（批量任务、单卡重生、失败重试成功），尾帧资格即激活。镜头 11 这种失败镜头：它重试成功的那一刻，它自己的尾帧自动跟上。

## 3. 触发器设计（两层）

**3.1 事件触发（即时性）**：首帧 task 完成事件的四个入口统一调 `_scheduleTailChain(gIdx)`：

- live 路径 `_applyTaskCompleted`（storyboard.js ~L8489）
- 5s 兜底轮询 `_applyStoryboardBatchSnapshotTask`（~L8556）
- 刷新重连 `_applyReattachedImageTask`（~L532）
- 单卡生成 `generateStoryboardSheet` 完成回调（~L7464 区域）

排程器聚合 3 秒窗口内的 gIdx，合并成**一个** `tail_frame_images` 批次发 `/api/batch/start`（避免 13 个单目标批次）。

**3.2 回页兜底扫描（最终一致）**：进入镜头页 / focus / 15s 对账时跑一次全量比对：凡满足 §2 不变量的组 → 入排程。这层是治本：页面不在场只是"晚开"，回页即补，永不永久丢失。今天卡住的 12 张尾帧，上线后一进页面就会被这层扫起来。

**防重**：排程器内存 pending 集合 + 比对 `/api/batch/active`（30 分钟窗口，lib/batches.ts L1855）里在途 tail 批次的 targets；卡片 loading 状态本身也是排重信号。事件层和扫描层共用同一排程器，天然不打架。

## 4. 替换与删除（避免双路径）

- 删 `_maybeAutoStartTailFramesFromCurrentProject`（L403）及三处调用（live finish L8453 / SSE 完成 L670 / 终态快照 L576）和 one-shot flag maps。
- `generateAllTailFrames`（L7961）保留为"批量执行器"，排程器和手动按钮都走它；它的三处静默中止改为**在涉及的尾帧卡上亮可见的失败/受阻提示**（被动提示，不弹拦截框）。
- 资格判定继续用 `tailFrameGenerationIntentForGroup`（frameRecommendations.js L279），P3 规则不变：合并段恒不自动、低分 solo 不自动（手动入口不拦）。

## 5. 进度条口径

标题进度 `keyframeProgressState.total` 现在 = 首帧数 + 预计尾帧数（L8372）。改为动态：尾帧批次被排程时把它的 target 数加进 total，完成/失败照常 +done/+fail。"约剩 x分x秒"沿用现有估算与单调钳制，不另起炉灶。

## 6. 边界规则（默认值，可改）

| 场景 | 默认行为 | 理由 |
|---|---|---|
| 手动**上传**首帧 | 不自动跟尾帧，只亮"建议生成" | 自动扣积分的面收窄；上传者在场，点一下不贵 |
| 尾帧已 ready，首帧被重生 | 不自动重生尾帧 | 沿用既有原则"尾帧不被系统自动判定需更新" |
| 尾帧自动生成**失败** | 该镜头退出自动链，卡片亮重试按钮 | 防审核类失败无限自动重试烧积分（镜头 11 首帧已连续两次被 moderation 拦，尾帧同样可能） |
| 用户中途取消首帧批次 | 已排程的尾帧批次照常跑完 | 成功才扣费；已开跑的不强拉，语义简单 |
| 合并段 / 低分 solo | 不自动（现状） | P3 已定 |

## 7. 不做什么

- **后端 finalize 钩子**（服务端续链）：暂不做。回页兜底扫描已消灭"永久丢失"，残留仅"不在场期间延迟开跑"，而尾帧结果本来就要回页才看。若实测仍不满意再单独议。
- 不动 `lib/batches.ts` 执行器、不动扣费链路、不动 schema。

## 8. 改动面与版本

- `public/modules/storyboard.js`：排程器 + 四入口接线 + 删旧续链 + 进度口径（主战场）
- `public/modules/frameRecommendations.js`：预计零改动（判定函数复用）
- `public/workspace.html`：bump `storyboard.js?v=149`（基于当前未提交的 v148）
- 服务端：零改动

## 9. 测试与验收

契约测试（node，仿 test:backend-stream-fallback 风格）：
1. 批中第 k 张首帧完成 → 3s 窗口聚合 → 单个 tail 批次、targets 正确
2. 失败镜头不入链；其重试成功后入链
3. 回页扫描：模拟"完成时页面不在场"→ 进页补排程
4. 防重：事件层 + 扫描层同时命中同一 gIdx → 只发一次
5. 上传首帧不触发；合并段不触发；尾帧 failed 不再自动重试

浏览器手测验收：打开"混沌圣地"镜头页 → 12 张尾帧应自动开跑；镜头 11 首帧重试成功后其尾帧自动跟上。
