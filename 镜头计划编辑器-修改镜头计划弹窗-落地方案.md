# 镜头计划编辑器（"修改镜头计划"弹窗）— 落地方案

> 作者：方案制定（Claude）｜执行：Codex｜审查/验收：本人
> 日期：2026-06-23
> 状态：待审批 → 审批后交 Codex 执行
> 范围纪律：最小影响面、不补丁叠补丁、决策已全部拍死，无悬而未决项。

---

## 0. 一句话结论

在镜头页加"修改镜头计划"弹窗（表格式就地编辑，支持增/删/改镜头并即时生效）**技术上可行、后端几乎零新接口**——增删改的数据落地链路已存在。真正要补的是 **3 道安全闸 + 1 个稳定 id 前置**，用来根治本仓已踩过的"删除被批次复活 / 下标串位 / 409 丢改动 / 孤儿媒体"。这些不是给旧逻辑打补丁，而是补上一条目前缺失的并发不变量（"镜头结构变更与批次生成互斥"）和一处写回身份校验。

---

## 1. 现状坐实（共享 ground truth，均已扣真实代码）

**截图前提纠正**：用户参考图是 **libtv（哩布哩布 AI 的节点画布视频平台）** 的镜头计划表，不是当前 Origin 镜头页。Origin 现在的镜头页（`public/modules/shots.js:677 renderShotList`，容器 `workspace.html:969 #shotListWrap`）是**卡片式**：每卡只有下拉（时长/节奏/景别/运镜/角度/焦距/景深/光线/构图，`shots.js:776-831`）+ 分镜板槽 + 一个隐藏 audio（`shots.js:840`）。**画面描述 visual / 对白 dialogue / 音效 audio 现在 UI 上不可编辑**，没有"查看提示词"按钮、没有"最终提示词"列（那在 prompts 页 `storyboard.js`）。所以这是**新增一个聚合编辑面**，不是换皮。

**数据模型**：镜头 = `project.shots` 数组，存 SQLite `projects` 表的 `data_json` 整行（`lib/projects-db.ts:71`，非子表）。字段口径权威来源 `lib/shot-plan-normalize.ts:194-230` + 枚举 `public/modules/shotSchema.js`。中文列→字段：镜号=`idx/order/id`、时长=`duration(/durationSec)`、画面=`visual(/description)`、景别=`shotType(/framing)`、光影=`light`、对白=`dialogue(/dialog)`、音效=`audio`、运镜=`camera(/movement)`；**最终提示词 = `storyboards[g].videoPrompt`，是下游产物，不在 shot 上**（`lib/batch-executors.ts:4085-4170`）。

**写入与已有链路**（弹窗可直接复用）：
- 所有镜头写入走通用 `PUT /api/projects/[id]`（带 `If-Match` 乐观锁/409，`app/api/projects/[id]/route.ts:240`）。**没有也不需要单条增删改 REST 接口。**
- 删除按钮已存在：`shots.js:769` → `handleShotAction` 的 `delete-shot`（`shots.js:1596-1612`）：`splice` → 重排 `order/id` → `_syncSingleShotSlotsAfterDelete`（`shots.js:425`）→ 清 staleFlags → `saveProject()` → `renderShotList()`。
- 增镜头逻辑已存在（仅 AI 动作入口，无按钮）：`main.js:7398-7413` addShot：构造默认字段 → `splice` → 重排 → `_syncSingleShotSlotsAfterInsert`（`shots.js:451`）→ `saveProject()`。
- 字段就地编辑已存在（仅下拉枚举）：`shots.js:859-875` blur/change → `_applyShotFieldValue` → `_markDownstreamStale("shot",{idx})` → `saveProject()`。
- 弹窗外壳可抄零依赖纯 JS 模板：`public/modules/episodes.js:240 _openNewEpisodeDialog`。
- 入口容器：`#shotsTopActions`（`workspace.html:893`），由 `_refreshShotPlanActionState`（`shots.js:233`）管理，现有"重新生成镜头计划"主按钮。

**已经兜住的关联（安全区，本方案不动）**：删/插镜头会自动重排 `storyboards/videoTasks` 下标并正确重映射剪辑时间线（`_remapEditDataAfterShotDelete/Insert`，`shots.js:321-423`：丢弃被删镜头的 timeline/segmentTags、后续 groupIdx 整体平移、旧 EDL 进 archive）；服务端 `assertGroupsCoverAllShotsOnce`（`lib/frame-workflow-state.ts:127`）校验插入空槽合法。改字段**只标记下游过期、不自动重生成、不自动扣费**（`detectShotsManualEdit` + `SHOT_MANUAL_COMPARE_FIELDS`，`lib/project-dependency-state.ts:636-669`）。

**裸露风险（本方案要解决的）**：
1. **删镜头 = 孤儿媒体永不清理**：仅把对象 push 进 `legacyStoryboardArchive`（`shots.js:306-319`），磁盘 `data/` 上的图/视频/导出片不删；离线脚本 `scripts/cleanup-orphan-media.js` 按 DB 表 filename 判引用，**不读 project JSON 的 storyboard/archive url**。
2. **批次进行中零拦截**：`delete-shot`/addShot 路径无任何"批次在跑"检查（`shots.js:1596`、`main.js:7398`）。
3. **executor 写回串位/复活**：写回走不受乐观锁约束的 `patchProjectForUser`（`projects-db.ts:996-1042`，确认不读 `expectedVersion`），按 `groupIdx` 写回；守卫只有 `groupIdx>=shots.length` 越界判断，且**尾帧写回连这个都没有**（`batch-executors.ts:2469` 用 `while(sbs.length<=groupIdx) push({})` 自动扩容）；多镜段模式下 `storyboardShotIndices('single-shot-strict')` 不再校验 `out[0]===groupIdx`（`frame-workflow-state.ts:213-217`）；**写回点无任何 shot.id / hash / token 身份校验**；project 上**没有图片/视频批次的 batchId/epoch**可做 CAS（只有 `shotPlanBatchId` 给镜头计划用）。这正是内存里 `project_tailframe_delete_no_resurrect`（30 分钟批次窗口复活）的同源缺口。
4. **409 默认丢改动**：`_serverSave`（`project.js:693-738`）1500ms 防抖 PUT，撞 409 时**无条件用服务器整包覆盖内存、本次改动丢弃**（`:727-733`）。
5. **改 visual 不回写 `characters[]`**：`shot.characters[]` 是 LLM 直出字符串（`normalizeCharacters`，`shot-plan-normalize.ts:80-83`，纯清洗不解析），改 visual 后无任何代码重算，重生成仍按旧角色取参考图（`reference-matcher.ts:267`）。

**行业惯例（背书关键决策）**：
- 稳定唯一 id 优于数组下标，是公认数据建模 best practice（React keys / StudioBinder panelId·sceneId / DataTables 讨论）。
- "渲染/生成进行中锁编辑"是成熟模式（Premiere 轨道锁、Remotion 渲染中离页二次确认）。
- AI 分镜编辑器（Boords / Storyboarder.ai / StoryboardHero）：列表视图就地点改即存、加帧保留已有帧、每镜独立 regenerate、明确的"过期"标记——与本方案就地编辑 + 只标记 stale 一致。

---

## 2. 关键决策（已拍板，无可选项）

**D1 — UI 形态：表格式"就地编辑 + 即时保存"，弹窗只作壳。**
弹窗内每行就是一个镜头，关键字段（画面/对白/音效/景别/光影/运镜/时长 + 现有下拉）直接可改，blur 即走现有 `_applyShotFieldValue → markStale → saveProject` 链路。**不做"弹窗内存草稿、确认才整体回写"的两段式**——两段式会引入"草稿 vs 服务器 version"二次对账，正是补丁叠补丁的温床，且与现有字段编辑链路冲突。理由额外背书：Boords/StoryboardHero 均为就地点改即存。

**D2 — 并发模型（核心）：整个"修改镜头计划"弹窗与活跃批次互斥。**
当该 project 有活跃批次（镜头计划/图片/视频/视频提示词任一在跑）时，"修改镜头计划"按钮**禁用**并提示"批次生成中，完成后可编辑"。弹窗打开期间页面背后的生成按钮天然不可达，因此弹窗内编辑（增删改）永不与写回并发。这是一条干净的不变量（"不能在镜头计划正在渲染时改它的结构"），不是 per-field 特判，一刀解决串位 + 409-in-modal 两个问题。背书：Premiere 轨道锁 / Remotion 渲染中锁编辑。**注意**：这与"不硬拦用户主动行为"的偏好不冲突——那条针对的是被动 UI 提示；此处是数据完整性不变量，必须硬拦，但仅拦"结构性批量编辑入口"，卡片上原有的单字段下拉快捷编辑保持不变。

**D3 — 稳定镜头身份字段（前置，最小切片采用行业 best practice）。**
需要一个跨增删/重排不变的镜头稳定标识，供写回守卫（D4）判定"这一格还是不是我当初那镜"。显示序号继续用 `order=index+1`（位置态），稳定标识只承担身份。
> **P1 预跑已发现关键事实（2026-06-23）**：本仓**已存在 `shotUid` 概念**——`lib/asset-library.ts` 的 DB 表有 `shot_uid` / `legacy_shot_id` 两列，`shotUidForProjectSlot`（`asset-library.ts:911-916`）与 `attachAssetLibraryCurrentToProject`（`:929-938`）已用 `uidToIdx`(按 shotUid) / `legacyToIdx`(按 `shot_${i}` 位置) **双路把已生成资产重连回镜头槽**；但 `shotUid` **当前没被填充**（`shot-plan-normalize` 不写它，运行时退化到 `shot.id` 或位置 `legacyShotId`）。同时 `shot.id`（`shot_${i+1}`）被 `shotUidForProjectSlot` 当作 uid 的末位 fallback，且 `legacyToIdx` 用的是 0 基 `shot_${i}`（与 1 基 id 错位）。
>
> **据此把 D3 的锚点定为：启用已有的 `shotUid` 字段，而不是改造 `shot.id`。** 即：在 `normalizeGeneratedShot` 给每个 shot 一次性分配稳定 `shotUid`（永不重排），生成任务把该 `shotUid` 写进 asset-library 的 `shot_uid` 列（同址替换现有 `legacyShotId=shot_${groupIdx}` 的注入点），让资产重连走稳定的 `uidToIdx` 路；`shot.id` **保持现状不动**（避免触碰 legacyToIdx 的 0/1 基假设）。这比repurpose `shot.id` blast-radius 更小，且把 asset-library 既有但未接通的稳定路一并接通（附带修掉其按位置重连的潜在串位）。**最终锚点以 P1 完整结果定稿**（见 §6）。

**D4 — executor 写回身份守卫（根治串位/复活）。**
批次任务创建时，为每个 group/task 捕获其目标镜头的稳定 `shot.id`（`expectedShotId`）。写回 `storyboards[groupIdx]=…` / `videoTasks[groupIdx]=…` 前校验 `freshShots[groupIdx]?.id === expectedShotId`，不匹配则**放弃该格写回（return null）**。同时给尾帧写回补上缺失的 `if (groupIdx >= freshShots.length) return null;` 越界拦截，与首帧/视频对齐。此守卫覆盖 D2 拦不住的边角（30 分钟重放窗口、批次刚结束的迟到写回）。这是把写回变幂等/安全的根因修复，不是外围打补丁。

**D5 — 删除清理：复用尾帧权威删除范式，媒体物理回收交离线脚本。**
删镜头时，对该镜头的首帧/尾帧/视频做**字段级权威清理**（复用 `markTailFrameDeleted`，`lib/visual-reference-state.ts:331` 那套"清字段 + 清对应 staleFlags 防复活"的范式，并扩出首帧/视频对应版本），而不是只 push 进 archive。**媒体物理文件不即时手删**（避免新写删除 helper 的 blast-radius + archive/DB 不同步误删风险），交既有 `npm run cleanup:orphans`（`scripts/cleanup-orphan-media.js`，24h 离线回收）。**但前提**：删镜头要让对应 `images/video_tasks/exports` DB 行可被离线脚本识别为孤儿——**探针 P3 先确认这些表行能否按 project/镜头映射**（见 §6），据此决定删镜头是否要顺带删 DB 行。媒体回收作为 Phase 2，不阻塞 Phase 1 上线（孤儿媒体只是磁盘占用，非正确性 bug）。

**D6 — 改 visual 回写引用：只处理角色，复用现成纯函数。**
改 visual 后用 `resolveCharacterMentions`（`lib/character-mention-resolver.ts:222`，纯函数、零副作用零 LLM）重算角色引用，回写 `shot.characters[]`。**口径继续存名字字符串（canonicalName），不改成 id**——避免下游 `reference-matcher` 口径迁移的 blast-radius。道具/场景**本期不自动重算**：道具是下游生成时 `text.includes` 实时匹配、不在镜头上持久成需失效的数组，场景为显式字段；强行加道具/场景 resolver 属新建能力，超范围。

---

## 3. 落地改动（按层，给 Codex 的执行蓝图）

> 通用纪律：前端改 `public/modules/*.js` 只 bump `workspace.html` import map 对应条目（裸 import，禁 `?v=`）；改 `main.js` bump 其 `<script>` 标签；改完 `npm run verify:frontend`。详见 §3.9。所有增删改**收敛到现有 `_syncSingleShotSlotsAfterDelete/Insert` + `saveProject`**，弹窗严禁自写第三份 splice/对齐逻辑。

### 3.1【前置】稳定 shot.id（D3）
- 新建 id 生成器（放 `shotSchema.js` 或 `lib/shot-plan-normalize.ts`）：`makeStableShotId()` → `"shot_" + Date.now().toString(36) + rand4`。
- `normalizeGeneratedShot`（`shot-plan-normalize.ts:194`）：若 shot 无稳定 id 则分配一次；已有则保留。
- **删除所有"按位置重排 id"的写法**：`shots.js:1599`、`main.js:7406-7408` 等处 `s.id = "shot_"+(i+1)` 改为**只重排 `s.order=i+1`，不动 `s.id`**。
- 前提：探针 P1 通过。

### 3.2 入口按钮（D2）
- 在 `#shotsTopActions`（`workspace.html:893`）内、与"重新生成镜头计划"并列，新增 `id="editShotPlanBtn"` "修改镜头计划"按钮。
- 由 `_refreshShotPlanActionState`（`shots.js:233`）统一管理显隐与 **disabled 态**：当 `_isShotStructureLocked()`（见 3.5）为真时禁用 + tooltip。

### 3.3 弹窗 UI（D1）
- 新模块 `public/modules/shotPlanEditor.js`（注册进 import map），导出 `openShotPlanEditor()`。外壳照抄 `episodes.js:240` overlay 模板。
- 渲染 `project.shots` 为表格，每行：序号(`order`，只读) / 时长(沿用 `_buildDurationOptions`) / 画面描述(textarea→`visual`) / 景别(沿用 `_buildSelectOptions(SHOT_TYPES,…)`) / 光影(`light`) / 对白(textarea→`dialogue`) / 音效(textarea→`audio`) / 运镜(`camera`) / 行操作(上移/下移/删除/在下方插入)。枚举控件全复用 `shotSchema.js`。
- "最终提示词"列**只读展示** `storyboards[idx].videoPrompt`（无则显"未生成"），不在此编辑。
- 每个可编辑控件 blur/change → 复用 3.4 的统一字段写入。textarea 字段（visual/dialogue/audio）是本方案**唯一新增的可编辑控件类型**。

### 3.4 增删改链路收敛（复用，不新写）
- **改字段**：抽一个 `applyShotFieldEdit(idx, field, value)`，内部就是现有 `_applyShotFieldValue + _markDownstreamStale("shot",{idx}) + saveProject`（`shots.js:868-872`）。弹窗与卡片共用它。visual 改动额外触发 3.8 的 characters 回写。
- **删镜头**：调用现有 `delete-shot` 同款流程（splice → 重排 order → `_syncSingleShotSlotsAfterDelete` → 清 staleFlags → 3.5 清理 → `saveProject`）。删除前若该镜头已有首帧/视频/已进剪辑，弹**消费提示型 confirm**（"该镜头已有分镜图/视频/已进剪辑，删除会一并移除"）——披露后果但不硬阻断（符合"不硬拦用户行为"）。
- **加/插镜头**：复用 `main.js:7398` addShot 的默认字段构造 + `_syncSingleShotSlotsAfterInsert`。新镜头用 `makeStableShotId()`。
- **重排（上移/下移）**：这是当前没有的能力。**决策：本期支持相邻交换（上移/下移一格），不做任意拖拽排序。** 交换 `shots[i]`↔`shots[i±1]` 后，必须同步交换 `storyboards`/`videoTasks` 同位元素并重建 `shotIndices/groupIdx`、remap editData——**复用 `_syncSingleShotSlots*` 的同款映射思路新增一个 `_swapAdjacentShotSlots(i,j)`**（与现有 delete/insert 同文件、同范式，不另起体系）。重排同样受 D2 互斥闸保护，避免与写回并发。

### 3.5 并发硬闸（D2，前后端双拦）
- **前端**：新增 `_isShotStructureLocked()`（放 `shots.js`，导出给弹窗）。两个信号合取：
  - 同步信号：`project.shotPlanStatus === 'generating'`（见 `shots.js:488` 已用）。
  - 批次信号：读 `getActiveBatchesShared(project.id)`（`utils.js:386`，5s 缓存，注意其按 `/api/batch/active` 返回 `{batches,…}` 且为异步——按该函数真实签名取 `batches[]`，必要时在进入弹窗前先 await 预热缓存），命中条件 `batches.some(b => ['queued','running'].includes(b.status) && ['shots','storyboard_images','tail_frame_images','video_segments','videos','video_prompts'].includes(b.batchType))`。
  - 零新接口。锁定时：入口按钮 disabled + tooltip；若弹窗已开则禁用增/删/重排。
- **后端**：在 `app/api/projects/[id]/route.ts` 的 PUT（约 `:240` 调 `updateProjectForUser` 之前）加守卫：当 `patch.shots` 存在且 `shots.length` 相对当前变化（结构性变更）时，用 `findActiveBatchForType`（`lib/batches.ts:1870`，已被 `batch/start/route.ts:164` 验证可用）对上述 batchType 逐查，命中任一活跃批次 → 返回 **423 Locked**（带 `{reason:'shot_structure_locked', batchType}`）。**落点必须在 route 层，不在 `projects-db.ts`**（executor 写回也走 `patchProjectForUser/updateProjectForUser`，在 DB 层加闸会误伤写回）。
- 前端收到 423 → toast"批次生成中，请稍后再改镜头结构"+ 刷新批次态。

### 3.6 executor 写回身份守卫（D4）
- **任务侧**：批次任务创建时给每个 group/task 落 `expectedShotId`（探针 P2 先定位 task payload 构造点）。
- **写回侧**：在以下写回点，写 `storyboards[groupIdx]`/`videoTasks[groupIdx]` 前加 `if (freshShots[groupIdx]?.id !== task.expectedShotId) return null;`：首帧 `batch-executors.ts:1908-1989`、`2154-2220`；尾帧 `2466-2520`；视频 `3180-3217`、`3229-3265`、`3494-3545+`。
- 尾帧写回额外补 `if (groupIdx >= freshShots.length) return null;`（`:2466` 附近，对齐首帧/视频）。
- `expectedShotId` 缺失（老批次/迁移期）时**回退到现有 `groupIdx>=shots.length` 行为**，不throw、不误杀（无 legacy 迁移，仅容错）。

### 3.7 删除权威清理（D5）
- 抽 `markShotProductsDeleted(project, idx)`：对 `storyboards[idx]` 复用 `markTailFrameDeleted` 范式清尾帧字段，并扩出清首帧（`firstFrameUrl/Prompt/Status/...`）与视频（`videoTasks[idx]` 的 url/status）字段 + 清 `_staleFlags` 对应键（防复活，对齐 `app/api/frames/delete/route.ts:73-94`）。
- delete-shot 流程在 `_syncSingleShotSlotsAfterDelete` 之前调它。
- 媒体物理回收：Phase 2，依 P3 结论决定是否顺带删 `images/video_tasks/exports` DB 行以喂给 `cleanup:orphans`。

### 3.8 改 visual 回写 characters[]（D6）
- 抽薄封装 `recomputeShotCharacters(project, idx)`：取 project character locks（`project.consistency.characters`，经 `ensureProjectConsistency` 规整）→ `resolveCharacterMentions({text: shot.visual, characters: locks, contextText: shot.visual})`（`character-mention-resolver.ts:222`）→ 取 `confidence>=0.9 && characterId` 的项的 `canonicalName` 去重 → 写回 `shot.characters`（照抄 `character-consistency-gate.ts:250-266` 的取值法）。
- 仅在 visual 字段编辑后调用；纯前端、无网络。

### 3.9 版本号 / 缓存
- 新增 `shotPlanEditor.js` → import map 增条目；改 `shots.js` → bump 其 import map pin；改 `main.js`（若挂载入口）→ bump html `<script>` 标签；改 `styles.css`/`workspace.html` → 按现有 cache-busting 规则。
- 改完跑 `npm run verify:frontend`（含 `test:cache-busting`）。

---

## 4. 测试（新增，匹配仓库 `scripts/test-*.mjs` + npm script 约定）
- `test:shot-stable-id`：splice/插入/删除/交换后，存活镜头的 `id` 不变、`order` 正确重排；新镜头 id 唯一。
- `test:shot-structure-lock`：活跃批次（各 batchType）下，前端 `_isShotStructureLocked` 为真；后端 PUT 带 shots 长度变化时返回 423；无活跃批次时放行。
- `test:shot-writeback-identity-guard`：构造"写回时 groupIdx 已指向不同 shot.id"，断言写回 return null 不串位；`expectedShotId` 缺失时回退旧越界行为；尾帧越界拦截生效。
- `test:shot-delete-cleanup`：删镜头后对应首/尾帧/视频字段被权威清理 + staleFlags 防复活键被清；archive 留痕正确。
- `test:shot-visual-recompute-characters`：改 visual 文本后 `characters[]` 按 mention 重算（纯函数，无网络）。
- 既有回归必须仍绿：`test:tail-frame-delete-no-resurrect`、`test:tail-chain`、`test:frame-workflow-state`、`test:project-dependency-state`、`test:continuity-chunking`、`test:cache-busting`。

---

## 5. 分阶段交付（每阶段独立可验收，不留半成品）
- **Phase 0（前置探针）**：P1/P2/P3（§6）。**P1 不过则 D3/D4 改路**（退化为"内容 hash 守卫"，见 §7 风险）。
- **Phase 1（安全闸，必须先上）**：D3 稳定 id + D4 写回守卫 + D2 前后端互斥闸 + 既有回归。**此阶段即使不加任何新 UI，也独立修复现存的串位/复活/结构并发隐患**，价值自洽。
- **Phase 2（编辑器 UI）**：D1 弹窗 + D5 删除权威清理（字段级）+ D6 characters 回写 + 重排。
- **Phase 3（媒体回收）**：依 P3 决定删镜头清 DB 行喂 `cleanup:orphans`，或保持离线 24h 回收。
- 顺序硬约束：**Phase 1 先于 Phase 2**——没有安全闸就上编辑器 = 把现存隐患从"偶发"放大成"高频"。

---

## 6. 先跑的探针（只读，安全；请转发 Codex，结果回我）

**P1 — 摸清镜头身份系统（shotUid / legacyShotId / shot.id），定稿稳定身份锚点**（预跑已指向"启用 shotUid"，需 Codex 完整核实）
- 读 `lib/asset-library.ts:80-120 / 220-345 / 770-945`，弄清 `shot_uid`、`legacy_shot_id` 两列的写入与读取，以及 `shotUidForProjectSlot`(:911) + `attachAssetLibraryCurrentToProject`(:919) 的 `uidToIdx`/`legacyToIdx` 双路重连。
- 确认 `shot.shotUid` 现在是否被填充：`grep -rnE "shotUid *[:=]" lib/shot-plan-normalize.ts lib/*.ts | grep -v node_modules`，并确认镜头计划生成时有没有给每个 shot 写稳定 shotUid。
- 列出所有写 `legacyShotId=\`shot_${groupIdx}\`` 的注入点（已知：`lib/video-gen.ts:206`、`lib/storyboard-material-images.ts:419`、`app/api/video/submit/route.ts:311/398`、`app/api/frames/upload/route.ts:117`、`app/api/frames/reference-upload/route.ts:161`）——这些是未来注入稳定 `shotUid` 的同址点，确认每处 groupIdx 来源。
- 读 `lib/image-gen.ts:342` 上下文，确认 `shot_${match[1]}` 在解析什么、是否依赖位置。
- 全仓确认除 `asset-library` 的 `legacyToIdx` 外，有无别处把 `shot.id`/`shotUid` 反解析成数字下标。
- **回报**：shotUid 是否已是"持久且稳定"字段；最终锚点建议【启用已有 shotUid】还是【改造 shot.id】+ 理由 + 受影响文件清单；legacyShotId 注入同址点 file:line 全清单。

**P2 — 定位批次任务创建时每个 group/task 的镜头引用落点**（决定 `expectedShotId` 塞哪）
```
grep -rn "registerExecutor\|batchType\|groupIdx\|shotIndices\|tasks.push\|buildTasks\|createTask" lib/batch-executors.ts | grep -iE "storyboard_images|tail_frame|video_segment|groupIdx|task" | head -60
```
人工确认：`storyboard_images / tail_frame_images / video_segments` 三类批次的 task payload 在何处构造、是否已带 `groupIdx`、能否在该处加 `expectedShotId = shots[groupIdx].id`。把构造点 file:line 回我。

**P3 — 确认 images/video_tasks/exports DB 行能否映射到镜头/项目**（决定删镜头能否喂离线孤儿回收）
```
grep -rn "CREATE TABLE\|project_id\|group_idx\|shot_id\|filename" lib/db.ts lib/batches.ts | grep -iE "images|video_tasks|exports|project_id|group_idx|filename" | head -60
```
人工确认：这三张表行是否带 `project_id`（甚至 `group_idx`），删镜头时能否定位并删除对应行。结论回我。

> 探针都是 grep，只读、零副作用，无需进 qd.sqlite 写库（遵守 DB 写红线）。

---

## 7. 我验收时要扣的点（审查清单）
- D3：grep 全仓确认再无"按位置重排 id"残留；存活镜头 id 跨增删稳定（实跑 `test:shot-stable-id`）。
- D4：六个写回点 + 尾帧越界拦截**逐一**有身份守卫；`expectedShotId` 缺失回退不误杀；实跑身份守卫测试。
- D2：后端 423 落在 route 层不在 DB 层（确认 executor 写回不被误伤）；前端禁用态随批次态实时更新（切项目/批次结束后解锁）。
- D5：删除清理复用 `markTailFrameDeleted` 范式且清了 staleFlags 防复活键；`test:tail-frame-delete-no-resurrect` 仍绿。
- 收敛性：弹窗未自写 splice/对齐逻辑，全走 `_syncSingleShotSlots*`；无第三份增删实现。
- 缓存：import map 条目 + main.js 标签均已 bump，`verify:frontend` 绿。
- 不变量：Phase 1 单独上线即修复隐患，与 Phase 2 解耦。

**已知风险/回退**：若 P1 发现 shot.id 被位置解析（D3 受阻），D4 改用"写回前比对该 group 的内容签名（visual+对白 hash，存入 task 与 storyboard 的 `sourceHash`）"作身份依据——`tailFrameSourceHash` 字段已有先例（`visual-reference-state.ts:341`），blast-radius 仍可控。此为唯一备选分支，其余决策无条件执行。

---

## 8. 明确不做（防 scope 蔓延）
- 不做全局稳定-id 重构（storyboards/videoTasks 仍按位置对齐）；只做 §3.1 最小切片。
- 不做任意拖拽排序（仅相邻上移/下移）。
- 不做道具/场景 mention 自动重算（无 resolver，超范围）。
- 不即时手删媒体物理文件（交离线 `cleanup:orphans`）。
- 不改 `_serverSave` 全局 409 语义（靠 D2 互斥从源头消除 modal 内 409 窗口）。
- 不为老项目写迁移/兼容（遵循既定纪律，老项目可直接删）。
