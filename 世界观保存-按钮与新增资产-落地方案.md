# 世界观保存：按钮切换 + 更新弹窗「新增资产」落地方案

> 状态：待审批。审批前不动代码。
> 关联：`世界观产品设计方案-v1-单剧多集.md`（世界观=剧级真相源）。

## 一、背景 / 现状（先对齐）

资产页右上「保存世界观」按钮：`public/workspace.html` 的 `#btnSaveWorldTemplate`（固定文案「保存世界观」），`public/main.js:7870` 绑 `saveAsWorldTemplate` → 打开 `_openSaveTemplateDialog()`（`public/modules/assets.js`）。

几个现成的关键事实：

1. **「是否已选世界观」状态现成**：`project.selectedWorldTemplateId` / `project.worldTemplateSnapshot`，风格页选世界观时经 `_persistStyleWorldIntent` 写入，资产页直接能读。
2. **弹窗本来就有「新建 / 更新已有」单选**，只是默认勾「新建」；下拉框已会把绑定的模板预选中（`currentWorldId`）。
3. **后端 update 本来就是「保留旧 + 追加新」**：`mergeWorldTemplateSnapshotIntoSource`（`lib/world-templates-db.ts:789`）按 `entityKey` 去重、`appendMissing:true` 只追加新实体、旧实体保留并补字段。
4. **模板列表接口只给摘要**（`summaryOnly:true`，含 count 不含实体明细）；要拿明细做 diff 得用 `_loadWorldTemplateDetail`（`assets.js:4464`）。摘要里**带 `sourceProjectId`**（build 时填 `project.id`）。
5. **新建有 409**：后端按 `world_<projectId>` 派生 id，项目已存过模板再点「新建」直接 409（`from-project/route.ts:32-33`）。

## 二、本次确认的两个口径

- **按钮判据**：`selectedWorldTemplateId` 非空 **或** 该项目已有派生模板 → 显示「更新世界观」；否则「保存世界观」。（避免撞 409）
- **新增 list 勾选**：要能**真剔除**——勾掉的新增资产这次就不进模板。需后端加「按实体排除」参数。

## 三、方案

### A. 按钮文案切换（需求 #1）

判据函数（前端，资产页渲染时算）：

```
hasBoundWorld =
     !!project.selectedWorldTemplateId
  || _getWorldTemplates().some(t => t.sourceProjectId === project.id)   // 已有派生模板
```

- 资产页渲染/进入时，按 `hasBoundWorld` 设 `#btnSaveWorldTemplate` 文案：`true`→「更新世界观」，`false`→「保存世界观」。
- 文案改由 JS 控制（workspace.html 里可留个默认值兜底）。

### B. 弹窗默认值（需求 #1 联动）

`_openSaveTemplateDialog()` 打开时：

- `hasBoundWorld` → 默认单选 = **更新已有模板**；下拉默认选中：优先 `currentWorldId`，否则那条 `sourceProjectId===project.id` 的派生模板。
- 标题相应改「更新世界观模板」（小细节，可顺带）。
- 非绑定态保持现状：默认「新建模板」。

### C. 锁定角色区 → 新增资产区（需求 #2 核心）

弹窗按模式切换中间这块区域：

- **create 模式**：保持现状的「锁定角色 / 候选角色 / 保存总计」+ 4 个整类 checkbox，不动。
- **update 模式**：替换为
  - **新增资产**（按类分组：角色 / 场景 / 道具 / 术语）
  - **新增总计 N 个**
  - **新增 list**：每个新增实体一行 + 勾选框（默认全勾）

数据与 diff：

1. 进 update 模式时 `_loadWorldTemplateDetail(选中模板)` 拉明细（异步，复用弹窗已有的 prime/loading 模式）。
2. **diff key 必须复刻后端 `entityKey`**（`world-templates-db.ts:454`）：
   `key = (characterId || id || sourceAssetId || name || title || role).toLowerCase()`
   角色/场景/道具同一套；术语是 map，按 map 的 key diff。
3. 新增 = 当前项目资产里、key 不在「模板已有 key 集合」里的实体。
4. 边界：拉不到明细 → 兜底提示并退回整类 include 老逻辑；无新增 → 显示「无新增资产，本次仅同步字段」。

> ⚠️ key 不对齐的话，弹窗显示的「新增 N 个」≠ 实际入库数，会误导，这是本方案第一风险点，务必对齐。

### D. 后端：按实体排除（需求 #2 生效层）

`app/api/world-templates/from-project/route.ts`：

- 新增可选入参 `excludeKeys: string[]`（小写 entityKey 列表）。
- build 出 `snapshotInput` 后、merge/upsert 之前，过滤掉 key ∈ `excludeKeys` 的 characters / characterCandidates / locations / props / 术语。建议在 `lib/world-templates-db.ts` 加一个 `filterSnapshotByExcludeKeys(snapshot, excludeKeys)`，route 调用。
- 安全性：merge 是 `appendMissing`，被排除的新实体不在 snapshot 里就不会被 append；已有实体本就在 source、不受影响。excludeKeys 只会来自「新增项」，所以只影响新增、不误伤旧资产。

前端 `_doSaveWorldTemplate`（`assets.js:5295`）update 模式带上 `excludeKeys` = 未勾选的新增实体 key 列表。

整类 include 开关与 excludeKeys 的关系：update 模式下「新增 list 勾选」已覆盖更细粒度，建议 update 模式**隐藏 4 个整类 checkbox**（避免双重语义打架）；create 模式保留。

## 四、改动清单（blast radius）

前端：
- `public/modules/assets.js`：`_openSaveTemplateDialog`（默认值 + 模式切区域 + 新增 diff + 隐藏整类 checkbox）、`_doSaveWorldTemplate`（带 excludeKeys）、新增 diff/key 工具函数；资产页渲染处设按钮文案。
- `public/main.js`：资产页渲染时按 `hasBoundWorld` 设 `#btnSaveWorldTemplate` 文案（或在 assets 渲染入口统一做）。
- `public/workspace.html`：改了 public 下 JS 后 **bump `?v`**（否则浏览器跑旧 ESM 缓存）。

后端：
- `app/api/world-templates/from-project/route.ts`：收 `excludeKeys`。
- `lib/world-templates-db.ts`：加 `filterSnapshotByExcludeKeys`。

后端面很窄（一个 route + 一个 helper），前端集中在 assets.js。

## 五、不做 / 风险

- 不写老数据迁移、不做老项目兼容。
- 风险1：diff key 与后端 `entityKey` 对齐（见 C，最重要）。
- 风险2：外部世界观（风格页选了别的项目的世界观模板）点「更新」会把当前项目资产并进那个共享世界观——符合 v1「剧级真相源」，保留为预期行为。
- 风险3：模板明细加载失败的兜底体验。

## 六、待你拍板

- 上面整体方案是否 OK，或哪块要调。
- 确认后我再动代码，分前端展示 / 后端排除两步落地、自测后给你复测点。
