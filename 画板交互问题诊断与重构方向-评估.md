# 画板交互问题诊断与重构方向 · 评估（不改代码）

> 范围：`workspace#workspacePage=shots`（画板，localhost 默认开）。
> 方法：扣真实代码（board.js / board_viewport.js / board_state.js / shotPlanDialog.js / main.js / utils.js）+ 全网行业方案对照，不靠逻辑推断。
> 结论先行：**架构选型是对的（自建 transform 视口 + 固定四列拓扑），不用换 React Flow/dagre。但有 3 个"随规模放大就崩"的交互根因（语义缩放/图片门控/尺寸响应）没做，外加 3 个数据/渲染层的"补丁味"问题。建议把这 6 条按根因分层修，别再往现有节点里塞补丁。**

---

## 0. 当前画板"实际能干什么"（先对齐现实，别误判）

`isBoardEnabled()`（main.js:252-260）在 **localhost 默认开**，开了之后 `workspacePage=shots` 渲染画板、隐藏旧镜头/提示词页。但画板上**几乎所有动作按钮是 `disabledButton` 占位**（board.js:60-64 工厂；确认视频进入下一步 124、生成镜头计划 220、生成图片/上传图片 250、一键全生成 258、生成新视频 278、撤销/恢复 135-136）。

也就是说**现状画板 = 只读投影 + 唯一一个能用的入口「打开脚本节点」**（board.js:237，由 shotPlanDialog.js:101-106 全局委托打开，能改镜头参数 / 调片段分组）。生成首帧、生成视频、确认进剪辑——画板上都点不了，要做这些得把 `originBoard` 关掉回旧页。

这是**方案一刻意的分期**（方案一-画板引擎-落地方案.md §6C 覆盖矩阵：图3/4/5/6/10/12/13/15 归到方案二~五），**不是 bug**。下面的"问题"分两类说清楚：① 真·交互缺陷（该修）；② 分期未做（按已批方案推进即可，不重复造）。

---

## 1. 交互问题（每条扣代码 + 行业做法 + 严重度）

### P0-1　没有"语义缩放/LOD"，是你截图里 31% 看不清的根因
- **现状**：节点 body 在所有缩放级别都渲染同一份完整 HTML（board.js renderNode 282-288），缩放只是给 `.board-world` 套 CSS `scale()`（board_viewport.js applyNow 171-178）。12+ 片段纵向堆叠（board_state.js layoutBoard 311-342，片段块高 = `58 + 行数×124`），整图几千 px 高，只能缩到 31% 才看得全 → 卡里 10~15px 的字 ×0.31 ≈ 3~5px，**全糊**。
- **行业做法**：缩到一定比例就切"概览表达"——只画标题 + 计数 + 色块，不画缩略图和明细（semantic zoom / level-of-detail，等同游戏的 mipmap）。Figma/FigJam/各大节点编辑器都这么干；React Flow 社区明确指出"缩到底全部可见会瞬间拖垮页面"，解法就是 LOD/语义缩放。
- **严重度：P0**。这一条同时治"看不清"和下面 P0-2 的"图片风暴"。

### P0-2　图片水合不按视口/缩放门控 → 首屏并发拉全部受保护图
- **现状**：`hydrate()`（board.js 329-334）调 `hydrateBoardImages(_root)`（43-58）**遍历整棵 `_root` 里所有 `img[data-board-src]`**，不管节点是否被裁剪隐藏、不管当前缩放，立刻对每张图 `resolveProtectedImageBlobUrl`（utils.js 1065-1095：带 Bearer 的 `fetch`→`blob`）。受保护图（角色/场景/道具 + 每片段段首帧 + 每视频封面）**首屏一次性并发开几十个鉴权请求 + 解码**。
- 注：utils 那层按 URL 有 `_protectedImageBlobCache` + `force-cache`（1069-1074），所以**不是每次刷新重复拉**，但**首屏一把梭**的风暴还在；blob URL 全程不 `revokeObjectURL`，是只增不减的会话级缓存（不是逐帧泄漏，但无上限）。
- **行业做法**：`IntersectionObserver` 按"进视口才加载"门控（原生、跑在合成线程、批量回调），配合 P0-1 的 LOD 阈值——低缩放直接不取缩略图。
- **严重度：P0**（大项目首屏卡顿/掉帧的主因）。

### P0-3　没有 ResizeObserver → 改窗口/折叠侧栏后，裁剪和相机都失准
- **现状**：`board_viewport.js` 全文 **无 ResizeObserver**（已 grep 确认）。视口尺寸只在 fit/缩放/裁剪时临时 `getBoundingClientRect()` 读一次（viewportSize 107-114）。方案一 §6B 明确要求"ResizeObserver 重算视口、保持相机中心；侧栏折叠同样响应"——**这条写了没做**。
- **后果**：拖窗口大小、或折叠/展开侧栏（`--sidebar-width` 变）后，`refreshCulling`（286-298）按旧尺寸算可视区 → 该显示的节点仍 `hidden`、或边缘节点错判；相机也不重新居中，要手动平移/缩放一下才"复活"。
- **行业做法**：ResizeObserver + rAF 合批重算（标准动作）。
- **严重度：P0**（折叠侧栏是高频操作，一折就"画板像卡住了"）。

### P1-1　连线每次刷新整层重建，与方案一"不整树重建"自相矛盾
- **现状**：节点走了增量（board.js ensureNodeElement 290-310，按 `renderHash` 跳过重渲），**但连线没有**：`setEdges`（board_viewport.js 267-284）每次 `edgesSvgEl.replaceChildren()` 再把所有 `<path>` 重建一遍。而 `refreshBoardPage`（board.js 377-396）由 `refreshAllPages`（main.js 1425→_refreshShotsSurface 1415-1419）在**每次项目状态更新时**触发（project.js 多处轮询/保存后调）。生成过程中项目频繁刷新 → 连线层被反复全量重建。
- **后果**：边数不多时性能可接受，但生成态会有连线闪烁，且违背方案一 §6B"按稳定 key diff、不整树重建"。
- **行业做法**：边按 `from->to` key diff，原地改 `path.d`，只增删变化的边。
- **严重度：P1**。

### P1-2　board_state 自己重写了"资产取图"逻辑 → 单源被破坏，发散风险（补丁味最重的一处）
- **现状**：board_state.js 79-202 **自带一整套** `resolveCharacterImageUrl / resolveSceneImageUrl / resolvePropImageUrl`（角色还带 entityType 闸、consistency locks 回退等 50+ 行），和资产页的取图口径是**两份实现**。方案一 §5 原话是"**复用** `firstFrameImageUrl` 等纯函数、**不重写**"，落地时却重写了。记忆里 v3 评审已点过这个雷（[[project_board_v3_resolver_review]]：角色 resolver 引错函数，扁平字段会空白）。
- **后果**：资产页改了取图规则，画板不会跟着变，迟早两边显示不一致；这正是你说的"补丁上打补丁"。
- **行业做法/原则**：一个口径一条计算路径（与你后台纪律同理）。让画板取图走资产卡现有的 `deriveAssetCardState` 单源，board_state 只做投影、不再自己判断回退。
- **严重度：P1**（正确性 + 可维护性）。

### P1-3　"每镜头首帧"是假的：同片段所有镜头行共用段封面 → 看着像 bug
- **现状**：buildSegments（board_state.js 286-309）里 `coverUrl = firstFrameImageUrl(sb)`（段级一张），然后**每个镜头行的 candidate 都塞同一个 coverUrl**（291-296）。所以一个含 3 个镜头的片段，3 行缩略图**完全一样**。
- **定性**：这是**方案三（分镜首帧候选模型，2026-06-28 已起草）未落地**的过渡态，不是新 bug。但现在 UI 上不加任何"占位/共用"标注，用户会当成图错了。
- **建议**：方案三落地前，先把共用封面的行显式标成"片段封面（占位）"，别让它冒充逐镜头首帧。
- **严重度：P1**（认知误导）。

### P2-1　缺小地图/导航；help 是占位；快捷键不可发现
- **现状**：右下角功能区有 fit/±/缩放到选中/100%（board.js onToolClick 344-360），但 **help 只弹一句 toast「下一阶段接入」**（357-359），键盘快捷键（空格平移、+/-/0、Shift+1 全览、Shift+2 缩放到选中，board_viewport.js 347-375）**没有任何地方告诉用户**。几千 px 高的图也**没有小地图**做鸟瞰跳转。
- **行业做法**：大图标配 MiniMap 鸟瞰 + 视口框（React Flow `<MiniMap/>` 是标准件）；快捷键进 help 浮层。
- **严重度：P2**（导航效率，按你 Mac 习惯给鼠标路径即可）。

### P2-2　鼠标滚轮要按住 Ctrl 才能缩放（触控板没问题）
- **现状**：onWheel（board_viewport.js 336-345）`ctrl/⌘ 或捏合 → 缩放，否则平移`。触控板双指=平移、捏合=缩放，**你在 Mac 上体验是对的**（Figma 同款）。但纯鼠标滚轮只能纵向平移，缩放必须按 Ctrl，鼠标用户会懵。
- **建议**：可选项——给个"滚轮直接缩放"的开关，或在 help 里写清。优先级低。
- **严重度：P2**。

---

## 2. 行业对照（结论：保留自建，补能力，不引框架）

| 关注点 | 行业主流 | 对画板的取舍 |
|---|---|---|
| 渲染范式 | React Flow/xyflow（绑 React）、tldraw、自建 transform 视口 | **保留自建**（origin 无 React，引框架 blast radius 比画板还大；方案一 §3.1 已论证，认同） |
| 大图布局 | dagre / ELK 自动布局 | **不引**。固定语义拓扑（参考图→计划→片段→视频）手写四列更清晰；行业共识"自动布局会模糊语义时就该手写" |
| 缩到底/概览 | **语义缩放 / LOD**（缩小切简化表达） | **要补**（P0-1）——治你截图里的"看不清" |
| 离屏/大量元素 | 视口虚拟化（onlyRenderVisibleElements）；但中小图反而更慢（重挂载代价） | 当前 12~30 片段属"中等"，**节点先维持 CSS 隐藏即可**；真正该门控的是**图片**（P0-2），别盲目上节点虚拟化 |
| 图片懒加载 | `IntersectionObserver`（原生、合成线程、批处理） | **要补**（P0-2） |
| 尺寸响应 | `ResizeObserver` + rAF 合批 | **要补**（P0-3） |
| 大图导航 | MiniMap 鸟瞰 + 视口框 | **建议补**（P2-1） |

---

## 3. 重构方向（按根因分层，非补丁）

不在现有节点 HTML 里塞条件分支，而是补/收口几个**层**：

1. **视口服务层（board_viewport.js 内补齐，对外不破坏接口）**
   - 加 `ResizeObserver`：尺寸变→重算视口、保相机中心、`refreshCulling`（治 P0-3）。
   - 暴露当前缩放档位（`k` + 一个 `lodLevel`），供渲染层和图片层读。
   - 连线改增量 diff（治 P1-1）。

2. **语义缩放 / LOD（治 P0-1，最高杠杆）**
   - 定 1~2 个阈值（建议 `k<0.45` 进概览档：节点只渲染标题+计数+状态色块，隐藏缩略图与镜头行明细；`k≥0.45` 渲完整卡）。
   - 渲染层按 `lodLevel` 出两套 body（同一节点，两种繁简），不改布局坐标。

3. **图片层换 IntersectionObserver 门控（治 P0-2）**
   - 只对"在视口附近 **且** 当前 LOD 需要缩略图"的 `img` 触发 `resolveProtectedImageBlobUrl`；离屏/概览档不取。
   - 顺手给会话级 blob 缓存定个上限或在切项目时清理（去掉"只增不减"）。

4. **数据层收口单源（治 P1-2 / P1-3）**
   - board_state 取图改走资产卡 `deriveAssetCardState` 单源，删掉自带的角色/场景/道具 resolver 重写。
   - 逐镜头首帧接 **方案三**；落地前先把共用段封面标"占位"。

5. **导航层（P2-1）**
   - 加 MiniMap 鸟瞰 + 视口框；help 改成快捷键浮层。

---

## 4. 优先级 + 与现有方案的关系（我的取舍）

- **P0 三件（LOD + IO 图片门控 + ResizeObserver）**：直接决定"画板放大到真实规模还能不能用"，建议**并入"方案一"的一次能力补丁刷新**（方案一本来就承诺了 ResizeObserver/裁剪/相机，等于补做欠账），不新开方案。
- **P1 三件（连线增量 / resolver 单源 / 首帧占位标注）**：correctness + 去补丁，建议跟 P0 同批做，或紧随其后。
- **P2（minimap / help / 滚轮缩放）**：体验打磨，单列。
- **画板上那些灰按钮（生成/导入/确认进剪辑）**：是**方案二/三/五**的活，按已批方案推进，**不在本评估范围重复造**。
- 以上全部**不触碰已拍板的产品形态**（受约束有序流程图、固定四列、片段为最小单元、显式选中态权威）——只补能力、收口实现。

---

## 5. 待你点头的一个范围问题

P0 三件我倾向**作为"方案一·能力补齐"刷新**来做（而不是散落补丁），因为它们正是方案一 §6B 承诺却没落地的部分。要不要我据此出一份可转交 Codex 的**落地方案**（文件清单 / 接线点 / 版本 bump / verify:frontend 测试点 / 分期），范围就锁 P0+P1？还是你只要这份诊断、P1-2 的 resolver 单源你想单独议？

---

## Sources
- React Flow · Performance（onlyRenderVisibleElements、虚拟化代价）: https://reactflow.dev/learn/advanced-use/performance
- React Flow · Layouting（dagre/ELK 自动布局取舍）: https://reactflow.dev/learn/layouting/layouting
- elkjs（Eclipse Layout Kernel JS 口）: https://github.com/kieler/elkjs
- React Flow · MiniMap 组件: https://reactflow.dev/api-reference/components/minimap
- Architecting for Massive Scale in React Flow（缩到底全可见会崩 / 语义缩放）: https://www.visualflow.dev/blogs/scale-studio-pro
- 语义缩放交互模式目录（semantic zoom / LOD / 渐进式披露）: https://github.com/prathyvsh/semantic-zoom
- Smashing Magazine · IntersectionObserver 懒加载: https://www.smashingmagazine.com/2018/01/deferring-lazy-loading-intersection-observer-api/
- Figma · Zoom & view options（缩放/视图惯例）: https://help.figma.com/hc/en-us/articles/360041065034-Adjust-your-zoom-and-view-options
