# Origin 升级需求 · 优先级与工时评估（v1）

> 日期 2026-06-08。方法：先扫真实代码现状（9 个方向逐一审计、扣到具体文件/字段/函数），再全网调研 2025–2026 行业成熟方案，不靠拍脑袋。工时为**单人含方案+开发+自测**的粗估。

---

## 一、结论先行

**假设**：产品现处「核心内容能力打磨 + 即将面向个人创作者变现」阶段，企业客户非近期重点。若此假设不成立，见 §五。

推荐顺序：

- **P0 立即（合规红线 + 低成本质量快赢）**：AIGC 合规标识（新增·法律红线）→ 敏感词库扩量 → 人物特写占比
- **P1 紧跟（内容质量 + 内容结构地基）**：表情/微动作 → 部/集逻辑
- **P2 变现启动时**：会员积分充值（接真支付）→ 字幕升级 + ASR
- **P3 规模化按需**：知识库语义升级 → 系统 skill 化 → 企业账号

**为什么没按你列的顺序：**

1. 你列在前的「表情 / 特写」确实该早做（质量快赢），保留在 P0/P1。
2. 但代码审计发现「敏感词库」其实已是**四层防线、相当成熟**，真正缺的是法律强制的 **AIGC 合成内容标识**（《标识办法》2025-09-01 已施行，导出成片必须加显隐标识）——这条代码里**零实现**，属合规风险，提为 P0 最高。
3. 「企业账号」是 9 项里改造最大、且只有真有企业客户才回本的，压到 P3，除非企业是近期目标。

---

## 二、现状成熟度总览（扣真实代码）

| 方向 | 代码现状档位 | 关键证据 | 复用价值 |
|---|---|---|---|
| 1 表情/微动作 | 半成品（规则驱动） | `buildShotPerformanceHint` 按关键词拼 `performanceHint`；shot schema 无 `expression/microAction` 字段，情绪靠 `/紧张|愤怒/` 死正则 | 高（规则库可复用） |
| 2 人物特写占比 | 半成品（枚举全·不控占比） | `SHOT_TYPES` 八档枚举+normalize 完整，但占比约束只控运镜（「≥40% 固定镜头」），特写占比/景别分布**零控制零校验** | 高（占比机制现成可复制） |
| 3 敏感词库/审核 | **已较完整（四层）** | 关键词扫描 + `content-sanitize` 规则改写 + `image-safety-rewrite` LLM 中性化 + `safe-image-gen` 自动救回 | 全可复用，扩词库即可 |
| 4 部/集逻辑 | **伪功能** | `episodes` 只是 `data_json` 里一个数组 + 前端字段镜像；无 episodes 表/series 模型/分集 API | 低（需升格为表+API） |
| 5 企业账号 | **几乎为零** | `users` 表无 role/tenant_id/org_id；owner_id 单用户隔离；admin 是平台后台非企业团队 | 低（几乎从零） |
| 6 知识库 | **已较完整（结构化卡片）** | `knowledge_cards` 表 + 12 模块 + 分阶段注入 + 审计 + admin UI；无向量 RAG | 全可复用，可增量加语义层 |
| 7 skill 化 | 半成品（编排骨架） | `registerExecutor` 注册 8 类执行器 + DAG 依赖 + 状态机；但是内部流水线硬接线，非 LLM 可调用 skill | 高（executor 即原子能力，包层 schema 即可） |
| 8 字幕/ASR | 字幕**已较完整**，ASR 没有 | `edit-export` 抽 dialogue→SRT→ffmpeg `burnSubtitles` 真烧录；无任何 ASR/语音转写 | 字幕全复用，ASR 从零接 |
| 9 会员积分充值 | **已较完整（账本真）**，支付占位 | `credits.ts` 真账本 + 扣费已接 5 条生成路径 + 套餐/兑换码/订阅；真支付 Stripe/微信/支付宝是占位 | 账本全复用，只缺接真支付 |

> 成熟度从高到低 ≈ **3 审核 ≈ 9 积分(支付除外) ≈ 6 知识库 > 8 字幕(ASR缺) ≈ 7 编排骨架 > 1 表情 ≈ 2 景别 > 4 分集(伪功能) > 5 企业账号(几乎为零)**。

---

## 三、逐项判断（现状 → 行业做法 → 判断 → 工时 → 依赖）

### 1. 表情 / 微动作 — P1 ｜ 2–3 周 ｜ 需先出方案（改 shot schema）

现状：表情靠 `buildShotPerformanceHint` 正则 + 提示词文案临时拼，无独立结构化字段，情绪靠死正则匹配、覆盖窄，无法精确指定「第 3 秒挑眉、第 5 秒攥拳」。

行业做法：① 首帧图阶段就把目标表情画出来（Nano Banana / Gemini Flash Image 可保身份不变只改表情，还能批量出表情差分表）；② 图生视频阶段 prompt 写「情绪从 A 到 B 的运动曲线 + 连贯微动作」而非静态表情名词（只写名词模型会把脸演僵）；③ 强情绪特写镜头预留 Wav2Lip / LivePortrait 后处理兜底，口型不必强求 100% 对齐（短剧实践容忍度高，契合你「不硬拦」风格）。模型层：海螺 Hailuo 微表情最强、可灵 2.6 起原生音画。

判断：核心质量项，且复用价值高（`buildShotPerformanceHint` 规则库现成可升级为「表演引擎」内核），性价比好。schema 加 `expression/microAction/gaze` 是设计型改动 → 先出方案审批再动码。

### 2. 人物特写占比 — P0/P1 ｜ 1–1.5 周 ｜ 最小影响面

现状：景别是一等枚举（`SHOT_TYPES` 八档）且有 normalize，但占比约束只控运镜（「≥40% 固定镜头」），特写占比/景别分布零控制零校验，全交 LLM 自由发挥。

行业做法：竖屏短剧强烈偏小景别（人物常占画面 70%+，有实测剧集小景别占七成），因为放大情绪、竖屏全景性价比低、特写+快切制造爆点。AI 分镜做法 = 景别做成必填枚举 + system prompt 下达配比目标（特写类 ~40–60%）+ 生成后量化校验（特写占比低则软提示/补镜）。注意：行业无公开「特写必须占 X%」硬数字，这是产品自定义指标。

判断：**最便宜的质量快赢**。直接复用你现成的「≥40% 占比」机制套到 `shotType` 维度，只读校验不动画面，最小影响面。可并入 P0 这批快赢。

### 3. 敏感词库扩量 — P0 ｜ 1.5–2 周 ｜ 合规相关

现状：已四层防线，但关键词表只 16 个词硬编码在 `.ts` 里（非可维护词库）；文本侧只扫描不改写救回（只图像有）；无第三方审核 API。

行业做法：**二者结合**——云 API 主审（尤其涉政，词库自建扛不住法律风险，交腾讯天御 / 阿里 / 易盾）+ 本地 DFA 词库兜底降本（开源 `houbb/sensitive-word`，6W+ 词、繁简/谐音/拆字变体）。必做**输入 prompt + 输出成片双重审核**；分级路由（高危拦 / 中危 LLM 改写 / 低危放行）；白名单防误杀（古装战争题材天然多「暴力」词）。腾讯天御中文变体识别最强、约 25 元/万条。

判断：基座已成熟，扩量是低成本高确定性、且与合规挂钩的事，P0。

### 4. 部 / 集逻辑 — P1 ｜ 4–6 周 ｜ 需先出方案 · 内容结构地基

现状：**伪功能**。`episodes` 只是 `project.data_json` 里一个数组 + `currentEpisodeIdx` 指针，切集靠前端字段镜像拷贝；无 episodes 表/series 模型/分集 API。世界观存储上已是剧级但「只活在风格圣经这一步，之后跨集断流」（你 v1 设计文档已点明，与代码完全吻合）。

行业做法：四层模型 **Series（剧/真相源）→ Season（季，短剧多单季但留字段）→ Episode → Shot**，各层带 tenant_id + 外键。资产三库分离（角色/场景/物品）挂 Series 级，各集「引用」而非「拷贝」，Episode 继承 Series 基线 + 存 override（渲染 = 基线 ∪ override），契合你「硬锁/软默认/手改优先」。导航三级：剧库 → 集列表（卡片带集号/状态/钩子悬念）→ 分镜时间线。出海短剧（ReelShort/DramaBox）单部 60–100 集按付费墙分块。

判断：这是**内容结构地基**，现在是假的，越晚做越多代码耦合到 JSON 数组 hack 上；v1 方案已定稿、是你相对纯播放平台的护城河。值得在 P1 把数据模型坐实。数据模型大改 → 先出方案。

### 5. 企业账号 — P3 ｜ 6–8 周+（仅 tenant_id 地基 2–3 周）｜ 看业务节奏

现状：几乎为零。纯手机号单用户，`users` 表无任何 role/tenant/org 列，owner_id 隔离；admin 是独立平台后台（无 RBAC）。

行业做法：tenant_id 行级隔离（中小团队事实默认、改造面最小）；层级「组织 → 角色 → 用户」，RBAC0 起步，**角色与权限解耦**——业务写 `has_permission("asset.delete")` 严禁写 `if role=="owner"`；基础角色 Owner/Admin/Member/Viewer；资产/积分归属从 user 上移到 org；用量打 tenant_id + user_id 双维度；企业版常见 SSO（钉钉/企微/飞书）、发票、用量看板、审计日志。迁移要点：每个现有手机号自动建「个人组织」，个人 = owner，**个人版/企业版同构一套码**。

判断：9 项里改造最大、回本最依赖「真有企业客户」，压到 P3。**但提醒**：tenant_id 这层地基越晚铺、耦合越多越贵——若企业客户在 6 个月路线图内，建议把「个人=个人组织 + 全表 tenant_id」这层廉价地基提前单独做掉（一次性回填，符合你不做老兼容的习惯），全套 RBAC/SSO 后置。

### 6. 知识库系统 — P3 ｜ 2–3 周 ｜ 依赖部集模型

现状：已较完整，结构化卡片（非向量 RAG）——`knowledge_cards` 表 + system/user scope + 12 模块 + lifecycle/version + 分 13 阶段注入 + token 预算 + 审计 + admin UI 齐。缺：不做语义检索；世界观卡还没真正接进 promptBlock（默认偏保守）。

行业做法：设定一致性本质是「实体-关系-时间线」结构化问题，**不是纯语义检索**——结构化卡片精确字段注入比向量更可控可追溯，覆盖 80% 需求。务实分层：结构化卡片为主（已有）+ `sqlite-vec` 语义兜底（只用于模糊回忆历史剧情），中文向量用 Qwen3-Embedding/Reranker；别为 RAG 引外部向量库（你 SQLite 起步，`sqlite-vec`/`sqlite-rag` 自包含零运维）；多剧多集真撑不住再上 pgvector / GraphRAG。

判断：基座已经很好，**别推翻**。真正该做两件小事——把世界观卡接进 promptBlock（补断流）+ 加 `sqlite-vec` 语义兜底，都是增量。依赖部集模型（集级知识快照），可随部集一起做。

### 7. 系统 skill 化 — P3 ｜ 3–4 周 ｜ 架构演进

现状：半成品骨架。有 `registerExecutor` 执行器注册表（8 类原子能力）+ DAG 依赖 + 状态机 + SSE，但是内部流水线硬接线，不是 LLM 可动态编排的 skill/tool（无 function-calling）。

行业做法：分清两标准——**MCP**（连接层，把内部 API 包成 agent 可统一调用的 server：tools/resources/prompts）+ **Agent Skills（SKILL.md）**（封装复合工作流，渐进式披露省 context）。分层：Tool = 无状态原子函数，Skill = 高层工作流内部调多个 tool。边界三条：权限（绑身份 scope）、计费（接你 credits，别让自动编排绕过到账路径）、可观测（全链路落日志）。代表：Block 内部 MCP 化项目完成率 +25%。

判断：`registerExecutor` 是极好的 skill 化底座，但这是架构演进、非用户可感的质量/变现项，无明确 agent 产品方向前不紧迫。建议增量：先把 2–3 个高频复合流程（自动配 BGM / 一致性校验 / 逐镜头提示词优化）封成 skill 跑通，别一上来造大平台。skill 声明 modelRole 而非硬编码（契合你 CLAUDE.md 治理）。

### 8. 字幕台词 + ASR — P2 ｜ 2–3 周 ｜ 部分依赖 TTS（见 §六）

现状：字幕已较完整且是真烧录（dialogue→SRT→ffmpeg `burnSubtitles`，带字体栈/双行/自适应字号/台词保护）；ASR 完全没有，字幕来源是剧本台词文本不是音频识别——所以用户上传的无台词素材没法自动出字幕。

行业做法：关键判断——你已有脚本台词，**默认不该靠 ASR 从零转写**。脚本链路走「TTS 时间戳优先（很多商用 TTS 直接回字/词级时间边界，连对齐都省）/ 否则 forced alignment（WhisperX 词级，亚 100ms）」。ASR 真正用武之地：① 上传的无脚本外部素材转写；② 对 TTS 吞字/错位做校正核对（ASR 结果与脚本 diff）。ASR 选型：云端火山豆包（字级时间戳、与你火山生态同源）/ 离线省钱上 FunASR + SenseVoice（带情感）/ 最高精度 FireRedASR。字幕内部用 ASS、ffmpeg `subtitles` filter 烧录。

判断：字幕主路径已通，「ASR 引入」没你想的那么急——它解决的是「上传素材/对齐校正」，不是主链路。先把字幕样式/断句升级（小），ASR 作为 P2 增量。**注意它和 TTS 强相关**：真正的体验闭环是 TTS 配音 → 时间戳 → 字幕，建议和 TTS（§六-2）一起规划。

### 9. 会员 / 积分 / 充值 — P2（变现就提 P0）｜ 2–3 周 ｜ 变现启动时

现状：已较完整，积分是真账本（`getBalance/charge/refund/grant`，事务防双花，已接 5 条生成路径，套餐 + 加油包 + 兑换码真到账 + 订阅 cancel/resume）；唯一缺口 = 真支付占位（Stripe 只建单返 `about:blank`，微信/支付宝「暂未实现」）。

行业做法：国产 AIGC 三件套范式（可灵/即梦/剪映高度一致）——三种积分有效期分层（每日免费当日清零 / 订阅赠送 30 天 / 充值 2 年），消耗顺序**先扣快过期**；汇率 1 元 = 10 积分；扣费三段式「预扣冻结 → 成功结算 → 失败全额退」；定价按模型档位 × 时长 × 分辨率。**支付红线**：iOS App 内虚拟商品必须走 IAP（苹果税 15–30%，必须分端定价，Web/安卓走微信支付宝直连）；对账以支付流水为准 + 幂等核销防重复到账；防薅羊毛用「每日免费而非一次性大额试用」+ 设备指纹。关注 2025.11 微信小程序 iOS 虚拟支付新通路。

判断：账本已成熟，只差接真支付这一段——确定性高、回本直接，但要不要现在做取决于「是否准备开始收费」。准备变现就提 P0，否则 P2。

---

## 四、推荐排期（单人节奏，假设个人创作者变现优先）

| 阶段 | 周期估 | 内容 |
|---|---|---|
| **P0** 合规+快赢 | ~4–5 周 | AIGC 合规标识(1–1.5w) · 敏感词库扩量(1.5–2w) · 人物特写占比(1–1.5w) |
| **P1** 质量+地基 | ~6–9 周 | 表情/微动作(2–3w) · 部/集逻辑(4–6w) |
| **P2** 变现启动 | ~4–6 周 | 会员积分充值接真支付(2–3w) · 字幕升级+ASR(2–3w) |
| **P3** 规模化按需 | ~11–15 周 | 知识库语义升级(2–3w) · skill 化(3–4w) · 企业账号(6–8w) |

> 工时是单人含方案+开发+自测的粗估。P3 三项中企业账号最大、最该按业务节奏决定是否启动。

---

## 五、什么情况下顺序会变（关键假设）

上面假设「个人创作者变现优先、企业非近期」。两种翻转：

- **若近期就要开始收费变现** → 会员积分充值（接真支付）从 P2 提到 P0，和合规一批做。
- **若近期要拿企业/B 端客户** → 企业账号的 tenant_id 地基从 P3 提到 P1 先铺（全套 RBAC 仍可后置），越晚铺耦合越多。

其余（内容质量 P0/P1、知识库/skill 化 P3）基本不受业务节奏影响。

---

## 六、遗漏的重要模块（对照代码 + 行业标准，9 项之外）

**1. 【红线·必补】AIGC 合成内容标识** — 代码里**零实现**（只有关掉 provider 水印的 config 和「禁止模型加水印」的 prompt，没有合规标识）。《AI 生成合成内容标识办法》2025-09-01 已施行，导出的图/视频/文本必须加**显式标识**（界面可感知角标/文字）+ **隐式标识**（文件元数据水印），平台分发端还要核验。法律强制、不做可能下架/罚款，优先级应在所有内容功能之上。工时 ~1–1.5 周。

**2. 【短剧核心·决策点】TTS 配音体系** — 代码里**没有独立 TTS/语音合成模块**，现在「配音」是靠提示词让视频模型自带音（grok-video 音画同出 / 可灵 2.6 原生音频）。但行业共识：原生音频模型对中文对白音色可控性弱于专业 TTS，主流短剧仍外挂 TTS 更可控。没有可控配音 → 字幕时间戳、情绪语气、音色一致都难闭环。**建议明确决策**：走「外挂 TTS（火山/魔搭 CosyVoice 等）+ 时间戳 → 字幕」闭环，还是赌视频模型原生音频。这条直接决定 §三-8 字幕/ASR 的做法。工时（若接外挂 TTS）~2–3 周。

**3. 【合规闭环·缺口】成片输出侧审核 + 人工复核队列** — 输入/图片审核已成熟，但**视频成片输出侧审核零命中**（admin 的 problem-queue 是任务异常不是内容审核）。图生视频常在中间帧出问题，只审输入不审输出有漏网风险。建议成片导出前过一道图片/视频审核 + 疑似违规进人工复核队列。工时 ~1–2 周（可并入敏感词那批）。

**4. 【运维风险·建议】数据备份/容灾** — `data/` 目录里有 **19 个** broken/recovered/restore/bak 文件（`qd.sqlite.broken-postrestore` 等），记忆里也有过数据库损坏恢复记录。SQLite 单库并发写下损坏是真实发生过的风险。商业化前建议把「定时备份 + WAL 校验 + 一键恢复」做成例行，别等真丢数据。工时 ~1 周。

**5. 【变现配套·提醒】用量/成本可观测 + 风控** — 积分账本有了，但按 provider 真实成本的「每次生成成本归集、毛利看板、异常薅羊毛风控」在变现规模化时必需（admin 有 token-stats/time-stats 雏形可扩）。不是独立大模块，和会员积分一起完善即可。

> 补充：若产品还含 C 端播放/分发侧，则「内容分发/播放端」是另一大块；但看代码现状 Origin 是创作端工具，这块可能不在范围——按你实际定位判断。

---

## 七、主要参考来源

- 合规：[AI 生成合成内容标识办法（网信办）](https://www.cac.gov.cn/2025-03/14/c_1743654684782215.htm)、[生成式 AI 暂行办法（网信办）](https://www.cac.gov.cn/2023-07/13/c_1690898327029107.htm)
- 内容安全：[文本审核厂商对比（腾讯云）](https://cloud.tencent.com/developer/article/2663004)、[sensitive-word（DFA 词库）](https://github.com/houbb/sensitive-word)、[数美 AIGC 安全围栏](https://www.ishumei.com/new/m/product/solution/aigc)
- 知识库/RAG：[sqlite-vec](https://github.com/asg017/sqlite-vec)、[sqlite-rag 混合检索](https://github.com/sqliteai/sqlite-rag)、[Qwen3 Embedding/Reranker](https://zhuanlan.zhihu.com/p/1916627276335945288)
- skill 化：[MCP 官方介绍（Anthropic）](https://www.anthropic.com/news/model-context-protocol)、[Agent Skills 工程博客](https://www.anthropic.com/engineering/equipping-agents-for-the-real-world-with-agent-skills)
- 视频质量：[AI 视频模型对比 2025](https://www.dreamega.ai/blog/ai-video-model-comparison-2025)、[竖屏短剧美学（澎湃）](https://www.thepaper.cn/newsDetail_forward_30425589)、[分镜 shot type 指南](https://www.katalist.ai/post/storyboard-shot-types)
- 字幕/ASR：[WhisperX 词级对齐](https://github.com/m-bain/whisperX)、[火山录音文件识别·字级时间戳](https://www.volcengine.com/docs/6561/1354868)、[6 开源中文 ASR CER 对比](https://cloud.tencent.com/developer/article/2642961)
- 商业化：[即梦积分规则](https://zhuanlan.zhihu.com/p/2014430896900567800)、[可灵定价](https://soravideo.art/zh/blog/kling-3-pricing)、[苹果 IAP 规则](https://zhuanlan.zhihu.com/p/721062621)
- 企业账号：[SaaS 多租户隔离（腾讯云）](https://cloud.tencent.com/developer/article/2637586)、[RBAC 设计（Logto）](https://docs.logto.io/authorization/role-based-access-control)

---

## 八、增补 v1.1：新增两项评估 + 统一「一致性引擎」建议

### 关键判断：这两项 + 表情/特写 + 已有首尾帧校验 = 同一套地基

代码审计发现：你新提的「角色一致性评分抽卡」其实就是仓库里**已经跑通的首尾帧一致性校验**（`frame-consistency-check.ts`：喂图→视觉模型出 `characterScore/sceneScore/propScore` 0–100 + grade + fail 串行重试≤2 写 needs_review）的一部分——角色分只是这套「三合一」校验里的一档，**没有独立模块**。而「空间方位」的 Tier C 校验门，方案文档自己写明「仿 `character-consistency-gate` + 复用 `image-caption`」。再加上「表情/微动作」「特写景别」也都挂在**同一批文件**（`character-consistency.ts` 的 performanceLock、`reference-matcher` 的 closeUpBoost、`shotSchema`）上。

所以**别把它们当 4–5 个独立任务**。最省事的打法：在 P1 集中建**一套统一的「生成 → 打分 → 抽卡重生 → needs_review」框架**，让身份一致性、空间方位、表情、景别都挂上去复用。既符合你「最小影响面」（一套门、不重复造），也是行业（NVIDIA Video Storyboarding、各短剧产线）的通行结构。合并建设可省约 1/3 重复工。

### 新增1：角色一致性 收紧 + 评分 + 抽卡 — P1 ｜ 3–4 周 ｜ 需先出方案

现状（半成品偏成熟的「门」，但「抽卡」零实现）：评分门已跑通（LLM 视觉判别、0–100 分、阈值硬编码 角色<60 fail/<75 warn、fail 串行重试≤2 写 needs_review）。**四个硬伤**：① 重试是「改提示词重生一张」的**串行**，不是 best-of-N **抽卡**（并行生 N 张挑最优）；② 全靠 LLM 判别，**没有人脸相似度硬门槛**；③ 角色分和 scene/prop 挤在同一次「三合一」校验里，**没拆成独立角色门**；④ 阈值/重试次数**硬编码、不可配**（只有超时可配）。

行业做法：**人脸相似度做硬门槛 + VLM 做软仲裁**。脸用 ArcFace/InsightFace 余弦相似度（同一人经验阈值 cosine ≥0.6、视频帧均 ≥0.7）一票否决；服饰/发型漂移用 CLIP/DINO 补（ArcFace 只看脸、看不到衣服）；整体气质与最终排序交 VLM。**抽卡 best-of-N：默认 N=4，主角特写 N=6–8**，低于阈值自动 reroll；关键提醒——**打分器准度比 N 更重要**，打分器不准时 N 越大越糟（过度优化）。角色锁定分级：主角走「角色档案 + LoRA + IP-Adapter」三重锁（产线实测跨镜头相似度 96%+），配角/路人走 InstantID/PuLID 零训练。

判断：直接提升核心产出质量、且复用已有评分骨架，性价比高，P1。**其中两件小事可立刻做（P0 级快赢）**：阈值/重试次数入 env 可配、角色维度从三合一拆出独立门（合计 ~1 周）。**新依赖提醒**：接 ArcFace 要部署人脸模型（GPU）或调云端人脸相似度 API——这是这项唯一的新基建成本。改生成循环属设计型改动 → 先出方案。

### 新增2：场景/人物/道具 空间方位（不漂移）— Tier A：P0/P1 0.5–1 周；Tier B/C：P2 3–5 周 ｜ 需先出方案

现状（仅占位）：仓库已有 `空间一致性-人物道具场景方位-方案.md`，但文档自己写明「**本轮不动代码**」，所以 Tier A/B/C **一行没落**，方位字段全仓 0 命中，纯靠自由文本提示词即兴；唯一相关的 `continuity/check-adjacent` 是**纯文本 LLM 检查**（不喂图、只出 warning、不防生成）。这是新增两项里最不成熟的。

行业做法 + **能力边界（重要）**：理论最强是「布局条件化」（GLIGEN / bounding-box / ControlNet / 区域提示）把「谁在画左/画右/朝向」显式喂给模型——**但这类强控基本只在开源 SD 生态可用，你走的火山 Seedance/即梦等闭源云模型接不进 GLIGEN/ControlNet**。所以在你的链路里只能走「近似」路线，务实四步（按改动从小到大）：① **首帧布局即锚点**——首帧图就是空间真相源，Seedance first-frame 模式从首帧往后演，空间关系天然继承；② **prompt 显式写方位与相对关系**（不写「两人对话」，写「A 在画左面朝右、B 在画右面朝左、桌子在中间」）；③ 分镜层加「画左/画右/朝向」字段 + 180° 轴线**软提示**（契合你不硬拦原则，参考 NolanAI 跨轴报警）；④ 生成后用 **GroundingDINO 几何定位（中心点位移判左右上下）+ VLM 复核**做方位校验门——注意 **VLM 单独判空间关系接近随机，必须几何验证兜底**。第④步正好复用新增1 的「生成→打分→重生」框架。

判断：Tier A（同画面去重 + 反重复负向词 + prompt 写方位）是**低成本快赢**（你现在 `constraints.negative` 槽位还空着，灌进去即可），可提前到 P0/P1；Tier B（结构化调度层）+ Tier C（空间校验门）受 provider 能力限制、ROI 不确定，放 P2，且**强烈建议先出方案再动**（方案文档也是这态度）。

### 更新后的排期（11 项）

| 阶段 | 内容 |
|---|---|
| **P0** 合规+快赢 | 合规标识 · 敏感词扩量 · 特写占比 · **空间方位 Tier A**（去重+方位提示） |
| **P1** 一致性引擎+地基 | **角色一致性评分抽卡** · 表情/微动作 · 部/集逻辑　〔前两项 + 特写 + 方位校验共用同一套打分/抽卡框架〕 |
| **P2** 变现+方位进阶 | 会员积分接真支付 · 字幕+ASR · **空间方位 Tier B/C**（调度层+校验门） |
| **P3** 规模化按需 | 知识库升级 · skill 化 · 企业账号 |

### 增补来源

- 一致性评分/抽卡：[Face-Sim/ArcFace 阈值](https://zenn.dev/taku_sid/articles/20250511_face_sim_metric?locale=en)、[跨场景人脸一致 CSFD 基准](https://arxiv.org/pdf/2505.11425)、[best-of-N test-time scaling](https://arxiv.org/pdf/2511.11483)、[InstantID/PuLID/FaceID 对比](https://apatero.com/blog/instantid-vs-pulid-vs-faceid-ultimate-face-swap-comparison-2025)、[短剧三重锁定 96%](https://tech.china.com/articles/20251224/202512241786827.html)
- 空间方位：[GLIGEN 布局定位](https://voxel51.com/blog/towards-controllable-diffusion-models-with-gligen)、[可控视频生成综述 2025](https://arxiv.org/pdf/2507.16869)、[SpatialReward：GroundingDINO 几何验证+VLM](https://arxiv.org/pdf/2603.22228)、[NolanAI 180° 轴线报警](https://cinemagics.com/film-school-the-180-degree-rule/)、[Seedance 2.0 防漂移指南](https://magichour.ai/blog/how-to-use-seedance-20)

---

## 九、增补 v1.2：图片局部修改 + 视频局部修改

### 一个把两项串起来的判断：视频局部修改 ≈ 图片局部修改 + 只重生这一镜

在你「先出首帧图 → 图生视频」的链路里，视频局部修改最务实的形态**不是去找"视频 inpaint"，而是改首帧图的局部 → 只把这一镜重新图生视频**。所以这两项是一对：把「图片局部修改」做扎实，「视频局部修改」的基础档几乎是白送；真·视频区域编辑（成本高、provider 受限）留作 P3 灰度增值。

### 关键代码事实：你现在的图片 API 没有 mask 局部重绘能力

审计确认：① `lib/image-gen.ts` 已有 `/images/edits` 端点接线（走 OpenAI 系 gpt-image/dall-e）+ 整图 i2i（toolbox `image_to_image`、首尾帧参考图）；② `video-gen.ts` 有 `[face-mask]`（用 canvas 给参考图做人脸遮罩、防 Seedance 乱改脸）——**mask 生成技术栈已在**；③ 但「首尾帧编辑」（`first-frame-edit-draft`/`frames/edit-draft`）本质是**改提示词 + 换参考图槽位 → 重生整图**（promptOverride/slotPatch/reconcile），**不是像素级局部重绘**；④ 用户侧的涂抹/框选 mask、真·inpaint，全仓没有。

调研出一个必须知道的前提：**你现在用的火山 Ark Seedream（`/images/generations`）根本没有 mask、没有独立 edits 端点，只能做整图指令编辑**；要"局部重绘"，得接火山的**另一套「即梦AI」接口**（交互编辑 inpainting，文档 85621/1976207），那才是火山生态里对口 mask 局部重绘的 API。

### 新增3：图片局部修改 — P2（MVP 可提前 P1）｜ 3–4 周 ｜ 需先出方案

现状（半成品）：整图编辑/i2i 有（`/images/edits` 接线、`image_to_image`、首尾帧改 prompt 重生）、mask 生成栈有（face-mask canvas）；缺真·局部 inpaint、mask UI、对口的火山即梦AI 接入、局部改时锁其余区域。

行业做法：2025-2026 业界倒向 **instruction-based（无需手画 mask、一句话改图）**；mask-based（涂抹遮罩）仍是精修刚需。三级落地：① **方案一（最小改动，MVP）**——直接用现有 Ark Seedream 整图指令编辑（改属性/换装这类容忍度高的场景），~1–1.5 周，零新接入但整图重渲、精度有限；② **方案二（可控边界）**——接火山「即梦AI」inpainting + 前端画笔/框选产 mask，mask 外严格不动，~3–4 周，火山生态内最对口；③ **方案三（质量拉满）**——外接 Flux Kontext（指令）/ Nano Banana，角色一致最稳，但引新供应商，走 model-routing 治理。

判断：高频刚需、与一致性引擎强协同（局部改 = 最精准的"重生"），ROI 高。整体 P2；方案一 MVP 可在 P1 搭一致性引擎时顺手做。新依赖：接即梦AI 需新 `modelRole` + mask 前端。设计型 → 先出方案。

### 新增4：视频局部修改 — P3 ｜ L0 2–3 周（随新增3顺带）/ 真 API 2–3 周 ｜ 需先出方案

现状（基本没有）：剪辑（`edit-edl`/online-editor/VevDemo）只到**时间线层**（trim/转场/BGM/字幕，不碰画面内容）；视频改动是**整段（segment）重生**；无任何像素级/区域视频编辑。

行业做法（修正一个旧认知）：闭源云"真·mask 像素 inpaint"仍罕见，但**语言驱动的视频局部编辑已成气候且有 API**——Runway Aleph（Replicate API）、可灵 Kling O1（fal 独家，宣称不用 mask 做 pixel-level 改）、阿里 Wan-VACE（开源 + 百炼 API，视频重绘/主体移除）。三级落地：**L0（首选）** 改首帧局部 → 只重生该镜头（复用你现成链路，不需任何视频 inpaint API，blast-radius 最小，绝大多数"改道具/去穿帮"够用）；**L1** 静态背景 object removal（开源 ProPainter/DiffuEraser 或 Wan-VACE API）；**L2** 真要"一句话改画面里一个物体"接 Aleph/Kling O1/Wan-VACE（贵、整段重渲、快速运动不稳，当灰度增值非主链路）。

判断：最不成熟、最贵、provider 受限；但 L0 是新增3 的副产品。放 P3：先白送 L0，真·视频编辑 API 留作灰度。强依赖 新增3 + 整段重生链路 + 一致性引擎（改完要校验）。

### 更新后的排期（13 项）

| 阶段 | 内容 |
|---|---|
| **P0** 合规+快赢 | 合规标识 · 敏感词扩量 · 特写占比 · 空间方位 Tier A |
| **P1** 一致性引擎+地基 | 角色一致性评分抽卡 · 表情/微动作 · 部/集逻辑〔可顺带 图片局部改 MVP〕 |
| **P2** 变现+增值 | 会员积分接真支付 · 字幕+ASR · 空间方位 Tier B/C · **图片局部修改** |
| **P3** 规模化按需 | 知识库升级 · skill 化 · 企业账号 · **视频局部修改**（L0 随新增3，真 API 灰度） |

### 增补来源

- 图片编辑：[火山即梦AI 交互编辑 inpainting（85621/1976207）](https://www.volcengine.com/docs/85621/1976207)、[Ark Seedream 统一编辑·无 mask 说明](https://docs.apiyi.com/api-capabilities/seedream-image/overview)、[Flux Fill](https://docs.bfl.ml/flux_tools/flux_1_fill)、[Flux Kontext](https://bfl.ai/flux-1-tools/)
- 视频编辑：[Runway Gen-4 Aleph API](https://replicate.com/runwayml/gen4-aleph)、[Kling O1 视频编辑 API（fal）](https://blog.fal.ai/introducing-kling-o1-video-available-exclusively-as-an-api-on-fal/)、[Wan-VACE 视频编辑 API（阿里云）](https://help.aliyun.com/zh/model-studio/wanx-vace-api-reference)、[ProPainter](https://github.com/sczhou/ProPainter)、[DiffuEraser](https://github.com/lixiaowen-xw/DiffuEraser)

---

## 十、增补 v1.3：日志监控 / 可观测性

### 现状：埋点底子不错，但工业级三件缺失，"告警"只是被动展示

审计确认 Origin 已有不少自研埋点，但都停在"自己埋、落 SQLite、admin 手搓页面"这一层：

- 系统日志 `sys-logs.ts` 是**内存环形缓冲（1000 条上限）**，钩 console.* 进环、带脱敏，`/api/auth/admin/logs` 读——**重启即失、非结构化、不持久不集中**（作者注释也写明"上线规模大时再换 pino + 文件 + 切割"）。全仓 **345 处 console.***，散落非结构化。
- 已有指标埋点：`observability-events.ts`（`recordObservabilityEvent` + `modelMetricsSnapshot` + 保留期清理）、`token-usage.ts`（按用户/模型归集成本）、`service-heartbeat.ts`（15s 心跳落库）、`image-perf` ndjson 埋点、admin 的 stats/time-stats/token-stats/problem-queue/key-pool。
- **三个工业级缺口**：① **错误追踪零接入**（无 Sentry，345 处 console.error 不聚合、不去重、无堆栈）；② **结构化持久日志缺失**（内存环不算）；③ **没有主动告警**——admin 里的"Key 池告警/模型 fallback 告警"只是**打开页面才看到的被动文本**，生成大面积失败/DB 异常/扣费异常**不会主动通知任何人**。

> 一句扎心的话：你 `data/` 目录里有 19 个损坏/恢复的 DB 文件、却一直没被及时发现，**根因就是没有主动告警**。这条不是锦上添花，是商业化前的"别盲飞"。

### 行业做法 + 给 Origin 的最小栈（80/20）

四支柱：结构化日志 / 指标 / 链路追踪 / 错误追踪；**OpenTelemetry 是统一格式标准**，SDK 选型优先 OTel 兼容，但小团队**不必一开始自建 Collector + Grafana 全家桶**。务实分级：

- **核心档（上线前必须有，1.5–2 周）**：① **Sentry**（`@sentry/nextjs`，错误追踪事实标准、小团队最划算，复用你前端 `?v` 版本号做 release 归因）；② **pino 结构化日志 + trace_id 贯穿**（替换内存环，JSON 持久化）；③ **主动告警** 接飞书/钉钉群机器人 + 最高级叠短信（你已接腾讯云短信可复用），覆盖 生成失败率突增 / DB 异常 / 扣费异常 / 拨测 down；④ **拨测** Uptime Kuma（512MB 自建）或火山拨测，盯首页/关键 API/兑换码回调；⑤ AIGC 业务指标在已有 `observability-events`/`token-usage` 上补几张 admin 图（生成成功率、各 provider 时延错误率、队列积压、扣费异常）。
- **进阶档（商业化跑起来，P2，2–3 周）**：接 **火山 APMPlus（Node.js、同生态、有免费额度、境内合规）+ 托管 Prometheus（VMP）+ 日志服务 TLS**，把指标/Trace/告警挪到托管平台，告警升级为多窗口分级 + runbook。
- **专用 LLM 可观测（P3，按需）**：等剧本/分镜**提示词生成**的成本与 prompt 迭代成痛点，再上 **Langfuse 自建版**（数据境内）；现在你管线以图像/视频生成为主，这类工具收益错位，先别买单。

### 判断与定位 — P1（核心档；其中"告警+错误追踪"够格提到 P0）｜ 核心 1.5–2 周 / 进阶 2–3 周

判断：这是商业化前的"运维地基/保险"，核心档便宜。**其中"主动告警 + 错误追踪"建议直接当 P0 做掉**——它直接堵住"静默故障"（DB 损坏、扣费异常都属此类），与你已知的"DB 备份红线""兑换码才是真到账路径"强绑定。进阶档（托管平台/看板/tracing）放 P2。复用价值高：`observability-events`/`token-usage`/`heartbeat`/admin stats 是现成指标底座，主要是接 Sentry + pino + 告警渠道。

> AIGC 业务指标（生成成功率、provider 时延错误率、扣费异常）比通用 APM 更该优先，且你已有埋点底子——这是 Origin 最该自己动手、也最省事的一块。这也正好把 §六 遗漏里的"用量/成本可观测 + 风控""数据备份告警"一并收口。

### 更新后的排期（14 项）

| 阶段 | 内容 |
|---|---|
| **P0** 合规+快赢+保险 | 合规标识 · 敏感词扩量 · 特写占比 · 空间方位 Tier A ·〔**日志监控-告警+错误追踪** 建议并入〕 |
| **P1** 一致性引擎+地基 | 角色一致性评分抽卡 · 表情/微动作 · 部/集逻辑 · **日志监控（核心档）** |
| **P2** 变现+增值 | 会员积分接真支付 · 字幕+ASR · 空间方位 Tier B/C · 图片局部修改 ·〔日志监控-进阶档〕 |
| **P3** 规模化按需 | 知识库升级 · skill 化 · 企业账号 · 视频局部修改 ·〔LLM 可观测 Langfuse〕 |

### 增补来源

- [火山引擎 APMPlus（接入/告警/免费额度）](https://www.volcengine.com/docs/6431/69088)、[Sentry 错误追踪 97% 份额](https://6sense.com/tech/error-an-exception-monitoring/sentry-market-share)、[Next.js 接 pino/OTel 日志](https://signoz.io/blog/opentelemetry-nextjs-logging/)、[Google SRE 多燃尽率告警](https://sre.google/workbook/alerting-on-slos/)、[PrometheusAlert 国内钉钉/飞书/短信转发](https://github.com/feiyu563/PrometheusAlert)、[Langfuse 成本/链路追踪](https://langfuse.com/docs/observability/features/token-and-cost-tracking)、[阿里云 ARMS 计费（50GB 免费）](https://help.aliyun.com/zh/arms/product-overview/product-billing-new-version)
