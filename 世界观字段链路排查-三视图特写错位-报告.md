# 世界观字段链路排查 · 三视图被特写图顶替 · 报告

日期：2026-06-10。性质：**只排查不动码**。触发：资产页从世界观导入的角色卡，"三视图"位置显示的是特写图，且标"已完成"。

## 0. 一段话结论

你的猜测是对的，而且比你猜的更系统：**资产侧和世界观侧对同一个字段名 `realPhotoUrl` 的语义不一致**（资产侧=整张三视图原图；世界观侧=特写优先的预览图），导入时这个"预览特写"被原样写回资产的 `realPhotoUrl`，前端卡片又最优先读这个字段，于是封面和三视图槽位都显示特写。更糟的是注入逻辑会**伪造** `panels.sheetUrl`（拿特写冒充三视图切片）并把 `reference.status` 直接标 `ready`，导致：UI 显示"已完成"、"生成全部图片"跳过该角色（真三视图永远不会生成）、下游选角/首尾帧参考全部被假三视图污染。文字字段同样有一条 role/identity/description 互相回填的"塌缩链"，截图里萧云的"role"显示整段简介、identity 和 description 重复，就是它造成的。

## 1. 背景：两侧的字段契约（先对齐口径）

**资产侧**（正常生成一个角色后，`lib/character-reference-update.ts:266-275`）：

| 字段 | 含义 |
|---|---|
| `imageUrl` / `rawUrl` / `realPhotoUrl` | 三个字段存**同一个 URL = 整张三视图原图**（pencilUrl 同值，转绘层已摘除的兼容残留） |
| `panels.sheetUrl / headshotUrl / frontUrl / sideUrl / backUrl` | 从原图切出来的分片，`panels.schema` 标记切片协议 |
| `reference.status` | `ready` 表示"有合格三视图可供下游引用" |

前端卡片（`public/modules/assets.js:558, 962-963, 1078-1083`）只取**一张图**（`originalUrl → realPhotoUrl → rawUrl → imageUrl` 第一个非空），封面用 `object-[left_top]` 裁左侧、三视图槽位用 `object-[right_center]` 裁右侧——设计前提是这张图是**宽幅三视图**。

**世界观侧**（项目沉淀成模板时，`lib/world-templates-db.ts:822-864`）：

- `referencePanels` 只从**角色锁的 referenceLock** 搬运（`:811-820, :828`），**从不读资产自己的 `panels`**；
- `realPhotoUrl` 被重定义成"预览图"：`firstText(headshotUrl, frontUrl, sheetUrl, asset.realPhotoUrl, …)`（`:829, :853`）——**特写优先**。这个语义还被 `lib/knowledge/__tests__/world-templates.test.js:151` 用断言锁死了（期望 lin-head.png）。

冲突就在这：同名字段，一侧是"三视图原图"，一侧是"特写预览"。

## 2. 截图 bug 的完整链路（逐步实锤）

1. **存模板**：`mapCharacterLockToWorldCharacter` 把 headshot 特写写进世界观角色的 `realPhotoUrl`（`world-templates-db.ts:829,853`）。若角色没锁/锁里 referenceLock 为空，连真三视图都不会进模板（`mapCharacterAssetToWorldCharacter :866-880` 也不读 `asset.panels`）。
2. **导入项目**：提取资产时注入（`app/api/assets/extract/route.ts:310` → `lib/world-asset-injection.ts`）：
   - `applyWorldImage` 把 `world.realPhotoUrl`（特写）直接写进资产 `realPhotoUrl`（`:194`）；
   - `buildInjectedPanels` 在模板没有 `referencePanels.sheetUrl` 时，**用特写冒充** `sheetUrl` 并打上 `human-character-sheet-v1` 协议标（`:164, :171-178`）；
   - `reference.status` 一律写 `'ready'`（`:203-210`）。
3. **前端展示**：卡片首选 `realPhotoUrl` → 封面和"三视图"槽位都是特写；`reference.status=ready` + 有图 → 状态标"已完成"（`assets.js:989`），顶部"生成完成 10/10"把它计为完成。
4. **卡死**："生成全部图片"只挑 `!item.imageUrl` 的资产（`assets.js:2058`），注入已把 `imageUrl` 填上 → **真三视图永远没机会生成**。

注意：即使模板里带了真三视图（`imageUrl`=sheet、`referencePanels` 齐全），第 2 步照样把特写写进 `realPhotoUrl`，前端照样显示特写——所以这不是"个别脏数据"，是**导入角色 100% 复现**的展示错位。

## 3. 问题清单（按严重度，新发现 vs 已知）

### P0 · 图片链路（全部新发现）

| # | 问题 | 位置 |
|---|---|---|
| 1 | `realPhotoUrl` 语义冲突：世界观侧存特写预览，注入后被资产侧当三视图原图用 | `world-templates-db.ts:829,853` ↔ `world-asset-injection.ts:194` ↔ `assets.js:558` |
| 2 | 伪造三视图：拿任意可用图冒充 `panels.sheetUrl` + 假 schema + 强标 `reference.status=ready` | `world-asset-injection.ts:164-210` |
| 3 | 同款伪造还有两处共享 resolver：世界观→角色锁同步（`character-consistency.ts:849-858`，PUT 项目时经 `projects/[id]/route.ts:164-173` 触发）、资产→默认锁（`character-consistency.ts:514`）。假 sheet 经它们流入选角/首尾帧/视频参考（`reference-matcher.ts:150`、`panel-selection.ts`、`frame-image-plan.ts:297`、`video-prompt-lifecycle.ts:142`） | 同左 |
| 4 | 被注入图的角色退出生成闭环："生成全部图片"跳过 + UI 标"已完成"，用户无从发现也无从补救（只能逐个点"重新生成"） | `assets.js:2058, :989` |
| 5 | 存模板时丢真三视图：只搬锁的 referenceLock，不读 `asset.panels`；没锁的角色模板里只剩特写 | `world-templates-db.ts:811-828, :866-880` |

### P1 · 文字字段塌缩（新发现，截图已实证）

| # | 问题 | 位置 |
|---|---|---|
| 6 | role/identity/description 互相回填：`role = firstText(role, identity, description, intro, name)`、`identity = firstText(identity, description, …)`，空字段被整段简介填充 → 萧云卡 role 显示一句长简介、identity 与 description 重复展示 | `character-lock-authority.ts:206-208`（更新世界观时）、`character-consistency.ts:816-817,825`（世界观→锁）、`world-templates-db.ts:845`（description 兜底 identity） |
| 7 | "资产权威投影"无条件清空：`temperament / actionTraits / aliases / species / canonicalPrompt` 即使资产侧为空也直接覆盖模板值（对比 clothing/equipment 有 hasOwn 守卫），每次"更新世界观"都会把模板这些字段抹掉 | `character-lock-authority.ts:210-226` |
| 8 | `mergeProjectCharacterLocksIntoWorldTemplate` 用 `{...旧, ...新}` 让新值整体碾压模板（含 `canonicalPrompt:''`、空骨架 voiceHint），与"只补缺"方向相反。目前无前端调用方，属带雷接口 | `world-templates-db.ts:1147-1169` |

### P2 · 流程与已知项

| # | 问题 | 状态 |
|---|---|---|
| 9 | 改信息→重新分析的死循环：信息有变时旧图归档、等待重生成（`extract/route.ts:633-650`），但世界观注入紧接着把**旧图**回填 + 标 ready → 修改过外观的导入角色永远拿不到新图 | 新发现 |
| 10 | 保存/更新弹窗 `include.terminology` 恒 `false`（`assets.js:5549-5554, 5627-5634`）且 `terminologyOverride` 前端从不发、后端只认 create（`from-project/route.ts:65-67`）→ 从资产页存的模板术语恒空 | 与 v2 方案 G3/G5 部分重叠，建议并入 v2 |
| 11 | 更新只补缺不覆盖、场景道具编辑进不了候选、弹窗信息项不全 | **已知**，v2 方案已覆盖（待你审批），本报告不重复展开 |
| 12 | 契约测试只锁 happy path：`test-asset-world-injection.ts` 的角色都自带真 `referencePanels.sheetUrl`，"模板只有特写"的退化路径无测试，伪造行为反而被测试默许（`:181-184` 断言 schema/sheetUrl 成立） | 新发现 |

## 4. 修复方向（我的定论，动码前等你确认）

**第一刀（建议先修，最小影响面，全在注入侧 `world-asset-injection.ts` 单文件）：**

1. `applyWorldImage` 角色分支：`realPhotoUrl/imageUrl/rawUrl` 只取**真三视图来源**（`world.imageUrl / world.rawUrl / referencePanels.sheetUrl`），不再取 `world.realPhotoUrl`（预览特写）。
2. `buildInjectedPanels`：只有 `sourcePanels.sheetUrl` 真实存在才写 `sheetUrl` 和 schema；headshot/front 等照搬但不冒充 sheet。
3. 模板没有真三视图时：**文本照常注入，图不填、不标 ready**，让角色留在"待生成"，"生成全部图片"自然补一张符合本项目风格的真三视图。这也是产品语义上更对的行为——特写本来就不该复用为三视图。

效果：模板带真三视图 → 照常秒导入；只有特写 → 正常走生成。第 9 条死循环同时被解掉（注入不再回填旧图）。

**第二刀（需要你拍板的设计项）：**

- 世界观侧字段语义：新增 `previewUrl` 存特写预览，`realPhotoUrl` 回归"资产真图"语义（需同步改 `world-templates.test.js:151` 的断言；按你的惯例不做老模板兼容，老模板重存一次即可）。
- 存模板时补读 `asset.panels`（解 #5，没锁的角色也能把真三视图带进模板）。
- 两处共享 resolver 的伪造点（#3，`character-consistency.ts:514, :849-858`）blast radius 大（角色锁是镜头/视频公共依赖），单列一批修，不混进第一刀。

**第三刀（文字字段，机械收紧）：**

- 三处塌缩链全部改成"只取本字段，空就空"（role=role、identity=identity、description=description||intro）；
- `character-lock-authority.ts` 的无条件清空加非空/hasOwn 守卫；
- #8 的接口要么改成补缺语义要么先下线；
- 术语问题并入 v2 方案一起做。

**配套**：给 `test-asset-world-injection.ts` 补退化用例（模板角色只有 `realPhotoUrl` 特写、无 referencePanels）：断言不伪造 sheetUrl、不标 ready、图保持待生成。

## 5. 验证建议（修复后）

1. `npx tsx scripts/test-asset-world-injection.ts`（含新退化用例）+ `test-world-consistency-sync.ts` + `test-character-lock-authority.ts`；
2. 手测矩阵：模板带真三视图导入（应秒有图）/ 只有特写导入（应待生成→生成全部出真图）/ 导入后改外观重新分析（应归档旧图并重生成）；
3. 回归：已有项目的非导入角色卡显示不受影响（改的是注入和模板投影，不碰正常生成链）。

---

## 6. 落地记录（2026-06-10，已执行）

按"根治不打补丁"原则一次改齐三刀，全部落地：

**字段语义定版**：世界观角色新增 `previewUrl`（特写优先，仅展示用）；`realPhotoUrl` 回归资产侧语义=写实原图；`referencePanels` 在锁的 referenceLock 之外补读 `asset.panels`（没锁过的角色也能把真三视图带进模板），并新携带 `schema/sourceImageId/sourceImageUrl`。注入侧角色只认 `referencePanels.sheetUrl`，有真三视图才填图（imageUrl/rawUrl/realPhotoUrl 三字段同源）+ 标 ready；没有就只注入文本，图留待生成。世界观→角色锁同步同口径：无真 sheet 不写 referenceLock、不标 ready。role/identity/description/appearance 全链路只取本字段，不再互相回填；导出实体过 `compactWorldEntity` 去空值（空值不携带，合并时自动让位）；`mergeProjectCharacterLocksIntoWorldTemplate` 改为逐字段非空合并。

**改动文件**：`lib/world-templates-db.ts`（mapReferencePanels/mapCharacterLockToWorldCharacter/mapCharacterAssetToWorldCharacter/entityPreviewUrl/mergeProjectCharacterLocksIntoWorldTemplate + compactWorldEntity）、`lib/world-asset-injection.ts`（applyWorldImage/buildInjectedCharacterPanels 重写）、`lib/character-consistency.ts`（worldCharacterPatchFromTemplate）、`lib/character-lock-authority.ts`（applyAssetAuthorityToWorldCharacter 收紧 + buildAssetAuthoritativeCharacterLock appearance 不吃简介）、`public/modules/assets.js`（模板预览链加 previewUrl；workspace.html assets.js v158→159）。`defaultLockFromAsset:514` 的 imageUrl 兜底**有意保留**：资产契约下 imageUrl 本来就是三视图原图，违规输入源已在注入侧掐断。

**测试**：契约/回归全绿——asset-world-injection（含新退化用例：模板只有特写→不伪造、不填图）、world-consistency-sync（含新退化用例）、character-lock-authority、knowledge world-templates 17/17、knowledge-context 17/17、world-pending-contract（修了一处**存量红**：风格页 freshness 修复把 `_persistStyleWorldIntent(...)` 改成链式调用后契约字符串没跟上，与本次无关）、world-injection-contract、knowledge-consistency-contract、assets-stale-flags、stale-warning-copy、character-reference-update、knowledge-snapshot-world-count、frame-image-plan 16/16、video-reference-manifest、project-dependency-state、frame-workflow-state 8/8；全仓 `tsc --noEmit` 0 错误。

**遗留与口径**：术语丢失并入 v2 方案处理；老模板不做兼容——存量模板里 realPhotoUrl=特写已无害（注入/锁同步都不再读它），带真三视图的老模板照常导入，只有特写的角色改走"文本进、图待生成"，重存一次模板字段即焕新；浏览器端待手测（资产页删角色图后从世界观重导/重新分析，确认待生成→生成全部出真三视图）。
