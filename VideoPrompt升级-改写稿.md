# VideoPrompt 升级 · 改写稿（供审 · 仍不改代码）

> 承接 `VideoPrompt升级-方案.md` 的决策表。本文给三份可直接落地的稿子：
> ① 正文生成规则瘦身改写（`SP_VIDEO_PROMPT_GENERATE`）；② 统一包装器 composer 拼接说明；③ 特写「便利贴」触发规则与文案。
> 审核通过后再落代码。日期：2026-06-02

---

## 〇、先确认一件最敏感的事：台词不会丢

方案 B 要把「逐字台词」从正文挪到「台词块」。已核对回归测试 `scripts/test-video-prompt-dialogue-regression.ts`：它断言的是 **`finalPrompt`（带块的版本）**，而且测试样例的正文里本来就**没有**台词、台词全靠 `dialoguePairs → 台词块` 提供。

**结论**：只要 composer 继续用 `dialoguePairs` 生成台词块，逐字保真在「提交层」完全不受影响，回归测试照过。唯一变轻的是「复制裸正文」——这你已同意。

---

## 一、正文生成规则瘦身改写（`SP_VIDEO_PROMPT_GENERATE`，lib/prompts.ts:1355）

### 1.1 新版输出结构：8 段 → 6 段

| 段 | 留/删 | 内容（新版） |
|---|---|---|
| 运镜系统 | **留·强化** | 机位/轴线叙述 + **片段内镜头转场**（同轴/越轴/平行、平滑忌快切）+ **机位运动与主体动作分写** |
| 角色 | **删** | 角色外观/服装 → 交「角色一致性主档块」+ 参考图；正文只在镜头叙述里点名字 |
| 场景 | **留·收短** | 环境/光照一句话（提交时块里没有场景，必须留） |
| 镜头 NN | **留·改** | 第一行标题；**删掉 `⟦…⟧` 标签行**；正文写「动作 + 微表情过程 + 神态 + 光感」，**不再抄逐字台词**（只标“某某开口”，台词见块） |
| 基调 | **留·并入画质** | 整体观感 + 色调/光质 + **画质词**（超清/真实皮肤纹理/真实景深/胶片颗粒） |
| 约束 | **留·并入特写防变形** | 原负向 + **特写防变形词**（五官清晰/面部稳定/不扭曲/比例自然） |
| 音障 | **留** | 环境声/无 BGM |

### 1.2 关键规则增删（相对现状）

- **删**：`⟦景别·角度·焦距·景深·运镜⟧` 标签行；「角色」段；「把所有 shot.dialogue 逐字写进正文」的要求。
- **改**：镜头段 = 标题 + 画面叙述（两行制，不再三行带标签）；台词在正文里只标发声时机（“老周沉声开口”），**不转写原文**。
- **加**：① 运镜系统统述**片段内转场**（轴线关系、平滑过渡、忌“快/急”切）；② **机位与主体动作分开写**（Seedance 防抖铁律）；③ 特写**微表情变化过程**（承接 §三 便利贴）；④ 基调并入**画质**；⑤ 约束并入**特写防变形**。
- **保留（不动）**：全中文硬规则、禁英文键值对、一镜一运镜一动作、Image N 引用规范、片段**最后一镜不写转场尾**。
- **字数**：单镜画面叙述收到 **60–110 字**（Seedance 偏好短），整体随段数减少自然变短。

### 1.3 新版「标准答案」示范（老周 · 片段内两镜）

> 这段会替换现有 SP 里的示范块，模型照此结构与文风输出。

```
运镜系统
两镜同处打烊后营业区、共用 180° 轴线。镜头01 从门框前景平稳缓推建立纵深；随老周抬头，以同轴推进自然过渡到镜头02 的中近景（平滑过渡、不快切、不越轴），随后机位固定。机位运动与角色动作分开：推进时角色只做小幅预备，落定后再做主要表演。

场景
奔海海鲜自助餐厅打烊后的营业区，冷白顶灯仍亮，不锈钢自助台与台面擦得锃亮，前景半开木门虚焦造纵深，空气里残留海水气息。

镜头 01
镜头从门框虚焦边缘后方朝营业区平稳缓推，先带出冷白顶灯、不锈钢自助台与收拾到一半的餐具；老周站在纵深尽头，把记账板贴在胸前，肩背收紧、抬下巴清了下嗓子作开场预备。

镜头 02
同轴推进落定为老周中近景，机位固定不晃。特写表演：他先垂眼扫过账板上一行字、喉结上下滚动一次，再抬眼、嘴角压住没笑出来，沉声开口作复盘开场（台词见台词表，按顺序由老周发声）；扇贝财务在侧只压着账单做冷硬反应、不抢话。顶灯硬光在鼻梁压出一道阴影。

基调
社交网络/华尔街之狼式冷峻都市商业摄影，结合海鲜自助餐厅拟人喜剧写实语境；洁净硬光、玻璃金属反射、冷白色温，Stainless Silver 与 Butter Gold 为主；live-action realistic cinematic，真人实景电影感、电影胶片颗粒感、自然镜头光学、真实景深、真实皮肤纹理、超清画质。

约束
禁止插画/动漫/卡通；角色全部真人写实；老周外貌全片严格一致；可见真实皮肤毛孔与细微眉眼不对称；特写须五官清晰、面部稳定、不扭曲变形、比例自然、表情不僵硬；真实布料重力褶皱与餐饮空间物理反射。

音障
无 BGM；仅顶灯轻微电流声、远处排风机低鸣、清洁后残留水声轻微回响、纸页翻动声、空场自然混响。
```

**对比要点**：无 `⟦⟧` 标签、无「角色」段、台词不转写（“沉声开口…台词见台词表”）、画质并进基调、特写防变形进约束、镜头02 写出微表情变化过程、运镜系统统述了两镜的同轴平滑转场。

### 1.4 自检清单变化（SP 末尾）

- 删：关于 `⟦⟧` 标签方向、逐字台词的自检项。
- 加：✗ 镜头段出现 `⟦…⟧` 或英文键值对 → 错；✗ 特写镜头没写微表情“变化过程”、只写“他很紧张/激动” → 错；✗ 相邻镜头用了“快切/急切”或机位与主体动作混写 → 错；✗ 片段最后一镜写了转场尾 → 错。

---

## 二、统一包装器 composer（所有 provider + 复制口径）

### 2.1 现状 → 之后

| provider/路径 | 现状 | 之后 |
|---|---|---|
| Seedance（火山，`isVolcano`） | 走 `buildSeedancePromptParts` 加块 | 走**统一 composer**（同款块） |
| Grok（`lib/video-gen.ts:779/798`） | **裸正文**提交 | 走**统一 composer**加块 |
| Sora（`lib/video-gen.ts:1559/1577`） | **裸正文**提交 | 走**统一 composer**加块 |
| 预演/审计（`batch-executors.ts:2621`） | 非火山=裸 prompt | 同步走 composer |
| 用户「复制」按钮 | 复制裸正文 | **不变**：仍复制裸正文（瘦身后干净叙述） |

### 2.2 统一后的拼接顺序（一份，所有 provider 共用）

```
镜头计划参数块（景别/角度/焦距/景深/光线/构图/运镜/时长/节奏）
+ 台词块（说话人→台词原文 + 口型/勿念名字 硬规则）   ← 逐字保真的唯一权威
+ 角色一致性主档块（roster：外观/服装/物种/声音）
+ 场景块（新增，见 2.3）
+ 前后衔接块（有上一/下一片段时）
+ 参考图/风格覆盖块（有参考图时）
+ 开场动态强制块
+ 【瘦身后的正文】
+ provider 尾部差异：Seedance 多参考图 → `--ratio X --duration Y`；首尾帧模式与 Grok/Sora → 时长/画幅走 body 顶层字段，不加尾巴
```

> 关键：正文已瘦身 → 块与正文**不再重复**，这正是 B 的目的。

### 2.3 新增「场景块」

现在提交时没有任何场景信息块（只有首帧图）。正文「场景」段收短后，给 composer 补一个轻量场景块（取自资产场景的 `effectiveVisualDescription/description`），保证非首帧或纯文生视频时场景不丢。**可选**，若你觉得首帧图够用也可不加（见 §五 待确认）。

### 2.4 复制按钮：保持只复制裸正文

`public/modules/videoPrompts.js` 的复制逻辑**不改**；正文瘦身后它自然变成干净叙述，适合手动粘到可灵/即梦。结构化参数/台词只在程序提交 API 时由 composer 补，不进剪贴板。

### 2.5 风险与验证点

- Grok/Sora 现在要吃中文块：需确认两者**提示词长度上限**（块都很短，风险低）；二者此前**没有**角色一致性/台词规则，统一后是补缺陷。
- 回归测试：`test-video-prompt-dialogue-regression.ts` 仍过（断言 finalPrompt）；建议**补 Grok/Sora 两条用例**，断言它们的提交串也含台词块。

---

## 三、特写「便利贴」（做法2：规则 + 后端按景别/情绪派生一句）

### 3.1 机制（一句话）

后端在拼每个镜头数据时，按该镜 `景别/情绪` 自动算一句 `perf`（表演提示），塞进给模型的输入里；模型生成正文时把它落实成画面叙述里的动作链。产物在**正文画面叙述**，不新增你要填的字段。

### 3.2 触发规则与文案（后端按优先级取，可叠加 1–2 条）

| 触发条件 | 注入文案 `perf` |
|---|---|
| 景别 ∈ 特写/大特写/近景/中近景 | `【表演提示·特写】写面部微表情的变化过程：眼神落点→眉/嘴角→喉结/呼吸（按时序，禁“他很X”静态情绪词）` |
| 情绪强度 ≥4 或 emotion=climax | `【表演提示·强情绪】克制爆发的过程：吸气/屏息→话卡喉咙→指节或下颌发力` |
| 景别 ∈ 大全景/远景/全景 | `【表演提示·远景】只写身体语言/重心/步态，不写面部微表情` |
| 镜头涉及道具互动 | `【表演提示·道具】手与道具的重量感/质感微动作（指腹蹭过、捏出折痕等）` |
| 任意特写（同时触发） | 给该镜「约束」追加：`五官清晰、面部稳定、不扭曲变形、比例自然` |

### 3.3 注入位置（`buildVideoPromptMessages`，lib/prompts.ts:1491-1545）

两处其一（建议都做）：
1. 在 `slimShots` 每个镜头对象里加 `perf` 字段（模型读 JSON 时看到）。
2. 仿照现有「台词清单」，在 user 消息里加一段**「⚠️ 本组表演提示」**汇总（镜头号 → perf），强提醒。

### 3.4 配套 SP 规则（加进 §一 的 SP）

```
【表演细节 - 按表演提示落实】
每个镜头可能带【表演提示】标记：
· 特写/近景：必须写出面部微表情的“变化过程”（眼神→眉/嘴角→喉结/呼吸），禁止只写“他很紧张/激动”这种静态情绪词；
· 远景/全景：写身体语言/重心/步态，不写面部微表情；
· 涉及道具：写手与道具的重量感/质感微动作。
```

### 3.5 例（便利贴 → 正文）

```
后端注入： 镜头02 perf=【表演提示·特写】眼神→眉/嘴角→喉结/呼吸（写过程）
模型产出（落在镜头02画面叙述）：
  他先垂眼扫过账板上一行字、喉结滚动一次，再抬眼、嘴角压住没笑出来……
```

---

## 四、落地范围预览（审核通过后才改，先不动）

| 文件 | 位置 | 改动 |
|---|---|---|
| `lib/prompts.ts` | `SP_VIDEO_PROMPT_GENERATE` :1355 | 瘦身改写（删 ⟦⟧/角色段/逐字台词；加 转场/微表情/画质/防变形；换示范与自检） |
| `lib/prompts.ts` | `buildVideoPromptMessages` :1491-1545 | 给每镜算 `perf` 并注入；可去掉对 ⟦⟧ 的下游期待 |
| `lib/shot-plan-normalize.ts` | `resolveShotFieldsForPrompt` :142 | 派生 `perf` 的辅助（按 shotType/intensity/emotion） |
| `lib/video-prompt-runtime.ts` | `buildSeedancePromptParts` :358 | 抽出**通用 composer**；新增「场景块」 |
| `lib/video-gen.ts` | Grok :779 / Sora :1559 | 由裸 `input.prompt` 改为走通用 composer |
| `lib/batch-executors.ts` | `plannedFinalPrompt` :2621 | 同步走 composer |
| `scripts/test-video-prompt-dialogue-regression.ts` | — | 验证仍过；补 Grok/Sora 用例 |
| `public/modules/videoPrompts.js` | 复制按钮 | **不动** |

---

## 五、待你确认（3 点，确认后我才动代码）

1. **场景块**（§2.3）：要不要给 composer 补轻量「场景块」？还是认为首帧图够用、不补？（建议：补，纯文生视频/无首帧时才不丢场景）
2. **便利贴注入**（§3.3）：`slimShots 内联 perf` + 「本组表演提示」汇总，**两个都做**还是只做内联？（建议：都做，强提醒命中率更高）
3. **正文台词**：确认正文**完全不转写台词**（只标“某某开口”）；若你希望正文仍保留台词便于复制阅读，则改为“正文保留台词、块仍为权威”（会有少量重复）。（建议：完全不转写，复制版接受变轻）
