# 账号体系改造 · 手机号身份 — 完整执行方案(手写实施版)

> 用法:这份是给你**照着手写代码**的清单,不含自动改动。每一节按"动哪个文件 → 改成什么 → 注意什么"组织。
> 配套高层说明见 `账号体系改造-手机号身份-方案.md`,本文件是它的落地细化版。
> 原则:**不做老数据兼容**(旧 dev 库直接删重建);**最小影响面**(改窄的一侧,旧列不重建表)。

---

## 0. 已锁决策(开工前确认)

| # | 决策 | 锁定值 |
|---|---|---|
| A | 旧 `username`/`email` 列 | **保留列、不重建表**;`createUser` 时把 `username` 写成手机号(满足 `UNIQUE NOT NULL`),`email` 写 `null`。phone 为唯一登录依据 |
| B | 验证码登录遇未注册号 | **提示"该手机号未注册,请先注册"**,不自动建号 |
| C | 短信服务商 | **腾讯云 SendSms**(云 API 3.0,2021-01-11) |
| D | 图形验证码防刷 | **一期先用频控兜底**(同号冷却+IP+全局日限),腾讯云图形验证码(Captcha)放一期尾巴/二期初(见第 10 节) |
| E | token 存储 | **维持 localStorage + Bearer 不动**;只缩短有效期(见 7.4),cookie 化放二期 |

> 范围假设:**仅国内手机号**(+86)。要做国际号再切 E.164 全量,本方案按国内 11 位处理。

---

## 1. 改造后的产品逻辑(一句话)

手机号 = 唯一账号。注册填「手机号+密码+昵称」收一条短信码即完成并自动登录;登录两种「手机号+验证码」或「手机号+密码」;忘记密码用「手机号+验证码」重设;登录后可改密码/改昵称/登出(登出服务端真失效)。

---

## 2. 环境变量(`.env.local` 新增)

```bash
# 腾讯云短信(云API 3.0 SendSms)
TENCENT_SMS_SECRET_ID=你的SecretId
TENCENT_SMS_SECRET_KEY=你的SecretKey        # 和 SecretId 成对,从 访问管理 cam/capi 取;不是 App Key
TENCENT_SMS_SDK_APP_ID=1400xxxxxx           # 短信控制台→应用管理 的 SmsSdkAppId
TENCENT_SMS_SIGN_NAME=你的签名内容           # 填签名"内容"本身(中文),不是签名ID
TENCENT_SMS_TEMPLATE_ID=1234567             # 验证码模板ID(注册/登录/找回可共用一个)
TENCENT_SMS_REGION=ap-guangzhou             # 默认即可
```

> 密钥只放 `.env.local`,别进 git、别贴聊天。`SmsSdkAppId` 必须和这对密钥**同一个腾讯云账号**,否则报 `UnauthorizedOperation.SmsSdkAppIdVerifyFail`。

---

## 3. 依赖

```bash
npm i tencentcloud-sdk-nodejs-sms
```

> 用按产品拆分的小包,体积比整包 `tencentcloud-sdk-nodejs` 小。**import 那一行以包 README 为准**(我没能渲染 npm 页),标准 v3 形态见第 5 节;若用整包则 `require("tencentcloud-sdk-nodejs").sms.v20210111.Client`。

---

## 4. 数据层(`lib/db.ts`)

### 4.1 `users` 表加 `phone`(用现有迁移模式)

`CREATE TABLE users` 定义里**新建库**直接加一列(给全新库用):
```sql
phone TEXT,            -- 唯一性靠下方唯一索引,非空靠应用层
```
**已存在库**走迁移(SQLite 的 `ALTER TABLE ADD COLUMN` 不能带 `UNIQUE`,所以分两步)。仿照现有 `migrateXxxColumns` 写一个,并注册进 `bootstrap` 末尾那串迁移调用里:
```ts
function migratePhoneIdentityColumns(db: Database.Database) {
  try {
    addColumnIfMissing(db, 'users', 'phone', 'phone TEXT');
    db.exec(`CREATE UNIQUE INDEX IF NOT EXISTS uq_users_phone ON users(phone) WHERE phone IS NOT NULL`);
  } catch (e) {
    console.warn('[db] migratePhoneIdentityColumns failed:', e);
  }
}
```
> `username`/`email` 的 `NOT NULL`/`UNIQUE` **不动**(改它们要重建表)。靠 4.4 的 `createUser` 把 `username` 填成手机号来满足约束。

### 4.2 `otp_codes` 表:`email` → `phone`

OTP 是一次性短期数据,不做兼容。两种做法二选一:
- **新建库**:直接把 `CREATE TABLE otp_codes` 里的 `email TEXT NOT NULL` 改成 `phone TEXT NOT NULL`,索引同理 `idx_otp_phone ON otp_codes(phone, purpose, created_at DESC)`。
- **已存在 dev 库**:这表是废码,最省事是删库重建或 `DROP TABLE otp_codes` 让它按新定义重建。

### 4.3 `UserRow` / `userToPublic`

`UserRow`(约 2072 行)加 `phone: string | null`。`userToPublic`(约 2109 行)输出改成:
```ts
return { id, phone: u.phone || '', displayName: u.display_name, createdAt: u.created_at };
// 不再返回 username/email(前端不依赖,见第 9 节)
```

### 4.4 死代码清理(可选,顺手)

`sessions` 表(约 179 行)全程未用,可删定义。不删也无害。

---

## 5. 短信模块(新建 `lib/sms.ts`)

封装腾讯云 SendSms,开发环境/未配置时回退 `console.log`(保留现在的调试便利)。

```ts
// lib/sms.ts
let _client: any = null;
function getClient() {
  if (_client) return _client;
  const tencentcloud = require('tencentcloud-sdk-nodejs-sms'); // import 行以包 README 为准
  const SmsClient = tencentcloud.sms.v20210111.Client;
  _client = new SmsClient({
    credential: {
      secretId: process.env.TENCENT_SMS_SECRET_ID,
      secretKey: process.env.TENCENT_SMS_SECRET_KEY,
    },
    region: process.env.TENCENT_SMS_REGION || 'ap-guangzhou',
    profile: { httpProfile: { endpoint: 'sms.tencentcloudapi.com' } },
  });
  return _client;
}

const SMS_CONFIGURED = () =>
  !!(process.env.TENCENT_SMS_SECRET_ID && process.env.TENCENT_SMS_SECRET_KEY && process.env.TENCENT_SMS_SDK_APP_ID);

/** 发验证码短信。phone 传 11 位国内号(无 +86)。 */
export async function sendSmsCode(phone: string, code: string): Promise<void> {
  if (!SMS_CONFIGURED()) {
    console.log(`[sms] (未配置,开发兜底) ${phone} → ${code}`);
    return;
  }
  const res = await getClient().SendSms({
    PhoneNumberSet: [`+86${phone}`],          // E.164
    SmsSdkAppId: process.env.TENCENT_SMS_SDK_APP_ID,
    SignName: process.env.TENCENT_SMS_SIGN_NAME,
    TemplateId: process.env.TENCENT_SMS_TEMPLATE_ID,
    TemplateParamSet: [code],                 // ★ 必须与你模板的变量个数一致(见下)
  });
  const status = res?.SendStatusSet?.[0];
  if (!status || status.Code !== 'Ok') {
    throw new Error(`[sms] 发送失败: ${status?.Code} ${status?.Message}`);
  }
}
```

**关键坑(对照官方错误码)**:
- `TemplateParamSet` 的参数个数必须**等于**你模板里 `{1}{2}...` 的变量数,否则报 `TemplateParamSetNotMatchApprovedTemplate`。先去控制台看你的验证码模板长啥样:
  - 模板是「验证码 {1},5分钟内有效」→ 传 `[code]`
  - 模板是「验证码 {1},{2}分钟内有效」→ 传 `[code, '5']`
- `SignName` 填**签名内容**(那串中文),不是签名 ID,否则报 `SignatureIncorrectOrUnapproved`。
- 验证码类模板变量默认最长 6 位纯数字(`TemplateParameterFormatError`),我们就是 6 位,OK。

---

## 6. OTP 模块改造(`lib/otp.ts`)

- 类型加 `login`:`export type OtpPurpose = 'register' | 'login' | 'reset_password';`
- 全模块 `email` 形参/查询 → `phone`(列名、`genCode`/限流逻辑不变)。
- `sendEmail()` → 删掉,改调 `sendSmsCode(phone, code)`(import 自 `lib/sms.ts`)。
- 限流键从邮箱换成手机号(同号 60s 冷却、单 IP 10 分钟 5 次,保留)。
- 新增**手机号规范化+校验**工具(给所有入口复用):
```ts
export function normalizePhone(raw: string): string | null {
  const s = (raw || '').replace(/\s|-/g, '').replace(/^\+?86/, '');
  return /^1[3-9]\d{9}$/.test(s) ? s : null;  // 国内 11 位
}
```

---

## 7. 鉴权核心(`lib/auth.ts`)

### 7.1 `createUser` 改签名
```ts
export async function createUser(opts: { phone: string; password: string; displayName: string }): Promise<UserRow> {
  // INSERT: username=@phone(满足旧 UNIQUE NOT NULL), email=null, display_name=@displayName,
  //         phone=@phone, password_hash, email_verified=1(旧列,忽略其语义)
}
```

### 7.2 `findUserByLogin` → `findUserByPhone(phone)`
按 `phone` 列查:`SELECT * FROM users WHERE phone = @phone LIMIT 1`。删掉原来按 `@`/用户名分流的逻辑。

### 7.3 JWT payload:`username` → `phone`
- `signToken`:payload 里 `username` 换 `phone`(其余 `sub`/`iat_ms` 不变)。
- `verifyToken`:解出 `phone` 而非 `username`(返回 `{ userId, phone }`)。
- 影响点:`lib/llm.ts:1338` 的 `usernameSnapshot: user.username || user.email` → 改 `user.phone`。

### 7.4 token 有效期(决策 E)
`TOKEN_TTL` 从 `'30d'` 调短,建议 `'7d'`。其它不动。

### 7.5 新增 `revokeUserTokens`(给登出/改密用)
```ts
export function revokeUserTokens(userId: number) {
  getDb().prepare(`UPDATE users SET token_revoked_at = strftime('%Y-%m-%dT%H:%M:%fZ','now') WHERE id = ?`).run(userId);
  clearUserAuthCache(userId);  // user-auth-cache 已有,30s 缓存要清
}
```
> 原理:`user-auth-cache.ts` 已经会比对 token 签发时间 vs `token_revoked_at`,签发早于 revoke 的 token 立即失效;之后重新登录拿的新 token 签发时间更晚,正常可用。粒度=该用户"全端下线",够用。

---

## 8. API 路由清单

> 统一约定:成功 `jsonOk(...)`;失败 `jsonError(detail, status)`(`lib/api-helpers.ts` 已有)。客户端 IP 取 `x-forwarded-for` 首段(参照现有 send-code 路由)。所有路由 `runtime='nodejs'`、`dynamic='force-dynamic'`。

### 改 · `POST /api/auth/login`(手机号+密码)
入参 `{ phone, password }`。流程:`normalizePhone` → `findUserByPhone` → bcrypt 比对(保留 DUMMY_HASH 恒定耗时防枚举)→ 校 `disabled_at` → `signToken` → `jsonOk({ token, user: userToPublic })`。

### 新 · `POST /api/auth/login/send-code`(验证码登录-发码)
入参 `{ phone }`。流程:`normalizePhone` → **查号必须已注册**(决策 B,未注册返回 404「该手机号未注册」)→ `sendOtpCode({ phone, purpose:'login', ip })` → `jsonOk`。

### 新 · `POST /api/auth/login/otp`(验证码登录-校验)
入参 `{ phone, code }`。流程:`normalizePhone` → `findUserByPhone`(无则 404)→ `verifyOtpCode({ phone, code, purpose:'login' })` → 校 `disabled_at` → `signToken` → `jsonOk({ token, user })`。

### 改 · `POST /api/auth/register/send-code`(注册-发码)
入参从 `{ username, email, password }` 改 `{ phone }`(注册发码阶段只需手机号即可,密码/昵称放到 verify 提交)。流程:`isRegistrationEnabled` 闸 → `normalizePhone` → **查号未注册**(已注册返回 409「手机号已注册」)→ `sendOtpCode({ phone, purpose:'register', ip })`。

### 改 · `POST /api/auth/register/verify`(注册-完成+自动登录)
入参 `{ phone, password, displayName, code }`。流程:`isRegistrationEnabled` 闸 → 校验 `phone`(normalize)、`password`(≥6)、`displayName`(2–20,trim 非空)、`code`(6 位)→ 查号冲突(409)→ `verifyOtpCode({phone,code,purpose:'register'})` → `createUser` →(UNIQUE 竞争兜底 409)→ `signToken` → `jsonOk({ token, user })`。**前端拿到即跳工作台 = 自动登录**(本就如此,保留)。

### 新 · `POST /api/auth/reset/send-code`(忘记密码-发码)
入参 `{ phone }`。流程:`normalizePhone` → 查号(为防枚举:**无论是否注册都回相同成功文案**,仅对已注册号真正发码)→ `sendOtpCode({ phone, purpose:'reset_password', ip })`。

### 新 · `POST /api/auth/reset/verify`(忘记密码-重设)
入参 `{ phone, code, newPassword }`。流程:`verifyOtpCode({phone,code,purpose:'reset_password'})` → `findUserByPhone` → `hashPassword` 写新 `password_hash` → `revokeUserTokens(id)`(让旧 token 全失效)→ `jsonOk`(可顺带签发新 token 直接登录)。

### 新 · `POST /api/auth/logout`(登出-服务端真失效)
鉴权:`requireUser`。流程:`revokeUserTokens(user.id)` → `jsonOk`。前端再清 localStorage。

### 新 · `POST /api/auth/account/change-password`(登录后改密)
鉴权:`requireUser`。入参 `{ oldPassword, newPassword }`。流程:`verifyPassword(old, user.password_hash)`(错→400)→ `newPassword` 校验 → 写新 hash → `revokeUserTokens` →(可重签 token 返回,免掉线)→ `jsonOk`。

### 新 · `POST /api/auth/account/profile`(改昵称)
鉴权:`requireUser`。入参 `{ displayName }`。校验 2–20 → `UPDATE users SET display_name=@d, updated_at=... WHERE id=@id` → `jsonOk(userToPublic(fresh))`。

### 不变 · `GET /api/auth/me`
逻辑不动,只是 `userToPublic` 输出字段变了(phone 替 username/email)。

---

## 9. 前端(`public/index.html` + `public/modules/utils.js`)

### 9.1 注册表单(约 514–536 行那块)
- 删:用户名输入、注册邮箱输入。
- 留/改:**手机号**(`type=tel`)、**密码**、**昵称**、**验证码**+「获取验证码」按钮。
- `handleSendOtp()`:只用手机号(+密码可不在这阶段校),POST `/api/auth/register/send-code` body `{ phone }`。
- 注册提交 `handleAction()` 的注册分支:POST `/api/auth/register/verify` body `{ phone, password, displayName, code }`。

### 9.2 登录区(约 529 行那块)
- 加一个**「密码登录 / 验证码登录」切换**(小 tab 或链接)。
- 密码登录:POST `/api/auth/login` body `{ phone, password }`。
- 验证码登录:先 `/api/auth/login/send-code` `{ phone }`,再 `/api/auth/login/otp` `{ phone, code }`。
- 校验工具:把 `_validUsername/_validEmail` 换成 `_validPhone = /^1[3-9]\d{9}$/`。

### 9.3 忘记密码(约 531 行的假按钮)
`onclick="showComingSoon('忘记密码')"` → 换成真流程:输手机号发码(`/api/auth/reset/send-code`)→ 输码+新密码(`/api/auth/reset/verify`)。可复用注册卡片的翻面交互做第三面,或弹个小表单。

### 9.4 登出
工作台「退出登录」(`workspace.html:1586`)现在只清 localStorage;加一发 `POST /api/auth/logout`(带 Bearer)再清本地、跳首页。`utils.js` 的 token 读写(`sw_auth_token`)**不动**。

### 9.5 版本号
改完 `public/` 下任何 JS/HTML,记得在 `workspace.html` bump `?v=`,否则浏览器跑旧缓存(项目惯例)。

---

## 10. 防刷与安全

- **短信频控(必做)**:`lib/otp.ts` 已有「同号 60s 冷却 + 单 IP 10 分钟 5 次」,键换手机号后保留;再加**单号单日上限**(如 10 条/天)防深夜轰炸。腾讯云侧也在控制台配「频率限制策略」做第二道闸。
- **图形验证码(决策 D)**:一期可先不上,靠上面频控;紧接着接**腾讯云验证码 Captcha**(同账号体系),加在「注册发码」「验证码登录发码」两个入口前,拦机器批量刷码。
- **防枚举**:登录失败统一「手机号或密码错误」;`reset/send-code` 不论是否注册都回同样成功文案(见 8)。注册/登录发码处会暴露"号是否已注册"(产品上可接受,要更严就也统一文案)。
- **token**:有效期降到 7d(7.4);cookie 化(httpOnly)二期再做,届时前端所有 Bearer 调用要一起改,blast radius 大。

---

## 11. 落地顺序(勾选清单)

**一期 · 能端到端跑通**
- [ ] `.env.local` 填腾讯云 6 个变量;`npm i tencentcloud-sdk-nodejs-sms`
- [ ] `lib/db.ts`:加 `phone` 列+唯一索引迁移;`otp_codes` email→phone;`UserRow`/`userToPublic`
- [ ] `lib/sms.ts`:新建,封装 SendSms + 开发兜底
- [ ] `lib/otp.ts`:email→phone、加 `login` purpose、改调 `sendSmsCode`、`normalizePhone`
- [ ] `lib/auth.ts`:`createUser`/`findUserByPhone`/JWT phone/`revokeUserTokens`/TTL=7d;`lib/llm.ts:1338` 改 phone
- [ ] 路由:login(改)、login/send-code(新)、login/otp(新)、register/send-code(改)、register/verify(改)、reset/send-code(新)、reset/verify(新)
- [ ] 前端:注册/登录表单重做、忘记密码真流程、`_validPhone`、bump `?v=`

**二期 · 自助+加固**
- [ ] logout(新,服务端失效)+ 工作台登出接它
- [ ] account/change-password(新)、account/profile 改昵称(新)
- [ ] 单号单日上限 + 腾讯云 Captcha 图形验证码
- [ ] (可选)删 `sessions` 死表

**三期 · 可选**
- [ ] 改手机号流程;token 改 httpOnly cookie;`/workspace` 服务端拦截

---

## 12. 自测清单(每条都实跑一遍)

1. **注册**:新号 → 收码(开发看后端 console;线上看真短信)→ 提交 → 自动进工作台;库里 `users` 有该行、`phone` 正确、`username=phone`、`password_hash` 是 bcrypt。
2. **重复注册**:同号再注册 → 409「手机号已注册」。
3. **密码登录**:对/错密码 → 成功签 token / 401「手机号或密码错误」。
4. **验证码登录**:已注册号收码登录成功;未注册号 → 404「未注册请先注册」(决策 B)。
5. **忘记密码**:发码→设新密码→旧密码失效、新密码可登;旧 token 立刻 401(验证 `revokeUserTokens`)。
6. **频控**:60s 内连点发码 → 429;同 IP 刷 5 次后 → 429。
7. **短信失败路径**:故意把 `TEMPLATE_ID` 填错 → 后端抛 `TemplateUnapprovedOrNotExist`,前端给友好提示(不要把腾讯原始错误码直接抛给用户)。
8. **登出**(二期):点退出 → 该 token 再调 `/api/auth/me` → 401。

---

## 13. 关键坑位速查

- SQLite `ALTER TABLE ADD COLUMN` **不能**加 `UNIQUE`/`NOT NULL`(无默认)→ phone 用「可空列+唯一索引」,非空靠应用层。
- `SmsSdkAppId` 与密钥必须**同腾讯云账号**,否则 `SmsSdkAppIdVerifyFail`。
- `SignName` 填**签名内容**非 ID;`TemplateParamSet` 个数必须**等于**模板变量数。
- 手机号入库前 `normalizePhone` 统一成 11 位(去 +86/空格),发短信时再补 `+86`,避免同号两种写法绕过唯一约束。
- 别把腾讯云原始错误码/Message 直接返给前端用户,统一成「验证码发送失败,请稍后重试」之类。
- 改密/重设密码后记得 `revokeUserTokens`,否则旧设备仍登录。

---

**下一步**:你按一期清单手写;过程中哪个文件/函数要我对着现有代码给更细的 diff 级指引,随时点名,我只给方案不自动改。
