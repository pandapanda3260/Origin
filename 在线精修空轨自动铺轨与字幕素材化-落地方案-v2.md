# 在线精修：空轨自动铺轨 + 字幕素材化 落地方案 V2（定稿）

> 状态：已审批口径整理稿，对应五轮代码核对结论。所有决策已拍板，无待选项。
> 范围红线：不改 AI 生成 EDL 链路、不改剪辑页 op 协议既有 op、不新增 Origin 后端 VOD 上传链路、不做老项目数据迁移。

## 1. 目标

空轨时一键自动铺入视频和 BGM；字幕停用 `Track text lane` 直写，改走官方 subtitle 素材导入；非空轨道一律不覆盖，保护用户在 SDK 内的手工编辑。

## 2. 已确认根因与代码锚点（全部实读核对）

1. 自动链路 `_triggerInitialVevDemoAutoSyncOnce` → `_autoSyncCurrentEdlVideosToVevDemo`（online_editor.js:1459/2309）在采集 EDL ids 后发现空 EDL 直接 return（2321-2326），首进不会生成 EDL。
2. `/api/edit/timeline` 的 `applyOp` 末尾无条件 `edl.version++`（route.ts:304）且 POST 一律写盘；自动同步签名含 `edlVersion`（online_editor.js:2285），ensure 走 `applyOp` 会导致签名失效、重复全量同步。
3. 当前 `subtitleApplied = UpdateProject 未抛错`（fe/index.js:2841-2864），服务端收下 text lane 但 SDK 不渲染时误报成功；直写 `Type:'text'` 实测不可用，废弃。
4. 仓内唯一可用的字幕上传通道在 bridge 侧：`uploadMaterial`（fe/actions.js:151-169，TTUploader + STS，token 走 BFF `/api/getUploadToken`）。Origin 后端无任何 VOD 上传客户端（lib/、scripts/ 无 ApplyUpload/UploadMediaByUrl 命中），不新增。
5. 上传分类取自 MIME：`file.type.split('/')[1]` → `getType()` 查 material.ts（actions.js:48/153/166）。SRT 真实 MIME 是 `application/x-subrip` 或空串，必构造 `type:'subtitle/srt'` 才能命中 `{ type:'subtitle', format:['srt','vtt','ass'], fileType:'object' }`（fe/material.ts）。
6. bridge 已有素材注册组合函数 `registerOriginMaterialToVevDemo`（fe/index.js:714-779）：CreateEditMaterial → 缺 EditMid 时 SearchEditMaterial 按 Source 回查，`payload.Type` 来自 `material.type`，Space 取 `material.vevSpace || 'origin'`（706 行）。喂 `type:'subtitle'` 即可复用。
7. `flattenVevTrackItems`（fe/index.js:185-198）带 `ID` 门，Origin 写入项无 ID（buildTrackFromOriginPlan，2715-2723），不可用于判空；`laneItemCount`（2696）只走一层、从未跑过 SDK 保存形状，也不用。判空一律走新递归计数。
8. 行版本与 EDL 版本是两个计数器：route 响应 `serverVersion = edl.version`（route.ts:317/349）；`project.version` 是 projects 行版本，走 If-Match（main.js:2299），`updateProjectForUser` 每写必 +1（projects-db.ts:971）并返回新行（:977，公开对象含 version，:188）。
9. `initOnlineEditor` 的 ctx 无 setProject/bumpProjectVersion（main.js:8178-8188）；`getProject()` 返回 main.js 闭包内同一引用，原地 mutate 全局可见。剪辑页 ctx 已有 `bumpProjectVersion` 先例（main.js:8165-8171）。
10. 运行时 API：`getVevEditorProjectData()`（fe/index.js:200，读 `editor.projectData`，其下 `LatestEditParam.Track` 与服务端同构）、`getVevEditorDuration()`（:214，按秒）；单位启发式先例 `getTrackComparisonTime`/`getTrackTimeUnitFactor`（:219/:1420）。
11. 本机 VevDemo 跑源码：watchdog `npm run dev`（vevdemo-dev-watchdog.sh:305），index.html 直引 `./index.js`（:15）。dist 为陈旧残留，不重建。
12. 验收项目 proj_1781093378957 实测：13 个 completed 全部 current、EDL timeline=0、importedToEdit 全 false、bgm=null → audio lane 预期 0。

## 3. 改动设计

### 3.1 服务端：`ensure-current-videos-edl`（route.ts + lib/edit-edl.ts）

在 `POST` 入口（route.ts:320 起）`applyOp` 之前拦截 `op === 'ensure-current-videos-edl'`：

- **EDL 非空**：返回 `{ ok:true, changed:false, edl, serverVersion: edl.version||0, projectVersion: proj.version }`。零副作用：不写盘、不 bump、不 `syncEditProjectClips`。
- **EDL 为空**：
  1. `collectEdlGenerationContext({ projectId, userId, project, targetDurationSec: 0 })`（edit-edl.ts:163）取 completed 且 current 的 clips。失败（无可用片段）按 `jsonError` 返回，前端静默跳过 ensure 继续走现状逻辑。
  2. 新增 `buildDeterministicEdlFromClips(clips, prevEdl)`（lib/edit-edl.ts）：按 `groupIdx` 升序，item 固定字段 `clipId / groupIdx / videoUrl / displayName / filename / downloadFilename / inPoint:0 / outPoint:durationSec / duration:durationSec / transitionIn:{type:'cut',duration:0} / transitionOut:{type:'cut',duration:0}`；`bgm` 透传 `prevEdl?.bgm || null`；`version = (prevEdl?.version||0)+1`。不复用 `normalizeGeneratedEdl` 的隐式 fallback。
  3. 同步 `storyboards[i].importedToEdit`（口径照抄 `set-edl`：timeline 内出现的 groupIdx 为 true，其余为 false）。
  4. `updateProjectForUser` 写盘一次，写后跑 `syncEditProjectClips`（与现有 POST 同款，route.ts:337-341）。
  5. 返回 `{ ok:true, changed:true, edl, serverVersion: edl.version, projectVersion: fresh.version }`，`fresh` 取 updateProjectForUser 返回值（projects-db.ts:977）。

已接受的边界：EDL 空但 importedToEdit 有脏 true 时，`collectEdlGenerationContext` 按标记子集生成（199-207 行过滤逻辑），不改该函数（最小影响面；set-edl 不变式使该状态罕见，生成后即自愈一致）。

### 3.2 Origin 前端：ensure 前置 + 策略消息（online_editor.js + main.js）

新增 `_ensureCurrentVideosEdl()`：POST `/api/edit/timeline { projectId, op:'ensure-current-videos-edl' }`，模块级 in-flight 单例防并发双发。返回后：

- `project.editData.edl = resp.edl`（原地 mutate `_oeCtx.getProject()` 引用）；
- 按返回 timeline 同步 `project.storyboards[i].importedToEdit`；
- `_oeCtx.bumpProjectVersion(resp.projectVersion)`；**禁止**用 `serverVersion` 推行版本；
- 不调用 `saveProject()`。

接入点（两处，均在 ids 采集前）：

- `_autoSyncCurrentEdlVideosToVevDemo`：绑定就绪检查（2311-2318）之后、`_collectCurrentEdlVideoResourceIds()`（2321）之前 `await _ensureCurrentVideosEdl()`；
- `importMaterialsToVevDemo`：绑定确认（2048-2056）之后、ids 采集（2057）之前 `await _ensureCurrentVideosEdl()`。

ensure 抛错/返回错误时：console.warn 后继续现状流程（fail-open，不阻断素材同步）。

其他改动：

- `main.js` 给 `initOnlineEditor` ctx 补 `bumpProjectVersion`（照抄 8165-8171 实现）。
- `_sendTimelinePlanToVevDemo(plan, options)`：消息体改为 `{ plan, policy: { mode:'fill-if-empty', silent } }`；ack 超时 15000 → 30000（2588）。
- `_applyCurrentEdlTimelineToVevDemo(materials, { silent })`：调用方传参——手动路径（2100）`silent = options.silent === true`；自动复用路径（2344）与自动全量路径（2371）`silent: true`。
  - 结果 `skipped === true`：手动 toast `轨道已有内容，未覆盖；如需按最新时间线重铺，请先清空轨道并稍候几秒后再点同步素材`；自动只 console。
  - 结果 ok 且未 skip 且 `plan.subtitles.length > 0`：触发 3.5 的字幕导入（同函数内，silent 透传）。
  - catch 分支的 `_showVevTimelineApplyToast` 在 silent 时改为仅 console。

### 3.3 Bridge：fill-if-empty 四象限判空（fe/index.js）

新增递归计数器（不复用 `flattenVevTrackItems`，不复用 `laneItemCount` 判空）：

```
countTrackItemsByType(track) → { video, audio, text }
// visit 模式照抄 hasNonOriginTimedTrackItem（2395-2409）：任意嵌套深度，
// 计数条件 = String(item.Type).toLowerCase() ∈ {video,audio,text} 且 Array.isArray(item.TargetTime)
```

`handleOriginApplyTimeline`（2747）在既有 `describeProject` 结果就位后：

- server 侧：`normalizeEditParamFromProject(projectInfo).Track` 计数；
- runtime 侧：`getVevEditorProjectData()?.LatestEditParam` 存在才计数；`projectData` 不可读或 `LatestEditParam` 缺失 = unreadable，以 server 为准（半初始化态归此类）；
- 四象限：
  - server 非空 + runtime 非空 → skip；
  - server 非空 + runtime 空 → 轮询 `describeProject` 等服务端保存清空，间隔 1200ms、上限 5s；仍非空 → skip，reason `sdk_track_not_empty_pending_save`；
  - server 空 + runtime 非空 → skip（保护未保存草稿），reason `sdk_runtime_draft_not_empty`；
  - server 空 + runtime 空/unreadable → 允许写入；
- skip 回执：`postToOrigin('vevdemo:timelineApplied', { ok:true, skipped:true, reason, serverCounts, runtimeCounts, runtimeTrackReadable })`，不调用 `updateProject`。

### 3.4 Bridge：时间单位 bootstrap + 运行时 duration 校验

不改 `resolveTimelineTimeUnit` 内部分支。在 `handleOriginApplyTimeline` 包一层：

- try `resolveTimelineTimeUnit(editParam, plan)`；catch 时仅当 `policy.mode==='fill-if-empty'` 且 3.3 已确认空轨 且 `err.reason === 'no_track_sample'` → 采用 `{ unit:'ms', source:'origin-empty-bootstrap' }`；其余照旧抛出（保持既有阻断）。
- 写入字段照既有 Object.assign 路径，`OriginTimelineTimeUnitSource: 'origin-empty-bootstrap'`。localStorage 手选单位存在时由 resolveTimelineTimeUnit 正常返回，bootstrap 不参与（用户手选优先）。
- 写入成功后运行时校验：每 500ms 读 `getVevEditorDuration()`，上限 5s：
  - `null/0` → `runtimeDurationVerified:false`（SDK 未刷新属预期，不回滚）；
  - `|d - plan.totalDurationSec| <= max(1, 0.08 × plan.totalDurationSec)` → `runtimeDurationVerified:true`；
  - `d / plan.totalDurationSec` 或其倒数落在 [600, 1800] 或 [6e5, 1.8e6] → 单位错实锤 → 用写入前捕获的 `editParam`（该对象在现有代码中未被 mutate，浅拷贝链已核实）按 `buildPayload` 同款字符串/对象分叉回滚 `updateProject`，回执 `ok:false, code:'timeline_time_unit', reason:'runtime_duration_mismatch'`；
  - 其他不匹配 → `runtimeDurationVerified:false` + 详情，不回滚。
- 总预算核对：草稿等待 ≤5s + 写入 + duration 轮询 ≤5s + 回滚（仅异常路径）< 30s ack ✓。

### 3.5 字幕素材化：独立消息 `origin:importSubtitles`

**Bridge 写轨侧删除**：`buildTrackFromOriginPlan` 删 text lane 注入（2727-2728）；删 `UpdateProject` 带字幕失败重试（2851-2864）与 `subtitleApplied/subtitleCount/subtitleError` 回执字段。`countTrackItemsByType` 仍计 text（用户自加标题属"非空"）。

**Origin 发送侧**：铺轨成功（ok 且未 skip）后：

1. cues 沿用 `_buildVevSubtitleCues`（2458）产物（即 `plan.subtitles`）；为空则跳过。
2. 新增 `_vevCuesToSrtText(cues)`：标准 SRT（序号 / `HH:MM:SS,mmm --> HH:MM:SS,mmm` / 文本行），模块自持不进 utils。
3. 发送 `origin:importSubtitles { projectId, vevSpace, filename, srtText, edlVersion, silent }`；`vevSpace` 取本次 reachableMaterials 里第一个带 `vevSpace` 的值，缺省空串；`filename = origin-subtitles-${projectId}-v${edlVersion}.srt`。
4. 等 `vevdemo:subtitlesImported` 回执，独立 timeout 120s。失败/超时：手动 toast `字幕素材导入失败，视频和 BGM 已同步`；自动只 console。不影响 timelineApplied 结果。

**Bridge 接收侧**（新 case `origin:importSubtitles`）：

1. 防重：`searchEditMaterial({ ProjectId: activeVevProjectId, Space })` 列表按 `Name === filename` 命中即回 `{ ok:true, reused:true, editMid }`，不重复上传（复用 registerOriginMaterialToVevDemo 内同款列表字段兜底链）。`Space = 消息 vevSpace || 'origin'`。
2. 构造 `new File([srtText], filename, { type:'subtitle/srt' })` —— 子类型必须是 `srt` 以命中 `getType()`（根因 5）。
3. Promise 包装 `uploadMaterial(file, Space, undefined, { onComplete, onError })` 上传。
4. onComplete 的 `info` 用 readFirst 容错提取 id（候选路径：`Vid/vid/Oid/oid/Mid/mid/Id/id` 及其常见嵌套），并 `console.info` 完整原始 `info`（首测探针）。提取失败 → 回执 `ok:false, reason:'upload_result_missing_id', raw:info`。
5. 组 material：`{ id: 'origin-subtitle-'+edlVersion, type:'subtitle', title: filename, name: filename, vevProjectId: activeVevProjectId, vevSpace: Space, vevSource: 'vid://' + 提取id }`，走 `registerOriginMaterialToVevDemo(material)` 完成 CreateEditMaterial + SearchEditMaterial 回读。
6. 回执 `vevdemo:subtitlesImported { ok, editMid, reused, reason?, raw? }`。`ok:true` 的唯一判据 = 回读拿到 EditMid（真实成功，替代旧假成功语义）。

### 3.6 缓存与 dist

- main.js:71 `online_editor.js?v=24` → `v25`；main.js 自身入口版本同步 bump（三处同号纪律），以 `npm run test:cache-busting` 为门禁。
- fe/index.js 无版本号体系（vite dev 直引）；**改前先手工备份**（该文件未纳入 git，vendor 重同步会回退，历史教训）。
- 不重建 dist。新增哨兵测试：watchdog 含 `npm run dev`、fe/index.html 含 `src="./index.js"`，防误起 dist 静态服务。

## 4. 测试清单

必须跑（存量）：`test:cache-busting`、`test:vevdemo-material-sort`、`test:online-editor-keepalive`、`test:vev-canvas-follow`。

废弃与新增：

- 删除 `scripts/test-vev-subtitle-track.mjs` 与 npm script `test:vev-subtitle-track`（text lane 语义作废，不留兼容别名）；顺带删除陈旧探针 `scripts/probe-vev-text-track.mjs`。
- 新增 `test:edit-ensure-edl`：空 EDL 生成（13 段字段齐全 / importedToEdit 同步 / bgm 透传 / 返回 projectVersion）；非空零副作用（不写盘、不 bump、不 sync clips）；源码序断言 ensure 调用先于两处 ids 采集。
- 新增 `test:vev-fill-if-empty`：判空走 `countTrackItemsByType` 且不依赖 `ID`；四象限 skip 分支不触达 `updateProject`；bootstrap 三重门（fill-if-empty + 空轨 + no_track_sample）；回滚仅限 1000x/1e6x 形态断言。
- 新增 `test:vev-subtitle-material`：buildTrackFromOriginPlan 无 text lane；File 构造含 `subtitle/srt`；`origin:importSubtitles`/`vevdemo:subtitlesImported` 收发存在；防重按 filename；`ok` 判据为回读 EditMid。
- 新增 `test:vevdemo-source-serving`：watchdog/index.html 源码加载哨兵。

测试风格沿用仓内契约测试（node 源码扫描断言），不依赖 tsx 沙箱。

## 5. 验收标准

1. proj_1781093378957 首进在线精修页：EDL 空 → 自动生成 13 段；SDK 空轨自动铺 13 段视频；audio lane 为 0（该项目无 BGM，属正常）。
2. 手动清空 SDK 轨道并等待保存后点"同步素材"：视频重新铺入；字幕以 subtitle 素材出现在素材面板（SearchEditMaterial 回读 ok）；有 BGM 且 enabled 的项目同步出 audio lane。
3. SDK Track 非空：自动进入不覆盖、不 toast；手动点击仅教学文案。
4. ensure 不引起 EDL version 空 bump、不触发重复全量同步；ensure 写盘后的后续整包 PUT 不因行版本滞后 409。
5. 字幕导入失败不影响视频/BGM 铺轨，且失败原因可见（手动 toast / 自动 console）。
6. 手动"清空→同步"后轨道当场显示新内容；若 SDK runtime 不即时刷新，以刷新页面后轨道可见为本主线通过，面板即时刷新另列问题单。

手测探针顺序：① TTUploader object 上传回执形状（console.info 的 raw info，决定 id 提取路径是否要补）；② SDK 素材面板字幕分类是否显示导入的 SRT；③ 空轨 bootstrap ms 写入后 SDK 时间线长度是否正常（验 1000x 形态防线）。

## 6. 风险与回退

- fe/index.js 未跟踪：改前备份；出问题整文件还原即回滚。
- 服务端 ensure 为新增独立分支，不触碰既有 op：回滚 = 删分支。
- 字幕素材化整段独立于铺轨主链：任一环失败不影响视频/BGM，可单独回滚。
- TTUploader object 模式回执形状未实测：已内置探针与容错提取，提取失败走显式错误不静默。
