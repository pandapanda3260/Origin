# 世界观产品设计方案 · v1（单剧 · 多集）

> 范围：一部剧 = 多集。**不做**多季 IP、跨剧复用资产库。
> 本版只定**产品逻辑**（世界观是什么、在一集内怎么用、在一部剧多集内怎么用、和现有概念的边界）。
> **不定**：字段 schema、API、代码实现——等产品逻辑拍板后再单独出技术方案。

---

## 0. 一句话现状（先说清楚我们站在哪）

世界观在**存储上已经是"剧级"**（全剧一份、所有集共享），但在**使用上几乎只活在"风格圣经"这一步**——之后跨环节、跨集都没真正贯通。换句话说：**地基铺好了，但只通了一个房间。**

---

## 1. 现状扫描结论（扣真实代码）

这部分是方案的事实依据，全部对照代码确认，不是推断。

### 1.1 世界观在"存储"上已经是剧级——这点天然契合目标

- `worldTemplateSnapshot`、`selectedWorldTemplateId` 挂在**项目（剧）层**，全剧一份（`lib/projects-db.ts:59-60`）。
- 它**不在** `EPISODE_FIELDS` 里（`public/main.js:99-106`），所以切集时不会被换走 → 所有集共享同一份世界观。**"一部剧一份世界观"已经成立。**
- 世界观模板表 `world_templates` 是用户级、可跨项目复用资产，且**只存内容、视觉风格字段被显式剥离**（`lib/world-templates-db.ts:27-93`）。内容/视觉分层这点已经做对了。

### 1.2 世界观在"使用"上只到风格圣经，之后**全程断流**

结构化世界观通道 = `buildWorldContextFromSnapshot` + `formatWorldContextForPrompt`（`lib/world-template-context.ts`）。它在全代码库**只被风格圣经的 prompt 调用**（`lib/prompts.ts:637 / 871`）。

| 生成环节 | 拿到世界观了吗 | 实情 |
|---|---|---|
| 写剧本（script create / 续写） | ✗ | 世界观只进审计，不进剧本 prompt |
| 风格圣经（style bible） | ✓ | 唯一完整注入点 |
| 抽资产（角色/场景/道具） | 半 | 走另一条通道，把**原始快照**当候选池塞进去（`app/api/assets/extract/route.ts` + `lib/prompts.ts:1199`） |
| 分镜（shots） | ✗ | 只拿到 styleBible 整包 |
| 出图·分镜稿提示词 | ✗ | 连 styleBible 里的 era 都没选进去 |
| 出图·首/尾帧图 | 仅 `era` | styleLock 只带了时代一项 |
| 视频提示词 | ✗ | styleBible 被精简，连 era/worldRules 都过滤掉 |
| 剪辑/导出 | ✗ | 无 |

**更深一层的漏斗问题**：风格圣经的输出 schema 只有 `era` + `worldRules` 两个自由文本字段能装世界设定（`lib/prompts.ts` 的 styleBible schema + `lib/style-template-constraints.ts` 字段白名单）。也就是说世界观里的**术语、禁忌、地点、道具、社会结构、力量体系、结构化设定**，过了风格圣经这道漏斗就**被压没了**——即使下游想用也已经没了。

### 1.3 分集目前是"前端伪功能"

- 后端**没有 episode 概念**：`episodes` 只是 `data_json` 里的一个数组（`lib/projects-db.ts:77`），没有 episodes 表、没有任何分集 API。
- 切集靠前端"字段镜像"：`EPISODE_FIELDS`（`public/main.js:99-106`）在 `project` 顶层和 `episodes[i]` 之间来回拷贝（`public/modules/episodes.js:46-66`）。
- **"续写新一集"名不副实**：`/api/script/workflow/continue` 只在**当前剧本末尾续写 3-5 句**（`SP_CONTINUE`），**不读世界观、不读角色、不读风格圣经、不生成真正的新一集**。"新一集"对象完全由前端本地拼装（`episodes.js:218-240`）。
- UI 文案写着"AI 将基于前集剧本和世界观生成续集，角色/场景会自动复用"（`episodes.js:148`）——**后端没实现这件事。**

### 1.4 已经有一套"按环节注入 + 审计 + 漂移追踪"的框架，但没搬世界观

- `lib/knowledge/*` 定义了 13 个 stage、能按 stage 注入上下文、并按 `项目+stage` 记录 hash 做漂移追踪（`lib/knowledge/compile-context.ts`、`context-db.ts`）。
- 但它目前只搬"规则卡"，**世界观只进审计快照、从不进喂给模型的 promptBlock**，而且注入开关默认全关。
- 含义：**注入的"管道"和世界观的"结构化提取器"都已存在，只是没接到一起、且没通到下游。** 这是好事——目标不是从零造轮子。

### 1.5 角色字段三套并存，口径不一

`project.characters`（顶层 legacy）、`project.assets.characters`（**每集一份**，在 EPISODE_FIELDS 里）、`project.consistency.characters`（**全剧一份**角色一致性锁）。世界观里的角色候选池是第四个来源。四者关系目前没有统一口径。

---

## 2. 世界观的产品定位（先把"它是什么"钉死）

**世界观 = 一部剧的"内容设定真相源"（内容宪法）。**

它装的东西：世界设定（时代/地理/社会/力量体系/规则）、叙事规则（允许的冲突 / 禁止的剧情 / 语气边界）、角色档案（候选池）、场景、道具、术语、禁忌。

它的**唯一职责**：让同一部剧的**所有集、所有生成环节，内容设定保持一致、不跑偏、不互相矛盾**。

它**不是**什么：
- 不是画风 → 归风格圣经 / 风格模板（视觉宪法）。
- 不是单集剧情 → 归剧本（集级剧情）。
- 不是角色长相锁 → 归角色一致性；但世界观是角色一致性的**上游来源**。

一句话四层定位：

> **世界观 = 剧级内容宪法；风格圣经 = 剧级视觉宪法；剧本 = 集级剧情；分镜/图/视频 = 集级执行。**

**贯穿原则：系统永不硬拦用户的主动行为。** 所有"约束 / 一致性"都以**可无视的提示**形式存在（被动指示、长期挂着），绝不弹拦截框、绝不阻止用户保存或继续。世界观是给 AI 兜底用的，不是用来管教用户的。

---

## 3. 作用域与数据流向（单剧 · 多集）

- **作用域 = 一部剧（project），全剧一份，所有集共享。**（现状存储已如此，保留。）
- **不引入"集级世界观"。** 一集不改世界观，只**消费**它、并在必要时**回填**它。
- 两个方向的数据流（这是 v1 的骨架）：
  - **向下（剧 → 集）**：世界观作为每一集、每一步生成的固定上下文注入。【解决"跨环节一致"】
  - **向上（集 → 剧）**：一集里新产生、且属于"全剧通用"的设定（新角色/新地点/新术语），沉淀回剧级世界观，供后续集继承。【解决"跨集一致"的闭环】

> 现状只有"向下"通了一半（到风格圣经），"向上"完全没有。

---

## 4.【集内】一集里世界观怎么用

**原则：世界观在一集的每一个生成环节都"在场"，作为内容约束 + 候选池。**

### 4.1 世界观字段分两类：硬锁 vs 软默认（核心机制）

世界观里的信息**可变性不一样**，必须分级。否则要么锁太死（同一个人不能换衣服），要么太松（换个镜头脸就变了）。代码里角色其实已经把 `appearance`(长相) 和 `clothing`(穿着) 分开存了，这套分级就是把它产品化。

**第一类 · 硬锁（不可变）**：跨集、跨镜头必须完全一致，违背就是错。
- 角色：长相 / 面部 / 身材 / 物种 / 性别 / 年龄段（身份级特征）
- 世界规则：力量体系 / 物理规则 / 禁忌
- 术语 / 称谓
- 场景：地点的固定结构特征（建筑、地貌本身）
- 道具：标志性道具的固定外观

**第二类 · 软默认（可变）**：世界观给一个**默认基线**，但某一集 / 某一镜可以覆盖。
- 角色：穿着 / 造型 / 发型 / 装备 / 情绪表演
- 场景：昼夜 / 天气 / 临时布置 / 氛围
- 道具：状态（完好/破损）/ 位置 / 持有者

**默认归类可调**：上面是默认分级，但允许用户对具体字段调档——比如某角色有"标志性红围巾"，可以把这件衣服从"软默认"提成"硬锁"；反之也行。（这条正好和 D4「用户可编辑世界观」接上。）

**这套分级怎么驱动三件事**：
- **注入（集内）**：硬锁字段 → 当**强约束**注入"绝对不能变"；软默认字段 → 当**默认值**注入"没特别指定就用这个"。
- **沉淀（集→剧）**：回填新设定时，要**标好它是硬锁还是软默认**。
- **冲突检测（剧内）**：**只有硬锁被违背才报警**（长相变了、违反世界规则）；软默认变化（换衣服、改天气）是正常创作，不报警——这能避免误报。

### 4.2 逐环节用法

逐环节的产品预期（括号内是现状差距）：

1. **写剧本**：给定"这集必须遵守的世界设定/术语/禁忌"+"可用的角色/地点/道具候选"，避免写出违背世界设定的剧情。（现状：剧本生成完全不带世界观）
2. **风格圣经**：继续带世界观；但世界设定要能完整传下去，不能被压成 era+worldRules 两段文本就丢了。（现状：漏斗丢字段，见 1.2）
3. **抽资产**：优先从世界观候选池**匹配复用**角色/场景/道具，而不是每集凭空新造。（现状：已半接，走原始快照通道）
4. **分镜**：镜头的场景、道具、时代、称谓要符合世界设定。（现状：没带）
5. **出图 / 首尾帧**：画面要锁住世界设定（时代、地理、场景特征、道具外观）。（现状：只带了 era）
6. **视频提示词**：动态描述不破坏世界规则。（现状：全丢）

**D1 已定 ✅ 按环节裁剪**：每一步只给它需要的那部分，避免 prompt 过长、模型抓不住重点（代码里已有先例 `projectWorldContextForStyleBibleStage`）。
裁剪时**同时按"硬锁/软默认"区分注入方式**：硬锁字段当强约束、软默认字段当可覆盖默认值（见 §4.1）。每个环节拿到的，是"它需要的字段 × 它的可变性级别"。

---

## 5.【剧内 · 多集】一部剧里世界观怎么用

**原则：世界观是这部剧所有集的共同地基；一集的产出可以丰富世界观，后续集继承。**

三件事：

1. **继承**：新建/续写一集时，默认继承全剧世界观，并把它作为该集所有生成环节的上下文。
   （现状：存储支持共享，但"续写新一集"根本没用它，见 1.3 → 这是 v1 必须补的核心。）

2. **沉淀（回填）**：某一集里新增的、属于"全剧通用"的角色/地点/道具/术语，要能登记进剧级世界观，成为后续集的候选池。这是跨集一致的闭环。
   （现状：完全没有。角色一致性锁虽是全剧的，但世界观本身不吸收集里的新增。）
   **D2 已定 ✅ 提示确认**：集里新增设定不自动入库，弹提示让用户确认后再回填剧级世界观，避免某一集的临时设定污染全剧。回填时一并标"硬锁 / 软默认"（见 §4.1）。

3. **冲突防护**：当某集的剧情/资产与剧级世界观矛盾（改了角色身份、违反世界规则、术语对不上），系统能发现并提示。
   （现状：只有"模板 drift"雏形 `knowledge-snapshot` 的 `driftInfo`，比的是模板、不是剧情。）
   **D3 已定 ✅ 弱化的长期提示，绝不硬拦**：只有硬锁被违背才提示（软默认变化不报）。形式是**常驻的被动指示**，不是打断式弹窗 / toast —— 在出问题的对象标题旁（该集标题、或冲突的角色/资产名旁）挂一个「！」小 icon，平时不挡路；用户**点它才弹窗**，列出具体哪些信息和世界观对不上。文案点名、说人话，例：「第3集把林舟的身份从警察改成了医生，和世界观对不上」，而不是「检测到潜在不一致」。

**本版不做**：多季 / 跨剧复用世界观资产库（属已排除的多季 IP）。但"把这部剧的世界观另存为可复用模板"已有雏形（资产库里的"保存为世界观模板"），**保留不强化**即可。

---

## 6. 与现有概念的边界（一张表对清楚）

| 概念 | 作用域 | 管什么 | 谁喂谁 |
|---|---|---|---|
| **世界观** | 剧（全剧一份） | 内容设定：世界/规则/角色档案/场景/道具/术语/禁忌 | 喂给：剧本、风格圣经、资产、分镜、图、视频 |
| **风格圣经** | 剧（全剧一份） | 视觉总纲：画风/色彩/镜头语言/光影 | 上游吃世界观；喂给下游视觉环节 |
| **角色一致性** | 剧（全剧一份） | 角色"长相/声音/表演"锁 | 上游来自世界观角色 + 集级资产 |
| **剧本** | 集（每集一份） | 这一集的剧情 | 上游吃世界观；下游产出分镜 |
| **分镜/图/视频** | 集（每集一份） | 这一集的执行产物 | 吃世界观 + 风格圣经 + 角色锁 |
| **分集** | 剧下的集列表 | 集的切换与归属 | 继承剧级世界观/风格/角色 |

---

## 7. 现状 → 目标 差距清单（只列差距，不给实现）

1. **下游断流**：世界观只到风格圣经 → 目标贯通到剧本/分镜/出图/视频。
2. **漏斗丢字段**：风格圣经只留 era+worldRules → 目标让结构化世界设定不被压没。
3. **分集是伪功能**：续写不带世界观、后端无 episode 概念 → 目标让新集真正继承世界观。
4. **无"集→剧"沉淀闭环** → 目标新增设定可回填剧级世界观。
5. **无剧情级冲突检测** → 目标软提示矛盾。
6. **角色字段四套口径**（顶层/集级 assets/一致性锁/世界观候选池）→ 目标理清关系（偏技术，留技术方案处理）。

---

## 8. 决策记录

- **D1 ✅ 按环节裁剪**，且按"硬锁 / 软默认"区分注入方式（详见 §4.1 / §4.2）。
- **D2 ✅ 提示确认**：集→剧沉淀走用户确认，回填时标可变性级别。
- **D3 ✅ 软提示 + 文案直接**：只对硬锁违背报警，点名具体冲突，说人话。
- **D5 ✅ 只做单剧·多集**：可复用世界观模板保留、不强化。

### D4' ✅ 手改优先，不阻拦

抽取与手编不冲突，都往同一份剧级世界观写。重新抽取时**只补空缺、不动用户改过的字段**——用户的显式编辑是最高权威。**不弹"是否覆盖"的拦截框**（遵守上面的贯穿原则）。

---

**产品逻辑到此全部锁定（D1–D5 + D4'）。** 下一步我出技术方案（字段 schema 含可变性标记、各环节注入点、分集改造、冲突指示的挂载位置），那是设计大改、会先以方案 md 交付等你审，审过才动代码。
