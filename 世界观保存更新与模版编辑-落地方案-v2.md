# 世界观保存 / 更新 / 模版编辑 · 落地方案 v2

> 状态：**待审批，未动任何代码**。
> 取代《世界观保存-按钮与新增资产-落地方案.md》（其后端 excludeEntityKeys 部分已落地进代码，按钮切换 / 新增 diff 部分未落地，由本方案覆盖升级）。
> 上位依据：《世界观产品设计方案-v1-单剧多集.md》——D2 确认式沉淀、D4' 手改优先、贯穿原则"系统永不硬拦用户主动行为"。

## 0. 一段话先说清楚

**是什么**：把"项目里的资产沉淀成世界观模版"这条已经存在一半的链路做完整——按钮按场景变文案（保存/更新）、弹窗列全所有世界观信息项并带"新增/有更新"徽标、素材页模版卡片增加编辑能力（删资产项、改术语、自动保存）。
**在哪**：前端集中在 `public/modules/assets.js`（保存弹窗、素材页卡片都在这个文件），后端集中在 `app/api/world-templates/from-project/route.ts` + `lib/world-templates-db.ts`。
**影响**：要动一处语义——现在"更新模版"是**只补空缺、不覆盖已有值**，和"把项目里改过的信息更新进模版"的预期直接冲突，必须升级成"勾选即授权覆盖"。
**建议**：不在现有弹窗上继续打补丁，而是把"候选模版构建 + 差异计算"收口到后端一个只读接口，三个弹窗（保存/更新/编辑）共用一套骨架。这是行业通行做法（Figma 发布弹窗 + Unity Prefab Apply 语义，见 §3）。

---

## 1. 需求回述（对齐口径）

五点需求归并为三个交互面：

| 交互面 | 需求点 | 一句话 |
|---|---|---|
| 资产页按钮 | #1 #2 | 全部资产生成完才出现；无绑定模版→「保存世界观」，有绑定→「更新世界观」 |
| 保存/更新弹窗 | #1 #2 #3 | 列**全部**世界观信息项的**标题**（不列明细）+ 勾选框；可改模版名、选新建/更新；更新模式只预勾"新增/有更新"项并带简短徽标；标题与按钮文案随模式切换 |
| 素材页编辑弹窗 | #4 #5 | 模版卡片加「编辑」；同款 UI 但资产项无勾选改删除 icon；术语列明细可编辑 + 自动保存 + 状态徽标（照抄镜头页画面描述那套） |

---

## 2. 代码现状（全部扣实代码，已有 vs 缺口）

### 2.1 已有的（这次直接复用，不重做）

1. **按钮**已存在：`#btnSaveWorldTemplate`（`public/workspace.html:773`），`main.js:7874-7875` 绑 `saveAsWorldTemplate`（`assets.js:4785`）→ `_openSaveTemplateDialog()`（`assets.js:5324`）。
2. **弹窗**已存在且已有大半骨架（`assets.js:5324-5442`）：模版名输入框、新建/更新单选（更新下拉会预选 `currentWorldId`）、**角色/场景/道具逐项勾选**（`_worldTemplateSaveEntitySectionHtml`，`assets.js:5299`，默认全勾）、术语整组开关（5375）。
3. **后端保存接口**已存在：`POST /api/world-templates/from-project`（create/update 双模式、`include` 四开关、`excludeEntityKeys` 按实体排除，route 全文 18-77 行）。create 撞派生 id 会 409（route:33-35）。
4. **"是否绑定世界观"状态**现成：`project.selectedWorldTemplateId` + `project.worldTemplateSnapshot`（`lib/projects-db.ts:60-61`），应用模版=快照拷贝（所以**改模版不回溯影响已应用项目**——编辑弹窗的安全性由架构保证）。
5. **"全部生成完"判定**现成：`_assetImagesReadyForConfirm()`（`assets.js:3158`），目前驱动"确认资产"区显隐（3189-3190），保存按钮还没用它。
6. **整包 upsert** 现成：`upsertWorldTemplate` 对 `data_json` 是整包覆盖（`lib/world-templates-db.ts:354-382`）→ 编辑弹窗可以走"前端持全量、改完整包 POST `/api/world-templates`"，**不需要新写编辑 API**。
7. **hash 工具**现成：`stableForDrift` + `shortKnowledgeHash`（`app/api/projects/[id]/knowledge-snapshot/route.ts:112-133`），可复用做逐项差异判定。
8. **自动保存参照**现成（镜头页首帧编辑器，`public/modules/storyboard.js`）：
   - 参数：防抖 800ms / 最长等待 4000ms / "保存中"最短可见 500ms（`FFE_AUTOSAVE_*`，65-67 行）；
   - 触发：input、change、compositionend 走防抖，blur 走 immediate（5628 / 5638 / 5703 / 5726）；
   - 状态集与文案：`_ffeFieldStatusText`（3236-3242）= 初始内容 / 保存中... / 已修改保存 / 已恢复初始 / 保存失败(+重试按钮)。
9. **带勾选列表+徽标文案的弹窗先例**：重新分析资产后的复核弹窗已有"新增X，建议勾选 / X信息发生了变化，建议勾选"这套行级状态（`assets.js:248-296`），文案风格直接沿用。

### 2.2 缺口（这次要做的）

| # | 缺口 | 证据 |
|---|---|---|
| G1 | 按钮显隐条件错：现在**有资产就显示**，不是"全部生成完才显示" | `checkAssetsConfirm`（`assets.js:3177`）：`saveTplBtn.hidden = !hasAssets`（3192） |
| G2 | 按钮文案不切换、弹窗标题/按钮/默认 radio 不随场景变（恒为"保存为世界观模板/新建/保存模板"） | `assets.js:5359 / 5368 / 5381` |
| G3 | 弹窗**信息项不全**：只列了角色/场景/道具 + 术语开关；基础设定（summary/setting/worldRules/storyRules/forbiddenRules）和风格偏好（preferredStyleTemplate*）这两组**没列**——而它们都会被存进模版 | 弹窗体 `assets.js:5324-5383` vs 模版字段全集 `buildWorldTemplateFromProject`（`lib/world-templates-db.ts:753-804`） |
| G4 | **无差异检测**：更新模式没有"新增/有更新"概念，全部默认全勾 | 5307 恒 `checked` |
| G5 | **更新语义不够**：后端 update = 只补空缺 + 追加新实体，**已有值永不覆盖**。项目里改了角色外观再"更新模版"，改动写不进去 | `mergeWorldTemplateSnapshotIntoSource`（842-892）→ `mergeMissingTemplateValue`（433-452，`isEmpty` 才写）、`mergeMissingEntityFields`（518-548） |
| G6 | **场景/道具的编辑根本进不了候选**：资产页编辑写的是 `project.assets.scenes/props`（`assets.js:3341-3395`，写 `item` 后 `_saveAssetsProject()`）；而 create 路径只读**提取完成时克隆的顶层旧值** `project.environments/props`（build:756-757；克隆点 `assets.js:719`）；update 的快照路径 locations/props 又**原样取快照**、只投影了角色（`buildWorldTemplateFromProjectSnapshot`:807-830）。角色是通的（consistency 锁 + asset authority 投影），场景/道具是断的 | 同左 |
| G7 | 素材页模版卡片只有"删除 + 关联到当前项目"，无编辑 | `_renderLibraryTemplates`（`assets.js:6025-6092`） |
| G8 | 更新模式下名称输入框预填的是"项目名 · 世界观"而非目标模版名，提交会把人家模版改名 | 5334 `defaultName` |

> G5、G6 是**语义级缺口**，单靠前端弹窗改造盖不住——这是"不要补丁上打补丁"的实际含义：弹窗再好看，写入层不覆盖、候选里没有场景编辑，更新就是假的。

---

## 3. 行业成熟方案调研（结论 + 映射）

| 方案 | 机制 | 映射到本需求 |
|---|---|---|
| **Figma 库发布弹窗**（[Publish a library](https://help.figma.com/hc/en-us/articles/360025508373-Publish-a-library)） | 发布时弹窗按 **Added / Modified / Removed** 分类列出所有变更资产，每项带勾选框，可取消勾选不发布 | 就是需求 #2 弹窗的原型：列全部、只预勾变更项、徽标分类。我们的"新增资产/有信息更新"对应 Added/Modified |
| **Unity Prefab Overrides**（[Override prefab instances](https://docs.unity3d.com/Manual/PrefabInstanceOverrides.html)） | 实例 vs 模版**直接对比**（不存基线），Overrides 面板逐项 **Apply（写回模版=覆盖）/ Revert（丢弃实例改动）** | ①差异检测不需要给项目新增"基线快照"存储——拿当前项目候选和当前模版直接比即可；②"更新世界观"= 把勾选项 Apply 回模版，**勾选即授权覆盖**，正好补 G5 的语义 |
| **novelcrafter Series Codex**（[Series Codex](https://docs.novelcrafter.com/en/articles/9387811-series-codex)） | 系列级 Codex 条目在所有书间共享，book/series 两层分区 | 印证 v1 "世界观=剧级真相源"定位；条目=我们的资产项 |
| **Sudowrite Story Bible**（[What is Story Bible](https://docs.sudowrite.com/using-sudowrite/1ow1qkGqof9rtcyGnrWUBS/what-is-story-bible/jmWepHcQdJetNrE991fjJC)） | 分区结构：Genre/Style/Synopsis/Characters/Worldbuilding，角色卡带 trait 集 | 支持弹窗"信息组 + 资产项"两层结构（§4.2）：组列标题、资产逐项 |
| **SillyTavern World Info / Lorebook**（[World Info](https://docs.sillytavern.app/usage/core-concepts/worldinfo/)） | 术语/设定=条目列表，逐条内联编辑 | 编辑弹窗术语区的形态：key+释义逐条列出、就地编辑 |
| **Canva Brand Kit**（[Brand Kit](https://www.canva.com/help/brand-kit/)）/ Adobe CC Libraries | 中央资产库，编辑影响后续使用，不强制回溯已有设计 | 印证我们快照模式的边界：模版编辑/删项**不影响已应用项目**，文案可明说，删除不必弹确认框 |

**调研结论**：行业通行解是四件套——①实例与模版**直接 diff**（无基线存储）；②**发布式勾选弹窗**（全列出、预勾变更、徽标分类）；③**Apply=覆盖**语义（勾选即授权）；④**中央库编辑不回溯**。没有主流产品用"项目里另存基线快照"来做差异，我们的快照架构（selectedWorldTemplateId + 模版本体即基线）与 Unity 模式天然一致。

---

## 4. 方案设计

### 4.0 三条总主张（反补丁的骨架）

1. **候选与差异收口到后端一个只读接口**。新增 `GET /api/world-templates/from-project/diff?projectId=&templateId?=`：后端用**既有**构建函数生成"候选模版"，与目标模版逐项比对，返回结构化清单。保存弹窗、更新弹窗都吃这一个接口 → "信息项不丢不多"（需求 #3）由后端构建函数**唯一**保证，前端永不复刻 entityKey/字段映射（旧方案自己标注的第一风险点，根除）。
2. **一个弹窗骨架、三个模式**：把 `_openSaveTemplateDialog` 重构为 `_openWorldTemplateDialog(mode)`，`mode ∈ save | update | edit`，文案/默认值/行为全部走模式矩阵（§4.3/§4.5），不复制三份 DOM 逻辑。
3. **写入语义升级而不替换**：`from-project` 增加 `applyMode: 'overwrite_selected'` + `selection` 参数；不传则旧 fill-missing 行为原样保留（最小影响面，老调用方零感知）。

### 4.1 世界观信息项清单（弹窗列什么——需求 #3 的口径）

以 `buildWorldTemplateFromProject`（`world-templates-db.ts:770-804`）的产出字段为**唯一全集**，分两层：

**信息组**（组级勾选，只列组标题+一行摘要，不列明细）：

| 组 | 覆盖字段 | 摘要示例 |
|---|---|---|
| 基础设定 | summary、setting{era,geography,society,powerSystem,rules}、worldRules、storyRules{allowedConflicts,forbiddenPlots,toneBoundaries}、forbiddenRules | "时代/地理/世界规则 等 6 项" |
| 术语 | terminology（map） | "12 条"（0 条时显示空态、勾选框禁用） |
| 风格偏好 | preferredStyleTemplateId/Name/Source | "水墨国风" |

**资产项**（逐项勾选，列名称+缩略图，不列明细）：

| 类 | 来源 | 备注 |
|---|---|---|
| 角色 | characters + characterCandidates | 候选角色排在尾部带「备选」小标；群像角色可带「群像」标（候选构建时从源资产带出 isCrowd，仅展示用，可选项） |
| 场景 | locations | |
| 道具 | props | |

**明确不列**（不相干的元数据）：coverImageId、schemaVersion、source、sourceProjectId。

> 注：新项目的术语大概率是空的——terminology 取自 `styleBible.terminology || styleBible.terms`（build:785），而风格圣经漏斗目前压掉了大部分世界设定（v1 方案 §1.2 已记录）。弹窗按实际条数显示即可，不需要为此做特殊处理。

### 4.2 diff 接口（新增，只读）

`GET /api/world-templates/from-project/diff?projectId=xxx&templateId=yyy`（templateId 可缺省=纯保存场景）

后端流程：①按 from-project 现有逻辑构建候选（有快照走 `buildWorldTemplateFromProjectSnapshot`，否则 `buildWorldTemplateFromProject`，与写入路径 route:54-67 完全同源）；②有 templateId 时与 `getWorldTemplate` 结果逐项比对；③返回：

```jsonc
{
  "groups": [
    { "key": "baseInfo",       "label": "基础设定", "summary": "时代/世界规则 等4项", "status": "changed" },
    { "key": "terminology",    "label": "术语",     "count": 12,                    "status": "unchanged" },
    { "key": "stylePreference","label": "风格偏好", "name": "水墨国风",              "status": "added" }
  ],
  "entities": {
    "characters": [ { "key": "char_01", "name": "林舟", "thumbUrl": "...", "candidate": false, "status": "changed" } ],
    "locations":  [ ... ],
    "props":      [ ... ]
  }
}
```

**比对规则**：
- 资产项：`entityKey`（`world-templates-db.ts:454-456`）匹配；`changed` = `stableForDrift` 整项 hash 不等。**必须把 `fieldMeta` 整体加进 omit 集**——它带 `updatedAt`，不排除会导致永远 changed（误报即失信，这是本方案第一风险点）。
- 候选有、模版无 → `added`；两边都有且 hash 等 → `unchanged`；模版有、候选无 → `removed_in_project`（见 §4.4 处理）。
- 术语组：map 逐 key 比，有新增 key 或值变化 → 组 `changed`；基础设定组/风格偏好组：组对象 hash 比。
- 无 templateId：全部项返回、status 统一 `added`（前端不显示徽标）。

### 4.3 资产页按钮 + 保存/更新弹窗（需求 #1 #2 #3）

**按钮显隐**（修 G1）：`saveTplBtn.hidden = !(hasAssets && _assetImagesReadyForConfirm())`（改 `checkAssetsConfirm`，`assets.js:3192` 一行）。未生成完不出现，生成完出现——与"确认资产"区同一判据，口径统一。

**按钮文案 / 弹窗模式判据**（沿用旧方案口径，避开 409）：

```
hasBoundWorld = (selectedWorldTemplateId 非空 且该模版在列表中存在)
             || 列表中存在 sourceProjectId === project.id 的派生模版
```
- `false` →「保存世界观」→ 弹窗 save 模式；
- `true` →「更新世界观」→ 弹窗 update 模式。
- 绑定的模版已被删除 → 自动退回 false 分支（兜底）。

**弹窗模式矩阵**：

| | save（新项目默认） | update（历史项目默认） |
|---|---|---|
| 标题 | 保存世界观模板 | **更新世界观模板信息** |
| 默认单选 | 新建模板 | 更新已有模板（预选绑定/派生模版） |
| 名称输入框 | "项目名 · 世界观" | **目标模版当前名称**（修 G8） |
| 信息项列表 | 全部信息组+资产项，**全勾** | 全部列出；仅 `added`/`changed` **预勾**，其余不勾（可手勾）；`added`→徽标「新增资产」、`changed`→徽标「有信息更新」 |
| 确认按钮 | 保存模板 | **更新模板** |

- 弹窗打开即调 diff 接口（update 带 templateId，save 不带），列表由接口数据渲染——两模式同一渲染函数。
- **radio 切换联动**：用户在 update 弹窗手动切到"新建" → 预勾策略变全勾、按钮文案变"保存模板"、徽标保留（仍有参考价值）；反之亦然。模式跟随 radio，不死绑入口。
- **无变更场景**：全部 unchanged → 列表顶部一行被动提示"当前项目与模版无差异"，**不禁用保存按钮**（贯穿原则：不硬拦）。
- **diff 接口失败兜底**：退化为现状行为（列资产三类+术语开关、全勾、无徽标），toast 说明"差异检测不可用，已列出全部信息项"。可保存，不阻断。
- 提交参数：勾选项转为 `selection`（见 §4.5）；update 时带 `applyMode:'overwrite_selected'`。

### 4.4 "项目中已删"的项怎么处理（决策）

模版有、项目里已删掉的资产项：**列出、置灰、不可勾、徽标「项目中已移除」，更新永不自动删除**。删除只能在素材页编辑弹窗里手动做（§4.6）。
理由：①Figma 把 Removed 也列出来供选择，但我们的模版是跨项目共享资产（剧级真相源），A 项目删了某角色不代表模版该删——自动删的误伤面大于收益；②保持"更新=只增改不删"的单向语义，用户心智简单。

### 4.5 后端写入语义升级（修 G5，需求 #2 的生效层）

`POST /api/world-templates/from-project` 增加可选参数：

```jsonc
{
  "applyMode": "overwrite_selected",      // 缺省 = 现状 fill-missing，老行为完全不变
  "selection": {
    "groups":   { "baseInfo": true, "terminology": true, "stylePreference": false },
    "entities": { "characters": ["char_01"], "locations": [], "props": ["prop_02"] }   // 勾选项的 entityKey
  }
}
```

`overwrite_selected` 语义（新 helper，放 `world-templates-db.ts`，与现有 merge 函数并列不混写）：
- 勾选实体：候选项 `clonePlain` **整项替换**模版同 key 项；模版没有则 append。
- 未勾实体：模版原项**原样保留**（包括项目里已删的）。
- 勾选组：基础设定/风格偏好=组字段整组覆盖；术语=按 key 覆盖+新增，**模版独有的 key 保留**（同样不做删除）。
- 未勾组：不动。
- create 模式忽略 `applyMode`：沿用现有 `excludeEntityKeys` 排除未勾项（前端把"未勾"转成排除列表，`assets.js:5412-5417` 现成逻辑）。

**不动的**：`mergeMissingTemplateValue` / `mergeEntityArrayByKey` / `mergeWorldTemplateSnapshotIntoSource` 原函数零修改；`/api/world-templates/[id]/characters` 合并路由不动。

### 4.6 候选构建补断点（修 G6，更新链路成立的前提）

`buildWorldTemplateFromProjectSnapshot`（807-840）目前只把**角色**投影到项目当前权威值，locations/props 原样取快照。补齐：
- locations/props 也按 `entityKey` 与项目当前资产合并投影（项目值优先覆盖快照值、项目新增追加），资产读取顺序对齐前端 `_worldTemplateProjectEntityList`（`assets.js:5238-5246`）：**先 `project.assets.scenes/props`，缺了退顶层 `environments/props`**。
- `buildWorldTemplateFromProject`（756-757）的 environments/props 读取同样改为"assets 优先、顶层兜底"。
- 改动收在这两个构建函数内部，diff 接口和写入接口自动同源受益；消费侧（风格圣经等读快照的链路）不受影响——它们读的是 `project.worldTemplateSnapshot`，不走这两个函数。

### 4.7 素材页模版卡片「编辑」+ 编辑弹窗（需求 #4 #5）

**卡片**：`_renderLibraryTemplates`（`assets.js:6025-6092`）在"关联到当前项目"旁加「编辑」按钮，点击 `GET /api/world-templates/:id` 拉全量 → `_openWorldTemplateDialog('edit', template)`。

**edit 模式与 save/update 的差异**：

| 区域 | save/update | edit |
|---|---|---|
| 模版名 | 输入框 | 同款输入框，改动走自动保存 |
| 新建/更新单选 | 有 | **无**（编辑就是改这一份） |
| 基础设定组 | 组标题+摘要+勾选 | 组标题+摘要，**只读展示**（v2 不做组内编辑，见 §5 范围裁剪） |
| 风格偏好组 | 同上 | 只读展示 |
| 资产项 | 勾选框 | **无勾选框，行右侧删除 icon**；点删=列表移除该项→触发自动保存。**不弹确认框**——模版应用是快照拷贝（`projects-db.ts:60-61`），删除零回溯影响，且符合"不硬拦"原则 |
| 术语 | 组开关/组勾选 | **列出明细**：每条 key + 释义；释义可编辑、可整条删除、可新增一条（key 重命名 v2 不做=删旧增新）；编辑触发自动保存 |
| 底部按钮 | 取消/保存(更新)模板 | 仅「完成」（关闭弹窗；保存已实时发生） |

**自动保存引擎**（照抄 FFE，参数与状态一字不差）：
- 防抖 800ms + 最长等待 4000ms + "保存中"最短可见 500ms（对齐 `storyboard.js:65-67`）；input/compositionend 走防抖，blur 立即；
- 状态徽标挂弹窗标题旁（弹窗级一个，不做行级）：`初始内容 / 保存中... / 已修改保存 / 已恢复初始 / 保存失败(+重试)`，文案对齐 `_ffeFieldStatusText`（3236-3242）；样式在 `styles.css` 写自定义类（**不依赖新 Tailwind 工具类**——workspace-tailwind 是静态预编译，没编译的类会静默失效）；
- 保存通道：整包 `POST /api/world-templates { template }`（upsert 整包覆盖，**无需新 API**）；成功后同步 `_worldTemplatesMem` 并重渲卡片；
- 「恢复」语义：进弹窗时 `clonePlain` 一份基线；术语行值被改过时行尾出现「恢复」小按钮，点击回基线值并触发保存，弹窗级状态显示"已恢复初始"——对齐 FFE 的 restored；
- 失败：状态变"保存失败"+重试按钮，重试=立即重发整包；弹窗关闭前有未落盘改动时走一次 immediate flush（对齐 FFE 的卸载兜底思路）。
- 并发说明：整包 last-write-wins，单用户产品下风险可忽略，不做乐观锁。

---

## 5. 范围裁剪（v2 明确不做）

1. **基础设定组的组内编辑**（编辑弹窗里改 worldRules/storyRules 明细）：生成性长文本、编辑 UI 面大，价值待验证，单独议。
2. **更新时的删除传播**（§4.4 已决策）。
3. **术语 key 重命名**（删旧增新可达）。
4. **老模版数据迁移/兼容**：缺字段按空处理（schemaVersion 已有），不写迁移。
5. **isCrowd 徽标**依赖群像方案落地节奏，本方案留接口（候选带 isCrowd 展示字段），不强求同步上。

---

## 6. 改动清单（blast radius）

**后端**（窄：1 个新只读路由 + 2 个文件内聚改动）
- `app/api/world-templates/from-project/diff/route.ts`：**新增** GET，只读。
- `app/api/world-templates/from-project/route.ts`：收 `applyMode` + `selection`，分支到新 helper；旧路径零改动。
- `lib/world-templates-db.ts`：新增 `diffWorldTemplateAgainstProject`（或同名 helper）+ `applyWorldTemplateSelection`（overwrite 语义）；`buildWorldTemplateFromProject` / `buildWorldTemplateFromProjectSnapshot` 的场景/道具读取补投影（§4.6）。

**前端**（集中在 assets.js）
- `public/modules/assets.js`：`_openSaveTemplateDialog` → `_openWorldTemplateDialog(mode)` 重构（模式矩阵 + diff 消费 + 徽标 + edit 模式 + 自动保存引擎）；`checkAssetsConfirm` 改按钮显隐一行 + 文案切换；`_renderLibraryTemplates` 加编辑钮。
- `public/styles.css`：弹窗状态徽标/删除行/术语编辑行样式。
- `public/workspace.html`：**bump `?v`**（assets.js v147→148；若动 main.js 同步 v278→279；styles 同步）。

**不动**：`mergeMissing*` 系列、characters 合并路由、knowledge-snapshot、世界观消费链路（风格圣经/资产抽取/镜头注入）、`_applyWorldTemplateReferenceFromStylePage` 应用链路。

---

## 7. 决策点汇总（均已给定论，不同意请直接 redirect）

| # | 决策 | 定论与理由 |
|---|---|---|
| D-a | 历史项目无变更时按钮显不显 | **显**（生成完即显「更新世界观」，弹窗内被动提示"无差异"）。入口稳定可预期，且用户可能就是想另存新模版 |
| D-b | diff 放前端还是后端 | **后端**。前端复刻 entityKey/字段映射必然漂移（旧方案自己标的第一风险），后端与写入路径同源 |
| D-c | 更新是否传播删除 | **不传播**（§4.4） |
| D-d | 编辑弹窗删除要不要确认框 | **不要**。快照架构保证零回溯影响 + 不硬拦原则；行内删除可视、弹窗未关可手动加回（术语）或重新从项目更新（资产） |
| D-e | 风格偏好组列不列、可不可勾 | **列且可勾**。它是模版实际字段（build:776），不列=丢项；项目换了风格模版时它就是一个真实的"有信息更新" |
| D-f | 更新模式 unchanged 项可不可勾 | **可勾**（默认不勾）。Figma 同款；用户可能想强制重写某项 |
| D-g | 自动保存状态徽标行级还是弹窗级 | **弹窗级一个**。整包保存本来就是弹窗粒度，行级会出现一排"已保存"噪音 |

---

## 8. 验证计划（审批通过、代码完成后执行）

1. 静态：改动 JS `node --check`；TS 路由走 `tsc --noEmit`（或现有构建）。
2. 接口实跑（curl）：①create 全勾/部分勾；②update + overwrite_selected（覆盖一个改过的角色、追加一个新场景、未勾项不动）；③update 不带 applyMode（验证老 fill-missing 行为原样）；④diff 接口：改一个角色外观→该项 changed、其余 unchanged；只动 fieldMeta/updatedAt→必须 unchanged（防误报回归点）。
3. 前端手测矩阵：新项目未生成完（无按钮）→生成完（保存世界观）→保存→改一个资产+加一个道具→按钮变更新世界观→弹窗预勾2项徽标正确→更新→再开弹窗全 unchanged；diff 断网兜底；编辑弹窗删资产项/改术语/断网保存失败+重试/恢复初始；模版编辑后已应用项目不受影响（关键回归）。
4. 版本号：workspace.html `?v` 已 bump、强刷验证无旧缓存。

---

## 附：信息项全集 ↔ 代码字段对照（防丢项核对表）

| 弹窗显示 | 模版字段 | 构建来源（world-templates-db.ts） |
|---|---|---|
| 基础设定 | summary / setting{era,geography,society,powerSystem,rules} / worldRules / storyRules{allowedConflicts,forbiddenPlots,toneBoundaries} / forbiddenRules | 763-786 |
| 术语（N条） | terminology | 785（源：styleBible.terminology/terms） |
| 风格偏好 | preferredStyleTemplateId/Name/Source | 725-751, 776 |
| 角色（逐项） | characters | 760-762, 787（consistency 锁 + asset authority） |
| 角色·备选（逐项） | characterCandidates | 788 |
| 场景（逐项） | locations | 789-795（§4.6 修读取源） |
| 道具（逐项） | props | 796-803（§4.6 修读取源） |
| （不列）元数据 | id/name/sourceProjectId/coverImageId/schemaVersion/source | 771-775 |
