# 镜头计划字段升级 方案 v3（收敛版 · 先不改代码）

> 共识：问题收敛到「**镜头计划有哪些字段**」。摄影造型维度没被建成字段、全压在 `visual` 自由文本里——这是根因。token / 存储 / Seedance 参数都不是瓶颈（已验证）。
> 本文只做设计，**不动一行代码**。评估与研究细节见同目录 `镜头计划-输出格式与内容质量-升级方案.md`；真实样张见 `镜头计划-实例预览.md/.json`。
> 三件事：① 字段升级 ② 上下游关联升级（尤其下游）③ 两层法生成。
> 日期：2026-06-01

---

## 0. 先回答“是不是就集中在字段”

**是。** 现在「该有的造型字段没有、该分开的揉成一团、该结构化的塞进一段话」。具体三层：
- **字段本身**（任务一）：景别背着角度、缺焦段/光位/构图/景深、`visual` 当杂物桶。
- **字段的上下游接线**（任务二）：就算补了字段，**不接下游它也到不了视频模型**——这是最容易被忽略、却最致命的一环。
- **字段的产出方式**（任务三）：一次性吐 6–14 个富字段，注意力被摊薄；两层法能让每个字段写得更足。

下面逐条展开。**原则：效果导向——该拆的拆、该删的删、该增的增，不为凑数加字段。**

---

## 1. 任务一：字段升级

### 1.1 字段变更总表

> 处理方式 ∈ 拆分 / 修改 / 删除 / 新增 / 保持。内容类型 ∈ 选项 / 台词 / 文字描述（+ 派生/系统）。
> 末两列直接回答你的问题：**这个字段有什么效果**、**内容能不能从上游拿到**。

| 原字段(中) | 原字段(英) | 处理 | 新字段(中) | 新字段(英) | 内容类型 | 效果导向 | 上游来源 / 可得性 |
|---|---|---|---|---|---|---|---|
| 景别(混装) | shotType | **拆分** | 景别(纯尺寸) | shotType | 选项 | 景别只管“画多大”，不再背角度 | 剧本+导演意图（LLM，已在产出）✓ |
| —（从景别拆出） | — | **新增** | 机位角度/视点 | angle | 选项 | 俯=弱化/仰=压迫/过肩=对话关系/主观=代入，权力与视角全靠它 | 剧本戏剧意图 + styleBible.cameraStyle（LLM 推断）✓ |
| — | — | **新增** | 焦段 | lens | 选项 | **最大控制力缺口**：定透视+景深基线+情绪（广角紧张/50mm真实/85mm亲密） | 景别+情绪规则映射 + styleBible.cameraStyle ✓ |
| — | — | **新增(派生)** | 景深 | focus | 选项(派生) | 隔离主体 or 纪实全清的情绪选择 | **由 lens+景别 计算**，不必 LLM 单独产 ✓✓ |
| — | — | **新增** | 光位/影调 | light | 选项组合 | 让模型合成“有方向的光影”而不是平光（模型对显式光位响应强） | **场景资产已有 lighting/timeSetting/weather** + styleBible ✓✓ |
| — | — | **新增** | 构图 | composition | 选项(可多选) | 前景分层/视线方向/留白，提画面完成度 | 创意推断 +`visual` 里本就隐含 ✓ |
| — | — | **新增(可选)** | 接法/转场 | cut | 选项 | 与下一镜的硬切/同轴/越轴/匹配剪辑，服务剪辑与镜头合并 | emotion 边界 + 相邻关系 + 分组 ✓ |
| 画面描述(80–130杂烩) | visual | **修改(收紧)** | 画面/调度 | visual | 文字描述 | 技术参数移走后，只写“谁动/怎么动/神态/什么静止”，60–110字 | 剧本（LLM）✓ |
| 运镜 | camera | 保持(修边) | 运镜 | camera | 选项 | 三处词表统一，补齐左右移/升降的兼容分组 | 已有 ✓ |
| 主题词 | keyInfo | 保持 | 主题词 | keyInfo | 文字描述(短) | 制片速读 | 已有 ✓ |
| 对白 | dialogue | 保持 | 对白 | dialogue | 台词 | 逐字驱动时长/口型 | 剧本原文 ✓ |
| 环境音 | audio | 保持 | 环境音 | audio | 文字描述 | 进“音障”段 | 剧本/场景 ✓ |
| 情绪 | emotion | 保持 | 情绪 | emotion | 选项 | 情绪曲线 + 合并分组 | LLM（已产出）✓ |
| 情绪强度 | intensity | 保持 | 情绪强度 | intensity | 选项(1–5) | 徽章 + 尾帧打分 | LLM ✓ |
| 剧本定位 | scriptRef | 保持 | 剧本定位 | scriptRef | 台词/原文 | 前端高亮联动 | 剧本原文 ✓ |
| 入画角色 | characters | 保持 | 入画角色 | characters | 选项(引用角色) | 身份参考绑定 | 资产角色 ✓ |
| 时长 | duration | 保持(口径另议) | 时长 | duration | 选项(档位) | 计划秒数/时间轴 | LLM 按台词 ✓ |
| 叙事节奏 | pace | 保持 | 叙事节奏 | pace | 选项 | 动作密度 | LLM ✓ |
| 场景 | sceneId/sceneName | 保持(去重) | 场景 | sceneId/sceneName | 选项(引用场景) | 绑定场景资产 | 资产场景 ✓ |
| 尾帧信号 | tailFrameSignals | 保持 | 尾帧信号 | tailFrameSignals | 系统信号(派生) | 尾帧决策 | LLM 打分 ✓ |
| 别名冗余 | description / dialog / framing / movement / scene / durationSec | **删除** | （收敛到规范名） | — | — | 路由产生的一堆重复别名，去掉只留单一字段名 | 纯冗余 |
| — | — | **新增(可选)** | 全片总览 | planMeta | 系统/计算 | 顶层补一个总览对象（总时长/镜头数/整体基调/调色锚点），给下游全局锚点 | Σshots + styleBible（计算/抽取）✓ |

**净变化**：拆 1（景别→景别+角度）、增 5–6（角度·焦段·景深·光位·构图·〔接法〕）、改 1（visual 收紧）、删 1 组（别名冗余）、新增可选 1（planMeta），其余保持。**只加高 ROI 的 6 个维度，不照搬“导演 15 项”全塞。**

### 1.2 新增字段词表（取值 + 语义）

- **焦段 lens**：`超广角16-24`(夸张纵深/紧张/疏离) / `广角35`(自然带环境) / `标准50`(接近人眼·对白默认) / `中长焦85`(压缩/浅景深/亲密) / `长焦135+`(强压缩/偷窥) / `微距`(道具特写)。
- **角度 angle**：`平视` / `略俯` / `俯拍` / `略仰` / `仰拍` / `鸟瞰` / `倾斜(Dutch)` / `过肩` / `主观POV`。
- **光位 light**（三轴组合）：方向 `顺/侧/侧逆/逆/顶/底` · 质感 `硬光/柔光` · 色温 `暖/中/冷` · 反差 `高/低`。
- **构图 composition**（可多选）：`三分法` / `中心对称` / `前景框架` / `引导线` / `留白` / `视线方向` / `景深分层`。
- **景深 focus**（派生）：`深焦` / `中等` / `浅景深` / `移焦`——默认由 `lens+景别` 推：长焦/特写→浅，广角/全景→深；只在需要打破默认时由 LLM 显式覆盖。
- **接法 cut**：`硬切` / `同轴前进` / `越轴反打` / `匹配剪辑` / `叠化` / `跳切`。

### 1.3 上游可得性结论（你的核心问题）

**全部可得，无需新增任何上游数据采集。** 三类来源：
1. **LLM 创意推断**（角度/构图/景别/焦段/情绪/visual）——这些本来就在模型“脑子里”做，只是以前闷在 `visual` 一段话里，现在**显式拆成字段**而已。
2. **继承现有上游资产**（光位）——场景资产抽取时已产出 `lighting / timeSetting / weather / atmosphere`，styleBible 有 `colorPalette / lighting / cameraStyle / mood`。`light` 字段默认**继承场景资产**，每镜只补“本镜关键光位”。
3. **由其它字段计算**（景深/planMeta）——`focus = f(lens, 景别)`；`planMeta = Σduration + styleBible 抽取`。**不进 LLM 输出，省 token、降不一致。**

> 一句话：升级**不要求上游多给任何东西**，只是把“本来就有、但被埋掉”的信息显式建成字段。

---

## 2. 任务二：上下游关联升级（尤其下游）

### 2.1 上游 → 镜头计划

几乎不用动：`buildShotsMessages` 已经把 script / styleBible / assets（含场景的 lighting/time/weather）/ totalDurationSec 喂进去了。唯一要做的是在**提示词里显式说明**“`light` 默认继承场景资产的 lighting/timeSetting，只补本镜关键光”。**无新数据接入。**

### 2.2 镜头计划 → 下游（关键，逐处列）

| # | 下游触点 | 位置 | 现状 | 要改 | 不改的后果 |
|---|---|---|---|---|---|
| 1 | **slimShots 透传** | `buildVideoPromptMessages` | 只透传 idx/dur/shotType/camera/pace/visual/dialogue/keyInfo/audio/emotion/characters | **加 angle/lens/focus/light/composition/cut** | **新字段永远到不了视频模型——头号必改项** |
| 2 | ⟦景别·焦距·运镜⟧ 标签 | `SP_VIDEO_PROMPT_GENERATE` | 焦距=范例写死、逐次即兴 | 改成读 `shot.lens/angle/focus`→`⟦景别·焦段·角度·运镜·景深⟧` | 焦段继续不可控、跨镜头漂移 |
| 3 | cameraList 锚点 | `buildVideoPromptMessages` | `镜头N:camera/shotType` | 追加 `lens/angle` | 运镜锚点信息不全 |
| 4 | timelineLines | 同上 | `景别=/运镜=/节奏=` | 追加 `角度=/焦段=/景深=/光位=` | 结构化参数区展示不全 |
| 5 | 运镜系统/基调段 | `SP_VIDEO_PROMPT_GENERATE` | 光/构图靠模型自由发挥 | 显式引用 `light/composition` | 造型仍不稳定 |
| 6 | **分镜图 prompt** | `SP_SHOT_TO_IMG_PROMPT`（单条+batch两份） | 主要读 `visual` | 读 `angle/lens/composition/light` | 分镜草图与镜头计划不一致 |
| 7 | UI 镜头卡 | `public/modules/shots.js` | 4 个 pill：时长/节奏/景别/运镜 | 加 `角度/焦段(/景深)` pill；`光位/构图` 折叠进“高级” | 用户改不了新维度 |
| 8 | 路由校验 | `app/api/shots/generate/route.ts` | `FRAMINGS` 含俯仰/主观/过肩 | 拆出 `ANGLES`；加 lens/focus/cut 的 `pickEnum`；收敛别名 | 角度仍混在景别、新字段不规整 |
| 9 | **枚举单一事实源** | 提示词 / 路由 / 前端三处 | 三份各异（左移/升降无兼容组） | 抽到一个共享常量，三处复用 | 漂移继续、手动选值无兼容判断 |
| 10 | 合并 / 尾帧 | merge + tailFrame | 用 duration+emotion+camera 兼容 | 基本不动；顺带补 camera 兼容分组 | 低风险 |
| 11 | 依赖/stale 哈希 | `computeShotPlanSourceHash` | 覆盖整个 shots | 不动（新字段在 shots 内已被覆盖） | 低风险 |
| 12 | 存储 | `projects.data_json` | `...base` 自动透传未知字段 | **不动（零成本）** | 无 |

> 重点就一句：**字段是“源头”，slimShots 是“水龙头”。** 只补源头不开水龙头，水（控制力）流不到视频模型。下游改造的 80% 价值集中在 #1、#2、#6。

---

## 3. 任务三：两层法生成

### 3.1 为什么两层（不是为 token，是为注意力 + 全局一致）

输出预算 28k，12–14 个镜头根本撑不爆。两层的真正理由：
- **注意力**：一次性吐十几个富字段，模型会“前详后略”；分层后每镜拿到聚焦注意力，`visual/light/composition` 能写足。
- **全局一致**：有些字段必须**看全片才能定**（固定镜头≥40% 配比、相邻同情绪运镜 A/B/C/D 兼容、情绪曲线、台词分配、焦段语言的整体节奏、合并分组）。

### 3.2 字段如何分到两层

| 层 | 目的 | 承载字段 | 为什么在这层 |
|---|---|---|---|
| **第 1 层 · 骨架**（一次全片） | 锁全局结构与配比 | idx、scene、emotion、intensity、duration、pace、**景别、角度、焦段、运镜、cut**、keyInfo、scriptRef、characters、dialogue、tailFrameSignals(打分) | 这些都要“看全片才能定”——配比/曲线/兼容/分组/台词分配 |
| **第 2 层 · 填肉**（按情绪段并行） | 逐镜写足造型细节 | **visual(动作/调度/神态)、light、composition**（+ focus 由 lens+景别计算） | 逐镜可独立写、可并行、能给足注意力 → 内容更丰富 |

> 契合点：第 1 层定的是**结构化旋钮**（可枚举、要全局协调），第 2 层写的是**描述性细节**（要笔墨、可局部并行）。字段升级表里“选项类”几乎都进第 1 层，“文字描述类”进第 2 层——天然分工。

### 3.3 落点（仍只是设计）

- 第 1 层 = 现有 `SP_SHOTS_GENERATE` 瘦身版（去掉重描述要求，只产骨架+结构化旋钮）。
- 第 2 层 = 新增一个“逐镜/逐段填肉”提示词，输入 = 第 1 层骨架 + 该段镜头 + 场景资产/styleBible，输出 = 补齐 `visual/light/composition`。
- 两层产物**合并成同一个 shot 对象**再落库——对下游和存储是同一个 schema，下游只认最终字段，不关心它是几层拼出来的。

---

## 4. 升级后字段总览（最终 schema 草图）

```jsonc
{
  "planMeta": {                 // 新增·可选·计算得到
    "shotCount": 12, "plannedDurationSec": 53,
    "overallTone": "冷峻都市黑色喜剧", "paletteAnchor": ["冷蓝","橙招牌"]
  },
  "shots": [
    {
      "idx": 11,
      "sceneId": "e1", "sceneName": "深夜海鲜店后厨",
      "duration": 4, "pace": "normal",
      "emotion": "climax", "intensity": 4,

      "shotType": "中近景",        // 纯景别（拆分后）
      "angle": "俯拍",             // 新增：角度/视点
      "lens": "标准50",            // 新增：焦段
      "camera": "摇镜头",
      "focus": "中等",             // 新增·派生：lens+景别
      "light": "顶光·硬光·冷白·高反差",      // 新增：光位（继承场景资产）
      "composition": "引导线(海报→桌角)·末端落地面",  // 新增：构图

      "visual": "从褪色海报摇到桌角玻璃板，旧报纸水渍压皱，'食物中毒''一人死亡'隐约露出；摇幅末端落到潮湿地面，濒死螃蟹仰面，钳子贴地抽停。",  // 收紧：只剩动作/调度/神态
      "dialogue": "——",
      "keyInfo": "旧案显影",
      "audio": "灯管电流声、纸张摩擦",
      "scriptRef": "旧报纸标题里'食物中毒'隐约露出",
      "characters": ["濒死螃蟹","小李"],
      "cut": "硬切",               // 新增·可选
      "tailFrameSignals": { "revealNeed": 4, "endingCompositionNeed": 3, "...": 0 }
    }
  ]
}
```

### 真实 Before / After（取自你库里「差评局」镜头 11，就是那个 `俯拍`）

| | 现状（旧） | 升级后（新） |
|---|---|---|
| 景别 | `shotType:"俯拍"` ← **没有尺寸**，被角度顶掉 | `shotType:"中近景"` + `angle:"俯拍"` ← **两个都在** |
| 焦段 | 无（下游即兴） | `lens:"标准50"` |
| 光位 | 埋在 visual 一句“冷白灯下” | `light:"顶光·硬光·冷白·高反差"` |
| 构图 | 埋在 visual | `composition:"引导线·末端落地面"` |
| 画面 | 80 字大杂烩（光/构图/动作混在一起） | 收紧为动作/调度，技术参数已拆走 |

这一镜就是“维度被合并”的活证据：旧结构逼你在「中近景」和「俯拍」里二选一，升级后两者并存、且补齐了焦段/光位/构图。

---

## 5. 边界 / 不做什么

- **本轮不改代码**，只交付本方案。
- `duration` 口径（`3–7` vs 合并方案的 `1–7`）属生成逻辑，归 `镜头合并与素材生成-方案.md`，此处不动。
- 不无限加字段——只加 6 个高 ROI 维度；`focus/planMeta` 走派生不进 LLM 输出，控制复杂度。

---

## 6. 待你拍板的 4 个选择点

1. **`angle` 是否把“过肩/主观”并进来**（一个字段管角度+视点），还是再拆一个 `viewpoint`？建议合并（更简单）。
2. **`light` / `composition` 用“选项组合”还是“受控自由文字”**？建议 light 用选项组合、composition 允许多选+短补充。
3. **`cut`（接法）v1 要不要纳入**，还是先上 5 个核心字段、cut 放二期？
4. **`planMeta` 顶层总览要不要**（多一点全局锚点 vs 保持极简）？

定了这 4 点，我再把 **第 1 层骨架提示词 + 第 2 层填肉提示词** 的改写稿整段写给你审——仍然不落代码。
