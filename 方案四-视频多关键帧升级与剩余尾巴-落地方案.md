# 方案四 · 视频生成多关键帧升级（接 Seedance 2.5）+ 1/2/3/5 剩余尾巴 — 落地方案

> 基于**当前最新代码**（HEAD `8dadba2`，2026-06-30）重扫后定稿，**取代**早先的《方案四-视频生成模式升级-接Seedance2.5》。方案 1/2/3/5 已落地，但有两处隐藏缺口直接卡住方案四（见 §1），本方案一并收。
> 只出方案不改代码。执行：Codex；审查/验收：本人。
> 纪律：改 model 调用/provider/env 前先读 `docs/model-config-governance.md`，走 `.env.local` + `lib/model-routing.ts` 的 modelRole，别在业务路由硬编码。

---

## 0. 一句话现状（扣码）

**"多关键帧"在后端完全不存在。** 当前视频生成仍只吃**段级一张首帧**（`batch-executors.ts:2938 resolveStoryboardFirstFrameUrl(sb)`）；画板那套"每镜头候选" `shotFrames[shotUid]` 只用于**存/显/选**，非段首镜头的选中候选**永远进不了视频 payload**（被 `shot-frame-candidates.ts:377` 镜像闸门挡死）。方案五加的三按钮（全能参考/智能多帧/首尾帧）只是现有 3 个 `submitMode` 的**中文换皮**，没有第四种"多关键帧"通道。Seedance 仍只有 3 role（first_frame/last_frame/reference_image）、两 Builder（A 首尾帧 2 槽 / B 多参考图）、`modesAreMutuallyExclusive=true` 硬互斥。

→ 方案四 = 真正把"片段内每镜头选中候选帧，按时序作多关键帧 + 资产参考图"打通到 Seedance 2.5。

---

## 1. 硬前置（不先收这两处尾巴，方案四无数据源 / 不生效）

### 前置 A（方案三尾巴）：打开并验证"每镜头候选真在生成"
- **现状**：`isPerShotFirstFrameEnabled()`（`ORIGIN_PER_SHOT_FIRST_FRAME`，feature-flags.ts:85）**默认 OFF**，`.env.local`/`.env.example` **均无此行 → 生产实际 OFF**。
- 它在 `storyboard_images` executor `batch-executors.ts:1781-1789` gate 了整条 per-shot 展开：flag OFF → `perShotTarget=null` → `shotFrames` 候选写入（:2007-2063 / per-shot patch :2152-2214）**全不执行**。即**每镜头候选目前根本没产出**，画板候选 UI 是空架子（只有段首一张）。
- **要做**：① 在生产 env 打开 `ORIGIN_PER_SHOT_FIRST_FRAME`；② 跑一个多镜头段验证 `shotFrames[shotUid].candidates` 真的每镜头都产出、选中可用；③ 确认 `per_shot_first_frame_requires_structured_mode`(:1787) 依赖的 `isMultiRefVideoModeEnabled`（默认 ON）满足。**这是方案四 §3 的唯一数据源，必须先绿。**

### 前置 B（方案二尾巴）：把"发车段长"真正接成能力派生
- **现状**：`planSegments` 的 `SegmentPlanOptions{targetMinSec,targetMaxSec,hardMaxSec}`（segment-planning.ts:16-20）只是**参数化的管子**；唯一生产调用点 `frame-workflow-state.ts:111` 裸调 `planSegments(shots)` 不传 option → **永远落硬编码 4/15**（segment-planning.ts:23-24）。**方案二只铺了管、没接能力派生。**
- **要做**：① capability 表加 `maxSingleGenSec`（§3.5）；② `frame-workflow-state.ts:111` 改为按激活模型派生 `{targetMin,targetMax,hardMax}` 传入（2.0→{4,15}、2.5→{20,25,25}）；③ `segment-roles.ts:43 buildShotRoles` 的 option 透传顺带接上；④ `test:segment-planning` 改参数化断言。
- 这步独立于 2.5，可现在做（默认仍 2.0 的 4/15，零行为变化）。

---

## 2. 三模式后端语义（现状 → 目标）

| 用户模式（画板，方案五已做） | 当前 submitMode | 当前 payloadMode/Builder | 目标（方案四） |
|---|---|---|---|
| 全能参考(默认) | `auto` | 自选（通常 B / 首尾帧） | auto：2.5+有每镜头候选时**优先多关键帧 C**，否则退 B/A |
| 智能多帧 | `reference_images` | B（首帧+多资产参考图） | **升级为多关键帧 C**（每镜头候选作关键帧 + 资产参考图） |
| 首尾帧 | `first_last_frame` | A（首+尾两槽） | 不变（A） |

- "智能多帧"是同一个用户模式，**行为随 capability 升级**：2.0/无候选=B，2.5+有候选=C。不新增用户可见模式（UI 不动）。
- 涉及枚举三处同步：`feature-flags.ts:9 VideoSubmitMode`、`video-payload-decision.ts:11/59`、`board.js:237`（`normalizeVideoSubmitMode` 当前只认 auto/reference_images/first_last_frame）。

---

## 3. 智能多帧 = 多关键帧 payload（核心，精确改点）

> 数据源已就绪（前置 A 打开后）：`shotFrames[shotUid].candidates` + `selectedCandidateId`（方案三真持久结构，frames/candidate API 全在），**方案四直接复用、不重建**。

1. **执行器加"多关键帧读取路径"**（`batch-executors.ts:2938` 旁，不删原 `resolveStoryboardFirstFrameUrl`）：当走多关键帧模式且 `groupShotIndices.length>1` 时，遍历 `groupShotIndices` → 每镜头读 `sb.shotFrames[shotUid].candidates` 的 `selectedCandidateId` 对应 url → **按镜头时序组装关键帧序列**（带 shotIdx/序号/时间码）。原段级 `frames.first` 路径保留给 auto/B/A（段首镜头那张，由现有镜像闸门维护，**不动 `shot-frame-candidates.ts:377` 闸门**）。
2. **新 payloadMode** `multi_keyframe_multi_ref`（`video-payload-decision.ts:11`）；`VideoPayloadDecision`（:43-57）的 `firstLastFrameMode`（只装 2 张）旁加多关键帧列表容器 `multiKeyframe:{ keyframes:[{path,shotUid,orderIndex,atSecHint?}], references:[...] }`；`resolveVideoPayloadDecision`(:207) 加分支、`deriveVideoSubmitInputMode`(:194) 加映射。
3. **新 `keyframe` role**（`video-reference-manifest.ts:11 VideoReferenceRole`）+ `buildReferenceBriefLine`(:272) 加分支（"Image N=第 k 镜关键帧，约第 t 秒"）；`reference-matcher.ts:385 addFirstFrameCandidate` 扩为"多关键帧列表"各占 budget 名额、按序 imageNo。
4. **新 Builder C**（`video-gen.ts` 新增，参照 `:2113 buildSeedanceFirstLastFrameBody` 的 body 形状）：按 Seedance 2.5 多关键帧 schema 拼 content（N 张 image + 各自 frame index/时间码 + 资产参考图）。**字段名按已公开规则+最佳已知形拼 + 留适配层**（§5 一单收口）。
5. **capability 结构扩字段**（`video-provider-capabilities.ts:53-66`）：加 `maxSingleGenSec` / `referenceBudget` / `supportsMultiKeyframe`，`bodyShape` 加 keyframe 取值；CAPABILITIES 加 **2.5 行**（30 / 50 / multiKeyframe:true / `modesAreMutuallyExclusive:false`，verifiedBy 记 §5 探针 task）。2.0 三行不动。
6. **互斥放宽（仅 2.5）**：`modesAreMutuallyExclusive` 对 2.5 设 false（允许多关键帧+参考图同请求）；2.0 保持 true。严格按 model id 分流，2.0 零变化。
7. **预算上调（能力派生）**：`VIDEO_REFERENCE_IMAGE_BUDGET=9`（video-reference-manifest.ts:6）+ `reference-matcher.ts:539` 的 `Math.min(...,9)` 两处改为读 capability `referenceBudget`（2.0=9、2.5=50）；注意角色子配额 `selectCharacterReferencePanels maxSlots:5`（reference-matcher.ts:557）随总预算一并评估上调。
8. **前端**：`board.js:982` 模式数组——"智能多帧"沿用（行为由后端按 capability 升级），无需加第 4 个按钮；把每镜头选中候选作为关键帧序列提交（已有 submitMode 透传管线，只扩 payload 内容）。

---

## 4. 策略：现在全做、上线切参数（不变）

- 按 Seedance 2.5 已公开规则（30s + 50 参考 + 帧参考/关键帧 at-points）**现在就把 §1–§3 全部写完**；30/50/发车 20-25 全做成"跟激活模型走"的能力派生（默认仍 2.0 的 15/9/4-15，**线上零行为变化**）。
- **上线当天**：① 把 `VIDEO_MODEL` env 指向 2.5 model id（数字/发车/预算自动切）；② 发一条测试单对齐关键帧字段名（§5）；③ 万一没对上，智能多帧临时走现成 Builder B（首帧+多参、吃满 50）兜底，不阻塞。

---

## 5. 上线当天"一单收口"（不阻塞现在动手）

Seedance 2.5 一公测，发一条测试视频（参照 2.0 验证 task `cgt-20260510020416-zrtrs`，记入 capability `verifiedBy`）确认：① 多关键帧 content 字段名（3 候选形：多个 `role:'keyframe'`+时间戳 / first+last+N 中间帧带 at_sec / 顶层 `keyframes:[{url,at}]`）；② 关键帧与 reference_image 可否同请求；③ 时间锚点单位；④ ≤50 拆分 + 单图/体字节上限；⑤ 30s duration 合法区间。Builder C 先按最可能形写 + 适配层，测出真名后切。**这是"上线开关"，不是"开工前提"。**

---

## 6. 剩余尾巴清单（1/2/3/5 未完成，独立于多关键帧）

| # | 尾巴 | 现状（扣码） | 要做 |
|---|---|---|---|
| T1 | **方案三：per-shot 候选未真产出** | `ORIGIN_PER_SHOT_FIRST_FRAME` 默认 OFF + env 未开（feature-flags.ts:85；gate 在 batch-executors.ts:1781） | = 前置 A：生产打开 + 验证候选真产出（**最优先，卡方案四**） |
| T2 | **方案二：发车未能力派生** | 仍硬编码 4/15（frame-workflow-state.ts:111 裸调） | = 前置 B：capability `maxSingleGenSec` + 调用点接派生 |
| T3 | **方案五 5-2：未选候选进素材库** | 底层 `ensureVevDemoBindingForVideoTask`(vevdemo-material-registration.ts:755) 就绪；但 `edit.js` 素材面板(~:4600) 只有 clips/uploads 两 tab，**无"未选候选"tab、无拖入懒注册 hook** | 加第三 tab 列未选候选（按 group 取 is_current=false 的 video_tasks）+ 拖入时调 `ensureVevDemoBindingForVideoTask` 注册 vid://。VevDemo 集成风险高，建议先小探针 |
| T4 | **方案五 5-3：死代码 DOM** | `btnGenAllImages`(workspace.html:923 + main.js:8814 仍绑) / `#pageBatch`(:1131) / `navBatch`(:242 hidden retired) / 启动 CSS(:62) 残留 | board 达对等后移除这些死 DOM + 解绑；`generateAllImages` 按每片段按钮取代后清 |
| — | legacyStoryboardArchive / subtitle / legacy_pencil | 归档 UI 已 gate off（project.js:31）保留兼容；subtitle 剪辑页合法在用；legacy_pencil 纯读兼容无写入 | **保留，不清**（老数据/合法用途） |

> T1/T2 是方案四硬前置（§1）。T3/T4 是方案五自己的 Phase 5-2/5-3，可独立排期，不卡方案四。

---

## 7. 分期

- **Phase 4-0（硬前置，最先）**：T1 打开并验证 per-shot 候选产出；T2 发车能力派生接线。两者绿了方案四才有数据源/段长口径。
- **Phase 4-1（脚手架，现在做）**：capability 加 2.5 行 + `maxSingleGenSec`/`referenceBudget`/`supportsMultiKeyframe` 字段；model 白名单加 2.5 id；预算/互斥改能力派生（默认仍 2.0）。
- **Phase 4-2（多关键帧 payload，现在做）**：新 payloadMode + keyframe role + 执行器多关键帧读取路径 + manifest keyframe 列表 + Builder C（已知形+适配层）。
- **Phase 4-3（接通默认）**：全能参考 auto 在 2.5+有候选时路由多关键帧；发车随能力派生生效。
- **上线当天**：切 `VIDEO_MODEL` + 一单收口（§5）。
- 尾巴 T3/T4 并行独立排。
- 顺序：4-0 →(并) 4-1 → 4-2 → 4-3 →（上线）切换收口。

---

## 8. 测试 / 验收
- `test:per-shot-candidates-on`：flag 打开后多镜头段每镜头产出 `shotFrames` 候选、选中可用（T1 验收）。
- `test:segment-planning`（参数化）：传 {4,15}=现状、{20,25}=2.5；frame-workflow-state 调用点确实传了能力派生 option（T2 验收）。
- `test:video-mode-routing`：submitMode×capability → 正确 payloadMode（2.0 智能多帧=B、2.5=C；首尾帧=A；auto 分流）。
- `test:multi-keyframe-payload`：执行器按 groupShotIndices 取各镜头选中候选、按时序排关键帧；缺候选退化 B；2.0 不受影响。
- `test:capability-derived-budget`：2.0 预算 9/互斥 true；2.5 预算 50/互斥 false；切 model 自动变。
- 验收硬线：**2.0 现网路径逐字节零变化**（capability gate）；回归全绿；改 model/env 已遵 `docs/model-config-governance.md`。

---

## 9. 风险与规避
| 风险 | 规避 |
|---|---|
| **per-shot 候选实际没产出（T1）** | 方案四第一步先打开 flag + 验证，没绿不进 4-2；否则多关键帧无数据源。 |
| 发车能力派生方案二没做（T2） | 方案四补接线，独立于 2.5、可先做，默认 2.0 零变化。 |
| 2.5 关键帧字段名官方未贴 | 现在按已公开规则+最佳已知形写 Builder C+适配层；上线一单对齐（10 行内）；对不上走 B 兜底。 |
| 改 budget/互斥污染 2.0 | 全按 model id/capability 分流，2.0 capability 不动，契约锁"2.0 零变化"；角色子配额 maxSlots:5 同步评估。 |
| 多关键帧与首尾帧物理互斥 | 2.0 硬互斥保留；2.5 按官方"50 多模态含帧参考"设计为可同请求，一单确认。 |
| VevDemo 素材库集成（T3） | 先小探针再接线，风险隔离，不阻塞方案四。 |
| 30s 长片计费/超时 | 计费口径需同步评估；轮询超时现 15min（video-gen.ts:1179）确认对 30s 够。 |

---

## 10. 待你点头
- **A. 先收前置 A/B**：确认方案四第一步先把 `ORIGIN_PER_SHOT_FIRST_FRAME` 打开+验证、并把发车接成能力派生（这俩是方案二/三留的尾巴，且卡方案四）。
- **B. 智能多帧语义 + 随能力升级**：每镜头选中候选按时序作关键帧 + 资产参考图；同一"智能多帧"模式 2.0=多参/2.5=多关键帧，UI 不变。
- **C. 能力派生**：2.5 激活即发车 20-25 / 预算 50 / 互斥放宽自动生效（按 model id 分流）。
- **D. 尾巴排期**：T3（未选进素材库）/T4（死代码）何时做——跟方案四并行还是单独排？
- **E. 计费**：30s/多参成本上升的积分口径，单独定还是并入。

---

## Sources
- 代码（本地，HEAD `8dadba2`，行号见正文）：`lib/batch-executors.ts`(video_segments :2821/:2938、storyboard_images per-shot gate :1781、写回 :2007-2214)、`lib/shot-frame-candidates.ts`(:364-417 镜像闸门)、`lib/visual-reference-state.ts:41`、`lib/video-payload-decision.ts`(:11/:207)、`lib/video-gen.ts`(:1056/:1345/:2113)、`lib/video-provider-capabilities.ts`(:53-124)、`lib/video-reference-manifest.ts`(:6/:11)、`lib/reference-matcher.ts`(:385/:539/:557)、`lib/model-routing.ts`(:63-72/:225-237)、`lib/segment-planning.ts`(:16-24)、`lib/frame-workflow-state.ts`(:111)、`lib/feature-flags.ts`(:9/:85)、`lib/vevdemo-material-registration.ts`(:755)、`public/modules/{board,board_state,edit}.js`、`public/workspace.html`、`.env.local`/`.env.example`。
- Seedance 2.5 能力（30s/50 参考/帧参考-at-points，官方请求字段名未公开）：https://the-decoder.com/bytedances-seedance-2-5-breaks-the-30-second-barrier-for-ai-video-generation/ ；Seedance 2.0 API 参考 https://docs.byteplus.com/en/docs/ModelArk/1520757 。
