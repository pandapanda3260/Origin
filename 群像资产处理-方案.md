# 群像资产处理 - 方案（匿名群体）

## 结论 / 方向
- 保留群像（选 B），但定义为**匿名群体**：只锁集体气质，不锁单脸，永远不内含需要保持一致的具名角色。
- 复用已有 `isCrowd` 字段标记群像，**不新建资产类别、不建成员清单、不写老数据迁移**。

## 为什么群像 = 匿名群体（fork 定论）
参考图只为「跨镜头必须一致的东西」而存在。群像要一致的是集体质感（人群规模、穿着风格、密度、神态分布），**单张脸不是一致性目标**——没人会注意「左起第三个少年」在不同镜头是不是同一张脸。反过来，一旦某个人的脸需要保持一致，他就已经是具名角色、该有自己的资产 —— 所以「群像内含要锁脸的主角」是自相矛盾。「反复出现的三师兄弟」属于三个角色资产，不是群像。
**结论：群像这个桶永远不支持具名成员一致性，不为 Fork 2 留设计。**

## 现状问题（已扣代码）
1. 抽取提示词 `SP_ASSET_CHARACTERS_EXTRACT`（`lib/prompts.ts:1125`）无群像概念，输出 schema 无 `isCrowd`，群像漏成普通身份角色。
2. `split-character-panels`（`app/api/assets/split-character-panels/route.ts`）对所有角色**无条件**切 头/正/侧/背 四视图，群像也被切。
3. casting face-lock（`app/api/assets/extract/route.ts:226` `appendCharacterCastingPrompt`）**无差别注入**，正在给一群人锁同一张脸的族裔/长相基线 —— 对群像是反的。
4. 唯一的群像识别在选帧（`lib/frame-image-plan.ts:253` `isCrowdCharacter`），靠名字/标签**正则**判定，脆；且不影响出图。

> 存量样本：项目 `proj_1780831509375` 的 `c4 考核少年少女群像`，`isCrowd` 缺失、五视图（sheet/headshot/front/side/back）全生成，即此问题。

## 设计（五条）

**1. 数据模型** —— 群像是 character 资产的子类型，用 `isCrowd=true` 标记；可选 `crowdSize`（粗档：几人/十几人/成群）。**无 members 字段。**

**2. 抽取**（`prompts.ts:1125`）—— schema 增 `isCrowd` 输出 + 判定规则：「一群无名同类人（考核群像/围观弟子/路人甲乙）→ `isCrowd=true`；能点名、有独立戏份的个体 → 普通角色」。`isCrowd` 时 `imagePrompt` 写**群体气质**（年龄段/穿着/密度/神态分布），禁止写成单人，并提示「人脸应多样」。判定提前到模型，下游正则降为兜底。

**3. 出图 / 切图** —— `isCrowd` 时 `split-character-panels` **跳过四视图切分**，只产一张整体正面群像图（取景可略宽，它的活是交代规模/穿着/密度）；casting 对 `isCrowd` 降级为只注入「时代/穿着/群体基调」，**不锁单脸**，允许人脸多样。

**4. 选帧**（`frame-image-plan.ts`）—— crowd 分支保留（用途已对）；`isCrowdCharacter` 判定主源从「名字正则」改为**读 `isCrowd` 字段**，正则兜底。「主角站在人群中」的镜头：选帧时 **主角图 + 群像图 同喂**（现有多角色同框机制），不靠群像图内含主角。

**5. 旧数据** —— 不写迁移（按既有习惯）；`c4` 这类存量错图，重生成或删项目即可。

## Blast radius
1 个抽取提示词 + 1 处切图 gate + 1 处 casting 降级 + 1 处选帧判定来源。不新建资产类别、不建成员关系、不迁移。

## 落地分步（认同后再动代码）
1. 数据模型 + 抽取（标 `isCrowd`/`crowdSize`、群体气质 prompt）
2. 出图：切图 gate + casting 降级
3. 选帧判定来源切到 `isCrowd` 字段

> 认同就按此动代码；要改哪条直接说。
