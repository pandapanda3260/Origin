# 管理后台瘦身方案 v1

> 2026-06-12 · 基于全量实扫 app/admin + app/api/admin + lib/admin-*（约 8700 行）+ 全网行业调研
> 性质：设计方案，待审批，未动任何代码

---

## 一、一句话结论

后台不是"功能太少要补"，是 **16 个页面承载了 7 个页面的事**，外加 **三套消耗账本各说各话**。瘦身方向：**16 页砍到 7 页，每个数字只允许有一个出处**，UI 收口到统一组件层。不打补丁，按"止血 → 骨架 → 合并 → 锁口径"四个阶段重排。

---

## 二、现状实扫（不是推断，每条带文件证据）

### 2.1 页面清单（导航 13 项 + 知识库 3 子页 + 登录 = 16 页）

| # | 页面 | 路由 | 干什么 | 数据来源 |
|---|------|------|--------|----------|
| 1 | 问题队列（首页） | app/admin/route.ts | 4 张异常卡 + 4 个 KPI + 系统日志 | problem-queue / stats / logs 三个 API |
| 2 | 用户管理 | app/admin/users/route.ts | 用户列表 + 禁用/恢复/强制下线 | users + user_credits + projects |
| 3 | 财务积分 | app/admin/billing/route.ts | 6 个 tab（余额/订单/Ledger/成本/价格/兑换码）+ 人工调账 | credit_ledger + billing_orders + api_price_catalog |
| 4 | Token 统计 | app/admin/token-stats/route.ts | KPI + 按用户 + 按分类 + 明细 + CSV | **token_usage_events**（lib/token-usage.ts:270） |
| 5 | 时间统计 | app/admin/time-stats/route.ts | KPI + 按用户 + 按环节 + 明细 + CSV | **任务表派生**（lib/time-usage/queries.ts:214,292,364,428） |
| 6 | 客服检索 | app/admin/search/route.ts | 一框搜 6 类实体（用户/订单/项目/批次/视频/导出） | 各业务表 |
| 7 | 任务管理 | app/admin/tasks/route.ts | 4 源任务列表 + 取消/重试/强制失败/退款检查 | batches / batch_tasks / video_tasks / exports |
| 8 | Admin 账号 | app/admin/staff/route.ts | admin 账号增删 + 改密 | admin_users |
| 9 | 系统配置 | app/admin/config/route.ts | 维护横幅 / 注册 / 视频 / 导出开关 + 全局并发上限 | system_config |
| 10 | Key 池 | app/admin/key-pool/route.ts | 7 个模型槽位健康 + 10 分钟指标 | model-routing 状态 + 内存指标快照 |
| 11 | 内容审核 | app/admin/content/route.ts | content_flags 人工复核（隐藏/忽略） | content_flags |
| 12 | 存储管理 | app/admin/storage/route.ts | 孤儿文件扫描 + 隔离清理 | 文件系统扫描（500 条硬截断） |
| 13-15 | 知识库 ×3 | app/admin/knowledge/{,dry-run,audits} | 知识卡编辑 / 注入预演 / 7 天审计 | knowledge 系列表 |
| 16 | 登录 | app/admin/login/route.ts | — | admin_users |

### 2.2 架构里的资产（瘦身时必须保留的部分）

实扫发现底子并不烂，有四样东西是行业正规做法，**动结构不动它们**：

1. **统一壳层**：所有页面经 `renderAdminPage()` 渲染，导航/标题/鉴权收口在 lib/admin-shell.ts 一处；
2. **审计制度**：写操作走 `withAdminAudit`，带 `x-admin-reason` + 幂等键（lib/admin-audit.ts，526 行）；
3. **两段式提交**：调账/任务操作/配置保存都是 dryRun 预检 → confirm → 提交（如 billing/route.ts:183-189）；
4. **问题队列的阈值制度**：lib/admin-thresholds.ts 集中定义卡死/退款比/高频失败阈值。

### 2.3 病根诊断（对应你说的六点感受，每条有实锤）

**病根 A：同一件事散在多页 —— 这是"页面多、功能重复"的直接原因**

- 任务处理在 2.5 个地方：首页问题队列卡片（只读 4 条预览）、任务管理页（全量操作）、还有 problem-queue API 自带的一套 requeue/force_fail/cancel 写路径（app/api/admin/problem-queue/route.ts:321-435）与 tasks API 的写路径平行存在 —— **同一个动作两套 API**；
- 用户信息在 4 个地方：用户管理、客服检索、财务积分"余额"tab、首页 KPI，四处各查各的 SQL；
- 搜索框有 4 个：用户管理、任务管理、客服检索、财务积分，各有各的字段规则。

**病根 B：算了没人看、看见的不准 —— 这是"不知道哪些信息有效"的直接原因**

- `/api/admin/stats` 计算并返回 userUsage / recentUsers / paidAmounts / onlineUsers 四块数据，**全仓 grep 无任何页面消费**（死载荷）；更糟的是其中 `totalTokens` 实际是 `credit_ledger.amount`（积分），标签却叫 Token（app/api/admin/stats/route.ts:51）——一旦哪天有人渲染它，就是一个看起来像真的的假数字；
- 首页"失败 / 卡住任务"卡片，查询只筛 `status = 'running'` 超时的（problem-queue/route.ts:40,51,61,71），**failed 状态根本不在里面**，标题和数据对不上；
- problem-queue API 算好了 `needsReviewTasks`（needs_review 是你这套任务体系里最需要人工处理的状态），**首页根本没渲染它**（app/admin/route.ts:201-220 只渲染另外四组）；
- 时间统计页自己的 notes 承认 V1 不统计剧本直连生成、提示词微调、资产抽取、VevDemo 回调（app/api/admin/time-stats/route.ts:105）——口径盲区只写在小字里。

**病根 C：三套消耗账本，三个页面，三种口径 —— 这是"内容多、逻辑乱"的直接原因**

| 账本 | 表 | 页面 | 量纲 |
|------|----|------|------|
| Token 账本 | token_usage_events | Token 统计 | token 数 |
| 积分/钱账本 | credit_ledger + billing_orders | 财务积分 | 积分、人民币 |
| 等待时间账本 | batch_tasks 等 4 表派生 | 时间统计 | 秒 |

三个量纲本身都合法，但做成了三个**长得几乎一样**的页面（toolbar + 4KPI + 两张汇总表 + 明细 + CSV，token-stats 与 time-stats 的页面代码结构逐段对应）。这正是行业说的 dashboard sprawl 的典型起源："复制一个页面改一点"。

**病根 D：UI 乱的根源是没有组件层**

每个页面自带一坨 inline `<style>`：token 页控件高 48px、time 页 42px、billing 页混用默认高度；admin-shell.ts 已经定义了完整的 CSS 变量体系（颜色/字号/圆角），但各页各写各的类名，变量形同虚设。**乱不是因为缺设计，是因为同一套设计被手抄了 13 遍。**

---

## 三、行业成熟方案要点（全网调研结论）

1. **异常驱动管理（Management by Exception）**：后台首页不是"数据陈列馆"，只展示突破阈值、需要人处理的事，每个域控制在 5-10 条以内，每条带处理动作和负责人，形成"清队列"的工作节奏。参考：[DataCult: Exception-First Dashboards](https://www.datacult.ai/2026/03/16/resources-exception-first-dashboards-design/)、[PortMA: Management by Exception](https://portma.com/resources/articles/management-by-exception-in-dashboard-reporting-s1-e013-01/)。你的问题队列首页方向是对的，缺的是把 needs_review 接上、把动作放上去。
2. **5 秒原则与信息层级**：打开页面 5 秒内抓到主信息；层级 = 结构/导航/分组/标注/过滤六件事，不是图表数量。参考：[5of10 Dashboard Design Guide](https://5of10.com/articles/dashboard-design-best-practices/)、[GoodData: Six Principles of Dashboard IA](https://www.gooddata.ai/blog/six-principles-of-dashboard-information-architecture/)、[Pencil & Paper: Dashboard UX Patterns](https://www.pencilandpaper.io/articles/ux-pattern-analysis-data-dashboards)。
3. **初期 SaaS 后台的标准配置**就五样：用户管理、客服查询、计费补救、审计日志、紧急开关——**明确不包含高级分析报表**。"把最耗团队时间的支持动作做顺"优先于一切。参考：[Sequenzy: SaaS Admin Panel](https://www.sequenzy.com/blog/how-to-build-saas-admin-panel)、[Flatlogic: Admin Panel Development](https://flatlogic.com/blog/admin-panel-development/)、[DronaHQ: Essential Admin Panel Features](https://www.dronahq.com/admin-panel-features/)。你现在的 16 页里，行业标配的五样其实都有，问题是被另外 11 页稀释了。
4. **指标单一事实源**：一个指标 = 一个定义 + 一条计算路径，定义集中登记；同一个数在两个页面对不上是后台信任的头号杀手。参考：[Improvado: Metrics Layer](https://improvado.io/blog/what-is-a-metrics-layer)、[PowerMetrics: Single Source of Truth](https://www.powermetrics.app/faq/single-source-of-truth-for-metrics)。
5. **仪表盘蔓延治理**：信号超过甜点区后，每加一个指标反而拖慢问题定位；定期删页、合并"复制改一点"的页面、每页有 owner。参考：[Grafana Dashboard Best Practices](https://grafana.com/docs/grafana/latest/visualizations/dashboards/build-dashboards/best-practices/)、[Datadog: Dashboards at Scale](https://www.datadoghq.com/blog/dashboards-monitors-at-scale/)、[DevOps.com: More Signal, Less Clarity](https://devops.com/more-signal-less-clarity-the-observability-paradox-no-one-wants-to-talk-about/)。

---

## 四、目标形态（定论）：16 页 → 7 页

```
运营（每天看）
 ├─ 1. 问题队列        ← 现首页 + 任务管理合并；补 needs_review；动作就地处理
 └─ 2. 客服检索        ← 现客服检索 + 用户管理合并；动作长在搜索结果上
账务（对账时看）
 ├─ 3. 财务积分        ← 保留；tab 收敛
 └─ 4. 用量统计        ← Token 统计 + 时间统计合并为一页两 tab
系统（出事/配置时看）
 ├─ 5. 系统与安全      ← 系统配置 + Key 池 + 存储管理 + Admin 账号合并
 ├─ 6. 内容审核        ← 保留独立（有人工复核流程语义）
 └─ 7. 知识库          ← 3 子页合并为一页三 tab
```

### 每页去留判决表

| 现页面 | 判决 | 理由 |
|--------|------|------|
| 问题队列（首页） | **重做为唯一任务工作台** | 方向对（异常驱动），但补 needs_review 渲染、把任务管理的操作按钮挪进来；"失败/卡住"卡片改名并修口径 |
| 任务管理 | **并入问题队列** | 它本质是问题队列的"全部任务"视图；两套任务写 API 收敛为一套（保留 tasks API，problem-queue 的 POST 写路径退役或转调用） |
| 用户管理 | **并入客服检索** | 你初期没有"批量管理用户"的场景，只有"出事了查这个人"的场景；禁用/恢复/强下线三个按钮直接长在搜索结果的用户卡片上 |
| 客服检索 | **升级为第 2 页** | 一框搜已经是对的形态，吸收用户操作后成为唯一"查人查单"入口 |
| 财务积分 | **保留** | 钱的事必须独立页；6 tab 收敛为 4（余额/订单/Ledger/价格与成本），调账保留两段式 |
| Token 统计 | **与时间统计合并** | 两页结构逐段同构；合并后共用 toolbar/筛选/明细组件，tab 切换量纲 |
| 时间统计 | 同上 | 同上；V1 口径盲区从小字提为页头声明 |
| Admin 账号 | **并入系统与安全** | 低频维护操作，不值一个导航位 |
| 系统配置 | **作为系统与安全的第一区块** | 开关/并发是出事时要找的东西，和 Key 池放一起 |
| Key 池 | **并入系统与安全** | 它的"红灯"已经在问题队列有卡片；详情视图并入系统页即可 |
| 存储管理 | **并入系统与安全** | 低频维护；500 条硬截断在页内标注 |
| 内容审核 | **保留** | 有独立的"待办→处理→记录"流程，且涉及合规，不与监控类混排 |
| 知识库 ×3 | **合并为一页三 tab** | lib/knowledge 是真实接入生成链路的子系统，不能砍；但三个导航位太奢侈，渲染函数 renderKnowledgeAdminPage(req, mode) 本来就是参数化的，合并成本极低（app/admin/knowledge/route.ts 仅 9 行） |
| 登录 | 保留 | — |

### 信息删减清单（比页面合并更重要）

- 删 `/api/admin/stats` 的 userUsage / recentUsers / paidAmounts / onlineUsers 死载荷（含错误口径 totalTokens），该 API 只留首页 4 个 KPI 真正消费的字段；
- 首页 KPI 维持 4 个不再加（用户/项目/在线/付费，全部权威表 COUNT，口径干净）；
- 系统日志面板从首页降级：日志是排查工具不是日常信息，挪到"系统与安全"页（首页保留最近 1 条 error 的提示条即可）；
- 每页自带的"加载中..."/错误提示散装文案，统一进组件。

---

## 五、准确性治理（你的原则 1，本方案的重心）

**制度：指标口径登记表。** 新增 docs/admin-metrics-registry.md，每个后台展示的数字一行：指标名 → 权威表 → 计算函数位置 → 已知盲区 → 契约测试名。Review 任何后台改动时先对这张表。这是行业 metrics layer 的最小化做法——不引入任何新框架，只是把"定义只许有一处"变成纪律。

**三条硬规则（写进 CLAUDE.md 的后台小节）：**

1. 页面（route.ts 的 HTML/JS）里**禁止出现业务口径**：不许在前端 JS 里 sum/filter 出新指标，所有数字由 API 给成品值；
2. 一个指标只许一条 SQL 路径：发现第二处计算同名指标 = bug（当前实锤：error 字段 `COALESCE(error_message, error_msg)` 的容错顺序在 problem-queue 与 tasks API 里手抄了 9 遍（5+4），下沉为一个 SQL 片段常量）；
3. 有盲区的统计必须页头声明：时间统计 V1 那四条"不统计什么"从 notes 小字提为页头常驻说明，宁可承认不全，不可显得全而不准。

---

## 六、UI 统一（不打补丁的做法）

不逐页修样式（那就是补丁上打补丁），而是**先建组件层、再整页迁移**：

1. admin-shell.ts 扩展出 ~8 个标准组件类：`adm-toolbar`（筛选条）/ `adm-kpi`（指标卡）/ `adm-table`（表格）/ `adm-badge`（状态徽章）/ `adm-empty`（空态）/ `adm-notice`（通知）/ `adm-tabs` / `adm-actions`（按钮组），尺寸字号全部引用既有 CSS 变量；
2. 各页 inline `<style>` 只允许保留布局差异（grid 列数），控件样式一律删除改用组件类；
3. 迁移顺序与第七节阶段对齐：哪页被合并重写，哪页同时换组件，**不存在"老样式新样式混跑"的中间态页面**。

---

## 七、分阶段落地（每阶段独立可验收，可随时停）

**阶段 0 · 止血（半天，不动 UI 结构）**
- 删 stats API 死载荷（含假 totalTokens）；
- 首页渲染 needsReviewTasks 卡片（数据现成，只加一个 renderQueueList 调用）；
- "失败/卡住任务"卡片改名"卡住任务"或补 failed 查询（建议：补最近 24h failed，与 stale 分行展示）；
- 验收：tsc 0 错 + 首页手测能看到 needs_review 任务。

**阶段 1 · 骨架（1 天）**
- admin-shell 组件类落地 + 导航重组为 7 页结构（先改导航和空壳，旧页面路由直接删，**不做老路由兼容**——后台只有内部人用）；
- 验收：7 个导航项可点，每页壳渲染正常。

**阶段 2 · 合并（2-3 天，按页验收）**
- 2a 问题队列吞任务管理（统一任务写 API 为一套）；
- 2b 客服检索吞用户管理；
- 2c Token+时间合并为用量统计；
- 2d 系统与安全四合一；知识库三合一；
- 每步独立提交，独立手测，出问题按你的规矩直接回滚该步。

**阶段 3 · 锁口径（1 天）**
- docs/admin-metrics-registry.md 全量登记；
- 契约测试锁关键指标 SQL（沿用现有 npm test 体系，新增 test:admin-metrics）；
- error 字段 COALESCE 下沉为共享常量。

总量约 5 个工作日。阶段 0 可以今天就做，与后续阶段解耦。

---

## 八、不做的事（边界）

- 不动 withAdminAudit / dryRun / 幂等 / 阈值表四样资产；
- 不动任何业务侧 API 和表结构（纯 admin 读写路径内改造）；
- 不引入前端框架/图表库/admin 模板（当前服务端渲染 + 原生 JS 的形态对单人维护是合理的，引框架才是真正的大手术）；
- 不做权限分级（RBAC）：现在 admin 就你一人，做了是空转；登记表里留一句"用户>3 人时再议"；
- 不做移动端适配优化（现有 media query 保底即可）。

---

## 九、需要你拍板的两个点（带我的默认判断)

1. **知识库三页你现在真的在用吗？** 我的默认判断是"在用、合并保留"（它接着生成链路）。但如果你过去一个月没打开过它，更狠的做法是整组下架（导航摘除，路由保留），等用到再挂回来——比合并还省。
2. **时间统计的去留。** 我的默认判断是"并入用量统计保留"，因为它是你看"用户等多久"的唯一窗口。但它的 V1 盲区多，如果你实际从不看它，阶段 2c 可以改为只保留 Token 统计、时间统计整页下架，省 1 天工作量。

不回复则按默认判断执行。
