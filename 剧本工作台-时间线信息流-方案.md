# 剧本工作台改造：时间线信息流 + 历史版本恢复 — 落地方案

状态：已审批，已落地（2026-06-10）。实现与本方案一致，另有两点实现期补充：① 三条生成路由的 done 事件额外带回 scriptTimeline 数组，前端写回内存（否则随后的整项目 PUT 会用旧数组盖掉后端的 append）；② 会话内"被替换版本沉为折叠卡"用增量插入实现，冷重建只在项目切换/强制时执行，避免与 live 气泡重复。
前置已拍板：做持久化聊天流；历史草稿折叠+可恢复；存项目 JSON；留最近 20 版全文；老项目不迁移；"历史版本"按钮已删。

## 1. 目标

剧本工作台从"只显示最新一版的单卡"改为**持久化时间线**：用户指令、系统事件、每一版剧本草稿都按时间记录、刷新后可回放；历史草稿折叠显示并支持一键恢复。解决两类问题：看不到自己做过什么（指令/历史全丢）、坏结果覆盖好结果后无法回滚（6-10 事故的防线二，防线一是已上线的五段式守门）。

## 2. 数据结构

分集字段新增 `scriptTimeline`（数组，加入 `EPISODE_FIELDS` 镜像），事件三类：

```js
// 指令：用户触发生成/改写时说的话
{ id, ts, type: "instruction", text, source }   // source: revise|expand|convert
// 草稿：每次产生新剧本全文记一版
{ id, ts, type: "draft", version,               // version 自增，从 1 开始
  script,                                        // 全文；被裁剪后此字段删除
  summary,                                       // 首行 ≤40 字，裁剪后仍保留
  source,                                        // generate|revise|expand|import|edit|restore
  instructionId,                                 // 关联触发它的指令事件，可空
  emotionSegments }                              // 可选，恢复时用；体积考虑只在 ≤20 版窗口内保留
// 系统事件：一句话
{ id, ts, type: "system", text }                // 如"已确认剧本""已删除当前剧本"
```

`id = ts + 随机短串`。咨询阶段聊天**不并入**该结构——`scriptConsult.messages` 现有存储/回放机制不动（没坏不修），渲染时整体排在时间线最前（咨询必然发生在第一版草稿前，消息无时间戳，组内保持原序）。

## 3. 写入点（7 处）

后端 3 处（落库时顺带 append，权威）：
- `full-create`（create/adapt/revise）：revise 记 instruction+draft，create/adapt 记 draft
- `consult/confirm`：记 draft(source:generate)
- `expand`：记 instruction(扩充方向)+draft

前端 4 处（`safeWriteBack`）：
- 手动编辑保存 `_saveScriptEdit`：draft(source:edit)
- 导入应用 `_applyImportedDraft`：draft(source:import) + system("导入内容已应用")
- 删除剧本 `_deleteCurrentScript`：system("已删除当前剧本")。**timeline 不清空**——删除本身是历史，且删错了还能从历史恢复
- 恢复版本（新功能）：draft(source:restore) + system

确认剧本 `confirmScript`：system("已确认剧本，进入风格制定")。
后端 append 基于路由内已读的 proj 计算新数组；生成期间用户并发改 timeline 的概率极低，极端情况丢一条历史记录，可接受（非关键数据）。

## 4. 渲染与回放

刷新后重建流（替代现状"只回放咨询"）：咨询气泡 → 时间线事件按 ts 升序。历史 draft 渲染为**折叠卡**：`v{n} · 来源 · 相对时间 + 首行预览`，点开看全文，卡上两个动作：展开/收起、恢复此版本。instruction 渲染为用户气泡（和现有指令气泡同款），system 渲染为现有 status 行样式。

**最新一版不在流里重复出现**——它就是底部那张现有工作卡（单例卡保留，编辑/扩充/确认都不变）。卡片角标区的写死文案 `版本 1.0 · 刚刚` 换成真实 `v{最新版本号} · 相对时间`；状态角标（待确认/已确认/已修改）保留不删——它承载确认门禁状态，删除 icon 也挂在它旁边。

## 5. 恢复版本行为

点"恢复此版本"→ 确认弹窗 → 写回：`script/scriptDraft = 该版全文`、`scriptApproved=false`、`scriptReviewState="draft"`，走现有 `markDownstreamStale("script")` 标记下游（**不清空**下游数据，沿用手改剧本的口径）；该版若带 emotionSegments 一并恢复，否则情绪段置空并提示"重新分析情绪"；append draft(source:restore)。

## 6. 容量与裁剪

每次 append 后执行：draft 全文只保留最近 20 条（更早的删 `script`/`emotionSegments`，留 summary，卡片变"仅摘要、不可恢复"态）；时间线总条数上限 200，超出从头 FIFO 丢弃。预估体积：20 版 × ~1KB ≈ 20KB/集，可接受。

## 7. 不做的事

- 不迁移老项目：无 `scriptTimeline` 的项目从启用后开始记录，流里只有咨询历史+当前卡
- 不动 `scriptConsult` 存储与后端 consult 路由
- 不建新数据库表，不动 projects 表结构（timeline 随 data_json 整行走，与 SSE 整包回拉/409 回拉天然兼容）

## 8. 改动清单

后端：`lib/script-timeline.ts`（事件构造+append+裁剪，纯函数）；3 个路由各 +5 行左右。
前端：`script.js`（回放渲染、折叠卡、恢复逻辑、5 个写入点）、`styles.css`（折叠卡样式）、`workspace.html`（折叠卡模板/版本行）。`episode_fields.js` 加字段。
预估：后端小，前端中等（主要在回放渲染和折叠卡交互）。

## 9. 验证

tsc + node --check；手测主链路：生成 → 改写×2 → 刷新（应看到咨询历史+2条指令+2张折叠卡+最新卡）→ 恢复 v1（下游标 stale、角标回"待确认"）→ 删除剧本 → 流里留下完整历史且可从历史恢复。守门拒绝的输出不产生 draft 事件（修改失败不污染时间线）。
