# 方案一 · 画板引擎落地方案（纯前端 / 只读投影 / flag 共存）

> 属于《画板模式 PRD v2》落地拆分的第 1 个方案。**本方案只做"画板能力"本身**：自建视口（平移/缩放/连线/裁剪）+ 固定拓扑布局 + 把现有项目数据**只读投影**成四节点流程图。不改后端、不接生成、不写任何业务态。
> **里程碑1 闸门**：真实项目能渲染成"参考图→镜头计划→片段/镜头首帧→视频"四节点流程图，能平移/缩放/全览/自动连线，无 console 报错，`npm run verify:frontend` 绿，且**默认 flag 关时线上零影响**。过闸后才进方案二。
> 执行：Codex 落地，我审查验收。所有接线点带文件路径+行号（已扣 HEAD 实读）。

---

## 1. 范围（必须卡死，避免膨胀）

**做**：
- 自建视口引擎：平移、缩放（滚轮对准光标）、全览/100%/缩放到选中、抓手、右下角功能区（图14）。
- 固定拓扑布局引擎：四列（参考图 / 镜头计划 / 片段×镜头网格 / 视频），横纵对齐、顺序固定、连线自动。
- 只读投影渲染：从 `project.shots/storyboards/videoTasks/assets` 投影出节点与缩略图（首帧/视频封面），含图7/图8 空态。
- 视口裁剪（大项目只渲可视区）。
- flag 共存：默认关，开则在 `shots` 路由下渲染画板、旧页保留可回退。

**不做（留给后续方案）**：
- ❌ 任何生成/重生/删除/导入（方案二+）；本方案动作按钮**渲染但置灰/inert**。
- ❌ 镜头计划弹窗（方案二）、候选数据模型与拖拽选中写入（方案三）、关键帧+参考图生视频（方案四）、视频 Radio/进剪辑/素材库/旧页退役（方案五）。
- ❌ 局部修改、尾帧。
- ❌ 不动后端、不改数据模型、不写 project。

**判据**：方案一全程**只读**——不触发任何 `saveProject`/`safeWriteBack`/批量接口。

---

## 2. 文件清单与接线点

### 2.1 新增模块（均需登记 import map，见 §7）
| 文件 | 职责 | 依赖 |
|---|---|---|
| `public/modules/board_viewport.js` | **业务无关**视口引擎：坐标系、transform、平移、缩放对光标、全览/选中/100%、功能区、SVG 连线层、视口裁剪。可单测 | 无（纯几何 + DOM） |
| `public/modules/board_state.js` | **纯函数** `buildBoardViewModel(project)`：投影 ViewModel（节点+边+缩略图地址）。可单测 | `frameRecommendations.js` 的 `firstFrameImageUrl/tailFrameImageUrl`（:7/:15，直接 import） |
| `public/modules/board.js` | 画板编排：`initBoard(ctx)`/`syncBoardProject(p)`/`refreshBoardPage()`；调 viewport + state，挂节点 HTML，绑功能区，调 `hydrateProtectedImageElements` 水合缩略图 | board_viewport、board_state、utils（`hydrateProtectedImageElements` utils.js:1137）、`module_singleton_guard`（`assertModuleSingleton("board", import.meta.url)`） |

### 2.2 改动的现有文件（最小、可回退）
| 文件 | 改动 | 锚点 |
|---|---|---|
| `public/workspace.html` | ① import map 加 3 个新模块条目；② #pageShots 内加 `<div id="boardRoot" hidden>`；③ bump `main.js?v=365`、`styles.css?v=254` | import map `:1844-1885`；#pageShots `:878`；标签 `:1888/:65` |
| `public/main.js` | ① `_refreshPageForActiveRoute` 的 `shots` 分支按 flag 分叉；② 注册 `initBoard(ctx)`、在 `syncStoryboardProject(project)` 各调用点旁加 `syncBoardProject(project)`；③ flag 开时运行时改导航名/隐藏 prompts + 进画板设 fixed-workbench | 派发 `:1943-1956`；init 区 `:8169-8210`；fixed-workbench `_syncFixedWorkbenchRoute :2061-2074` |
| `public/styles.css` | 新增 `.board-*` 自定义 CSS（视口/节点/连线/功能区） | 文件尾追加；bump `?v=` |
| `package.json` + `scripts/` | `verify:frontend` 链加 2 个新测试脚本 | `package.json:11` |

> **不改** switchPage、不改导航绑定（main.js:2129 通用绑定）、不改 import 裸路径制度。flag 关时以上行为与现状逐字节一致。

---

## 3. 视口引擎 `board_viewport.js`（核心，业界成熟做法）

### 3.1 两套坐标系 + 一个 transform
- **world**（布局坐标，未缩放 px）：节点/连线都用 world 坐标定位。
- **screen**（视口像素）。
- 变换：`screen = world * k + t`（k=scale，t=平移向量）。world 容器 CSS：`transform: translate(${t.x}px, ${t.y}px) scale(${k})`（translate 在前、scale 在后，恰好对应 `world*k + t`，GPU 加速；与 timmywil/panzoom 同路线）。
```
worldToScreen(wx,wy) = { x: wx*k + t.x, y: wy*k + t.y }
screenToWorld(sx,sy) = { x: (sx - t.x)/k, y: (sy - t.y)/k }
applyTransform()  // 写 world.style.transform；rAF 合批
```

### 3.2 平移（拖空白 / 抓手 / 滚轮）
pointerdown 命中空白（非节点）→ 记 `t0` 与起点 `s0`；pointermove → `t = t0 + (s - s0)`；pointerup 结束。抓手工具激活时整面可拖。trackpad 双指（wheel 无修饰键）= 平移 `t -= (deltaX,deltaY)`。

### 3.3 缩放对准光标（成熟公式，已核实）
```
onWheel(e with ctrl/⌘ 或捏合):
  factor = e.deltaY < 0 ? 1.1 : 1/1.1
  k1 = clamp(k*factor, K_MIN=0.05, K_MAX=2.5)
  c = 光标相对视口坐标
  t.x = c.x - (c.x - t.x) * (k1/k)     // 让光标下的 world 点缩放后不动
  t.y = c.y - (c.y - t.y) * (k1/k)
  k = k1; applyTransform()
```
（公式来源：affine zoom-to-cursor，`t = c − (c − t)·k₁/k₀`。）

### 3.4 全览 / 100% / 缩放到选中（功能区菜单，图14）
- 全览(fit)：`bbox`=所有节点 world 包围盒；`k=clamp(min(vw/bboxW, vh/bboxH)*0.9, K_MIN,K_MAX)`；`t` 居中。
- 100%：`k=1`，保持当前视图中心或光标锚点。
- 缩放到选中：同 fit，bbox 换成选中节点（方案一可先支持"无选中=fit"）。

### 3.5 SVG 连线层
- 一个 `<svg class="board-edges">` 放在 **world 容器内**（随同 transform 缩放，连线与节点同步，无需逐帧重算）。
- 每条边按拓扑自动画三次贝塞尔：起点=源节点右中锚点，终点=目标节点左中锚点，控制点水平外推（React Flow 同款 `M x0 y0 C x0+dx y0, x1-dx y1, x1 y1`）。
- 边数据来自 ViewModel.edges，不让用户连/改。

### 3.6 视口裁剪（性能）
transform 变化时（rAF 合批）：对每个节点算 `screenRect`，与 `视口±buffer` 求交，决定 `node.hidden` 或 mount/unmount。大项目（如 30+ 片段、上百卡）只渲可视区。连线同理可裁（端点节点都不可视且 bbox 不相交则不画）。

### 3.7 功能区（图14）DOM + 行为
右下角固定浮层：`撤销 / 恢复 | 抓手 | − / 比例% / + | ?`。比例点击出菜单：缩放至 100% / 全览 / 缩放到选中。
- **方案一**：撤销/恢复**置灰**（本阶段无可撤销的结构操作，方案二/三接入候选拖序/增删镜头后启用）；其余全实现。
- 比例输入框显示 `Math.round(k*100)%`，可点 ± 步进。

### 3.8 对外接口（给 board.js 用）
```
createViewport(rootEl, { worldEl, edgesSvgEl, onViewportChange })
  → { setTransform, fit, zoomTo, zoomToSelection, screenToWorld, worldToScreen,
      mountNode(id, el, {x,y,w,h}), setEdges(edges), refreshCulling(), destroy() }
```

---

## 4. 布局引擎（固定拓扑、对齐锁定）

纯函数 `layout(viewModel) → { nodes:[{id,x,y,w,h,kind}], edges:[{from,to}] }`。四列 x 固定（常量可调）：

```
COL_REF.x   = 0
COL_PLAN.x  = COL_REF.x + REF_W + GAP
COL_SEG.x   = COL_PLAN.x + PLAN_W + GAP
COL_VID.x   = COL_SEG.x + SEG_W + GAP
```
- **片段纵向堆叠**：segment[i] 的 y = 累加前序片段高度 + 间距；片段块高 = Σ(镜头行高)。
- **镜头行**：片段内每镜头一行（顺序 = shotIndices），行内候选卡水平排列（方案一通常 1 张，左对齐、顶对齐）。
- **视频区**：与所属片段块同 y、同高，置于 COL_VID。
- **参考图/镜头计划**：相对整个片段堆叠垂直居中。
- **对齐锁定**：所有位置由本函数算，用户**不能拖动节点位置**（方案三只允许"镜头行内候选左右拖"，且仍受行约束）。

连线（自动）：`ref→plan`、`plan→每个 segment 块`、`每个 segment 块→其 video 区`。

---

## 5. ViewModel 投影 `board_state.js`（纯函数、可单测）

```
buildBoardViewModel(project) → {
  reference: {
    empty: !(hasAnyAsset),                       // true → 渲染图7「点击导入参考图」
    characters:[{name,coverUrl,refs:[...]}],
    scenes:[...], props:[...]
  },
  shotPlan: {
    generated: Array.isArray(project.shots) && project.shots.length>0,
    shotCount, steps:{confirmShots, prepareAssets, composePrompt}  // 读现有审批/闸门位
  },
  segments: project.storyboards.map((sb,gIdx) => ({
    gIdx,
    shotRows: (sb.shotIndices||[]).map(si => ({
      shotIdx: si,
      shotUid: shotUidOf(project, si),
      // 方案一只读：现状每片段仅一张段首首帧 → 投影为该行缩略图；真正"每镜头独立首帧"在方案三+后端
      candidates: projectShotCandidates(sb, si),  // [{ url }]，空则占位
      coverUrl: firstFrameImageUrl(sb)            // frameRecommendations.js:7
    })),
    video: {
      coverUrl: currentVideoCoverFor(project, gIdx),
      status: videoStatusFor(project, gIdx),       // 待生成/生成中/已生成/失败（只读快照）
      candidateCount: countVideoHistory(project, gIdx)
    }
  })),
  edges: buildEdges(...)                            // ref→plan→每segment→其video
}
```
要点：
- **只读投影、不造数据**：方案一不引入 `shotFrames/imageCandidates/selected*`（那是方案三）。现状数据怎样就怎样画，缺帧画占位。
- **老数据兜底**=读时默认候选（firstFrameImageUrl 等多字段回退），**不写盘、不迁移**（符合不为老项目兼容原则）。
- 纯函数：输入 project JSON、输出 ViewModel，**不碰 DOM、不调接口**，便于契约单测。

---

## 6. 节点渲染（只读、自建轻量卡）

board.js 按 ViewModel 生成节点 HTML，挂进 viewport，再 `hydrateProtectedImageElements(boardRoot)` 水合缩略图（首帧/视频封面用 `<img src=受保护URL>` 或 `<img data-img>`）。

- **不复用** `renderImageGrid`(storyboard.js:7002)/`renderVideoResultCard`(videoPrompts.js:1333)——二者绑死单一 DOM 容器 + 内部交互态，非纯渲染器。**复用** `firstFrameImageUrl/tailFrameImageUrl` 取址纯函数 + `hydrateProtectedImageElements` 鉴权。
- **参考图节点**：`reference.empty` → 图7 单按钮空态（按钮 inert，仅展示）；否则图8 角色/场景/道具分组卡 + 「生成镜头计划」按钮（inert）。
- **镜头计划节点**：图9 Shot-Plan 卡 + 三步状态 + 「打开脚本节点」（inert）。
- **分镜首帧网格**：图11 片段→镜头行→首帧缩略卡（含图7/图8 与占位空态）。
- **视频节点**：图13/图4 封面缩略 + 状态角标（只读）。
- **进度监控（方案一只读快照）**：节点头展示静态计数（如 `首帧 8/9`、`视频 待生成`），数据从 ViewModel 算；**实时 ETA 倒计时留到方案二**（生成动作经画板触发时再接 `_showKeyframeHeaderProgress` 等现有实现）。

所有动作按钮（生成/打开脚本/一键全生成/生成新视频/确认下一步）**渲染但置灰**，hover 提示"下一阶段接入"。

---

## 6A. 视觉设计：背景 + 卡片 UI（与现有风格一致，不自创视觉）

**统一走现有设计令牌**（styles.css `:root`）：`--bg-main:#ECEFF1`、`--bg-card:#fff`、`--accent:#0B1320`、`--accent-soft:rgba(11,19,32,.10)`、`--border:rgba(144,164,174,.30)`、`--text:#0B1320`、`--text-dim:#2C3E50`、`--green/-soft`、`--radius:16px`、`--surface-high:#CFD8DC`。画板**不引入新色板**。

### 背景（回答"是不是现有灰色浮点背景" → 是）
画板底 = `--bg-main`(#ECEFF1 现有浅灰) + **浮点圆点栅格**，沿用 app 现有 `radial-gradient` 圆点技法（与侧栏 active 态 `radial-gradient(circle,…0.9px,transparent 1.4px)`、5px tile 同语汇），调成"浅灰底·深点"的 FigJam/飞书式画板面：
```css
.board-surface {                /* 挂在 world 容器，随平移/缩放一起动，给空间纵深 */
  background-color: var(--bg-main);
  background-image: radial-gradient(circle, rgba(11,19,32,0.10) 1px, transparent 1.5px);
  background-size: 22px 22px;
}
```
视口外层 viewport 容器纯 `--bg-main` 兜底。极小缩放(k<0.15)时圆点层 `opacity` 渐隐避免糊（可选；"屏幕空间自适应栅格"列为后续打磨，非本期必须）。

### 卡片 UI（每类节点，复用现有卡片语汇）
| 节点 | 复用现有样式 | 规格 |
|---|---|---|
| 参考图框 | `.shot-workbench-card` 磨砂玻璃 | 半透白 `rgba(255,255,255,.58)` + `backdrop-blur(24px)` + 圆角 28px + 软阴影 `0 18px 48px rgba(11,19,32,.065)`；内部 角色/场景/道具 分组行（主图 cover + 名称 + 参考内容堆叠卡） |
| 镜头计划卡 | **深色**（对齐图9 + 深色弹窗） | 底 `--accent`#0B1320、白字；三步进度点（① 确认镜头 / ✓ 准备资产 / ③ 合成提示词）；"打开脚本节点 →"主按钮（本期 inert） |
| 首帧候选卡 | `.bc-thumb` 语汇 | 缩略 16:9（约 160×90）、圆角 10px、`object-fit:cover`、`--surface-high` 占位底；卡顶小标签"镜头N首帧图M"(10px `--text-dim`)；空态=占位图 + 置灰"生成图片/上传图片" |
| 片段块容器 | 浅描边分组 | 圆角 16px、`1px var(--border)`、极浅底；左侧竖排"片段N"标；内部镜头行纵向左对齐 |
| 视频卡 | `.bc-card`/`.bc-status` 语汇 | 白底、`1px rgba(144,164,174,.15)`、圆角 16px、hover 描边 `#0B1320`；缩略 + 状态徽章 `.bc-status--active/done/fail`(accent-soft/green-soft/红)；右上角单选位（本期只显示不可点） |
| 功能区/工具条 | 胶囊按钮语汇 | 圆角 2rem、hover `--accent-soft`；图标用 `material-symbols-outlined`（app 已用） |

**一致性原则**：圆角/阴影/`--accent` 主色/`material-symbols-outlined` 图标/`--accent-soft`·`green-soft` 徽章全部沿用现有；深色仅用于镜头计划卡（与图9/图10 一致），其余浅色磨砂落在浅灰浮点背景上。

---

## 6B. 交互与状态全覆盖（补齐被写薄的逻辑）

| 维度 | 本期行为 |
|---|---|
| 悬停/选中/焦点 | hover 卡片浮起（沿用 `.shot-workbench-card:hover`）；单击节点=选中（描边高亮 `box-shadow:0 0 0 2px var(--accent-soft)`，沿用 `.task-card.is-active` 语汇），供"缩放到选中"；空白单击取消选中。左键详情面板(图5)/右键菜单(图6)留方案五，本期只做选中视觉 |
| 键盘 | 空格+拖=平移；`+/-/=` 缩放；`0`=100%；`Shift+1` 全览 / `Shift+2` 缩放到选中（对齐图14 菜单标注）；`Esc` 取消选中。Mac 指引风格 |
| 初始视图 | 首次进画板自动 fit 全览；之后按项目记住相机（localStorage `board_cam_<projectId>` 存 k/t），刷新/切回不跳 |
| 尺寸变化 | `ResizeObserver` 重算视口、保持相机中心；侧栏折叠(`--sidebar-width` 变化)同样响应 |
| **数据刷新保相机 + 增量渲染** | `syncBoardProject` 重投影后**保留当前 k/t 不重置**；节点按稳定 key（片段 `gIdx` / 镜头 `shotUid` / 视频 `taskId`）diff 复用 DOM，只增/改/删变化节点，**不整树重建**（防闪烁、防相机丢失）。后台他处生成完成 → 画板增量刷新对应卡状态 |
| 加载/错误/空态 | 项目加载中=遮罩（复用 `.sw-page-loading` 语汇）；无项目=引导；投影异常=兜底提示不白屏；空项目=图7 单按钮 |
| 层级 z-index | 背景 < 连线 SVG < 普通节点 < 选中节点 < 功能区 < 顶栏 |
| 顶栏 | 右上角"确认视频，进入下一步"（本期 inert）+ 画板标题/副标题；不遮功能区 |
| 清晰度 | SVG 连线矢量天然清晰；缩略图按 DPR 取尺寸；transform 像素吸附减模糊（可选） |

---

## 6C. 设计稿元素覆盖矩阵（图1–15 → 归属，看得见"没漏，只是分期"）

| 图 | 元素 | 本期(方案一) | 后续 |
|---|---|---|---|
| 图1 | 整体四节点流程图 | ✅ 渲染 + 视口 | — |
| 图2 | 参考图区(角色/场景/道具) | ✅ 只读渲染 | "参考内容+"补充入口(后期) |
| 图3 | 首帧候选行(右侧加卡/拖序/取最右) | ⚠️ 只读渲染结构 | 候选模型+拖序+选中写=**方案三** |
| 图4 | 视频区(多候选/Radio) | ⚠️ 只读渲染 | Radio选中+生成=**方案五** |
| 图5 | 左键详情/参考编辑面板 | ❌（仅选中视觉占位） | **方案五** |
| 图6 | 右键菜单 | ❌ | **方案五** |
| 图7 | 初始"点击导入参考图" | ✅ 空态(inert) | 导入动作=**方案二** |
| 图8 | 导入后 + 生成镜头计划按钮 | ✅ 渲染(inert) | 动作=**方案二** |
| 图9 | 镜头计划卡 + 打开脚本节点 | ✅ 渲染(inert) | 弹窗=**方案二** |
| 图10 | 镜头计划弹窗 | ❌ | **方案二** |
| 图11 | 片段-镜头结构 + 一键全生成 | ✅ 结构渲染(inert) | 生成=**方案二** |
| 图12 | 尾帧按钮 | ❌（主流程已移除，§7 决策） | 高级"加结束关键帧"=方案四/五 |
| 图13 | 视频空态"生成新视频" | ✅ 空态(inert) | 生成=**方案五** |
| 图14 | 右下角功能区 | ✅ 全实现（撤销/恢复置灰） | 撤销接结构操作=方案三 |
| 图15 | 镜头参数下拉 | ❌ | **方案二**(并入弹窗) |

**结论**：15 张稿的元素**全部有归属**——方案一把整张流程图的**结构与视觉**只读渲染到位（图1/2/7/8/9/11/13/14 ✅，图3/4 结构就位待写交互），动作与编辑按矩阵分期到方案二~五，没有任何元素被丢弃。

---

## 7. 版本制度与构建（硬约束，漏了 verify 会红）

1. **import map**（workspace.html:1844-1885）新增三条（值带 `?v=1`）：
   `"/modules/board.js": "/modules/board.js?v=1"`、`board_viewport.js?v=1`、`board_state.js?v=1`。源码 import 一律裸路径 `/modules/board.js`（禁内嵌 `?v=`）。
2. 改 `main.js` → bump `workspace.html:1888` `main.js?v=365`→`366`（只增不减，契约要求 ≥357）。
3. 改 `styles.css` → bump `workspace.html:65` `styles.css?v=254`→`255`。
4. 改某画板模块代码 → 只 bump 其 import map 那一条 `?v=N`。
5. `npm run verify:frontend`（package.json:11）必须绿：`test:cache-busting` 会校验新模块在 import map 有条目、值形如 `key?v=数字`、HTML 与 map 版本不分裂。

---

## 8. 入口与 flag 共存（默认关、线上零影响、可秒回退）

- **flag**：`isBoardEnabled()` 读 `localStorage.getItem('originBoard')==='1'`（Vasily 在 Mac 上：打开页面→地址栏右侧圆圈箭头旁右键→检查→Console 输入 `localStorage.originBoard='1'` 回车→刷新即开；关则 `='0'`）。
- **派发分叉**（main.js:1951 `if (page==='shots')`）：
  - flag 开：`syncBoardProject(project); refreshBoardPage();` + 隐藏 #pageShots 内旧的镜头/分镜容器、显示 `#boardRoot`；运行时把 `navShots` 文案设「画板」、给 `navPrompts` 加 `hidden`；调 `_syncFixedWorkbenchRoute` 让画板走 fixed-workbench（锁 window 滚动，**不进 scroll_anchor_guard 的 SCOPES**，避免误判滚回）。
  - flag 关：完全走原 `refreshShotsPage()+refreshImagesPage()`，导航/页面**逐字节不变**。
- **可回退**：flag 关或删 board* 模块即恢复旧页（符合"卡壳先回滚"原则）。

---

## 9. 测试（接入 verify:frontend）

新增两个 `.mjs`，挂进 `package.json:11` 的 `verify:frontend` 链：
- `scripts/test-board-viewport.mjs`：纯几何单测——① `screenToWorld(worldToScreen(p))≈p`；② zoom-to-cursor 后"光标处 world 点"不变；③ fit 后所有节点落在视口内；④ 边界 clamp（K_MIN/K_MAX）。
- `scripts/test-board-projection.mjs`：喂 fixture project JSON，断言 `buildBoardViewModel` 节点数=storyboards 数、edges 拓扑正确（ref→plan→segs→videos）、空态/缺帧占位正确、**不产生写操作**。
- 同时 `test:cache-busting` 自动覆盖三个新模块的 import map 登记。

---

## 10. 里程碑1 验收清单

- [ ] flag 开：真实项目渲染为四节点流程图，参考图/镜头计划/片段镜头网格/视频区都在、连线正确。
- [ ] 平移、滚轮对光标缩放、全览、100%、缩放到选中、抓手、比例显示全可用。
- [ ] 缩略图（首帧/视频封面）带鉴权正常显示，无 401 黑图。
- [ ] **背景 = 浅灰 `--bg-main` + 浮点圆点栅格**，随平移缩放，风格与现有一致（§6A）。
- [ ] **卡片视觉与现有令牌一致**：磨砂参考图框 / 深色镜头计划卡 / `.bc-thumb` 首帧卡 / `.bc-card` 视频卡，圆角·阴影·主色·徽章沿用现有（§6A）。
- [ ] 悬停浮起、单击选中高亮、键盘（空格拖/±/0/Shift+1·2/Esc）可用（§6B）。
- [ ] 窗口/侧栏尺寸变化视口自适应、相机中心不丢；首次 fit、再进按项目记住相机。
- [ ] **数据刷新保相机 + 增量渲染**：他处生成完成后画板按 key diff 增量刷新，不整树重建、不重置缩放、不闪烁（§6B）。
- [ ] 空项目显示图7「点击导入参考图」；无镜头计划/无帧的占位正确。
- [ ] 设计稿图1–15 元素按 §6C 矩阵各有归属，本期该 ✅ 的均已渲染。
- [ ] 大项目（造 ≥30 片段 fixture）平移缩放不明显卡顿（视口裁剪生效）。
- [ ] 动作按钮置灰、点击不触发任何写/接口（只读保证）。
- [ ] flag 关：线上旧页与现状逐字节一致。
- [ ] `npm run verify:frontend` 绿（含两个新测试）。
- [ ] 无 console 报错、无 module-singleton 违例。

---

## 11. 风险与规避

| 风险 | 规避 |
|---|---|
| 缩放/平移坐标错位（最常见） | 用 §3 成熟公式 + `test-board-viewport.mjs` 单测把数学钉死，先证数学再接 DOM |
| 静态 Tailwind：动态类名失效 | 画板 CSS 全走 `styles.css` 自定义 `.board-*`，不靠 Tailwind 动态类；视口/连线纯 transform+SVG，不依赖工具类 |
| 复用旧渲染器踩内部态 | 只复用 `firstFrameImageUrl`/`hydrateProtectedImageElements` 两层纯能力，自建只读卡，不碰 renderImageGrid/renderVideoResultCard |
| 滚动守卫误判 | 画板走 fixed-workbench、不注册 SCOPES；复用 `shots` 路由但不产 `.shot-workbench-card` 时守卫自然空转 |
| 大项目性能 | 视口裁剪 + rAF 合批 transform；连线放 world 内随变换、不逐帧重算 |
| 误触写操作破坏"只读" | 动作按钮 inert + 评审时 grep 确认 board* 不引 saveProject/safeWriteBack/批量接口 |
| 线上被波及 | flag 默认关、运行时分叉；关时不改任何旧行为，可秒回退 |

---

## 12. 交接 Codex 的执行顺序

1. 建 `board_viewport.js`（坐标系+transform+平移+缩放对光标+fit/100%/选中+功能区+SVG 连线+裁剪）→ 写 `test-board-viewport.mjs` 先过数学。
2. 建 `board_state.js`（`buildBoardViewModel` 纯函数 + 投影/空态/兜底）→ 写 `test-board-projection.mjs`。
3. 建 `board.js`（`initBoard/syncBoardProject/refreshBoardPage`，挂节点 HTML + 水合 + 绑功能区，动作按钮 inert）。
4. workspace.html：import map 三条 + `#boardRoot` 容器 + bump 标签。
5. main.js：派发分叉（flag）+ initBoard 注册 + syncBoardProject 接线 + fixed-workbench。
6. styles.css：`.board-*` 视觉。
7. 跑 `npm run verify:frontend`，对照 §10 验收。

> 每步可独立验证；flag 关时全程不影响线上。过闸后进《方案二·镜头计划弹窗+片段分组/发车》。

---

## Sources
- 前端集成事实（本地实读，行号见正文）：`public/workspace.html`、`public/main.js`、`public/modules/{utils,frameRecommendations,storyboard,videoTasks,shots,scroll_anchor_guard,module_singleton_guard}.js`、`package.json`、`scripts/test-frontend-cache-busting.mjs`、`tailwind.workspace.config.cjs`、`public/styles.css`。
- 视口数学/CSS transform 平移缩放：https://harrisonmilbradt.com/blog/canvas-panning-and-zooming ，https://medium.com/@benjamin.botto/zooming-at-the-mouse-coordinates-with-affine-transformations-86e7312fd50b ，https://github.com/timmywil/panzoom
- 节点+SVG 连线范式（自建参照 React Flow 底层）：https://reactflow.dev/
