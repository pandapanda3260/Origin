# 镜头合并 · 落地顺序与改动清单

> 配合《镜头合并与素材生成-方案 v2》。本文只排**顺序 / 落点 / 怎么不破现网**，不改代码。
> 日期：2026-06-01

---

## 不破现网的三条底座

1. **全程 feature flag（默认 OFF）**：flag OFF 时所有路径与今天**完全一致**，现网零影响；flag ON 才启用合并。每个阶段的改动都用 `if(flag)` 包住。
2. **数据前向兼容、零迁移**：`shotIndices` 本就是数组，老项目的 `[idx]` 就是"1 镜头的退化合并段"。新代码按 `shotIndices` 通读，就把老项目自然当**全 solo** 渲染——**不写迁移、不 bump schema 兼容**（老项目要弃就弃，按既定习惯）。
3. **最小爆炸面**：`single-shot-strict` 的约 42 处调用**不逐个改**，改成在 `storyboardShotIndices`（`frame-workflow-state.ts:154`）**内部"flag 开就放行多镜头"**，一处搞定；`assertStoryboardsAlignedWithShots`（:71）同理 flag 开时放宽。

---

## 阶段顺序（按流水线依赖；每步可独立验证）

### P0 · 先吃免费午餐（纯配置，秒回滚）
- **落点**：env `VIDEO_MIN_DURATION_SECONDS=4`（已有线，经 `model-routing.ts:189` → `resolveGenerationDurationSec`，`video-reference-manifest.ts:178-200`）。
- **做什么**：把 5s 地板降到 4，跑方案 §6 测试①（发 `duration:3 / 4`）。若 4s 节奏就够，先享受一波快节奏，且**不依赖合并**。
- **不破现网**：纯 env，改坏删掉即回 5。

### P1 · 加总开关
- **落点**：新增 feature flag（仿现有 flag 模式，如 `ORIGIN_MULTI_SHOT_SEGMENT`，默认 OFF）。后续所有改动挂它。

### P2 · 分组 + 放开不变量（核心，flag-gated）
- **落点**：
  1. 新增**分组函数**（贪心 `MIN=4 / MAX=15`，方案 §2），在**镜头计划阶段**算好写 `storyboards[g].shotIndices`；`frame-workflow-state.ts:57` `makeSingleShotStoryboardSlots` → flag 开时建合并 slot。
  2. `storyboardShotIndices`（`frame-workflow-state.ts:154`）：flag 开时 `single-shot-strict` **放行 N 镜头**（**一处改，42 调用点不动**）。
  3. `assertStoryboardsAlignedWithShots`（:71）：flag 开时放宽。
- **验证**：flag OFF 老/新项目都跟今天一致；flag ON 新项目能产出 2–N 镜头的段、老项目仍当全 solo。

### P3 · 首尾帧生成逻辑（flag-gated）
- **落点**：`batch-executors.ts`（`storyboard_images` :1336 / `tail_frame_images` :1800）按"**段首才出首帧、solo+分值才出尾帧**"；`public/modules/shots.js` / `storyboard.js` 的建议标 + 一键生成圈选 + 采纳规则（方案 §3）。
- **验证**：合并段只出 1 张段首首帧；solo 维持原样。

### P4 · 生成模式（flag-gated）
- **落点**：`video-payload-decision.ts` / `batch-executors.ts:2539` —— 合并段**强制 `first_frame_multi_ref`（参考模式），不进 `first_last_frame`**；`video-prompt-runtime.ts` 合并段**不注入尾帧/结尾约束块**（`targetEndConstraintBlock` 等）。
- **验证**：合并段请求体是"首帧 + 参考图"，无 `last_frame`。

### P5 · 多-beat 提示词（flag-gated）
- **落点**：`app/api/video-prompt/generate/route.ts` + 批次路径 —— 把**整组 `groupShots`** 喂 `buildVideoPromptMessages`（`prompts.ts:1413`，已 group-aware），而非单镜头。
- **验证**：合并段出一条"镜头01 / 镜头02"多-beat 散文。**这步连同方案 §6 测试②（身份 / 口型）一起实跑确认**。

### P6 · 收尾（可并行 / 随时）
- **UI 文案统一**：`videoPrompts.js` / `storyboard.js` 把"片段 / 镜头"理顺成"**片段 = 一组镜头**"。
- **知识卡**：`lib/knowledge/seed-system-cards.ts` 改掉"1 storyboard = 1 镜头"旧前提。
- **时长区间**：`app/api/shots/generate/route.ts:157` clamp → `[1,7]`、`prompts.ts` 三处区间（3-5 / 1-10 / 3-6）统一（可并进 P2）。

---

## 明确不做
- 不写老数据迁移、不为 schema 做兼容（老项目 = 全 solo 自然可读；要弃就弃）。
- 不动 solo / 现有 1:1 路径的既有逻辑（flag OFF 即原样）。

## 开关翻转（默认 ON）的 go 条件
方案 §6 三个测试全过：① 4s 下限 OK；② 合并段身份 / 口型 OK；③ 对照现状不劣 —— 才把 flag 默认 ON。

---

## 一句话次序
P0 env 降 4s 试水 → P1 加 flag → P2 分组+放开锁 → P3 首尾帧 → P4 模式 → P5 多-beat → P6 文案/知识/区间 → 测试全过再默认开。
