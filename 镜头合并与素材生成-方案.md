# 镜头合并与素材生成 方案（定稿部分 v2）

> 范围：把"一个片段 = 一个镜头"升级为"一个片段 = 按时长动态合并的 1~N 个镜头"，以解决短镜头被 Seedance 最低时长顶高、破坏快节奏的问题。
> 本文只收录**已达成结论、可据此动手**的部分；仍开放的问题集中在文末「待定」。多-beat 提示词为下一议题，本文暂不展开。
> 日期：2026-06-01
> **v2 变更**：待定 A（首尾帧 / 参考图能否同调）已实证为**互斥**；据此定下生成模式（B）——**合并段恒走"首帧 + 参考图"**；第 3 节尾帧逻辑随之**收紧为"仅 solo"**；第 1 节补"按模型版本"的口径。
> **v2 补充**：待定 C 的"多-beat 提示词格式"经核查**早已就绪**（videoPrompt 已是多-beat 结构、生成器已 group-aware），C 降级为"接通 + 对齐模式 + 实测"；§4 补 UI 现状（分镜页 / 提示词页均已按 group 渲染、带多镜头脚手架）。

---

## 0. 背景与动机

- **现状**：一个片段对应一个镜头。生成前，`lib/video-reference-manifest.ts` 的 `resolveGenerationDurationSec` 对 seedance/volcano 做 `Math.max(plannedDurationSec, 5)`，把任何 <5s 的镜头顶到 5s，破坏快节奏。
- **关键发现**：这道 5s 地板里**有 1 秒是自家代码加的**；Seedance 真实下限是 **4s**。但 4s 仍是硬下限——<4s 的快镜头无法单独生成，必须靠"相邻镜头合并成一个片段、一次生成"来实现。
- **架构现状（好消息）**：底层已为多镜头预留——`storyboards[g].shotIndices` 本就是数组（现被 `single-shot-strict` 锁成 `[idx]`）；生成层已按组取 `groupShots = shotIndices.map(...)`（`lib/batch-executors.ts:1350`）；UI 已能渲染"N 个镜头"。所以这是"**放开闸门 + 重新接线 + 校准规则/知识**"，不是推倒重建。

---

## 1. Seedance 关键约束（事实基线）

来自官方文档、官方 SDK 行为、及与代码字段一致的资料（见文末「来源」）：

- **时长**：`duration` 取整数 **4–15 秒**，默认 4，顶层字段；**没有离散枚举**（可送任意整数 4–15）。最大 15s。
- **图片名额**：一次最多 **9 张图**（**首帧、尾帧也算在这 9 张内**），外加 3 段视频 + 3 段音频，混合总上限 12 个文件。
- **时间锚点**：一条生成片**只有 `first_frame` 和 `last_frame` 两个时间锚点**；其余图都是"非时间参考"（锁角色脸/服装/道具/风格/场景），**不存在中间关键帧**。
- **两种模式互斥（已实证）**：Ark API 上"**首尾帧模式**（`first_frame` / `last_frame`）"与"**参考图模式**（`reference_image` / `reference_video` / `reference_audio`）"是**两套独立、不能在一次调用里混用**的生成方式——要精确首尾帧就用首尾帧模式，要身份参考就用参考模式，二选一。四方印证：自家代码实测（`video-gen.ts:146/504`）、官方契约实现文、Atlas 双独立端点、开源 ComfyUI 节点。即梦网页端能 @ 混用，是网页 app 层，**不是 Ark API**。
- **多镜头**：一条 ≤15s 的片子里靠**提示词**写"Shot 1… Shot 2…"产生带剪辑点的多镜头（官方 Multi-Shot 技法）。中间镜头的画面靠"提示词 + 身份参考"软引导，不能用帧钉死。
- **以上数值随模型版本而变**：Seedance **1.0** 仅 5/10s；**1.5 Pro** 为 2–12s；**2.0** 为 4–15s / 9 图。本方案按 **2.0（`doubao-seedance-2-0`）**。**动手前务必确认上线实际调用的 model 串**（`2-0` / `2-0-260128` / `-fast` / 还是某个 1.x）；国内路由 `doubao-*`，全球路由 `dreamina-*` + 不同 endpoint，勿混。

---

## 2. 合并 / 分组规则（LOCKED）

**参数**：`MIN = 4`（Seedance 下限，已定）；`MAX = 15`。镜头计划单镜头时长区间拟定为 **1–7s**。

**原则**：**尽量少拼**——只有撑不到 MIN 的镜头才合并，且只并够数为止；镜头顺序不变（保叙事结构）。

**算法（按镜头顺序、前向贪心）**：

1. 当前镜头 ≥ MIN → **单独成段（solo）**。
2. 当前镜头 < MIN → 往后依次并入下一镜头，**累计 ≥ MIN 即停**。
3. 末尾若并不到 MIN（尾巴 < MIN）→ **整体并回上一段**。

**为什么不需要溢出兜底**：1–7s 区间下，上一段 ≤ `MIN−1+7 = 10`、尾巴 ≤ `MIN−1 = 3`，并回后 ≤ 13 < 15，数学封死，不会超 15。

**退化情形**：整条片仅一个 < MIN 的孤镜头 → 自然 padding 到 MIN，无需特殊处理。

**片段大小由此动态决定**：多数（≥4s 的镜头）= 1 个；一个短镜头 + 垫够它的最少后续 = 2 个；连续很短（多个 1–2s）才会到 3–4 个。

**例**：时长 `[1,1,1,5, 2,3, 7]` → 段1 `[1,1,1,5]`=8s、段2 `[2,3]`=5s、段3 `[7]`=7s。

**落点**：分组结果写进 `storyboards[g].shotIndices`；"段首/段尾"由该数组顺序推出；分组在**镜头计划阶段**算好并写入。

---

## 3. 原材料与生成模式（LOCKED）

**核心**：因为"首尾帧模式"与"参考图模式"互斥（见 §1），而合并段内部有多次镜头切换、身份漂移风险高，所以生成模式按"段类型"分流，每个镜头要不要出图也随之决定。四个维度要分清：**标记 / 一键生成是否自动跟随 / 用户能否手动 / 生成后是否采纳**。

### 3.0 生成模式（按段类型）

- **solo 段（≥MIN 的单镜头，占多数）**：维持现有每镜头逻辑——**首帧 + 可选尾帧**（按尾帧分值；要尾帧走首尾帧包夹，不要则首帧 + 参考）。
- **合并段**：**恒定 首帧（段首）+ 身份参考簇，无尾锚点**。理由：① 内部多次切换更吃身份参考；② 下一段永远有自己的受控首帧，硬切落点天然干净，结尾锚点几乎没用——拿尾锚点换身份参考是稳赚。

### 3.1 首帧

| 镜头 | 标记 | 一键生成 | 手动生成 | 采纳 |
|---|---|---|---|---|
| 段首（含 solo） | 建议生成 | 自动生成 | 可，不拦 | 采纳 |
| 非段首 | 不建议生成 | 不跟随 | 可，不拦 | 不采纳（生成了也不用，前端不提示） |

### 3.2 尾帧（只与 solo 有关）

| 镜头 | 标记 | 一键生成 | 手动生成 | 采纳 |
|---|---|---|---|---|
| **solo 且分值达标** | 建议生成 | 自动生成 | 可，不拦 | 采纳 |
| 其余（合并段的任何镜头，或 solo 但分值不达标） | 不建议生成 | 不跟随 | 可，不拦 | solo → 采纳（回到原首尾帧逻辑）；非 solo → 不采纳；前端都不提示 |

> 相对 v1：尾帧"建议生成"从"段尾（含 solo）"**收紧为"仅 solo"**。合并段的段尾不再出尾帧——合并段恒走参考模式，没有尾锚点的位置。

### 3.3 中间镜头 / 合并段段尾

- 都**不出任何图**，只贡献多-beat 提示词里自己那段文字；画面靠"段首首帧 + 段级身份参考 + 提示词"带出来。

### 3.4 要点

1. **合并段每段只出 1 张首帧**（段首），其余镜头（含段尾）都无图；身份靠段级参考簇，不靠逐镜头帧。
2. **每个片段至少有开场锚点**（段首首帧必生成）；合并段无结尾锚点（有意为之），solo 的结尾锚点可选（分值决定）。
3. **首帧无 solo 例外**（非段首不可能是 solo）；尾帧只对 solo 有意义。

### 3.5 尾帧分值的角色（已收窄）

尾帧分值现在**只管 solo**：判断这个独立镜头要不要一个受控结尾（要不要走首尾帧包夹）。输入仍应包含"与下一段切换有多硬 / 是否需要精确收尾定格"。**它不再是合并段的模式开关**——合并段恒定参考模式。

### 3.6 一个边角（先不写特殊逻辑）

整条片的**最后一段**若恰是合并段、又需要精确收尾定格（logo / 定帧），才需要手动给它上尾锚点。极少见（合并段都是 sub-4s 快碎镜头，结尾基本不需钉），手动兜一下即可。

---

## 4. 现有系统据此要改的点

- **时长规则**：`app/api/shots/generate/route.ts:157` 的 `clampNum(..., 1, 10, 4)` 改成 `1–7`（或最终区间）；`lib/prompts.ts` 镜头表提示词里 3-5 / 1-10 / 3-6 的自相矛盾统一；生成前 5s 地板降到 4（或做成配置）。
- **合并 + 模式**：放开 `single-shot-strict`（约 42 处调用）+ 对齐不变量 `assertStoryboardsAlignedWithShots` + 一次 `FRAME_WORKFLOW_SCHEMA_VERSION` 迁移；分组在镜头计划阶段算好写入 `shotIndices`；**合并段强制走 `first_frame_multi_ref`（参考模式），不进 `first_last_frame`**；solo 维持现有 `resolveVideoPayloadDecision` 那套（首帧 + 可选尾帧）。
- **素材**：首帧默认全生成 → 改为只对段首生成；尾帧 → **仅 solo + 分值**；"一键生成"按建议标圈选；手动不拦、按 §3 采纳规则处理。
- **知识库**：现有知识卡（`lib/knowledge/seed-system-cards.ts`）里"一个 storyboard 对应一个可执行镜头"的旧前提需重写，灌入本文第 1 节的 Seedance 约束。
- **UI（好消息，基本就绪）**：分镜页（`storyboard.js`）与提示词页（`videoPrompts.js`）**都已按 group(片段) 渲染**（videoPrompt 存在 `storyboards[gIdx]`、按 `getStoryboardGroups()` 出卡），且早带"一段含多镜头"脚手架（`X个镜头/Y个片段` 计数、`镜头 01-03` 区间标签、按 `gIdx` 操作）。放开 `single-shot-strict` 后基本**自动按段渲染**，结构不用重做；只需**统一文案口径**（"片段 = 一组镜头"，把当成单镜头的 toast/标签理顺）。
- **多-beat 提示词（详见 §5 C）**：videoPrompt 已是多-beat 结构、`buildVideoPromptMessages` 已 group-aware；改动是**把整组喂进生成器**（而非逐镜头），并让合并段装配**不注入尾帧 / 结尾约束**（对齐参考模式）。

---

## 5. 待定

- **C｜多-beat 提示词（格式已就绪，只剩接通 / 对齐 / 实测）**：videoPrompt 早已是多-beat 结构（运镜系统 / 角色 / 场景 / 镜头01 / 镜头02 / 基调 / 约束 / 音障）、生成器 `buildVideoPromptMessages` 已 group-aware——**设计层已完成，不需新格式**。剩三件：① **接通**：放开 `single-shot-strict`，把整组喂进生成器（现每次只喂 1 镜头 → 只出 1 beat）；② **对齐模式**：合并段走参考模式，装配里别注入尾帧 / 结尾约束（`targetEndConstraintBlock` 等）；③ **实测**：N>1 在 production 基本没真跑过——散文质量 + 多说话人对白 / 口型衔接需实跑一次确认（见 §6 测试 ②）。
- **（已解决，已并入正文）** A 互斥 → §1；B 模式（solo = 首 + 可选尾；合并段 = 首 + 参考）→ §3.0；D 9 名额：合并段 = 段首首帧 1 + 身份参考最多 8（身份优先，不用尾帧当参考），solo 走现有逻辑 → §3。

---

## 6. 上线前测试（建议顺序）

1. **定下限**：把 5s 地板改为4s地板，在**上线 model 串**上发 `duration:3` 和 `duration:4` 单镜头请求，确认真实下限 = 4 且短片观感。若 4s 节奏就够，大部分合并改造可省。
2. **定合并质量**（A 已答互斥，不必再测"混合"）：取一个真实合并段，按定稿模式手工拼一次——**首帧（段首）+ 身份参考簇 + "Shot1/2/3 带秒数"提示词**，看三件事：① beat 节奏对不对；② **内部切换处身份是否一致**（这是参考模式要扛的关键）；③ 整体观感。
3. **对照**：与现在 1:1 的输出在同素材上比节奏与质量。

---

## 来源

- 火山方舟 · 创建视频生成任务 API（官方，页面 JS 渲染，正文未取到）：https://www.volcengine.com/docs/82379/1520757
- LaoZhang AI《How to Call the Seedance 2.0 API》——明确 FAQ：首尾帧与参考图模式**分开**，不能在一次任务里混：https://blog.laozhang.ai/en/posts/seedance-2-api
- Atlas Cloud《Seedance 2.0: A Guide to All-Round Reference》：9 图 + 3 视频 + 3 音频、@ 标签、身份 70 / 动作 30、Multi-Shot；Image-to-Video 与 Reference-to-Video 为**独立任务**：https://www.atlascloud.ai/blog/case-studies/generative-ai-model-seedance-2-0-a-guide-to-all-round-reference
- ComfyUI-Jimeng `nodes.py`（首尾帧 role 构造，开源实证，不掺 reference_image）：https://github.com/xuhongming251/ComfyUI-Jimeng/blob/main/nodes.py
- Apidog《How to Use Seedance 2.0 API 2026》：duration 4–15 整数，与代码 endpoint 一致：https://apidog.com/blog/seedance-2-0-api/
