# Origin 积分计量计费 — 需求描述

> 说明：本文只描述"要实现什么效果"，不涉及怎么实现（不含技术方案、表结构、文件设计）。

## 一、一句话目标

让 Origin 能对"**每个用户、每次产生 API 费用的操作**"自动按真实成本扣积分，并让**管理员**和**用户**都能清楚看到：扣了多少、为什么扣、还剩多少。

## 二、计费范围

- **要扣分的操作**：所有会调用 AI API、产生真实费用的操作——出图、风格化、分镜词/分镜图、首尾帧、视频段、镜头、提示词改写等（即现在那一整套生成能力）。
- **不扣分的操作（扣 0 分）**：不调用付费 API 的本地操作——编辑文本、拖时间线、浏览、保存项目、查看素材等，一律 0 分，且不应产生扣分记录干扰。
- 这是 Origin **自身运行时**的能力，对所有真实用户始终生效，不依赖任何人工或对话触发。
- 现状是"按操作固定扣分"（文本固定 1 分 / 图 30 分 / 视频 150 分）；本需求是把它升级为**按真实用量 × 真实价格**来算分。

## 三、计费规则（核心口径）

1. **钱与分的固定关系**：把 API 的**人民币成本**换算成积分时，**1 元 = 100 积分**，固定不变，只能在代码里改、不在后台暴露、不随活动浮动。
   （注：这是"消耗扣分"的换算率，和"充值时 1 元买多少积分"的售卖率是两回事，互不影响。）

2. **价格来源**：维护一张 **token 价格表**，记录每个 API / 每个模型的**最新单价**，并统一按央行正常汇率**换算成人民币**。当前阶段只支持**手动更新**（不自动抓价）；价格更新只需改这一处，全系统生效。每条价格记录需带**最后更新时间**，便于管理和判断是否过期。（首版数据见第七节）

3. **算分方式**：监控到一次操作的【消耗量 + 消耗类型 + 用的哪个 API/模型】→ 查价格表得到这次的人民币成本 → ×100 换成积分 → 取整后扣除。
   - 按量计费的（文本类）：按**输入 / 输出 token 数** × 对应单价分别计算。
   - 按次计费的（图片按张、视频按段）：按**固定单价 × 数量**计算。

4. **取整**：不足 1 分的**向上取整**为整数分，且**最低扣 1 分**（避免出现 0 分的免费消耗）。

5. **失败处理**：生成失败 / 没产出有效结果的调用**不扣分**（不产生扣分记录），不让用户为废结果买单。

6. **余额门槛（允许负数）**：发起一次操作时，只要当前积分 **> 0 就放行**——哪怕这一次扣费会把余额扣成负数，也允许它做完。只有当积分 **≤ 0** 时才拦截并提示充值。**不做"操作前余额够不够"的预检**（避免用户明明还有分、却因为"这次可能不够"被提前拦，体验差）。

## 四、要实现的效果

### 1）监控与扣分
- 每一次产生费用的操作，在**调用成功后实时**生成一条扣分记录，能追溯到：**谁、什么时间、哪个操作（模块/功能）、用了哪个 provider/模型、消耗多少（token 数或张/段数）、折合多少人民币、扣了多少积分、扣完还剩多少**。
- 失败的调用不产生扣分记录。

### 2）管理后台
- 能看到**全部扣分记录明细**（上面那些字段，含 token 数与人民币成本）。
- 支持**搜索**（按用户）、**筛选**（按时间、模块、provider/模型、消耗类型）、**导出**（如 CSV）。
- 能看到当前生效的 **token 价格表**和"1 元 = 100 积分"规则，一眼知道现在是怎么算的。
- 能看**汇总统计**：按 provider / 模型 / 用户 看总消耗、总扣分（便于看成本与毛利）。

### 3）用户工作台
- 在"订阅 / 积分"页的"**最近账务流水**"里，用户能看到自己最近的记录：**时间、什么操作、+/- 多少分、剩余多少分**；**不显示 token 数和人民币成本**（成本细节只在后台看）。扣分、充值、赠送、退还都在同一条流水里。
- 显示口径与后台一致，同一笔在两边对得上。

## 五、与现有积分系统的关系

- 本需求只**新增"按使用扣分"**这一能力；余额、充值（兑换码等）、退还等已有功能继续沿用，不重复造。
- 扣分 / 充值 / 退还都进**同一本账**，用户和后台看到的是同一份流水。
- 注意：现有扣分逻辑是"禁止负余额 + 操作前预检不足即报错"，本需求（第三节第 6 条）会改成"允许负余额、只在 ≤ 0 时拦截"，落地时这部分需相应调整。

## 六、已确认的口径（2026-06-09 定稿）

1. **取整**：向上取整、最低扣 1 分。
2. **失败调用**：不扣分。
3. **扣分时机**：每次 API 调用**成功后实时扣**（按真实用量）。
4. **用户流水详细度**：只显示"操作 + 扣了多少分（+ 时间、余额）"，**不显示** token 数 / 人民币成本。
5. **余额门槛**：**允许余额为负**；只在积分 **≤ 0** 时拦截，不做操作前余额预检。

## 七、初始 token 价格表（v0 · 采集于 2026-06-09，待你确认）

> 这是"价格表"的首版数据，供你先确认。每条带**最后更新时间**；上线后由后台手动维护。
> **换算汇率：1 美元 = 6.82 人民币**（央行人民币汇率中间价，2026-06-02 为 6.8187）。

### 7.1 各环节用哪个模型（环节 → 模型）

| 环节（模块/功能） | 角色/槽位 | 模型 | 计价类型 |
|---|---|---|---|
| 剧本生成 | brain（+ structured 辅助） | claude-opus-4-8 | 按 token（输入/输出） |
| 项目分类 / 风格判定 | projectClassifier | doubao-seed-2-0-pro-260215 | 按 token |
| 风格圣经抽取 | styleBible | gpt-5.5 | 按 token |
| 创作者画像 | profileDerive | gpt-5.5 | 按 token |
| 分镜提示词 | structured | gpt-5.5 | 按 token |
| 镜头拆分 | structured | gpt-5.5 | 按 token |
| 视频提示词 | structured | gpt-5.5 | 按 token |
| 一致性 / 首尾帧校验 | continuity / frameConsistencyCheck | gpt-5.5 | 按 token |
| 资产图 / 风格化 / 分镜图 / 尾帧图 | image | gpt-image-2（主）/ doubao-seedream-4-5（备） | 按张 |
| 视频段生成 | video | doubao-seedance-2-0-260128 | 按 token（≈ ¥1/秒） |
| 文本兜底（失败重试） | code80 fallback | gpt-5.4 | 按 token |

### 7.2 各模型单价（原币 / 美元 / 人民币）

> 单价单位：文本/视频为「每百万 token」；图片为「每张」。

| 来源平台 | 模型 | 计价类型 | 原始单价 | 美元(USD) | 人民币(¥) | 最后更新 |
|---|---|---|---|---|---|---|
| Anthropic（默认经 zerail 网关 ⚠️） | claude-opus-4-8 | 输入 / 输出 | $5 / $25 | $5 / $25 | ¥34.1 / ¥170.5 | 2026-06-09 |
| OpenAI（默认经 zerail/官方 ⚠️） | gpt-5.5 | 输入 / 输出（缓存 $0.5） | $5 / $30 | $5 / $30 | ¥34.1 / ¥204.6 | 2026-06-09 |
| OpenAI（经 code80 网关 ⚠️） | gpt-5.4（兜底） | 输入 / 输出 | $2.5 / $15 | $2.5 / $15 | ¥17.05 / ¥102.3 | 2026-06-09 |
| 火山方舟（原生 ¥） | doubao-seed-2-0-pro-260215 | 输入 / 输出 / 缓存 | ¥3.2 / ¥16 / ¥0.64 | $0.47 / $2.35 / $0.09 | ¥3.2 / ¥16 / ¥0.64 | 2026-06-09 |
| OpenAI（默认经 zerail 网关 ⚠️） | gpt-image-2 | 按张（低/中/高质量·1024²·估）<br>token：图入 $8 / 图出 $30 每百万 | ¥0.04 / ¥0.36 / ¥1.44 | $0.006 / $0.053 / $0.211 | ¥0.04 / ¥0.36 / ¥1.44 | 2026-06-09 |
| 火山方舟（原生 ¥） | doubao-seedream-4-5-251128 | 按张 | ¥0.25 / 张 | $0.037 | ¥0.25 | 2026-06-09 |
| 火山方舟 Seedance（原生 ¥） | doubao-seedance-2-0-260128 | 按 token（纯生成 ¥46/百万 token，≈ ¥1/秒） | ¥46 / 百万 token | ~$6.74 / 百万 token | ¥46 / 百万 token（≈ ¥1/秒） | 2026-06-09 |

### 7.3 必须知道的几点（影响价格准不准）

1. ⚠️ **网关问题（最需要你确认）**：brain（Claude）、文本类（GPT-5.5/5.4）、主力图片（gpt-image-2）默认**不是走官方 API，而是走第三方网关**（zerail `gateway.zerail.com`、code80 `code.ai80.vip`）。表里填的是**官方参考价**，你实际付给网关的价可能不同（网关常另按人民币计价）。**上线前请用网关账单核对这几项的真实结算价。**
2. **火山系原生人民币**：doubao / seedream / seedance 本来就按人民币计价，不需要汇率换算；表里"美元"列是按中间价反算的，仅供参考。
3. **claude-opus-4-8**：按当代 Opus 4.x 标准价（$5/$25）填，请用网关账单确认是否一致。
4. **gpt-image-2 按质量档**：它按 token 计费，每张成本随质量（低/中/高）和尺寸变化，表里是 1024×1024 估值；最终按实际 `IMAGE_QUALITY` 设置选档。
5. **视频按 token 不按秒**：Seedance 2.0 实际按 token 计费，"¥1/秒"只是约数；扣分按真实返回的 token / 时长算。
6. **数据时效**：本表采集于 2026-06-09，价格会变；后台维护时更新对应行的"最后更新"。
