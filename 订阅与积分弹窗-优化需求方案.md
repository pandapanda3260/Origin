# 订阅与积分弹窗 · 优化需求方案

> 范围：`#billingModal` / `#billingContent` 弹窗页（前端 `public/modules/billing.js` + 后端 `app/api/billing/*` + `lib/credits.ts`）。
> 结论基于真实代码扫描 + 行业成熟做法，非逻辑推断。文末附 reason 代码全量清单与参考资料。
> 状态：**已按 Vasily 决策落地（v1）**。决策与实施说明见 §7、改动清单见 §6。

---

## 一、一句话结论（先看这个）

| # | 问题 | 现状真相 | 关键缺口 | 改动面 |
|---|------|---------|---------|--------|
| 1a | 当前套餐没有 card | 前端 `billing.js:361` 主动 `filter(p => p.code !== 'free')` 把 free 过滤掉了；付费档虽有 card，但按钮是禁用的"当前套餐" | 缺"当前套餐 card + 取消按钮" | 前端 |
| 1b | 取消订阅逻辑 | 取消/恢复接口**已存在**（`subscription/cancel`、`subscription/resume`），按钮藏在顶部状态条里 | 缺"卡片上的取消入口" | 前端 |
| 1c | 取消后到期降级 | ⚠️ **后端没有任何到期降级逻辑**；`period_end` 全程没人写入、`cancel_at_period_end` 只被写不被消费 | "用到截止日期后自动降级"**完全没实现** | 后端 + DB |
| 2a | 流水分页 | 后端 `listLedger(limit, offset)` + `countLedger` + `/api/billing/ledger` 返回 `{items,total,limit,offset}` **已就绪**；前端拉 20 条全量平铺、无翻页 | 缺前端翻页 UI | 前端 |
| 2b | 流水中文化 | 前端直接显示后端 `reason` 原文（`script.adapt`/`batch:video_segments`…）；`_kindLabel` 中文映射存在但只在 reason 为空时兜底，永不触发 | 缺 reason→中文映射 | 前端 |

**最重要的一条**：问题 1 里你说的"取消后可以继续使用到截止日期"——前半句（设取消标记、继续用）现成接口能做到；但后半句（到期真正停止/降级）**当前代码根本没有执行者**，而且付费订阅的 `period_end` 从来没被写过。这块要单独决策是否纳入本次范围（见 §6）。

---

## 二、现状扫描（真实代码）

### 2.1 套餐卡片

- 后端 `app/api/billing/me/route.ts:27` 已经返回**全部档位**并标 `isCurrent`：
  `const plansWithCurrent = PLANS.map(p => ({ ...p, isCurrent: p.code === bal.planCode }))`，数据是齐的。
- 前端 `billing.js:361` 渲染套餐网格时：
  `var planCards = plans.filter(function (p) { return p.code !== 'free'; }).map(...)`
  → **free 档被直接过滤**，所以免费用户在"订阅套餐"网格里看不到自己的当前档。
- 付费档命中 `isCurrent` 时（`billing.js:379-380`）：CTA 文案是 `当前套餐`、并加 `disabled`。即**付费当前档有 card，但按钮是死的**，没有取消入口。
- 另有一张顶部状态条 `.plan-current-card`（`billing.js:505-532`）展示"当前套餐名 + 三个余额小卡 + 支付方式切换"。取消/恢复按钮（`#btnCancelSubscription`/`#btnResumeSubscription`）就藏在这张状态条底部，且仅付费档渲染（`billing.js:462` `if (isPaidPlan)`）——免费用户根本看不到，付费用户也很隐蔽。

→ 你截图里在 free 档，所以：网格只有 Pro/Studio，顶部状态条只有名字没取消按钮。与你描述完全一致。

### 2.2 取消 / 恢复订阅链路

- 前端：`cancelSubscription()`（`billing.js:257`）POST `/api/billing/subscription/cancel`；`resumeSubscription()`（`billing.js:263`）POST `/api/billing/subscription/resume`。
- 后端 `subscription/cancel/route.ts`：`UPDATE user_credits SET cancel_at_period_end = 1`。
- 后端 `subscription/resume/route.ts`：`UPDATE user_credits SET cancel_at_period_end = 0`。
- `cancel_at_period_end` 的**消费方只有读取**：`lib/credits.ts:49 getBalance()` 读出来透传给前端展示。

⚠️ 关键缺口（全仓 grep 确认）：
- **没有任何"到期降级"执行者**：没有定时任务、没有读时惰性降级。到了 `period_end` 之后，`plan_code` 不会被改回 `free`、`subscription_credits` 不会清零。`cancel_at_period_end=1` 设了之后就是个"死标记"。
- **`period_end` 从来没有写入点**：全仓只有 `lib/db.ts:833` 建表、`lib/credits.ts:48` 读取，**找不到任何 UPDATE 写它的地方**。`checkout/route.ts` 只往 `billing_orders` 插一条订单记录，并不激活订阅（支付是占位）。付费档目前实际只能靠后台 `app/api/admin/billing/route.ts:322` 手动调 `subscription_credits` 开通，且也没写 `period_end`。
  → 结果：前端"到期 YYYY-MM-DD"这行**实际永远没有日期**（`periodEnd` 恒为 null），取消功能本身也没有"截止日期"可遵守。

### 2.3 账务流水

- 后端**分页能力已完整**：`lib/credits.ts:350 listLedger(userId, limit, offset)`（offset 已支持，硬上限 200）、`lib/credits.ts:371 countLedger()` 返回总数；`app/api/billing/ledger/route.ts` 已返回 `{ items, total, limit, offset }`。
- 前端**没用上**：`loadBillingSummary()`（`billing.js:87`）固定拉 `?limit=20`，`renderBillingPage()`（`billing.js:479`）把 `_ledger` 全量 `.map()` 平铺，无页码、无 `total`、无翻页状态。
- 中文化问题根因（`billing.js:484`）：
  `var label = item.reason || _kindLabel(item.kind || item.entry_type);`
  → 优先用 `item.reason`。而 `reason` 在各扣费点是**自由文本**（见 §附录），大量是英文/代码串：`script.adapt`、`edit.export`、`batch:video_segments`、`refund:video_segments`…
  `_kindLabel`（`billing.js:41`，9 个 kind→中文）只在 reason 为空时兜底，**实际永不触发**。

---

## 三、行业成熟做法（调研结论）

**取消订阅（Stripe 标准模型）**：`cancel_at_period_end=true` 让用户用满已付周期、到期再真正取消，期间可随时反向置回恢复——和我们现有 cancel/resume 接口设计一致。Stripe 较新版本进一步建议把取消**立刻解析成一个具体时间戳**（`cancel_at`），而不是只存布尔标记，避免后续条目变动导致截止点漂移。UX 侧：取消时明确告诉用户"权益保留至 X 日"，并提供"恢复/撤销取消"，重挽留弹窗最多一次（多州法规要求一键取消）。
→ 对我们的启示：**取消时就把 `period_end` 落成确定日期**，并补上"到期降级"的执行者，否则"用到截止日期"只是文案。

**流水分页（offset vs cursor）**：cursor/keyset 适合海量、强一致的金融流水（并发插入不串行错位）；offset 适合**数据量小（<1 万）、需要页码导航、可分享页码**的场景。我们是"单用户自己的积分流水 + 你明确要页码翻页 + 后端已支持 offset"，**offset 足够且最省**。需带 `total`/`has_more` 元数据、固定排序键（已按 `created_at DESC`）。
→ 选 **server 端 offset 分页**（后端零改动），不做前端一次性全量再切页（流水可能很长）。

**流水类型国际化**：通用最佳实践是**把稳定的 enum/code 与展示用文案解耦**——code 作 key 去查一张本地化文案表，绝不把展示字符串当数据存。
→ 我们的 `kind`（9 值枚举）干净可靠，应作为映射主键；`reason` 里的稳定前缀（`script.*`/`batch:*`/`edit.export*`）作为更细一层覆盖；自由文本只用于审计，不直接展示给用户。

---

## 四、需求一：当前套餐 card + 取消订阅

### 4.1 当前套餐以 card 形式显示在"订阅套餐"网格内
- 取消 `billing.js` 的 `free` 过滤：当 `isCurrent` 时一律渲染成 card（含 free）。
- **UI 与 Pro/Studio 两张卡完全一致**：复用同一个 `.plan-card` 组件与渲染逻辑（同样的深色卡体、价格、积分 chip、特性列表、`.plan-cta` 按钮），新卡与旧卡**唯一差异**是：① 命中 `.is-current` 的"当前"角标 + cyan 边（这是代码里早有的"当前档"标记，Pro/Studio 成为当前档时同样会套用）；② CTA 文案/动作不同（取消/恢复/当前，而非升级）。CTA 按钮本身仍用与其它卡相同的 `.plan-cta` 样式，不做任何 bespoke 改色。无需新增样式。
- 卡片顺序沿用套餐配置顺序（free / pro / studio），不额外重排。

### 4.2 当前档 card 的按钮按"档位 + 取消状态"分支
| 场景 | 按钮 | 行为 |
|------|------|------|
| 付费档 · 自动续订中 | **取消套餐订阅** | 调 `cancel` → `cancel_at_period_end=1`，刷新后变下一行 |
| 付费档 · 已取消（到期停止） | **恢复自动续订** + "到期 YYYY-MM-DD 后降级为免费版"提示 | 调 `resume` → 恢复 |
| 免费档（当前） | 状态角标"当前套餐"，**无取消按钮** | 免费版无可取消订阅，给一行浅说明即可 |

- 复用已有 `cancelSubscription()`/`resumeSubscription()` + `.plan-action-btn`（`styles.css:705`）+ 防连点守卫 `_subActionBusy`（`billing.js:565`），逻辑现成。
- 顶部 `.plan-current-card` 状态条建议保留为"余额总览头部"（三个余额小卡 + 支付方式），把取消/恢复入口**收敛到当前档 card 上**，避免两处重复。（决策点见 §7-A）

### 4.3 取消后的完整逻辑（含后端缺口补全）
取消语义 = **到期前继续享有权益，到期后降级为免费版**。要真正成立，必须补三件事：

1. **取消时落定截止日期**：`cancel` 接口在置 `cancel_at_period_end=1` 的同时，若 `period_end` 为空，则按"当前周期末"写入一个确定日期（对齐 Stripe `cancel_at` 思路）。
2. **到期降级执行者**（二选一，见 §6 推荐惰性方案）：到 `period_end` 后把 `plan_code` 改回 `free`、清零 `subscription_credits`、复位 `cancel_at_period_end=0`，并写一条 `kind:'adjust'` 流水（如"订阅到期降级为免费版"）。
3. **前端展示**：取消后明确显示"权益保留至 X 日"，提供"恢复自动续订"。

> 注：付费订阅的"激活并写 `period_end`"目前也缺（支付占位）。本次至少要保证"被后台/兑换开通的付费档"在取消时有确定截止日期可用；完整付费激活链路是更大议题，建议另立。（决策点 §7-C）

---

## 五、需求二：账务流水分页 + 中文化

### 5.1 分页（server 端 offset，10 行/页）
- 翻页控件放在"最近账务流水"**标题右侧、水平右对齐**（标题行改成 `flex justify-between`）。
- 每页 10 行：前端按 `?limit=10&offset=page*10` 取数；用返回的 `total` 算总页数。
- 控件形态：`‹ 上一页 第 m / n 页 下一页 ›`，首/末页禁用对应按钮；仅 1 页时整组隐藏。
- 翻页只重渲流水区块（独立翻页状态 `_ledgerPage`），不整页 `renderBillingPage()`，避免顶部卡片闪烁。
- 后端零改动（`/api/billing/ledger` 已支持 `limit`/`offset`/`total`）。

### 5.2 中文化（前端映射字典，最小影响面）
标签解析顺序（推荐）：
1. `reason` 命中**稳定前缀表**（`script.*`/`edit.export*`/`batch:*`/`refund:*` 等）→ 映射中文；
2. 否则 `reason` 含中文（`工具箱*`/`兑换码*`/`开户赠送*`）→ 原样展示；
3. 否则用 `kind` 兜底表（9 值，扩充现有 `_kindLabel`）；
4. 最终兜底"账务记录"。

这样新增扣费点即使没登记，也能靠 `kind` 落到合理中文，不会再露英文。完整映射见 §附录。
- 可选增强（非必须）：每行加一个 kind 小色标 / 展示扣后余额 `balanceAfter`。保持克制，先只做中文化 + 分页。

> 替代方案（更高保真，但 blast radius 大）：后端在 `simplifyLedger`/`listLedger` 直接下发本地化 `reasonLabel`，或统一各扣费点为稳定 `reasonCode` 枚举。需碰 10+ 个调用点。**不推荐本次做**，前端字典已足够。（决策点 §7-B）

---

## 六、改动清单与影响面

| 模块 | 文件 | 改动 | Blast Radius |
|------|------|------|-------------|
| 前端·套餐 card | `public/modules/billing.js` | 去掉 free 过滤；当前档 card 渲染取消/恢复按钮分支 | 窄（弹窗内） |
| 前端·分页 | `public/modules/billing.js` | 流水区块加 `_ledgerPage` + 翻页控件 + 按页取数 | 窄 |
| 前端·中文化 | `public/modules/billing.js` | 扩 `_kindLabel` + 新增 reason 前缀映射 + 改 `billing.js:484` 解析顺序 | 窄 |
| 前端·缓存 | `public/workspace.html` | 改完 `billing.js` 必须 bump `?v=` | 必做（否则浏览器跑旧 ESM 缓存） |
| 后端·取消落日期 | `app/api/billing/subscription/cancel/route.ts` | 置标记时补写 `period_end`（若空） | 中 |
| 后端·到期降级 | 新增（见下方两方案） | 到期把档位降回 free | 中 |
| 样式 | `public/styles.css` | 基本复用 `.plan-card.is-current`/`.plan-badge-current`/`.plan-action-btn`，几乎不新增 | 极窄 |

**到期降级执行者 — 两方案**
- **方案 A · 读时惰性降级（推荐，最小面）**：在 `lib/credits.ts getBalance()` 读到 `cancel_at_period_end=1 && period_end < now` 时，就地降级（改 free / 清订阅积分 / 复位标记 / 写一条 adjust 流水）。优点：无需定时任务、用户一访问即生效、与现有读路径同源；缺点：用户长期不登录则降级"延后到下次读"（对配额无害，因为不读就不消费）。
- **方案 B · 定时任务**：复用 `lib/scheduled-jobs.ts` 注册一个每日 job 扫描到期订阅批量降级。优点：不依赖用户访问；缺点：多一套 job + 运维面，且本仓定时设施虽有但要新接线。
→ 建议 **A**，与"不为老项目做兼容、优先窄改"一致；B 可作未来增强。

---

## 七、决策与实施说明（已落地）

- **A. 取消入口落点** → 定：取消入口放**当前套餐 card** 上，"取消套餐订阅"与"升级此套餐"**互斥同位**（同一 CTA 槽）；顶部状态条**不放**取消按钮。已实施：付费当前档 CTA = 取消套餐订阅 / 已取消时 = 恢复自动续订（并提示"到期 X 后降级为免费版"）；free 当前档 = 不可取消的"当前套餐"。
- **B. 中文化实现层** → 定：**前端映射**，不动后端。已实施：reason 稳定前缀 → 中文；已是中文原样；**未覆盖的非中文保留原文（必要英文可保留，不强译）**；reason 空时按 kind 兜底。
- **C. 到期降级** → 定：**纳入**。已实施：`cancel` 接口落定 `period_end`（为空时取 +1 个月，对齐 Stripe cancel_at）；`lib/credits.ts` 新增 `settleExpiredSubscription()` 做到期惰性降级，仅在 `GET /api/billing/me` 读取入口调用，**不碰扣费热路径**（守住"别影响正常逻辑"）。
- **D. 范围收敛** → 仅改需求涉及部分。未触碰 `getBalance`/`chargeCredits`/`refundCredits` 共享事务；未做老数据迁移。

**已知边界（v1）**：到期降级走"惰性结算"——用户打开订阅弹窗（拉 `/api/billing/me`）时触发。若付费用户取消后长期不开弹窗且持续使用，订阅积分会延后到下次读取才清零（不读不消费，对配额无害）。若需"到期即刻强制降级"，可后续把 `settleExpiredSubscription` 接到扣费入口或加每日定时任务（§6 方案 B），属更大 blast radius，单列。
**付费激活链路**仍是占位（支付未对接、正常流程不写 `period_end`），故"取消→到期降级"目前主要对**后台/兑换开通的付费档**成立；完整付费激活另议。

### 7.1 追加决策（第二轮）
- **免费档卡片按钮**：免费版作为当前档时没有可取消的订阅，按钮显示**置灰的"当前套餐"**（与付费当前档同口径，保证三张卡按钮位一致）。已实现（`billing.js` 中 free-current 分支即此）。
- **档位改名**：显示名 + 内部 `code` 一起改为 **free / plus / pro**。映射：原 Pro($99,2000)→`plus`/Plus、原 Studio($299,8000)→`pro`/Pro、Free 不变。改动仅限 `lib/billing-config.ts` + `mocks/billing.ts`（plan code 无业务硬编码、全数据驱动，故无需改前端/路由）。`description` 文案未动；旧 DB 里 `plan_code=pro/studio` 的记录会失配（不做老兼容）。

---

## 附录：reason 代码全量清单 + 建议中文映射

`kind` 枚举（`lib/credits.ts:19`）：`text | image | video | export | topup | redeem | refund | gift | adjust`
batchType→kind（`lib/batch-task-accounting.ts:18`）：`video_segments/videos→video`；`storyboard_prompts/video_prompts→text`；其余→`image`。

| 来源文件 | reason 原文 | kind | 建议中文 |
|---------|------------|------|---------|
| `script/workflow/full-create/route.ts:73` | `script.full-create` | text | 剧本生成 |
| 同上 | `script.adapt` | text | 剧本改编 |
| 同上 | `script.revise` | text | 剧本修订 |
| 同上 `:140/:202` | `script.error` / `script.persist.error` | refund | 剧本生成失败退还 |
| `lib/edit-export.ts:349` | `edit.export` | export | 导出成片 |
| `lib/edit-export.ts:370` | `edit.export.failed:*` | refund | 导出失败退还 |
| `lib/edit-export.ts:270` | `edit.export.cancelled:*` | refund | 导出取消退还 |
| `lib/batches.ts:1506` | `batch:video_segments` / `batch:videos` | video | 视频生成 |
| 同上 | `batch:storyboard_prompts` | text | 分镜镜头生成 |
| 同上 | `batch:video_prompts` | text | 视频提示词生成 |
| 同上 | `batch:*`（其余，如角色/场景/分镜图） | image | 图片生成 |
| `lib/batches.ts:1608` | `refund:*` | refund | 生成失败退还 |
| `lib/exports-reap.ts:33` | `orphan export reap` | refund | 导出超时退还 |
| `lib/toolbox-billing.ts:28` | `工具箱视频生成` / `工具箱图片生成` | video/image | 原样（已中文） |
| `lib/toolbox-billing.ts:46` | `工具箱*失败退款` | refund | 原样（已中文） |
| `app/api/billing/redeem/route.ts:85` | `兑换码 <code>` | redeem | 原样 / 兑换码充值 |
| `lib/credits.ts:78` | `开户赠送 100 积分` | gift | 原样 |
| `app/api/admin/knowledge/route.ts:506/756` | `admin_knowledge_preview` / `admin_preview_topup` | text/gift | 后台调用（一般不对普通用户展示） |

`kind` 兜底表（扩充 `billing.js:41 _kindLabel`）：
`text→文本生成`、`image→图片生成`、`video→视频生成`、`export→导出成片`、`topup→积分充值`、`redeem→兑换码充值`、`refund→失败退还`、`gift→赠送积分`、`adjust→余额调整`。

---

## 参考资料
- Stripe — Cancel subscriptions（`cancel_at_period_end` 模型）：https://docs.stripe.com/billing/subscriptions/cancel
- Stripe — 新版 `cancel_at` 枚举（解析为确定时间戳）：https://docs.stripe.com/changelog/basil/2025-05-28/cancel-at-enums
- SaaS 取消流程 UX（保留至周期末）：https://uxmag.com/articles/10-cancellation-flow-examples-and-why-they-work
- API 分页最佳实践（offset vs cursor / 元数据）：https://www.getknit.dev/blog/api-pagination-best-practices
- 枚举/类型本地化（code 与展示文案解耦）：https://dev.to/jkostolansky/localize-your-rails-enums-55ki
