# 镜头计划 · 输出格式与内容质量升级 方案（评估 + 待批提案）

> 范围：只评估并升级**镜头计划（镜头表）的输出格式与内容质量**，**不改生成逻辑、本轮不动代码**。
> 本文是「评估 + 提案」，供审阅后再决定是否实施。
> 关联：`lib/prompts.ts:1132 SP_SHOTS_GENERATE`、`app/api/shots/generate/route.ts`、`lib/prompts.ts:1330 SP_VIDEO_PROMPT_GENERATE`、`public/modules/shots.js`。
> 日期：2026-06-01

---

## 0. 一句话结论

现在的镜头计划**简单清晰、节奏/对白/运镜连贯性这几条工程纪律做得相当好**，但**摄影语言层面偏薄**：真正决定“画面长什么样、对视频模型可控”的几个旋钮（**焦段、机位角度、光位影调、构图、景深**）要么**没有结构化字段**，要么**埋在一段自由文本 `visual` 里靠模型即兴**，要么**被推迟到下游视频提示词阶段才临时编**。结果是「能拍、但不够像有导演/摄影指导在掌镜」。

逐条回答你的问题：

| 你的问题 | 结论 | 说明 |
|---|---|---|
| 专业度是否达标？ | **半达标** | 拆镜/节奏/对白时长匹配/运镜节制——专业；摄影造型（光、镜、构图）——业余偏弱 |
| 输出是否简单清晰、专业高效？ | **简单清晰✓，专业高效△** | 字段少、好读，但“高效”靠的是把复杂决策省掉了，不是浓缩了 |
| 对视频生成是否有足够控制力？ | **不足** | 只锁了「景别 + 运镜」两个旋钮；焦段/角度/光/景深/构图都没锚定，交给下游和模型自由发挥 |
| 生成时艺术素养/专业度够不够？ | **不够、且位置错了** | 专业词汇（轴线/焦段/浅景深）只活在**下游视频提示词**的 system prompt 里，**镜头计划本身**没有，等于“该做造型决策的环节没做，留到后面即兴补” |

---

## 1. 现状：现在的镜头计划长什么样

**生成**：`SP_SHOTS_GENERATE` 让模型一次产出 6–14 个镜头的严格 JSON。**单镜头当前结构化字段**：

```
idx, sceneId/sceneName, duration, pace,
shotType(景别), camera(运镜), visual(80-130字自由文本),
dialogue, keyInfo(2-6字), audio, emotion, intensity,
scriptRef, characters, tailFrameSignals{...}
```

**用户实际能调的旋钮**（`public/modules/shots.js` 镜头卡只暴露 4 个下拉）：**时长 / 节奏 / 景别 / 运镜**。其余都在 `visual` 文本里。

**下游怎么用**（`buildVideoPromptMessages` 的 `slimShots`）：只把 `idx, duration, shotType, camera, pace, visual, dialogue, keyInfo, audio, emotion, characters` 传给视频提示词阶段。也就是说——

> **真正流到视频模型的“画面控制信息”只有：景别 + 运镜 + 一段 `visual` 自由文本。**
> 焦段、机位角度、光位、景深、构图，**镜头计划里根本没有**，全靠下游 `SP_VIDEO_PROMPT_GENERATE` 的示范临场编（示范里那个 `35mm / 50mm` 是写死在范例里的，**没有上游锚点**，每次生成都在“猜”）。

---

## 2. 专业标杆（research-backed）

**① 行业 shot list 标准字段**（StudioBinder / Boords / LTX 等）：Scene/Shot #、Description、**Shot Size（景别）**、**Camera Angle（角度）**、**Camera Movement（运镜）**、**Lens / Focal Length（焦段）**、**Aperture / DOF（光圈/景深）**、**Lighting（光位、自然/人工、时间）**、Audio/Dialogue、Props/Location。
→ 焦段、光圈/景深、角度、光位**都是独立列**，专门为了“跨拍摄日保持景深与透视一致、防畸变”。

**② 华语「导演拍好一个镜头的 15 项思考」**：故事内容、表演内容、**景别、构图、角度、高度、运镜、影调**、概念、氛围、主要元素、剪辑、VO、字幕、音效。
→ **景别 / 构图 / 角度 / 高度 / 运镜 / 影调 是 6 个分开的决策**。我们现在把「景别+角度+高度」塞进一个 `shotType` 枚举，「构图/影调」丢进自由文本，「焦段/景深」直接没有。

**③ 镜头语言 = 控制力**：焦段是“画面情绪”的主旋钮——广角夸张纵深/制造紧张与疏离，50mm 接近人眼最真实，85mm+ 压缩空间、浅景深、亲密/偷窥感；浅景深隔离主体用于情绪/亲密戏。这些在我们系统里**完全没有被计划层决定**。

**④ AI 视频模型 2026 的可控旋钮**（Kling 3.0 / Sora 2 / Veo 3.1 / Seedance 2.0 提示词指南）：
- 结构顺序 **Scene → Characters → Action → Camera → Audio/Style**（或 SAEC）。
- **用精确摄影术语**（slow push-in / tracking / crane / orbit）模型才会精确响应；模糊词无效。
- **塞镜头元数据**（如 `70mm anamorphic`）能直接触发专业级质感。
- **显式景深**（“shallow DOF, tack sharp on the eyes”）。
- **显式光位**（“golden hour, 45° key light, rim light”）而不是“bright light”。
- 明确**什么在动、什么静止、速度与方向**。
- 提示词宜紧（≈150 词内），高信息密度名词/动词，别堆墙。

> 结论：模型**吃的就是焦段/景深/光位/角度这些结构化造型参数**。我们没在计划层产出它们，等于把模型最听得懂的控制信号丢掉了。

---

## 3. 差距清单（具体、可定位，便于后续动手）

1. **缺「焦段」锚点（最大控制力缺口）**。计划层无焦段字段；下游 `⟦景别·焦距·运镜⟧` 标签里的 `mm` 是范例写死、每次即兴。→ 画面透视/景深/情绪不可控、跨镜头不一致。

2. **`shotType` 枚举把三件事揉成一件**：`["大全景…大特写, 俯拍, 仰拍, 主观镜头, 过肩镜头"]` 把**景别（size）+ 角度（俯/仰）+ 关系（过肩/主观）**混在一个字段。**后果**：你无法表达「近景 + 仰拍」「特写 + 过肩」——一选俯拍就丢了景别。这是个实打实的专业建模错误。

3. **`visual` 是超载的杂物桶**：80–130 字里要同时塞场景/动作/光线/构图，模型填得时多时少、时有时无，造型质量全凭运气，且**不可被单独编辑/复用/校验**。

4. **光位、构图、景深无结构化表达**，全靠 `visual` 里碰运气提一嘴；下游想用也抓不到稳定字段。

5. **专业词汇在错误的环节**：轴线/焦段/浅景深/跟焦这些词只写在**下游** `SP_VIDEO_PROMPT_GENERATE`。镜头计划——也就是“导演+摄影指导该做造型决策”的环节——**反而没有这些词的约束**，把造型决策推迟到了翻译环节即兴补。

6. **三处“运镜词表”各不相同（格式碎片化）**：
   - 提示词 `SP_SHOTS_GENERATE`：…快速拉远、跟随、环绕、手持轻晃、甩镜头、摇镜头。
   - 路由 `route.ts MOVEMENTS`：又多了 `固定机位/推/拉/摇/跟/航拍/手持/轨道` 等别名。
   - 前端 `CAMERA_MOVES`：又多了 `左移/右移/上移/下移/升降`——而这几个**提示词从没教过模型**，A/B/C/D 运镜兼容分组里也**没有它们的组**（用户手动选了 `左移`，相邻兼容校验无从判断）。
   → “输出格式”没有单一事实源，三处漂移。

7. **提示词内部自相矛盾的时长口径**：正文同时出现「6-12 个」「6-14 个」、「默认 3-5 秒」「3-7 秒」「铺垫 3-4/主戏 5-6」。`镜头合并与素材生成-方案.md` 已主张单镜 **1–7s**，但本提示词与路由 clamp 仍是 **3–7**。（属逻辑/参数口径，本轮不动，仅登记。）

---

## 4. 升级提案

分两层，**对应你“只升级内容、不改逻辑”的约束**：

### Tier A — 纯内容升级（零代码、零逻辑改动，立即可做）

**只改 `SP_SHOTS_GENERATE` 这一段提示词文本**，不加字段、不动路由、不动 UI、不动下游。因为 `visual` 本来就流到视频提示词，把它的**写法专业化**就能立刻提升控制力。三件事：

1. **给每个 `visual` 开头加一行造型标签**（把下游的 `⟦…⟧` 思路提到计划层）：
   `⟦景别·焦段·机位角度·运镜·景深·光位⟧` —— 例：`⟦中近景·50mm·平视·固定镜头·浅景深·顶光冷白⟧`
2. **把 `visual` 正文收紧为“调度/动作/神态”**：技术造型参数进标签，正文只写**谁在动、怎么动、神态、关键道具、什么静止**，60–110 字，删空话。
3. **灌入专业词表 + 正反例**（见 §5），要求焦段/角度/光位/景深/构图都**必须给值**，并对“以 XX 视角/营造 XX 氛围”这类业余写法明令禁止（复用下游已有的禁令风格）。

> Tier A 的好处：`visual` 已被 `slimShots` 透传到视频提示词，**当天改、当天见效**，且**完全不碰生成逻辑**。风险最低、ROI 最高。

### Tier B — 输出格式升级（schema 结构化，待你批准后再小幅接线）

把造型决策**升级为独立结构化字段**（“格式升级”本体）。新增/调整如下，全部用中文枚举、保持简单：

| 字段 | 含义 | 取值（建议枚举） |
|---|---|---|
| `shotType` *(收窄)* | **纯景别** | 大全景/远景/全景/中景/中近景/近景/特写/大特写 |
| `angle` *(新)* | 机位角度+高度 | 平视/略俯/俯拍/略仰/仰拍/鸟瞰/倾斜(Dutch)/过肩/主观POV |
| `lens` *(新)* | 焦段（定透视+景深基线） | 超广角16-24/广角35/标准50/中长焦85/长焦135+/微距 |
| `focus` *(新)* | 景深 | 深焦/中等/浅景深/移焦 |
| `light` *(新)* | 光位影调 | 方向(顺/侧/侧逆/逆/顶/底)·质感(硬/柔)·色温(暖/中/冷)·反差(高/低) |
| `composition` *(新)* | 构图 | 三分法/中心对称/前景框架/引导线/留白/视线方向/景深分层 |
| `cut` *(可选)* | 与下一镜接法 | 硬切/同轴前进/越轴反打/匹配剪辑/叠化/跳切 |

其余字段（duration/pace/camera/dialogue/keyInfo/emotion/intensity/scriptRef/characters/tailFrameSignals）**原样保留**。`visual` 同 Tier A 收紧为动作/调度。

> 注意：新字段靠 `route.ts` 的 `...base`（`stripShotInternalFields` 保留未知键）会**自动落库**，但**不会**被 `slimShots` 透传给视频模型、也不会出现在 UI 下拉、也不会被枚举校验——所以 Tier B 要真正“对视频生成产生控制力”，需要 §6 那几处最小接线。**这就是为什么 Tier B 标记为“待批后再动代码”。**

### Before / After（同一镜头，用项目里“老周复盘”示例）

**现状（旧）**
```json
{
  "idx": 2, "sceneName": "海鲜自助营业区", "duration": 5, "pace": "normal",
  "shotType": "中近景", "camera": "固定镜头",
  "visual": "老周面前的中近景，机位固定，他翻开记账板沉声开口，扇贝财务在旁压着账单冷硬反应，后排员工屏息。冷白顶灯，不锈钢台面反光。",
  "dialogue": "老周：「来，复盘。」", "keyInfo": "老周开场",
  "emotion": "rising", "intensity": 3
}
```

**Tier B（新）**
```json
{
  "idx": 2, "sceneName": "海鲜自助营业区", "duration": 5, "pace": "normal",
  "shotType": "中近景",
  "angle": "平视",
  "lens": "标准50",
  "camera": "固定镜头",
  "focus": "浅景深",
  "light": "顶光·硬光·冷白5600K·高反差",
  "composition": "主体偏右三分线·前景账板虚化框边·后排员工压暗作陪体",
  "visual": "老周右手翻开记账板、下巴微抬沉声开口；扇贝财务在侧压住账单只做冷硬反应不抢话；后排员工屏息，画面其余静止。",
  "dialogue": "老周：「来，复盘。」", "keyInfo": "老周开场",
  "emotion": "rising", "intensity": 3,
  "cut": "同轴前进"
}
```

**Tier A（不加字段、只把上面信息塞进 visual 标签头）**
```
visual: "⟦中近景·50mm·平视·固定镜头·浅景深·顶光冷白高反差⟧ 老周右手翻开记账板、下巴微抬沉声开口；扇贝财务在侧只做冷硬反应不抢话；后排员工屏息，其余静止。前景账板虚化作框，主体压右三分线。"
```

### 字段词表（写进提示词，教模型“怎么选、为什么”）

- **焦段 lens**：超广角16-24=夸张纵深/环境压迫/紧张；广角35=自然带环境；标准50=接近人眼最真实（对白默认）；中长焦85=压缩空间/浅景深/人像亲密；长焦135+=强压缩/偷窥/疏离；微距=道具特写。
- **角度 angle**：平视=中性平等；俯=弱化/审视/孤立；仰=强化/压迫/权威；鸟瞰=全局/宿命；倾斜Dutch=失衡/不安；过肩=对话关系；主观POV=代入。
- **光位 light**：顺光=平/信息；侧光=立体/戏剧；侧逆/逆光=轮廓/氛围/情绪；顶光=审讯/冷峻；底光=诡异。硬光=硬朗高反差，柔光=柔和低反差。冷暖色温服务基调。
- **景深 focus**：深焦=环境/纪实/群戏都清；浅景深=隔离主体/情绪/亲密；移焦=把注意力从 A 转到 B。
- **构图 composition**：三分法/中心对称/前景框架/引导线/留白/视线方向/景深分层（前-中-后）。

---

## 5. 实施时的最小接线点（仅当你批准 Tier B 时才动；本轮不做）

> 列在这里只为透明，本文不执行。Tier A 不需要其中任何一项。

1. `route.ts`：为 `angle/lens/focus/light/composition/cut` 加 `pickEnum` 校验（不校验也能落库，但建议规整）；把 `shotType` 枚举里的 `俯拍/仰拍/主观/过肩` 迁到 `angle`。
2. `buildVideoPromptMessages` 的 `slimShots`：把新字段加进透传，并在 `⟦…⟧` 标签与“运镜系统/基调”段里引用（这步才真正把控制力送到视频模型）。
3. `public/modules/shots.js`：镜头卡增 `角度/焦段/光位/景深` 下拉（可折叠在“高级”里，保持默认界面清爽）。
4. **统一运镜/景别词表为单一事实源**（修复 §3.6 的三处漂移）：一个常量，提示词、路由、前端共用。
5. 顺带对齐 §3.7 的时长口径（与 `镜头合并` 方案的 1–7s 收敛——属逻辑，单独议）。

---

## 6. 建议落地顺序

1. **先上 Tier A**（纯提示词、零代码）：改 `SP_SHOTS_GENERATE`，加 `⟦…⟧` 造型标签 + 词表 + 收紧 `visual` + 正反例自检。同素材跑 2–3 条对比旧输出，看画面控制力与一致性是否提升。
2. Tier A 验证 OK 后，再决定 **Tier B**（结构化字段 + §5 接线），把造型从“文本里”升级成“可编辑、可校验、可透传的字段”。
3. 词表/枚举**单一事实源**与时长口径一并收口。

---

## 来源

- StudioBinder《Shot List Template — Ultimate Guide》：https://www.studiobinder.com/blog/shot-list-template-free-download/
- Boords《Free Shot List Template (2026)》：https://boords.com/shot-list-template
- 影视工业网《导演，拍好一个镜头的 15 项思考》：https://cinehello.com/stream/131251
- In Depth Cine《Understanding Focal Length in Cinematography》：https://www.indepthcine.com/videos/focal-length
- Adorama《Language of Lenses in Cinematography》：https://www.adorama.com/alc/language-of-lenses-in-cinematography/
- Cinema Luts《Depth of Field & Cinematic Storytelling》：https://www.cinema-luts.com/depth-of-field/
- Atlabs《Kling 3.0 Prompt Guide (2026)》：https://www.atlabs.ai/blog/kling-3-0-prompting-guide-master-ai-video-generation
- LetsEnhance《12 essential camera movements for AI video》：https://letsenhance.io/blog/all/ai-video-camera-movements/
- LTX Studio《AI Video Prompt Guide》：https://ltx.studio/blog/ai-video-prompt-guide
