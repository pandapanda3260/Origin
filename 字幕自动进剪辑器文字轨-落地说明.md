# 字幕自动进剪辑器文字轨 — 落地说明（方向A）

2026-06-11 落地，待手测。

## 这是什么

之前剪辑器（VevDemo）里没有任何字幕：素材片段是无字原片，自动铺轨只铺视频轨+BGM 轨，字幕只存在于"一键成片"导出时服务端烧录。结果是：剪辑器里看不到字幕，且剪辑器导出的成片字幕全丢。

现在：**自动铺轨时，台词会按时间码生成文字片段，铺进剪辑器的文字轨**。剪辑器里可见、可改、可删，导出由火山渲染自带字幕。

## 字幕从哪来（与一键成片同一套规则）

行来源（`online_editor.js` 复刻 `lib/edit-export.ts` buildSrt）：

1. 优先 `storyboards[gIdx].videoPrompt` 里的 `角色名：「台词」` 引号提取；
2. 没有则回退该段绑定镜头（`storyboard.shotIndices`，无效回退 `[gIdx]`）的 `shots[i].dialogue`，按说话人切分。

时间分配：段内均分。首条从段起 +0.15s 开始，行间留 0.05s，段尾留 0.05s，每段最少 0.5s 可用时长。

## 写入协议（壳层 fe/index.js）

协议依据：火山官方 volcengine/mcp-vod 仓库的直接剪辑 Track 文字片段示例（与我们已有 video/transition 写法同一套协议）：

```json
{
  "Type": "text",
  "Text": "台词内容",
  "TargetTime": [150, 2300],
  "FontSize": 41,
  "FontColor": "#FFFFFFFF",
  "Extra": [{ "Type": "transform", "PosX": 0, "PosY": 1574, "Width": 1080, "Height": 90 }],
  "OriginSubtitle": true
}
```

要点：

- 文字 lane 放 Track 数组**最前**（官方 guide 要求，渲染盖在视频上层）；
- TargetTime 跟随既有 timeUnit 决策（ms/us 同视频轨）；
- 字号 = 画布高 ×0.032，夹在 18–56；位置 = 底部 82% 高度处全宽条；白字。
- 布局/对齐是首版估值，**手测后可调**：改 `fe/index.js` 的 `buildSubtitleLaneFromPlan`（PosY 比例、fontSize 比例、FontColor）。

## 降级语义（fail-soft）

`updateProject` 带文字轨失败时：自动去掉文字 lane 降级重试一次，**视频铺轨永不因字幕挂掉**。降级时 Origin 侧弹软提示"字幕轨写入失败，本次按无字幕铺轨"（不拦流程），详细错误在浏览器控制台 `subtitleError`。

## 已知边界

- 铺轨是整体覆盖（既有行为）：自动铺轨只在素材同步完成后触发一次；之后用户在剪辑器里手动改字幕不会被反复冲掉，但再次触发自动铺轨（如重新同步素材）会覆盖文字轨。
- SDK 可视化时间线对 text 片段的渲染是合理推断（SDK 有"文字"面板、协议同源），**未实测**——这正是手测要确认的第一件事。
- 不做老工程迁移：已铺过轨的旧工程重新同步一次素材即可带上字幕。

## 手测清单

1. 打开一个有台词的项目 → 进剪辑器 → 等自动同步+铺轨完成；
2. 看时间线最上方是否出现文字轨、片段数量是否符合台词条数；
3. 播放预览：字幕是否在底部、时机是否跟台词对得上、字号位置是否合适；
4. 拖动/编辑一条文字片段确认可改；
5. 导出视频 → 成片是否带字幕；
6. 若文字轨没出现：跑 `node scripts/probe-vev-text-track.mjs`（一次性探针，本机跑）dump 工程 Track 实况发我；该探针手测通过后删除。

## 防回归

`npm run test:vev-subtitle-track`（21 断言：哨兵+行为）。fe/index.js 是未跟踪手改文件，vendor 重同步会冲掉字幕代码——测试变红即报警。相关既有测试 keepalive 18 断言、material-sort 13 断言、cache-busting 全绿。

版本：online_editor.js v11 / main.js v331（三处同号纪律已过 test:cache-busting）。
