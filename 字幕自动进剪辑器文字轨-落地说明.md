# 字幕自动进剪辑器文字轨 — 已废弃

2026-06-11 复核结论：直接写 `Type:'text'` 到 VevDemo `EditParam.Track` 不作为最终方案继续使用。

原因：

- 真实写入探针显示：`UpdateProject` 可以成功，但 text lane 会被服务端/SDK 丢弃或不渲染，不能证明字幕导入成功；
- 当前实现已改为官方 subtitle 素材路径：Origin 生成 SRT，VevDemo bridge 上传 object subtitle，`CreateEditMaterial` 注册后再 `SearchEditMaterial` 回读；
- `npm run test:vev-subtitle-track` 现在锁的是 subtitle material 导入契约，不再锁 text lane。

当前验收边界：

- P0：字幕作为 VevDemo 官方 `subtitle` 素材导入并回读成功；
- P1：确认 SDK 是否有稳定 API/schema 可以把 subtitle material 自动放入时间线字幕轨。
