# 方案三 · 分镜首帧候选模型 — 落地方案

> 《画板模式 PRD v2》落地拆分第 3 个方案。**只出方案不改代码**。这是整条线**最重的数据模型演进**：把"片段(group)级一张段首首帧"改成"**片段内每镜头(shotUid)各一组候选首帧 + 显式选中**"。
> 全程扣真实代码（路径+行号）。执行：Codex；审查/验收：本人。依赖**方案二**的稳定 `shotUid`（Phase2-1）。

---

## 0. 范围与依赖

**做**：
- 数据模型：`storyboards[g].shotFrames[shotUid] = { candidates[], selectedCandidateId }`（镜头级候选）。
- 选中权威：显式 `selectedCandidateId`（非 DOM 位置）；新生成/上传/编辑**追加候选不覆盖**、默认选中最新；拖到最右 = 设为选中。
- 后端逐镜头生成：storyboard_images 从"一段 1 张"扩成"段内每镜头 1 张候选"；每片段"一键全部生成"。
- 候选操作走**窄动作端点**（选中/排序/删除/追加），不整包 PUT。
- 读兼容垫片：保 `resolveStoryboardFirstFrameUrl` 等 10+ 下游不改即可工作。
- 全程 **flag 共存**（`ORIGIN_PER_SHOT_FIRST_FRAME` 默认关，旧页零影响、可秒回退）。

**不做（留后续）**：
- 视频侧"每镜头首帧=关键帧"的消费（方案四，本方案视频仍读段首单帧）。
- 局部 mask 修改（未落地，方案三只做"改提示词/参考重生→追加候选"；编辑入口预留 `source:'edit'`）。
- 视频候选（方案五）。
- 为老项目迁移（只读时投影）。

**依赖**：方案二 Phase2-1 的 `shotUid` 落库 + 写回身份守卫（`shotFrames` 以 shotUid 为键；写回复用 `group-slot-write-guard.ts` 的 shotUid 绑定校验）。

---

## 0.5 关键现状（扣码，决定方案形态）

| # | 事实（file:line） | 影响 |
|---|---|---|
| ① | **首帧严格 group 级**：每段只有 1 张段首首帧（`buildFrameImageGenerationPlan` 用 `primaryShot=groupShots[0]`，frame-image-plan.ts:479；executor 一段 1 张，batch-executors.ts:1740/1984）。合并段段内第 2..N 镜头**无自己的首帧**。 | 这是核心缺口；逐镜头候选是新建。 |
| ② | **读取咽喉** `resolveStoryboardFirstFrameUrl(sb)`（visual-reference-state.ts:41-47，三源链 `frames.first.url‖firstFrameUrl‖firstFrame.currentUrl`），被 10+ lib（video-payload-decision:215、start-runs:366、video_segments:2744、manifest 等）+ 前端 `firstFrameImageUrl`(frameRecommendations.js:7) 依赖。 | 改造**保此咽喉为兼容垫片**（返回段首选中候选），10+ 下游不改即工作，是控爆炸面的关键。 |
| ③ | **完全无候选/选中概念**：`firstFrame.currentUrl` 单值，重生即覆盖；视频侧 `_markGroupVideoCurrent` 也是单值覆盖。**无现成 `selectedCandidateId` 可抄**（全新建）。 | 候选+选中是全新数据层。 |
| ④ | **历史双轨不一致**：`firstFrame.history[]`（visual-reference-state.ts:117-134，去重+slice20、不含失败、**维护良好但几乎无人读**）vs `imageHistory[]`（UI 历史弹窗读，frames/plan:180、storyboard.js:3977，但**结构化 storyboard_images 重生从不写它**，只 asset_images + set-current-from-history 写）。 | 新模型用 `candidates[]` **收敛取代两轨**，不再三轨并行。 |
| ⑤ | **前端按 group 单图渲染**：`_storyboardFramePanelHtml('first',…)`(storyboard.js:3305) 一段一张；合并段非段首镜头渲染只读镜像卡（`mergedChild`，图被抹）。首帧**无拖序**（`_initGalleryDrag` 在 shots 布局直接 return，:7137）。staleness **已无角标**（只进顶部按钮文案）。 | 候选行 UI 是新建；但**落点干净**（见⑥）。 |
| ⑥ | **`board_state.js:286-308` 已留 shim**：方案一投影已按 `group.shotIndices` 出 `shotRows`（每行带 shotUid），但每行复用 group 封面 `firstFrameImageUrl(sb)`、造合成单候选 `[{id:'first:gIdx:shotIdx',url:coverUrl}]`(:294)。 | **这就是方案三前端的天然落点**：把合成单候选换成读 `shotFrames[uid].candidates`。 |
| ⑦ | **一键全生成**=项目级单按钮 `generateAllImages()`(storyboard.js:8459) 批所有 group；`generateStoryboardSheet(gIdx)`(:7594) 是现成单组入口；`updateStoryboardCard(gIdx,…)`/`renderStoryboardFrameCard`(render_hooks.js:188) 已能单卡增量刷。 | 每片段按钮可复用单组入口；后端扩逐镜头即可。 |
| ⑧ | **frames 路由**：upload 写首帧单值（frames/upload:181-220）；delete **只支持尾帧、拒首帧**（frames/delete:62）；set-current-from-history 是唯一"切换图"语义（从 imageHistory 选，:180-281）；reference-upload 写 `firstFrameEditDraft`（参考图，不动 frame URL）。 | 候选操作端点新建，但可仿 set-current-from-history 的"专用动作"范式。 |
| ⑨ | **staleness** `storyboard_${g}`（group 级），`computeFirstFrameSourceHashForShotIndices`(frame-workflow-state.ts:230) **hash 整组 shots** → 改段内任一镜头都标该段唯一首帧脏（粗）。 | 候选化后 sourceHash 下沉到 shotUid，精准只标变更镜头。 |

**行业佐证（Firefly/Ideogram，本方案核心原则）**：变体进网格/可切换比较；**重生应追加候选、绝不覆盖原图**（"variations extend the workspace, never overwrite without confirmation"）；每候选保留生成参数元数据；选中/分支动作就在候选卡上。→ 直接背书"追加不覆盖 + 显式选中 + 候选带 prompt/sourceHash 元数据"。

---

## 1. 目标数据模型

```
storyboards[g] = {
  ...,
  shotIndices: [s0, s1, ...],            // 方案二：片段含哪些镜头
  shotFrames: {                          // 【新】镜头级首帧候选
    [shotUid]: {
      candidates: [
        { id, url, source:'gen'|'upload'|'edit',
          prompt, sourceHash, taskId, createdAt, status:'ready' }   // 只收成功；失败不入候选
      ],
      selectedCandidateId                 // 显式选中（数据权威）；默认=最新追加
    }
  },
  // 兼容字段保留：frames.first / firstFrame / firstFrameUrl 由垫片(§3)派生写，老数据/下游照旧能读
}
```

要点：
- **键 = shotUid**（方案二落库），不是数组下标；写回用 `group-slot-write-guard.ts` 的 `currentUids` 比对防漂移。
- **候选只收成功**（status:'ready'）；失败仍走现有 `markFirstFrameFailed` 错误态、不入候选（对齐现状 history 不含失败）。
- **收敛历史双轨**（现状④）：新模型 first-frame 不再维护 `firstFrame.history` 与 `imageHistory` 两轨；`candidates[]` 即唯一来源。老数据读时投影（§3）。
- **migration-free**：无 `shotFrames` 的老项目，读时把段首单帧投影成"段首 shotUid 的 1 个只读默认候选"；新生成（flag 开）才写 `shotFrames`。不写迁移、不改老盘。

---

## 2. 选中权威与候选操作（窄动作端点）

- **选中 = 显式 `selectedCandidateId`**，下游一律读它，**绝不读 DOM 位置/最右几何**（根治刷新/增删后串位）。
- **追加不覆盖**（行业原则）：生成/上传/编辑 → `candidates.push(新)` + `selectedCandidateId=新.id`（默认选中最新）。**取消现状"重生即删旧值覆盖"**（storyboard.js:9212 删旧值的写法在新模型下不再用）。
- **拖到最右 = 便捷选中**：拖动只改 `candidates` 顺序（持久化为 id 序列），拖到末位时把 `selectedCandidateId` 设为该候选——既满足"把想用的拖到最后"（Vasily 口径），又以 id 为权威。
- **窄动作端点**（仿 set-current-from-history 专用动作，避免整包 PUT/409，对齐 SEGMENT_VIDEO_HISTORY 的 set-current 教训）：
  - `POST /api/frames/candidate { action:'select', projectId, groupIdx, shotUid, candidateId }`
  - `…{ action:'reorder', …, orderedIds:[…] }`
  - `…{ action:'delete', …, candidateId }`（放开首帧删除——现状 frames/delete 拒首帧，新端点按 shotUid+candidateId 删单候选；删的是当前选中则回退到相邻候选）
  - 追加由 storyboard_images executor 写回（§4）/ upload（§4.3）完成。
  - 全部服务端校验 shotUid∈该 group、candidateId 属该 shot，写回 `shotFrames[uid]` + 重算垫片字段（§3）+ 必要时同步 stale。

---

## 3. 读兼容垫片（控爆炸面的关键）

不重写 10+ 下游读点；把**读取咽喉**改造成"新模型→旧字段"的薄派生：
- `resolveStoryboardFirstFrameUrl(sb)`（visual-reference-state.ts:41-47）升级为：**优先**取"该段**段首 shotUid** 的 `selectedCandidateId` 候选 url"；无 shotFrames 时回落原三源链（老数据/flag 关）。
- 同步把段首选中候选**镜像回** `frames.first.url`/`firstFrameUrl`/`firstFrame.currentUrl`（每次候选选中/生成时由窄端点/executor 写），让所有旧读者（video-payload-decision:215、start-runs:366、video_segments:2744、manifest、前端 firstFrameImageUrl）**零改动**继续工作。
- 前端 `firstFrameImageUrl(sb)`(frameRecommendations.js:7) 保持返回段首兜底；board 候选行另走 `shotFrames[uid]`（§6）。
- **边界**：视频侧本方案仍只消费段首单帧；"段内每镜头首帧=多关键帧"喂视频是**方案四**（届时改 manifest/payload 读 `shotFrames` 全集），不在本方案动。

> 这样方案三只需改"咽喉 + 写回"，不必改 10 个下游文件——根因修复 + 最小爆炸面。

---

## 4. 后端逐镜头生成（flag 后核心）

### 4.1 plan 构建
`frame-image-plan.ts:463 buildFrameImageGenerationPlan`：现固定 `primaryPos=0`（段首）。改为**对 group 内每个 shotUid 各建一个 plan**（该镜头自己的 visual 作主、段内其余镜头作 context、资产按该镜头联合文本匹配）。即一段产 N 个 per-shot plan。

### 4.2 executor 写回
`batch-executors.ts:1740 storyboard_images`：从"1 group → 写 `storyboards[g]` 单帧"改为"1 group → 遍历 shotUid → 各生 1 张 → `candidates.push` 到 `shotFrames[uid]` + 设 selected"。写回经 `writeGroupSlot` + `group-slot-write-guard` 校验 shotUid 绑定（防 group 重排写错）。legacy_pencil 路径（:2160）在 flag 开时不走（结构化为唯一路径）。

### 4.3 上传 / 编辑追加候选
- `frames/upload`（:181）：请求加 `shotUid`，写入变为 `candidates.push({source:'upload'})` + 选中，而非覆盖单值。
- `edit-first-frame` FFE 模态（storyboard.js:5756，整图重生式）：产物 `candidates.push({source:'edit'})` + 选中。**局部 mask 编辑仍未落地**，就绪后用同一 `source:'edit'` 追加语义无缝接入。

### 4.4 target 粒度
batch target 现为 group（`ctx.target.groupIdx`）。**本方案保持 group 粒度发起**（一键生成该段所有镜头），executor 内部按 shotUid 循环；**单镜头单候选重生**作为候选行上的"重新生成"按钮，target 扩 `{groupIdx, shotUid}`（executor 只重跑该 shot、push 新候选）。

---

## 5. 每片段"一键全部生成"

- 现状：项目级单按钮 `generateAllImages()`(storyboard.js:8459)。**改为每片段一个按钮**（board 片段块右侧，对齐图11），调现成 `generateStoryboardSheet(gIdx)`(:7594) 单组入口（target=该 group，executor 逐镜头生成）。
- **完成自动消失**：该片段所有 shotUid 都已有 ≥1 候选 → 按钮隐藏（判据=遍历 `shotFrames[uid].candidates.length>0`）。
- 单镜头行另有"生成/重新生成"补单张（§4.4）。
- 项目级"全部生成"可保留为 board 顶部兜底（可选），但主交互是每片段。

---

## 6. 候选行 UI（在 board，落点 board_state/board_candidates/board.js）

- **落点**：`board_state.js:286-308` 的 `shotRows`——把合成单候选(:294)换成读 `shotFrames[uid].candidates`（含 selectedId）；空则占位卡（上传/生成）。
- **渲染**：每镜头一行，行内候选卡水平排列（`.bc-thumb` 16:9 语汇，方案一§6A），选中候选高亮（描边/角标）；卡顶"镜头N候选M"。变体可横向切换比较（Ideogram 范式）。
- **操作**（卡上就近，Firefly 范式）：选中（点卡/单选）、重新生成（追加候选）、上传、编辑（FFE）、删除候选、下载。
- **拖序**：候选行**新建独立拖序**（现 `_initGalleryDrag` 在 shots 布局 return，不复用；候选行拖动与 group 横滑解耦）；拖动改 `candidates` 顺序→ `reorder` 端点；拖末位→ `select`。
- **鉴权**：每张候选 `<img>` 渲染后必过 `hydrateProtectedImageElements`（utils.js:1137），否则 401 黑图。
- **增量刷新**：候选化后 `renderStoryboardFrameCard`(render_hooks.js:188) 的单容器更新改为**按候选 id 定位**增量刷（配合方案一§6B 的 key diff 保相机）。

---

## 7. staleness 下沉到镜头级

- `computeFirstFrameSourceHashForShotIndices`(frame-workflow-state.ts:230) → 新增**单镜头 hash** `{shotUid, shot[idx], styleBible, world}`，存进候选 `sourceHash`。
- stale 判定：某 shotUid 的"选中候选 sourceHash ≠ 当前镜头 hash" → 该镜头首帧脏（精准，只标变更镜头，不再整段连坐）。
- flag key 可保 `storyboard_${g}`（group 级，下游兼容）但内部按 shotUid 聚合；或细分 `storyboard_${g}_${shotUid}`（择一，倾向前者控爆炸面 + 内部精算）。
- seed 点 `project-dependency-state.ts:336` 保持 group 级触发，精度由 per-shot sourceHash 兜。

---

## 8. flag 与共存（默认关、旧页零影响、可回退）

- 后端 flag `ORIGIN_PER_SHOT_FIRST_FRAME`（默认 **off**）：
  - off：storyboard_images 仍一段一张段首帧（**线上零行为变化**）；垫片回落原三源链；board 投影走老 shim（段首封面）。
  - on：executor 逐镜头生成 `shotFrames`；垫片派生段首兼容字段；board 显示全量候选行。
- 前端 board 候选行受方案一的 `originBoard` flag + 本 flag 双门；旧 storyboard.js 群组单图渲染**不动**（方案五退役）。
- 可秒回退（关 flag 即回老行为）。

---

## 9. 模块 / 版本 / 测试

- 后端：`frame-image-plan.ts`、`batch-executors.ts`(storyboard_images)、`visual-reference-state.ts`(垫片+候选 helper)、`frame-workflow-state.ts`(sourceHash/字段枚举)、`app/api/frames/candidate/route.ts`(新窄端点)、`frames/upload`、`group-slot-write-guard.ts`(复用)。
- 前端：`board_state.js`/`board_candidates.js`/`board.js`(方案一模块)；新模块/改动按 import map 单源 bump，`npm run verify:frontend`。
- 测试（新增）：
  - `test:shot-frame-candidates`：追加不覆盖；selectedCandidateId 默认最新；select/reorder/delete 端点语义；删选中回退相邻。
  - `test:per-shot-frame-gen`：flag on 时一段生 N 张（每 shotUid 一张）；group-slot-write-guard 防 shotUid 漂移；flag off 时一段 1 张（零变化）。
  - `test:first-frame-compat-shim`：`resolveStoryboardFirstFrameUrl` 在 新模型/老数据/flag 关 三态都返回正确段首 url；下游 video-payload 不报缺帧。
  - `test:first-frame-stale-pershot`：改段内某镜头只标该镜头脏，不连坐全段。
  - 回归须绿：`test:frame-workflow-state`、`test:frame-workflow-merge`、`test:tail-frame-delete-no-resurrect`、`test:project-dependency-state`、`test:cache-busting`。

---

## 10. 分期（方案三内部，每阶段独立可验收）

- **Phase 3-1 模型 + 端点 + 垫片**：定义 `shotFrames`/`selectedCandidateId`；新窄端点（select/reorder/delete）；`resolveStoryboardFirstFrameUrl` 升级为兼容垫片 + 段首镜像回写；board_state 读 `shotFrames`（老数据投影）。**后端生成暂不变**（仍段首一张，投影到段首镜头行）。flag 脚手架就位、默认 off。
- **Phase 3-2 后端逐镜头生成**：`frame-image-plan` per-shot + executor 写 N 候选 + upload/FFE 追加；每片段"一键全生成"；per-shot sourceHash/stale。flag on 才生效。
- **Phase 3-3 候选行 UI**：board 候选行渲染 + 拖序 + 选中 + 各操作 + 增量刷。
- 顺序硬约束：3-1（含垫片，保证下游不破）先于 3-2（改生成）先于 3-3（UI）。

---

## 11. 验收清单

- [ ] flag off：线上首帧行为逐字节不变（一段一张、下游正常生视频）。
- [ ] flag on：一段生成段内每镜头各一候选；段首选中候选经垫片正确喂下游，video-payload 不报缺帧。
- [ ] 重生/上传/编辑**追加候选不覆盖**；`selectedCandidateId` 为权威，刷新/增删后选中不丢、不串位。
- [ ] 拖序改顺序、拖末位设选中；候选删除回退相邻。
- [ ] 每片段一键全生成、完成自动消失；单镜头可补单候选。
- [ ] 改段内某镜头只标该镜头首帧脏，不连坐全段。
- [ ] 候选 img 全部 hydrate，无 401 黑图；board 增量刷不丢相机（接方案一§6B）。
- [ ] 历史双轨已收敛到 candidates，无三轨并行残留。
- [ ] `npm run verify:frontend` + 新测试绿；无单例违例。

---

## 12. 风险与规避

| 风险 | 规避 |
|---|---|
| 改读取咽喉波及 10+ 下游 | **不改下游**，只把 `resolveStoryboardFirstFrameUrl` 做成兼容垫片 + 段首镜像回写（§3），下游零改动；`test:first-frame-compat-shim` 三态锁。 |
| group 重排致候选写错 shotUid | 复用 `group-slot-write-guard` 的 shotUid 绑定校验；依赖方案二 shotUid 先落库（硬前置）。 |
| 历史双轨/三轨并行越改越乱 | 新模型 candidates 唯一来源，显式收敛、不再维护 firstFrame.history/imageHistory（§1）。 |
| 逐镜头生成放大算力/积分 | 每片段一键生成可控；单镜头补单张；flag 渐进；失败不计费沿用现状。 |
| 选中靠 DOM 位置串位 | selectedCandidateId 为唯一权威，绝不读几何（§2），契约测试锁。 |
| flag on/off 行为分裂 | 垫片三态测试 + 默认 off + 可秒回退；旧 storyboard.js 不动。 |
| 视频侧误用段首单帧当全部关键帧 | 明确边界：方案三视频仍段首单帧，多关键帧消费=方案四（§3 边界）。 |

---

## 13. 给我审 / 待你点头

- **A.（已确认 2026-06-25）** 段内**每个镜头**各生一张首帧候选——锁定后端逐镜头生成口径，也是方案四"智能多帧"关键帧的来源。✅
- **B.（已拍板）** 这里的"双轨"是**首帧图片历史**（`firstFrame.history` vs `imageHistory`，低层存储），与视频生成模式无关。新模型 `candidates[]` 即首帧变体唯一来源（本身即历史），旧两轨新模型不用、只读时投影——非删能力，是 candidates 取代图片历史轨。如无异议即定。
- **C. 删候选（我的建议，待否决）**：放开首帧候选删除（现状 frames/delete 拒首帧）；删选中回退相邻候选、不留空。
- **附：视频生成模式决策（2026-06-25，记录在此，落点方案四/五，非本方案）**：视频卡显式模式 [全能参考(默认 auto) | 智能多帧(=关键帧+参考) | 首尾帧 | …]，首尾帧不删、为并列模式之一。已更新进 PRD v2 §7/§8/§9.1。

---

## Sources
- 代码（本地，行号见正文）：`lib/frame-image-plan.ts`、`lib/frame-workflow-state.ts`、`lib/visual-reference-state.ts`、`lib/batch-executors.ts`、`lib/video-payload-decision.ts`、`lib/video-reference-manifest.ts`、`lib/video-creation/start-runs.ts`、`lib/project-dependency-state.ts`、`lib/group-slot-write-guard.ts`、`app/api/frames/{upload,delete,set-current-from-history,reference-upload,plan}/route.ts`、`public/modules/{storyboard,frameRecommendations,board_state,render_hooks,utils}.js`、`public/workspace.html`；先例 `SEGMENT_VIDEO_HISTORY_PLAN.md`、`docs/image-local-edit-plan.md`。
- 行业资料（候选/变体 UX：追加不覆盖、显式选中、保留元数据、就近操作）：https://www.shapeof.ai/patterns/variations ，https://helpx.adobe.com/firefly/using/generate-images.html ，https://getimg.ai/image-variations
