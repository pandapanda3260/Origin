# 方案二 · 镜头计划弹窗 + 片段分组 / 发车 — 落地方案

> 《画板模式 PRD v2》落地拆分第 2 个方案。**只出方案不改代码**。
> 本方案**吸收并取代**既有 `镜头计划编辑器-修改镜头计划弹窗-落地方案.md`（D1–D6 + Phase1 安全闸全部沿用），并在其上叠加本期新增的三块：**图15 参数行、片段分组列、20-25s 发车参数化**，以及与方案一画板的接线。
> 所有"现状"均扣真实代码（路径+行号）。执行：Codex；审查/验收：本人。

---

## 0. 范围与依赖

**做**：
- 深色"镜头计划"弹窗（图10）：表格式**就地编辑即存**（增/删/改/相邻重排），从画板②"打开脚本节点"打开。
- 图15 每镜头参数行（时长/节奏/景别/运镜/角度/焦距/景深/光线/构图）chip 下拉，复用现有枚举与写入链路。
- **片段分组列**（新能力）：展示每镜头片段归属，支持手动合并/拆分/移动镜头到某片段。
- **20-25s 发车参数化**：把 `planSegments` 的目标段长改为"由视频模型能力派生的配置"，值默认仍 4/15、随 Seedance 2.5 上线翻到 20-25（见 §0.5 纠偏与 §5）。
- **Phase1 安全闸先行**（沿用旧方案）：启用稳定 `shotUid` + executor 写回身份守卫 + 结构编辑 423 互斥锁。

**不做（留后续方案）**：候选/拖序/选中写入（方案三）、生视频升级与发车值翻转的真正生效（方案四）、视频候选与进剪辑（方案五）、局部修改、道具/场景 mention 重算、任意拖拽排序（仅相邻上移/下移）、为老项目迁移。

**依赖**：方案一画板（②节点"打开脚本节点"是主入口；flag 关时也可挂 legacy 入口 `#shotsTopActions` 共用同一弹窗模块，保证独立可验证）。

---

## 0.5 关键现状纠偏（扫码结论，先看，决定本方案形态）

| # | 旧假设 | 扣码真相 | 对方案的影响 |
|---|---|---|---|
| ① | "片段分组/发车"是新功能 | **早已实现且默认开启**：`lib/segment-planning.ts` `planSegments()`（MIN=4/MAX=15，"够下限即停、尽量少拼"），槽位 `makeSingleShotStoryboardSlots`(frame-workflow-state.ts:70)，写入点 `batch-executors.ts:3723`，前端 `storyboard.js:6136 getStoryboardGroups()` **纯读**。flag `ORIGIN_MULTI_SHOT_SEGMENT` 默认 true。 | 发车 = **改 `planSegments` 贪心方向 + 抬常量（一处）+ 同步 `test-segment-planning.ts`**，不是从零造。 |
| ② | 20-25s 发车可直接上 | **物理冲突**：Seedance 2.0 单次生成 `duration` 上限 **15s**（`镜头合并与素材生成-方案.md:23`），MAX=15 正是贴着模型上限定的；20-25s 段当前模型**生不出来**。Seedance 2.5（已核验 30s）才放得下。 | **本方案只把发车参数化，不翻值**；值翻转（4/15→20-25/25）随 Seedance 2.5 在方案四生效。现在翻 = 带 BUG 进下一步，违背原则，**否决**。 |
| ③ | 弹窗加"片段分组列"= 小改 | **手动合并/分组 UI 完全不存在**（grep `mergeSelected/手动合并/拆分片段` 零命中）；`shotIndices` 全由后端 `planSegments` 算定，用户只能改单镜 `duration` 间接影响。 | 片段分组列是**新功能**：要前端分组编辑 UI + 写 `storyboards[].shotIndices` 的 PUT 路径 + `assertGroupsCoverAllShotsOnce`(frame-workflow-state.ts:90) 覆盖校验 + **手改优先**（手动分组不被 auto `planSegments` 覆盖）。 |
| ④ | shotUid 已可用 | **仍未落库**：`shot-plan-normalize.ts` 不写 `shotUid`；`asset-library.ts` 素材按位置 `legacy_shot_id='shot_<idx>'` 关联（:934）。 | 发车方向改 + 手动分组都会**重排片段↔镜头映射**，按位置关联的历史素材会**错挂**。→ **shotUid 前置（Phase1）比旧方案时更紧迫**，必须先做。 |
| ⑤ | 用 423 锁 | 现网用 **409**（乐观锁 stale_version）+ `findActiveBatchForType`(batches.ts:1870) 防 shots 批次重入；**无 423**。 | 保留 409 管版本冲突；**新增 423 管"批次运行中禁止结构编辑"**（route 层，语义正确：Locked=忙锁），二者不同语义并存。 |

---

## 1. 弹窗形态与入口（D1，沿用 + 深色 + 画板接线）

- **形态**：表格式**就地编辑 + 即时保存**（blur/change 即走现有 `_applyShotFieldValue → _markDownstreamStale → saveProject`）；**不做草稿/确认两段式**（旧方案 D1 理由：避免草稿 vs version 二次对账，是补丁温床）。行业背书（datagrid UX）：就地编辑减少上下文切换、点改即存；简单字段就地、复杂字段可"详情"。
- **深色**（对齐图10）：弹窗外壳深色专注模式（底 `--accent`#0B1320、白字），与画板②深色镜头计划卡同色系；外壳模板抄 `episodes.js:240` overlay。
- **入口**：
  - 主：画板②"镜头计划"节点的 `打开脚本节点 →`（方案一中本期 inert，方案二接通 `openShotPlanDialog()`）。
  - 兼容：flag 关时挂 legacy `#shotsTopActions`(workspace.html:893) 的"修改镜头计划"按钮，共用同一模块（保证方案二独立可验证、不依赖 board flag 开）。
- 入口按钮 disabled 态由 §5 的 `_isShotStructureLocked()` 统一管理（批次运行中禁用 + tooltip）。
- 顶部"1/3 完成后可批量生视频"、底部"→ 下一步：准备资产"沿用图10。

---

## 2. 字段与图15 参数行

### 2.1 字段→后端名（扣 `shot-plan-normalize.ts:194-230` + `shotSchema.js`）
| 列 | 后端字段 | 控件 | 取值源 |
|---|---|---|---|
| 镜号 | `order`(=idx+1) 只读 | — | 位置态 |
| 时长 | `duration`(+`durationSec` 双写) | chip 下拉 | `SHOT_DURATION_OPTIONS=[1..7]`(shots.js:30) |
| 节奏 | `pace` | chip | `SHOT_PACE_OPTIONS`(shots.js) |
| 画面描述 | `visual` | textarea | — |
| 景别 | `shotType` | chip | `SHOT_TYPES`(shotSchema.js) |
| 运镜 | `camera` | chip | `CAMERA_MOVES` |
| 角度 | `angle` | chip | `ANGLES` |
| 焦距 | `lens` | chip | `LENSES` |
| 景深 | `focus` | chip | `FOCUS_OPTIONS` |
| 光线 | `light` | chip | `LIGHT_PRESETS` |
| 构图 | `composition` | chip | `COMPOSITION_PRESETS` |
| 对白/旁白 | `dialogue` | textarea | — |
| 音效 | `audio` | textarea | — |
| 片段分组 | `storyboards[g].shotIndices`（派生） | 分组控件 | §3 |
| 最终提示词 | `storyboards[idx].videoPrompt` | **只读查看**（无则"未生成"） | 下游产物 |

枚举控件全复用 `_buildSelectOptions`(shots.js:526)/取值 `_shotFieldCurrent`(:595)/写入 `_applyShotFieldValue`(:616)；textarea（visual/dialogue/audio）是本方案唯一新增控件类型（旧方案 D1 同款）。

### 2.2 图15 参数行 UI（答"不知道怎么设计好看"，datagrid 范式背书）
- **限列 + 展开**（行业范式"默认只显有限列 + Customize 展开"）：每行默认显示高频 4 项（时长/景别/运镜/光线）为浅底圆角 chip 下拉；行尾 `▸ 更多参数` 展开第二排（节奏/角度/焦距/景深/构图），对齐图15 底部"◁◁◁ 收起"。
- **行操作收进 kebab**（范式"多操作收一个 ⋯ 菜单"）：行尾 `⋯` → 上移/下移/在下方插入/删除/（分组操作见 §3）。避免一排图标杂乱。
- **留白保易读**（范式"别挤"）：chip label+值同行、hover 显全值、足够行高；不强塞九个下拉在一行。
- 复杂字段（visual/dialogue/audio）用 textarea 就地展开编辑（范式"复杂字段用详情/展开"）。

---

## 3. 片段分组列（新能力，手改优先）

**目标**（用户原话）：弹窗加"片段分组"列，按 `sceneId`/现有合并逻辑成片段，用户可在此列调整某镜头的片段归属。

### 3.1 展示
- 每镜头行显示其所属片段（如"片段 2"），同片段镜头视觉编组（左侧色条/缩进，对齐图11 片段块）。数据读 `storyboards[g].shotIndices`（`getStoryboardGroups` storyboard.js:6136 已有读法，复用）。

### 3.2 手动调整（合并/拆分/移动）—— 新写一处"分组写入"
- 操作（放行 kebab 或分组列控件）：**与下一镜合并 / 从片段拆出 / 移动到片段 N / 新建片段**。
- 写入：改 `project.storyboards[].shotIndices` 重组（连续、覆盖全部、无重叠），**复用 `_syncSingleShotSlots*` 同范式新增 `regroupShotSegments(newGroups)`**，不自写第三份 splice/对齐；写后必须过 `assertGroupsCoverAllShotsOnce`(frame-workflow-state.ts:90) 校验，违例回滚 + toast。
- 落盘走通用 `PUT /api/projects/[id]`（If-Match/409 乐观锁，route.ts:227），无新 REST 接口。
- 受 §5 的 423 结构锁保护（批次运行中禁止分组）。

### 3.3 手改优先（auto vs manual 冲突，根因设计、非补丁）
- 现状：`planSegments` 在镜头计划生成时**自动**算分组。若用户手动改了分组，后续任何触发 `makeSingleShotStoryboardSlots` 重算的路径会**覆盖**手动结果。
- **拍板**：项目级标记 `project.segmentationMode`（`'auto'` | `'manual'`）。用户一旦手动改分组 → 置 `'manual'`；`manual` 下镜头计划/重算**不再调 `planSegments` 重组**，保留用户 `shotIndices`（仅在镜头增删时用 `_syncSingleShotSlots*` 局部维护覆盖性，不整体重算）。提供"重置为自动分组"显式入口回到 `'auto'`。
- 与"手改优先/不硬拦"一致（[[feedback_vasily_no_hard_block]] 同源哲学）。
- 注意：手动分组改变片段↔镜头映射 → 依赖 §5 的 shotUid，否则历史素材按位置错挂（纠偏④）。

---

## 4. 20-25s 发车（参数化，值随 Seedance 2.5）

### 4.1 落点（单一计算路径）
`lib/segment-planning.ts` `planSegments()`（:36）+ 常量 `SEGMENT_MIN/MAX_DURATION_SEC`(:17-18)。下游 `segment-roles.ts:47`、`test-segment-planning.ts`、`test-frame-workflow-merge.ts`。

### 4.2 改造（根因、能力派生，不硬编码）
- 把 `planSegments(shots, opts)` 的目标窗改为**由视频模型能力派生的配置**：`{ targetMinSec, targetMaxSec, hardMaxSec }` 来源 = 当前激活视频模型的单次时长上限（`video-provider-capabilities.ts` / model-routing）。
  - 当前 Seedance 2.0（15s）→ 配置仍 `min4 / max15`（即现状，零行为变化）。
  - Seedance 2.5（30s，方案四接入）→ 配置 `targetMin20 / targetMax25 / hardMax25`（留 30s 余量）。
- 贪心方向改为"**逼近目标窗才停**"（从"够 MIN 即停、尽量少拼"→"尽量多拼到 target 窗内"），并保留"末段过短并回上一段""硬上限护栏"。
- 同步改 `test-segment-planning.ts` 断言为**参数化断言**（给定窗口验证分组），不写死 4/15。

### 4.3 为什么不现在翻值（关键）
20-25s 段在 Seedance 2.0（15s 上限）下**生不出整段**，会产出无法生成的片段 = 带 BUG。故**本方案只交付"参数化 + 贪心方向可配"，默认值不变（线上零行为变化）**；**值翻转随方案四（Seedance 2.5 GA）一并打开**。这是"不接受带 BUG 进入下一步""效果优先"的直接落实。

---

## 5. Phase1 安全闸（先行，沿用旧方案 D3/D4/D2 + 因发车更紧迫而强化）

> 顺序硬约束：**Phase1 安全闸必须先于弹窗 UI 与分组列**——没有稳定身份与互斥锁，增删/重排/手动分组会把现存的"串位/复活/素材错挂"从偶发放大为高频。

- **D3 稳定 shotUid（前置，已比旧方案更紧迫）**：`normalizeGeneratedShot`(shot-plan-normalize.ts:194) 一次性分配永不重排的 `shotUid`；生成任务把 `shotUid` 写进 asset-library 的 `shot_uid` 列（替换现注入 `legacy_shot_id='shot_<idx>'` 的同址点：video-gen.ts、storyboard-material-images.ts、video/submit、frames/upload 等，旧方案 P1 已列清单）；**删除所有"按位置重排 `shot.id`"写法**（shots.js:1599、main.js:7468-7485 改为只重排 `order`）。因发车/手动分组会重排映射，这条是素材不错挂的根。
- **D4 executor 写回身份守卫**：批次任务创建时给每 group 落 `expectedShotUid`；首/尾/视频六个写回点（batch-executors.ts:1908/2154/2466/3180/3229/3494 一带）写回前校验 `freshShots[groupIdx]?.shotUid === expectedShotUid`，不匹配 `return null`；尾帧补越界拦截；`expectedShotUid` 缺失回退旧 `groupIdx>=length` 行为不误杀。
- **D2 结构编辑互斥（前端禁用 + 后端 423）**：
  - 前端 `_isShotStructureLocked()`：`project.shotPlanStatus==='generating'` ∪ `getActiveBatchesShared`(utils.js:386) 命中 `['queued','running']` × `['shots','storyboard_images','tail_frame_images','video_segments','videos','video_prompts']`。锁定时入口 disabled + 弹窗内增/删/重排/分组禁用。
  - 后端 route 层（`app/api/projects/[id]/route.ts` PUT，:240 前）：当 patch 含 `shots` 长度变化或 `shotIndices` 结构变化时，用 `findActiveBatchForType`(batches.ts:1870) 逐查上述 batchType，命中 → **423 Locked** `{reason:'shot_structure_locked', batchType}`。**落 route 层不落 DB 层**（executor 写回也走 `patchProjectForUser`，DB 层加闸会误伤写回）。前端收 423 → toast + 刷新批次态。
  - 与现有 409（stale_version 版本冲突）**并存不同语义**：409=你的版本旧了，423=正在生成不让改结构。

（D5 删除权威清理 / D6 visual 回写 characters 沿用旧方案 §3.7/§3.8，本方案不重述，纳入 Phase2。）

---

## 6. 增删改 / 重排链路收敛（复用，不新写第三份）

- 改字段：`applyShotFieldEdit(idx,field,value)` = 现有 `_applyShotFieldValue + _markDownstreamStale + saveProject`；弹窗与卡片共用。visual 改动触发 D6 characters 重算。
- 删/加/插镜头：复用 `delete-shot`(shots.js:1596)/addShot(main.js:7398) + `_syncSingleShotSlotsAfterDelete/Insert`；新镜头用 `shotUid`。删有产物的镜头弹**披露型 confirm**（不硬拦）。
- 相邻重排（上移/下移）：新增 `_swapAdjacentShotSlots(i,j)`，与 `_syncSingleShotSlots*` 同文件同范式，同步交换 `storyboards/videoTasks` 同位并重建 `shotIndices/groupIdx` + remap editData；受 423 锁。**不做任意拖拽排序**。
- 分组重组：`regroupShotSegments`（§3.2），同范式、过覆盖校验。

---

## 7. 模块与版本制度

- 新模块 `public/modules/shotPlanDialog.js`（导出 `openShotPlanDialog()`）；登记 import map（workspace.html:1844）；改 `shots.js`/`segment-planning` 相关前端只 bump 对应 import map 条目；改 `main.js`（挂入口）bump `main.js?v=`；改 `styles.css` bump。跑 `npm run verify:frontend`。
- 与方案一一致：弹窗深色样式走 `styles.css` 自定义 `.spd-*`，不靠 Tailwind 动态类。

---

## 8. 测试（接 verify:frontend / scripts/test-*.mjs）

- `test:shot-stable-id`：增删/插/交换/手动分组后存活镜头 `shotUid` 不变、`order` 正确；新镜头 uid 唯一。
- `test:shot-structure-lock`：活跃批次下前端锁 + 后端 PUT（shots 长度变 / shotIndices 变）返 423；无批次放行；409 版本冲突路径不受影响。
- `test:shot-writeback-identity-guard`：写回时 group 指向不同 shotUid → return null 不串位；缺失回退；尾帧越界拦截。
- `test:shot-regroup`：手动合并/拆分/移动后 `assertGroupsCoverAllShotsOnce` 通过；`segmentationMode='manual'` 后重算不覆盖手动分组；"重置自动"恢复。
- `test:segment-planning`（改写为**参数化断言**）：给定 `{4,15}` = 现状分组；给定 `{20,25}` = 逼近目标窗分组；末段并回、硬上限护栏生效。
- 既有回归须绿：`test:tail-frame-delete-no-resurrect`、`test:frame-workflow-state`、`test:frame-workflow-merge`、`test:project-dependency-state`、`test:cache-busting`。

---

## 9. 分期

- **Phase 2-1 安全闸（先上、独立有价值）**：D3 shotUid + D4 写回守卫 + D2 前端禁用+后端 423。即使不加新 UI 也独立修复现存串位/复活/素材错挂隐患。
- **Phase 2-2 弹窗 + 参数行 + 分组列**：D1 深色弹窗 + 图15 chip 行 + 片段分组列（手改优先）+ D5 删除清理 + D6 characters 回写 + 相邻重排 + `planSegments` 参数化（值仍 4/15）。
- **发车值翻转（4/15→20-25/25）= 不在本方案生效，随方案四 Seedance 2.5 GA 打开**（本方案只把开关/参数铺好）。
- 顺序硬约束：Phase2-1 先于 Phase2-2。

---

## 10. 验收清单

- [ ] 安全闸：grep 无"按位置重排 id"残留；shotUid 跨增删/重排/分组稳定；六写回点 + 尾帧越界守卫齐；后端 423 在 route 层、不误伤 executor 写回；前端禁用随批次态实时更新。
- [ ] 弹窗：深色、从画板②"打开脚本节点"打开（flag 关时 legacy 入口亦可）；就地编辑即存；图15 chip 行高频4+更多展开、行操作收 kebab；最终提示词只读。
- [ ] 片段分组列：显示归属正确；手动合并/拆分/移动写 `shotIndices` 过覆盖校验；`segmentationMode='manual'` 后不被 auto 覆盖；"重置自动"可回退。
- [ ] 发车参数化：默认 `{4,15}` **线上行为零变化**；切 `{20,25}` 配置（测试中）分组逼近目标窗；`test:segment-planning` 参数化断言绿。**未在生产翻值**。
- [ ] 收敛性：无第三份 splice/对齐/分组实现，全走 `_syncSingleShotSlots*` 同范式。
- [ ] `npm run verify:frontend` 绿（含新测试）；无 console 报错 / 单例违例。

---

## 11. 风险与规避

| 风险 | 规避 |
|---|---|
| **20-25s vs Seedance 15s 物理冲突** | 本方案只参数化、不翻值；翻值随方案四 2.5 GA（§4.3）。绝不在 15s 模型上产 20-25s 段。 |
| 素材按位置错挂（发车/分组重排放大） | Phase2-1 先落 shotUid + 写回守卫，再上分组/重排（顺序硬约束）。 |
| auto vs manual 分组互相覆盖 | `segmentationMode` 标记 + manual 下不重算（§3.3），显式"重置自动"。 |
| 结构编辑与批次写回并发串位 | 423 路由锁 + 前端禁用（§5）；写回身份守卫兜边角。 |
| 423/409 语义混淆 | 409=版本冲突（现有）、423=批次忙锁（新增），文档/前端文案区分。 |
| 改 `planSegments` 波及下游 | 同步改 `segment-roles`/两个 test；参数化断言防回归。 |
| 弹窗自写对齐逻辑（补丁化） | 强制复用 `_syncSingleShotSlots*`，验收 grep 第三份实现。 |

---

## 12. 给我审 / 待你点头

- **A.（已按你 06-25 答复落定）** 片段分组列可手动调归属、手改优先；如你希望 V1 分组列**只读展示**（不开手动调整，降复杂度），告诉我，我把 §3.2/§3.3 降级为只读、手动分组移到后续。
- **B. 发车参数化但不翻值**：确认接受"方案二只把 20-25s 的开关铺好、真正生效随方案四 Seedance 2.5"（这是避免带 BUG 的硬建议）。
- **C. 423 新语义**：确认引入 423（批次忙锁）与现有 409（版本冲突）并存。

---

## Sources
- 代码（本地，行号见正文）：`lib/segment-planning.ts`、`lib/frame-workflow-state.ts`、`lib/batch-executors.ts`、`lib/shot-plan-normalize.ts`、`lib/asset-library.ts`、`lib/batches.ts`、`lib/video-provider-capabilities.ts`、`app/api/projects/[id]/route.ts`、`app/api/batch/start/route.ts`、`public/modules/{shots,storyboard,shotSchema,utils,episodes}.js`、`public/workspace.html`；既有方案 `镜头计划编辑器-修改镜头计划弹窗-落地方案.md`（D1–D6/探针 P1–P3，本方案吸收）、`镜头合并与素材生成-方案.md`、`落地顺序-镜头合并.md`。
- 行业资料：就地编辑数据表格 UX（限列+展开/行操作收 kebab/复杂字段详情/留白）https://mui.com/x/react-data-grid/editing/ ，https://www.pencilandpaper.io/articles/ux-pattern-analysis-enterprise-data-tables ，https://www.simple-table.com/blog/editable-react-data-grids-in-cell-vs-form-editing ；编辑锁/渲染中锁编辑范式（Premiere 轨道锁 / Remotion 渲染锁）与稳定 id（React keys / StudioBinder）见既有方案背书。
