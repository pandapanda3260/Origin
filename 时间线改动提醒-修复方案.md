# 时间线改动提醒 — 修复方案（已实施）

针对"手动改时间线后弹出的提醒"，修三件事：文案难懂、UI 又丑又占地、点确认后任务不继续。

## 最终交互（按你定的最简版）

手动改过时间线后再点「一键成片」，弹出**轻量确认弹窗**（替换原来那个大琥珀色警告框）：

- 内容：一行文字 **"提示：旧的内容将会被覆盖"**
- 按钮：左 **取消**、右 **确定**；右上角 **✕**
- 行为：
  - **确定** → 把当前（改过的）时间线设为新基线 + **自动接着成片**，无需再点第二次。
  - **取消 / ✕ / 点弹窗外区域** → **什么都不做**，关掉弹窗、时间线保持现状。
- 样式沿用项目通用 `showConfirm` 的主题 token（`bg-surface-container-lowest` / `bg-primary` 等），自动适配页面 UI。

## 改动点

**`public/workspace.html`**
- 删除 `editAutoComposeBlocker` 大框（含"保留为新基线 / 回到上次自动成片"两个按钮）。

**`public/modules/edit.js`**
- 新增 `_confirmOverwriteTimeline()`：本地轻确认弹窗，确定=`true`，取消/✕/点外面/Esc=`false`（不做事）。
- 新增 `_acceptTimelineAndCompose()`：调 recovery `accept-current` 设新基线 → resync → 自动 `_autoComposeEditVideo()` 续跑。
- `_autoComposeEditVideo()`：命中 `MANUAL_TIMELINE_EDIT_DETECTED` 时不再弹大框，改为置 `needOverwriteConfirm` 标志；待本次动作 `_editActionEnd` 收尾后再弹确认，确定则续跑。
  - 推迟到收尾后再弹，是为避免在 busy 态里递归触发成片被忙碌守卫挡掉。
- 删除旧的 `_setAutoComposeBlocker()` / `_resolveAutoComposeTimeline()` 及两个旧按钮的事件绑定。
- 清理 `_syncEditExportButtonState()` 里随大框失效的 `blockerVisible` 判断。

## 影响面

- 只动剪辑前端 `edit.js` + 那段 HTML。
- 后端 `recovery` 路由不动：`accept-current` 仍在用；`rollback` 分支现已无前端入口（无害保留，要清可单独说）。
- 不动公共 `showConfirm`、不动额度逻辑。

## 验证

- `node --check public/modules/edit.js` 通过。
- 全仓 grep：旧标识符（`_setAutoComposeBlocker` / `_resolveAutoComposeTimeline` / `editAutoComposeBlocker` / 两个旧按钮 id）零残留。
- 新符号 `_confirmOverwriteTimeline` / `_acceptTimelineAndCompose` / `needOverwriteConfirm` 定义与调用一致。
- HTML 净删除一个完整 `<div>…</div>` 块，标签平衡不变。
- 待你在页面实跑确认：改时间线→一键成片→弹窗；点确定自动连跑到下载；点取消/✕/点外面无变化。
