# 空间一致性 · 人物/道具/场景方位 方案（评估 + 待批提案）

> 范围：解决成片里的两类空间问题——①同一画面出现重复的人/道具（数量失控）；②人物、道具、场景之间的相对方位跨镜头漂移（上一镜杯子在桌边，下一镜跳到桌中）。本文是「评估 + 提案」，**本轮不动代码**，供审阅后再决定实施。
> 关联：`lib/prompts.ts:1132 SP_SHOTS_GENERATE`、`lib/prompts.ts:1330 SP_VIDEO_PROMPT_GENERATE`、`lib/frame-image-plan.ts`、`lib/video-reference-manifest.ts`、`lib/image-gen.ts:923`（场景定版图）、`lib/character-consistency-gate.ts`、`lib/edit-edl.ts:14`（3D 空间评分）、`lib/world-template-context.ts`。
> 日期：2026-06-01
> 关联方案：`镜头计划-输出格式与内容质量-升级方案.md`（造型旋钮）、`镜头合并与素材生成-方案.md`（参考模式互斥 / 合并段恒走参考模式）。本文是同一“计划层缺字段”病根的**第三条轴：空间方位**。

---

## 0. 一句话结论

**系统现在锁的是“身份”（谁长什么样），没锁“方位”（谁/什么在画面的哪个位置、彼此什么关系）。**

人物身份有一整套机制（`character-consistency` 身份锁 + 视觉锁 + `character-consistency-gate` 拦截 + 角色/场景/道具 LOCK 段）；但**空间位置只活在 `visual` 和视频段落的自由文本里**，每个镜头、每个片段各自“重新即兴一次”。于是出现两个互相独立的故障：

| 你的问题 | 直接根因 | 病灶环节 |
|---|---|---|
| ① 同画面多个道具 / 多个人 | **多图参考的副作用**（每张参考图都可能被“实例化”一遍）+ 计划层**没有数量基数**（`characters` 只有名字没有“几个”）+ **没有反重复负向词** | 首帧生成 `frame-image-plan.ts`、视频生成 `SP_VIDEO_PROMPT_GENERATE` |
| ② 方位前后不一致 | **全系统没有持久的“场景空间状态”**：参考图锁外观不锁位置；位置只在自由文本里、每镜重掷；场景定版图有图但**没把布局抽成可执行约束** | 计划层 `SP_SHOTS_GENERATE`、首帧 `frame-image-plan.ts`、视频段 `SP_VIDEO_PROMPT_GENERATE` |

两个问题、两套解法，但落点高度重合，且**都能挂在现有机制上扩展**（LOCK 段、参考角色、一致性门、剪辑期 3D 空间评分都已存在），不是推倒重建。

---

## 1. 现状：系统现在怎么处理空间（逐环节）

1. **世界观/场景候选**（`lib/world-template-context.ts`）：`locations[]`、`props[]`、`characterCandidates[{name,role,appearance}]` 都是**扁平字符串列表**，没有几何、没有方位。代码自己注明“只提供内容语境，不是视觉风格模板”（`:267`）。→ **没有空间模型**。

2. **场景定版图**（`lib/image-gen.ts:923-930`）：生成场景素材时确实出一张“ONE single continuous image of ONE location … wide establishing view … main spatial layout, entrances/exits, floor, walls, ceiling, key furniture/equipment”。→ **有“定版图”这张图，但它的布局没有被抽成结构化文字约束**，下游只当一张外观参考图用。

3. **镜头计划**（`SP_SHOTS_GENERATE`）：`characters` 字段是“本镜头实际入画的角色名数组”（`:1264`），**只有谁、没有几个、没有在哪**；方位信息全塞进 80–130 字的 `visual` 自由文本（`:1238`），模型时多时少地即兴。`sceneId` 能把多镜头归到同一物理空间（`:1275`），但**同空间内的相对布局没有被携带**。

4. **首/尾帧素材**（`lib/frame-image-plan.ts`）：按出现顺序取**最多 6 个道具**作参考（`:479 slice(0,6)`）+ 角色参考，拼成 `PROP LOCK / CHARACTER LOCK / SCENE LOCK / COMPOSITION RULES`（`:898` 一带，`content-sanitize.ts:114` 可见这套段名）。这些 LOCK **锁的是“长什么样 / 不可改动外观”**，不锁“在画面哪个位置”。尾帧要求“相比首帧必须有可见差异：姿态/**道具物体位置**/前后景关系…”（`:772`）——**鼓励位置变化，却没有‘相对场景默认位的锚’去约束它往哪变**。

5. **参考清单**（`lib/video-reference-manifest.ts`）：`VideoReferenceRole = first_frame|target_end|scene|character|prop`（`:9`），每条带 `useFor / immutable`（锁哪些字段）。图片预算 7 张（`:4`）。`constraints.negative` **有这个槽位但没人往里灌反重复词**（`:84`）。提示块只教模型“别重复描述参考图锁定的字段，写动作/情绪/人物调度…”（`:244`）——**“人物调度”被点名了，但它是自由文本，不是结构化方位**。

6. **视频提示词**（`SP_VIDEO_PROMPT_GENERATE`）：`场景` 段、`约束` 段都是自由中文白话，**每个 group 独立生成**，场景描述与物体位置各写各的；`约束` 段已有“真实布料重力褶皱与空间物理反射”这种**合理性**提示（`:1363`），但**没有‘数量固定 / 方位沿用首帧’这类一致性硬约束**。

7. **剪辑期已有“3D 空间”评分**（`lib/edit-edl.ts:14`“三维空间——轴线/空间方位连贯” + `edit-edl-graph.ts`）：这是**事后打分**，发现漂移但**不在生成时强制**。它是本方案校验门的“反应式表亲”。

> 小结：**身份一致性是“生成时强约束 + 拦截门”，空间一致性目前是“自由文本 + 事后打分”。** 升级的本质，是把空间也提到“强约束 + 拦截门”这一档。

---

## 2. 两类问题的根因（可定位）

### 2.1 重复（多个人/多个道具）

- **多图参考会“各画一遍”**：把同一主体的多张图、或角色参考 + 含人的场景图一起喂进去，多图条件下模型会出现“多主体混淆 / 参考保真下降 / 语义漂移”（学界 Kaleido / MAGREF / Phantom 都在专门修这个）。我们首帧阶段一次塞**最多 6 个道具 + 多个角色参考**，正是高发配置。
- **计划层没有数量基数**：`characters:["老周","龙虾"]` 不带“各 1 个”；`visual` 里“后排几名员工”这种模糊多数，模型每次渲染人数都不同。
- **没有反重复负向词**：`constraints.negative` 空着。Seedance 官方范式就是显式负向“No extra people, no duplicate character, no face morphing…”，我们没用上。
- **参考没去重 / 没标“单实例”**：同一规范实体可能进多张参考图；参考块也没告诉模型“这是同一个人，只许有一个，参考图只用于锁外观、不得据此新增第二个实例”。

### 2.2 方位漂移（相对位置前后不一致）

- **根因只有一个：没有持久的场景空间状态。** 位置只存在于每镜重新生成的自由文本里；参考图“锁主体不锁画面”（references latch onto the subject, not the frame）——所以参考图帮不了方位。场景定版图这张**唯一可能的空间锚**，其布局没有被抽成下游能执行的结构化约束。
- 后果：每个首帧、每个片段都在**重掷**人/物的摆位。杯子这镜在桌边、下镜在桌中，不是模型“忘了”，是系统**从没告诉过它桌上有个固定的位置**。
- **缺“位移是有意的”这层语义**：位置本应只在剧情发生时变化（有人拿起杯子、坐下、开门）。现在“有意的变化”和“随机的漂移”无法区分，也不会被**延续**（杯子第 5 镜被挪到中间，第 6 镜应继续在中间，而不是复位）。

---

## 3. 专业标杆（research-backed）

- **影视连续性的标准做法 = 连续性记录表（continuity log）+ 轴线（180°）**：拍摄现场用 continuity log 记录“每个镜头里角色位置、动作、道具”，剪辑时逐镜核对“是否同一件衣服、是否拿着同一件东西”。轴线规则**不止管人，也管物**（“the guy and his TV / the dog and a tree”）；多主体 = 多条轴线，逐条不许跨。→ 这正是我们缺的那张“**场景调度表**”，且与我们 `edit-edl` 的“轴线/3D 空间”评分同源。
- **多图参考导致重复是公认问题**：Kaleido 用 R-RoPE 稳定多图整合、MAGREF 用区域感知掩码处理任意数量主体、Phantom 做跨模态对齐——业界把“多参考 → 主体重复/漂移”当作要专门解决的失败模式。工程侧对策：**参考去重、@ 角色标注、显式数量、反重复负向词**。
- **2026 视频模型的跨镜头一致性靠“空间记忆 + 定版镜 + 多镜叙事”**：新一代用“Flow-Guided Feature Propagation”记住“几秒前物体的摆放”，让“客厅这套景在大全景和桌上物体特写之间保持一致”；多镜故事板（Kling 3.0 等）+ 参考生成共同维持环境/布局。→ 印证“**定版图 + 上一同场景帧作为布局参考**”这条工程路径。
- 与本仓库已锁定的约束兼容：`镜头合并与素材生成-方案.md` 已定“**合并段恒走参考模式**（首帧 + 身份参考簇，无尾锚点）”。本方案的“把定版图/上一同场景帧当**布局参考图**传入”天然落在参考模式里，不与首尾帧模式冲突。

---

## 4. 核心思路（两条解法）

### 解法 A — 参考与数量卫生（治“重复”）

把“数量/单实例”变成**计划层的结构化事实 + 生成层的硬约束 + 负向词**：
1. 计划层给出**带计数的入画清单**（每个角色/道具“几个”）。
2. 首帧/视频参考**按规范实体去重**，绝不把同一实体拆成多张参考图并行喂入。
3. 每条参考标注“**identity only · 单实例**”；提示明确“参考图只锁外观，不得据此新增第二个实例”。
4. 自动灌入**反重复负向词**（no extra people / no duplicate character / no cloned object / single instance of each subject）。

### 解法 B — 持久化“场景调度层”（治“漂移”）

引入一个一等公民：**每个场景一份“调度表 / 空间锚”**（blocking / stage layout），并把它当 LOCK 一样下发、当门一样校验。

- **是什么**：每个场景在素材阶段产出一份**结构化布局**（与定版图配对）：
  - 固定地标与轴线：门在画面左、自助台沿中轴由近及远、窗在右后；主轴线方向。
  - 复现道具/角色的**默认机位相对位**：记账板在桌近端右边、老周站纵深尽头中央、账单在台中偏左。
  - **用“机位/场景相对”表述**（画面左/右、近/远、贴边/居中、相对某地标、放在/靠/旁），**不是绝对像素**——因为镜头会换角度；同时编码“支撑关系/比例”（杯**放在**桌上、人站**在**地面），这同时给“合理性”。
  - 定版图是它的“视觉一半”，结构化文字是“可执行一半”。
- **怎么传**：镜头属于场景 S 时，其首帧提示词与视频段都收到 S 的布局作为 **`STAGE LAYOUT LOCK`**（与现有 SCENE/PROP/CHARACTER LOCK 平级）；镜头只写**相对默认位的“位移增量”**（“老周此刻坐下、仍居中；账单被拿到桌中”），不重述整套布局。
- **怎么延续**：把**上一同场景帧（或定版图）作为“布局参考图”**传入（参考模式内，与合并段决策兼容），让位置**视觉上**也被钉住，而不只靠文字。
- **怎么记账有意位移**：用一个**场景级状态时间线**记录有序的位移增量（杯子→桌中后保持），把“有意变化”与“随机漂移”分开，并向后延续。可挂在已有的 `tailFrameSignals`（`actionLandingNeed / visualTransformationNeed / endingCompositionNeed`，`shot-tail-frame-signals.ts`）上派生。
- **怎么校验**：新增一个**空间校验门**（仿 `character-consistency-gate.ts`），首帧出图后跑一次视觉检查（复用 `lib/image-caption.ts` 等图像理解）：**数对不对（抓重复）+ 标记位是否符合布局（抓漂移）**，违例则拦截/自动重试。剪辑期 `edit-edl` 的 3D 空间评分作为反应式兜底。

---

## 5. 分环节做什么（落点对照）

> 自上而下，越早锚定越省事。每条给“要做什么 + 落点”。

**① 场景素材 / 定版阶段**（场景抽取提示词；定版图 `image-gen.ts:923`；存于 projects/world 模板）
- 把场景模型从 `{name, description}` 扩成含 `layout`：地标 + 轴线 + 复现道具/角色默认位（机位相对表述 + 支撑/比例关系）；定版图作为其规范图像。**这是全片空间的唯一事实源。**

**② 镜头计划**（`SP_SHOTS_GENERATE`，`prompts.ts:1132`；路由 `app/api/shots/generate`）
- 把 `characters:["…"]`（`:1264`）升级/并行为**带计数与机位相对位的入画清单** `castInFrame:[{name,count:1,mark:"纵深尽头中央"}]`；新增 `propsInFrame:[{name,count,mark}]`；新增**逐镜 `layoutDelta`**（相对场景默认位变了什么）。
- 禁止“几名/一些”这类模糊多数，除非确为群演且给数量区间。
- 这正是另两份方案点的同一处病根（“该在计划层做的决策被推迟到下游即兴”），只是这次是**方位与数量**这根轴。

**③ 首/尾帧素材**（`lib/frame-image-plan.ts`）
- 新增 `STAGE LAYOUT LOCK` 段（与 SCENE/PROP/CHARACTER LOCK 平级），按 `castInFrame/propsInFrame` 的 mark 摆位。
- **参考去重**：同一规范实体只留一张参考图；道具入画数按本镜清单（不再无脑取 `slice(0,6)`，`:479`）。
- 把**定版图 + 上一同场景帧**作为**布局参考**传入（把 `:524` 的“self first frame = 身份/连续性锚”概念扩展为“**空间布局锚**：保持固定景物与复现道具/角色的标记位，除非本拍明确移动它”）。
- 自动灌**反重复 + 计数负向词**。

**④ 参考清单**（`lib/video-reference-manifest.ts`）
- 新增角色 `layout`（或 `scene_plate`），与表示外观的 `scene` 区分：layout 用于构图/方位锚，不当“可被复制出人来的内容”。
- 每条 `promptHint` 带“单实例 · 仅锁外观”；把 `constraints.negative`（`:84`）灌满反重复词；强制“一个规范实体≤一张参考图”。

**⑤ 视频提示词**（`SP_VIDEO_PROMPT_GENERATE`，`prompts.ts:1330`）
- 在 `场景 / 约束` 段内加**结构化空间子块**：固定数量、各主体标记位、什么静止；明确“**方位沿用首帧/定版图，除非本拍剧情移动它**”。
- 这正是 `镜头合并` 方案的待定项 C（多-beat 提示词）里“**段内多次镜头切换时身份/位置要一致**”所需的那块约束。

**⑥ 生成后校验**（新门，仿 `character-consistency-gate.ts` + 复用 `image-caption.ts`；`edit-edl` 3D 空间评分兜底）
- 首帧出图后：数人/数道具对照清单（抓重复）+ 标记位对照布局（抓漂移）；违例拦截或自动重试。

---

## 6. 落地顺序（Tier A/B/C，对齐你惯用的分层）

**Tier A — 纯提示词 + 小改去重（零 schema，最高 ROI，治大半重复）**
- `SP_SHOTS_GENERATE`：要求 `visual`/清单给**确切数量**，禁模糊多数。
- `frame-image-plan` 与 `SP_VIDEO_PROMPT_GENERATE` 的约束文案：加“单实例 + 反重复 + 方位沿用首帧/定版图”；自动灌 `constraints.negative`。
- `frame-image-plan`：**按规范实体去重参考**（小代码改动，反重复立竿见影）。
> 当天可见效，不动 schema。

**Tier B — 结构化“场景调度层”（治漂移的本体，待批后接线）**
- 场景加结构化 `layout`；镜头加 `castInFrame/propsInFrame/layoutDelta`；下游 `STAGE LAYOUT LOCK` 透传；参考清单加 `layout` 角色；把上一同场景帧当布局参考传入。
> 这步才真正让位置**跨镜头一致**，而不只是**每镜各自看着合理**。

**Tier C — 空间校验门**
- 数量 + 方位的视觉校验门（拦截/重试）+ 复用 `edit-edl` 3D 空间评分做反应式兜底。

实施建议：先 Tier A 用同素材跑 2–3 条对比（重复是否消失、方位是否更稳）→ 验证 OK 再上 Tier B 结构化层 → 最后 Tier C 校验门收口。词表/枚举与另两份方案一样收敛到**单一事实源**。

---

## 来源

- Kaleido《Multi-Subject Reference Video Generation》（R-RoPE 解决多图参考主体混淆）：https://arxiv.org/pdf/2510.18573
- MAGREF《Masked Guidance for Any-Reference Video Generation》（区域感知掩码处理任意数量主体）：https://learn.thinkdiffusion.com/magref-generate-ai-videos-with-multiple-people-and-objects-from-images/
- Phantom《Subject-consistent video generation via cross-modal alignment》：https://arxiv.org/pdf/2502.11079
- Atlas Cloud《Seedance 2.0: A Guide to All-Round Reference》（@ 角色标注、身份优先、反重复负向词、Multi-Shot）：https://www.atlascloud.ai/blog/case-studies/generative-ai-model-seedance-2-0-a-guide-to-all-round-reference
- Sagnik Bhattacharya《Seedance 2.0 Reference Images for Consistent Characters》（references latch onto the subject not the frame；new reference = new character）：https://sagnikbhattacharya.com/blog/seedance-reference-images-characters
- Digen《AI Video Generation Scene Stability 2026》（Flow-Guided Feature Propagation 记住几秒前物体摆放）：https://resource.digen.ai/ai-video-generation-scene-stability-2026/
- AI Magicx《AI Multi-Shot Video: Consistent Characters and Scenes 2026》（定版镜 + 参考维持跨镜头布局）：https://www.aimagicx.com/blog/ai-multi-shot-video-character-consistency-2026
- StudioBinder《Storyboard Blocking》与 StoryboardArt《Continuity for Storyboards》（continuity log 记录角色/道具位置）：https://www.studiobinder.com/storyboard-blocking/ ；https://storyboardart.org/storyboard-tutorials/continuity-for-storyboards/
- Number Analytics《The Art of Continuity: 180-Degree Rule》（轴线既管人也管物；多主体多轴线）：https://www.numberanalytics.com/blog/art-of-continuity-180-degree-rule
