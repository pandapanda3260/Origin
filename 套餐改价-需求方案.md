# 套餐改价 · 需求方案（涉及钱，已全量扫描代码）

> 目标：调整三档价格与月度积分。基于真实代码扫描，非推断。**先不动代码，待审批**。
> 结论一句话：数值改动点很少且集中，但有 **3 个必须一起处理的隐患（货币符号、admin 额度上限、积分发放链路缺失）**，漏了就会"改了价/积分但发不出来或显示错币种"。

---

## 一、目标值与换算口径（先对齐，最关键）

先把两个"分"分开（之前是我把它们混写了，导致歧义）：

- **货币 = 分钱**：`price_cents` 存人民币分钱，1 元 = 100 分钱。现有 `9900` = 99 元；checkout 把它当 `amountCents` 喂给微信/支付宝（按分钱计）。→ 1599 元 = `159900`、7999 元 = `799900`。**这跟积分无关，纯粹是钱的单位。**
- **积分 = 你的计费模型（非对称，利润来源）**：充值 **1 元 = 50 积分**；消耗（扣费）**1 元成本 = 100 积分**。收 50、按 100 消耗 ≈ 2 倍毛利。你给的套餐积分正是按 1:50 算的：1599×50≈80000、7999×50≈400000 ✓。

| 档位 | 价格 | price_cents（货币·分钱） | monthly_credits（积分） |
|---|---|---|---|
| Free | 0 元 | `0` | `100` |
| Plus | 1599 元 | `159900` | `80000` |
| Pro | 7999 元 | `799900` | `400000` |

> 结论：`price_cents` 用 159900/799900（货币），`monthly_credits` 用 80000/400000（积分，已含你的 1:50）。消耗端的 1:100 是**计量计费逻辑**（现有，本次不动）。

---

## 二、现状扫描结论（真实代码）

1. **价格/积分的权威源**：`lib/billing-config.ts` 的 `PLANS`（free/plus/pro）。前端卡片 100% 由后端 `/api/billing/me` 下发的 `plans[]` 渲染，前端不硬编码数值（除体验卡，见下）。
2. **货币真相 = 人民币(CNY)**：`billing_orders.currency` 默认 `'CNY'`（`lib/db.ts:910`）；后台营收用 `yuan()` 格式化（`app/admin/billing/route.ts:123`）；支付通道是微信/支付宝（RMB）。**唯独前端把价格显示成 `$`** —— 这是个一直存在的展示 bug，你给"元"正好和后端一致，本次顺带修正。
3. **付款不会自动到账（重要现状，必须知道）**：点"升级套餐"→`checkout` 只在 `billing_orders` 建一条 `pending` 订单 + 返回"[占位] 支付未对接"（`payUrl: about:blank`）；**没有真实支付，也没有任何代码在"付款成功后给账户加积分/开通套餐"**。查遍 `app/api/billing/*`：无支付回调/webhook；`orders/[id]` 只读状态、从不置 `applied`；前端 `waitForOrderApplied` 会一直等不到。**当前唯一真正到账的路径是兑换码**（`redeem`→`grantCredits`）。也没有"每月发放/重置"定时任务。即：`monthly_credits` 目前只是"展示值 + checkout 占位值"，**"用户付完款自动充积分"这套流程没实现**（与之前 `period_end`/到期降级同类的未接线缺口）。
4. **额度上限会拦死大额积分**：`ADMIN_MANUAL_ADJUST_LIMITS`（`billing-config.ts:47`）被 `app/api/admin/billing/route.ts:441-464` 的 `manualAdjustGuard` 使用。

---

## 三、改动清单（精确到文件:行）

### 3.1 必改 —— 数值（后端权威源 + mock）

| 文件 | 位置 | 现值 | 改为 |
|---|---|---|---|
| `lib/billing-config.ts` | plus `price_cents`(23) / `monthly_credits`(25) | `9900` / `2000` | `159900` / `80000` |
| `lib/billing-config.ts` | pro `price_cents`(33) / `monthly_credits`(35) | `29900` / `8000` | `799900` / `400000` |
| `mocks/billing.ts` | plus(18/20)、pro(30/32) | 同上旧值 | 同上新值（保持测试/mock 一致） |

> Free 行不动。`limits`（并发 1/4/10）、`features`、`description` 不动（你没要求；要改另说）。

### 3.2 必改 —— 货币符号 $ → ¥（前端展示，3 处都在 `public/modules/billing.js`）

| 位置 | 现状 | 改为 |
|---|---|---|
| `_formatPlanPrice` 价格(443) | `'$' + ...` | `'¥' + ...` |
| `_formatPlanPrice` 周期(444) | `/yr`、`/mo` | `/年`、`/月`（建议） |
| topup 卡价格(563) | `'$' + ...` | `'¥' + ...` |
| 体验卡硬编码(583) | `$3.5` | `¥XX`（见 §4，当前 dormant 不渲染） |

> 改前端必须 bump `main.js` 里 `billing.js?v` + `workspace.html` 里 `main.js?v`（缓存规则）。

### 3.3 必同步处理 —— admin 额度上限（**money-bug，漏了 Pro 发不出来**）

`lib/billing-config.ts:47-51` `ADMIN_MANUAL_ADJUST_LIMITS`：

| 字段 | 现值 | 与新积分的冲突 | 建议 |
|---|---|---|---|
| `absoluteBlockAbove` | `100_000` | **Pro 400,000 单笔被直接 403 拦死** | 提到 ≥ `500_000` |
| `dailyAdminAbsCap` | `50_000` | **Plus 80,000 / Pro 400,000 单笔即超日上限** | 提到足够多笔（如 `2_000_000`） |
| `secondConfirmAbove` | `10_000` | Plus/Pro 都触发二次确认 | 可保留（大额确认是好事） |

> **这三个上限只在"后台手动调账"那条路生效**（`manualAdjustGuard`）。将来"付款自动发放"或"兑换码"走的是 `grantCredits`，**不受这些上限约束**，40 万不会被拦。
> 所以是否上调取决于你会不会用**后台手动**给用户补大额：会 → absoluteBlockAbove 提到 ≥ 50 万、dailyAdminAbsCap 提够；只走付款自动发放/兑换码 → 可暂不动。

---

## 四、关联决策点（不在你明确要求内，但同屏涉及钱，建议一起定）

- **A. 货币符号写法**：`¥1599 /月`（推荐）还是 `1599 元/月`？周期要不要中文化（/月、/年）？
- **B. 积分包（弹窗里"积分购买 / Credits"那一排：现在是 500 / 2000 / 10000 积分，分别 19 / 69 / 299 元）**：两件事——① 它们现在也显示成 `$`，要不要一起改 `¥`（建议改，否则同屏一会儿 ¥ 一会儿 $）；② 它们的"元→积分"现在约 26~33 积分/元（如 19 元→500 积分），**和你套餐 1:50 不一致**，要不要按 1:50 重定（如 19 元→950 积分），还是保持现状？
- **C. 体验包 trial**（`$3.5 / 510 积分`，前端硬编码、当前因后端无 `pack_trial` 而**不渲染**）：清理掉，还是改币种保留？
- **D. admin 上限新值**：用我上面的建议值，还是你指定？
- **E. 发放链路**：本次是否纳入"订阅积分自动发放/每月重置"的实现？（较大、独立，建议单列）

---

## 五、影响面 / 不在范围

- **不在范围**：`reference-site/`（`tsconfig.json:20` 已 `exclude`，是参考快照，不参与构建/服务）——里面有同名 `billing.js`/`price_cents`，**不要动**。
- **无需改**：`app/api/billing/checkout/route.ts` 自动读 `plan.price_cents`/`monthly_credits`，数值改了它自动跟随；后台 `yuan()` 营收展示本就是 CNY；DB 列 `currency` 默认 CNY。均不用动。
- **历史遗留（无需动）**：`me/route.ts:24-26` 未知档兜底里的 `price_cents_monthly/yearly` 是 dead 字段（前端读的是 `price_cents`）。
- **数据安全**：积分列是 INTEGER，40万无溢出；`toLocaleString()` 显示"400,000"正常。
- **老数据**：改 `monthly_credits` 不动存量用户余额（余额是 `user_credits` 实际值，与 plan 配置解耦）。

---

## 六、确认后我会怎么做（待批）
1. 改 `billing-config.ts` + `mocks/billing.ts` 数值；
2. 改 `billing.js` 三处 `$`→`¥`(+周期中文化)，bump 版本号；
3. 按你定的值上调 `ADMIN_MANUAL_ADJUST_LIMITS`；
4. （可选）处理 topup/trial 币种与数值；
5. `tsc --noEmit` + `node --check` + 实跑校验。

> 最需要你先回我的：**§1 换算口径（1599元=159900分）对不对**、**§3.3 积分怎么发放 / 要不要上调上限**、以及 §4 的 A/B/C。
