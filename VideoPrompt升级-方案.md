# VideoPrompt 升级方案 v2（按你的反馈定稿方向 · 仍不改代码）

> v2 变更：① 去重定为**方案 B**（瘦身正文 + 所有路径统一包装）；② **取消「片段边界转场」**（交给后续拼接流程），但**保留「片段内镜头之间」转场**；③ 微表情部分改用大白话并讲清落地；④ 依据**全部换成 Seedance 口径**（之前偏 Veo/Sora）。
> 仍然**不动一行代码**。日期：2026-06-02

---

## 0. 决策回执（你定的，我怎么落）

| 你的反馈 | 本版怎么落 |
|---|---|
| ①去重倾向 **B** | 正文砍成「纯导演叙述」，硬参数/台词/角色档全部交给「块」，并把 **Grok / Sora / 复制按钮也统一走包装器**（见 §2） |
| ②片段前后 cut 不用加 | **不新增 `cut` 字段**；片段**最后一镜不写转场尾**，边界转场交拼接流程 |
| ③微表情看不懂 | §4 用大白话重写 + 明确「就是写进 VideoPrompt 正文的画面叙述里」+ 两种落地做法 |
| ④片段内镜头转场要有 | 保留；用 Seedance 原生的「时间轴/运镜叙述」表达，平滑有动机、忌快切（见 §5） |
| ⑤主要参考 Seedance | §1 整段换成 Seedance 官方/权威依据 |

---

## 1. Seedance 官方依据（替换之前的 Veo/Sora）

> 来源见文末。Seedance = 字节 / 即梦 / 火山引擎，是本程序的主力 provider（`isVolcano` 分支）。

1. **核心公式（6 步 / 8 要素）**：`主体 → 动作 → 环境 → 镜头 → 风格 → 约束`；展开版再加 `光线 / 画质`。官方建议**单镜 60–100 字**、别堆术语。
2. **多镜头 = 时间轴提示（timeline prompting）**：开头先声明「几个镜头 / 总时长 / 画幅」，再按时间段给运镜（如「0–3s 超广角缓降 → 3–7s 由远及近略仰 → 7–11s 中景跟随」）。**你的程序已经在这么做**（计划时间轴 + cameraList + 镜头 NN）。
3. **运镜三铁律**：① **一镜只给一个主运镜**；② 用**节奏词**（缓/平稳/轻柔）而非堆参数；③ **机位运动和主体动作要分开写**（“主体转身；镜头保持固定”），混在一起最容易抖。**忌 “fast/快”**——快运镜+快切+复杂场景几乎必抖、必出伪影。
4. **微表情 = 写「变化过程」不是「静态状态」**（Seedance 被训练理解面部/肢体的**时序演变**）。反例“她很伤心”；正例“先抿嘴 → 眼眶渐红 → 别过脸”。
5. **人物稳定**：① 全程**主体名词保持一致**防跨镜变脸；② 特写**必须加防变形约束词**（五官清晰/面部稳定/不扭曲/比例自然/动作不僵硬）。
6. **画质是显式要素**：超清、电影质感、真实皮肤纹理、自然景深等，Seedance 公式里单列，不该只埋在“基调”里。

> 一句话：Seedance 喜欢**短、结构化、过程化、机位与表演分写**——这和你选的 B（瘦身）天然契合。

---

## 2. 需求一（方案 B）：正文瘦身 + 所有路径统一包装

### 2.1 目标态

```
                        ┌──────────────────────────────────────┐
所有 provider & 复制 ───▶│ 统一 composer（注入“块”）             │──▶ 提交/复制
                        │  镜头参数块 + 台词块 + 角色档 + 衔接 + …│
                        └──────────────────┬───────────────────┘
                                           │ + 瘦身后的正文
VideoPrompt 正文（瘦身）── 只写：运镜叙述 / 场景(短) / 各镜画面叙述(动作+微表情+神态+光感) / 基调·画质 / 约束 / 音障
```

### 2.2 正文：留什么、移走什么

| 正文原段 | B 之后 | 去哪了 |
|---|---|---|
| `⟦景别·角度·焦距·景深·运镜⟧` 标签 | **删** | → 镜头参数块（已存在，`buildShotPlanBlock`） |
| 镜头里逐字台词清单复述 | **删**（叙述里自然提到“某某开口”即可，不再抄全文） | → 台词块（已存在，含说话人/口型硬规则） |
| 角色 段（外观/服装罗列） | **删** | → 角色一致性主档块 + 资产参考图 |
| 运镜系统 | **留**（块里没有，正文独有的导演价值） | — |
| 场景 | **留但收短**（提交时块里没有场景，只有首帧图） | — |
| 镜头 NN 画面叙述 | **留并强化**（动作+微表情过程+神态+光感，去掉技术参数） | — |
| 基调 + **画质** | **留**（块里没有；画质单列，见 §3） | — |
| 约束 | **留**（补特写防变形词，见 §4） | — |
| 音障 | **留** | — |

### 2.3 需要改的三处（代码先不动，先确认范围）

1. **正文生成 SP 瘦身**：`SP_VIDEO_PROMPT_GENERATE` 去掉 `⟦⟧` 标签和“逐字台词写进正文”的要求，改成“只写导演叙述 + 画面叙述”。
2. **统一 composer**：现在只有 Seedance 分支调 `buildSeedancePromptParts` 加块；B 要让 **Grok / Sora 分支也走同一套块拼接**（`lib/video-gen.ts` 的 Grok/Sora 分支现在是裸提交）。顺带把缺的「场景块」补进 composer（可选）。
3. **复制按钮**：保持**只复制正文**（你定的）。复制源本来就是正文，瘦身后这个按钮无需改逻辑，拷出来就是干净的导演叙述，适合手动粘到可灵/即梦；结构化参数/台词只在程序提交 API 时自动加，不进复制内容。

### 2.4 风险与缓解

- 风险：Grok/Sora 之前没吃过这些块，提交格式变。**缓解**：Seedance 是主力，Grok/Sora 是次要路径；而且它们之前**根本没有角色一致性/台词规则**，统一后其实是修了一个隐性缺陷。
- 复制内容：只含瘦身后的正文（干净叙述）、不含块；手动粘到可灵/即梦通常足够，需要精确参数时以程序 API 提交为准。

---

## 3. 需求二：少量专业元素（Seedance 口径）

**结论先行**：按 Seedance，你其实**几乎不需要新增“字段”**——它对 mm 焦段不像 Veo 那么敏感，更吃“结构化 + 过程化 + 节奏词”。所以建议**只新增 1 个显式元素 `画质`**，其余是**写法增强**（不增加你要填的字段）。

| 项 | 处理 | 取值/写法 | Seedance 依据 |
|---|---|---|---|
| **画质 imageQuality** | **新增（1 个）** | 超清 / 电影质感 / 真实皮肤纹理 / 自然景深 / 胶片颗粒 | 官方公式显式列“画质”要素 |
| 运镜节奏词 | 增强（不新增字段） | 缓推 / 平稳横移 / 轻柔跟随；**禁“快/急”** | 忌 fast，用节奏词 |
| 机位 vs 主体动作分写 | 增强 | “老周翻账板；镜头保持固定不晃” | 官方铁律：分开写防抖 |
| 焦段 mm | **弱化** | 从 `⟦⟧` 移除后不再强调 mm，改用景别+节奏词 | Seedance 对 mm 不敏感 |

**已定**：`画质` 并进「基调」一句，不单开段（基调本就是“整体观感”段，画质属同类，模型读的是词不是段标题）。

---

## 4. 需求三：微表情细节（大白话 + 怎么落地）

### 4.1 大白话：这是在解决什么

视频模型如果你不**明说**脸上要有小动作，特写里的人会“**面瘫**”——眼睛不动、不眨眼、表情像照片。所以**只要镜头是特写/近景，就得在提示词里写清楚脸上正在发生的细微变化**：眼神往哪看、眉毛、嘴角、喉结吞咽、呼吸、瞳孔轻颤。

### 4.2 Seedance 的关键点：写「过程」不写「状态」

- ❌ 状态：“他很紧张。”（模型给不出细节）
- ✅ 过程：“他先垂眼扫账单、喉结滚一下，再抬眼、嘴角压住没笑出来。”（眼神→喉结→嘴角，一条变化链）

### 4.3 你最关心的——「是加载到 VideoPrompt 中么？」

**是。** 它不是新增一个让你手填的字段，而是**最终变成 VideoPrompt 正文里那段“画面叙述”的文字**。落地两种做法：

| 做法 | 机制 | 命中率 | 复杂度 |
|---|---|---|---|
| **做法1 纯提示词规则** | 在生成正文的 SP 里加一条：“当景别∈特写/近景，画面叙述必须写出脸部微表情的变化过程” → 模型生成时自动写进正文 | 中 | 最低 |
| **做法2 规则 + 后端喂一句（推荐）** | 后端按本镜 `景别/情绪` 自动拼一句提示（如“本镜中近景+climax → 请写眼神/喉结/嘴角的变化过程”）塞进模型输入，让它聚焦 → 同样落在正文画面叙述里 | 高 | 低 |

> 两种做法的产物都在**正文画面叙述**里，不改你现有的填写流程。**已定：做法2**——程序按景别/情绪自动给每个特写镜头“贴便利贴”提醒。

### 4.4 触发规则（按景别/情绪，不是无脑全加）

| 触发 | 写什么 |
|---|---|
| 特写 / 大特写 / 近景 / 中近景 | **面部微表情变化过程**：眼神落点→眉→嘴角→喉结/呼吸 |
| 情绪强度 ≥4 或 climax | 克制爆发的**过程**：吸气→话卡喉咙→指节捏白 |
| 全景 / 远景 | **不写**面部，改写身体语言/重心/步态 |
| 有道具互动 | 手与道具的**重量感/质感**微动作 |
| 任意特写（Seedance 必备） | 「约束」段补**防变形词**：五官清晰/面部稳定/不扭曲/比例自然 |

### 4.5 例

```
❌ 现在： 切到老周中近景，他沉声开口："来，复盘。"
✅ 升级： 老周翻开账板，指节捏得发白；先垂眼扫一行账目、喉结滚动一次，
         再抬眼、嘴角压住没笑出来，沉声："来，复盘。"
         （约束段补：五官清晰、面部稳定、不扭曲）
```

---

## 5. 需求四：片段内多镜头 + 转场（Seedance 口径）

### 5.1 现状：多镜头本身已对齐 Seedance

Seedance 的多镜头官方做法就是「时间轴提示」，你的 `buildVideoPromptMessages` 已经在做（声明镜头数/时间轴 + 逐镜 `镜头 01/02…`），并且已强制**一镜一运镜一动作**。**这块基本不用大改**。

### 5.2 片段内镜头之间的转场——这样处理

1. **不新增 `cut` 字段**（你定的）。转场写进 **运镜系统 / 时间轴叙述**：交代相邻两镜的**轴线关系**（同轴推进 / 越轴反打 / 平行）、是否同机位、切点落在动作哪个瞬间。
2. **平滑、有动机优先；忌快切**（Seedance：快切+快运镜→抖+伪影）。**没必要的转场就不加**（你说的“没必要/不合适就不加”）——能用同轴推进/景别变化自然过渡的，就不要硬切。
3. **片段最后一镜不写转场尾**：片段之间的转场交给后续拼接流程，正文不替它操心。
4. **机位与主体动作分写**（Seedance 铁律），多镜头时尤其重要。

### 5.3 例（片段内两镜）

```
运镜系统
两镜同处打烊后营业区、共用 180° 轴线。镜头01 沿门框前景缓推建立纵深；
随老周抬头的动作，以同轴推进自然切到镜头02 的中近景（不快切、不越轴），机位随后固定。

镜头 01
门框虚焦后方朝营业区平稳缓推，交代冷白顶灯与不锈钢台面……（主体动作；镜头缓推）

镜头 02
老周中近景，镜头固定不晃；他翻开账板、喉结滚动一次再抬眼……（见 §4 微表情）
```
（无 `⟦⟧` 标签、无逐字台词——这些在块里）

---

## 6. 需求五：可阅读性

1. **瘦身后天然更短**（契合 Seedance 60–100 字/镜），不再正文+块各写一遍。
2. **每镜按 Seedance 顺序写**：主体+动作(含微表情过程) → 镜头(节奏词) → 光感 → 画质/风格 → 约束。
3. 段落标题零符号、顺序固定：运镜系统 → 场景 → 镜头… → 基调/画质 → 约束 → 音障。
4. 多镜头之间用运镜系统统述轴线/转场，读起来一条线。
5. 技术参数不再进正文（去 `⟦⟧`），正文只剩“发生了什么 + 怎么拍”，更白话。

---

## 7. 边界 / 不做什么

- 本轮**不改代码**，只定方向。
- **不新增 `cut` 字段**；不处理片段之间的转场（拼接流程负责）。
- 不堆 mm 焦段、不堆术语（Seedance 反而更差）。
- 镜头数量/是否拆多次生成/时长口径 → 归镜头合并方案，不在此动。
- 角色一致性主档块**不动**（B 里它是所有 provider 的统一来源，正文反而不再重复它）。

---

## 8. 决策已全部锁定 → 下一步

| 点 | 已定 |
|---|---|
| 去重 | **方案 B**：正文瘦身，所有 provider + 复制统一口径 |
| cut | 不新增；片段**边界**转场交拼接流程；片段**内**镜头转场写进运镜叙述 |
| 复制按钮 | **只复制正文**（瘦身后的干净叙述）；按钮逻辑不用改 |
| 画质 | **并进「基调」一句**，不单开段 |
| 微表情 | **做法2**：程序按景别/情绪给每个特写镜头“贴便利贴”，产物落在正文画面叙述 |
| 依据 | 以 **Seedance** 为准 |

**下一步（仍不改代码）**：写 ① `SP_VIDEO_PROMPT_GENERATE` 瘦身改写稿 ② 统一 composer 的块拼接说明 ③ 特写“便利贴”规则的具体文案，交你审后再落代码。
