# 工具箱 · 图片/视频生成 优化方案

> 范围：仅限【工具箱】(`app/api/toolbox/*`、`lib/toolbox-*.ts`、`public/modules/toolbox.js`)。本轮只出方案，不改代码。不触碰任何主线生成链路。
> 日期：2026-05-31

---

## 1. 当前代码扫描结论

工具箱是一个**与主线分镜流程并列、面向单条素材的自助生成模块**，前端是静态页 `public/modules/toolbox.js`（非 React），后端是独立 API：

- 图片生成：`app/api/toolbox/image/generate`（模式：`text_to_image` / `image_to_image`）
- 视频生成：`app/api/toolbox/video/generate`（模式：`image_to_video` / `first_last_frame_video`）
- 重绘高清：`app/api/toolbox/items/[id]/enhance`
- 历史/上传/删除：`history`、`items`、`items/[id]`
- 支撑库：`toolbox-modes.ts`（枚举/归一化/提示词拼接）、`toolbox-db.ts`（`toolbox_items` 表读写、视频状态回收）、`toolbox-billing.ts`（积分扣/退）、`toolbox-limits.ts`、`toolbox-media.ts`

关键架构事实（决定后面问题的根源）：

1. **工具箱复用主线能力**：图片走 `lib/image-gen.ts:generateImage`，视频走 `lib/video-gen.ts:generateVideo`，积分走 `lib/credits.ts`，资产入库走 `lib/asset-library.ts`。工具箱本身不重写这些链路。
2. **图片生成是"同步阻塞 + 串行循环"**：路由里 `for` 循环对 count（≤4）逐张 `await generateImage`，整批在一个 HTTP 请求内完成，无逐张进度、无异步队列。
3. **视频生成是"同步阻塞"**：工具箱**未传** `deferProviderPolling`，`generateVideo` 会在请求内一直轮询 provider（最长 15 分钟）才返回。前端另起 6s 轮询 `/history` 作为兜底，`toolbox_items` 与 `video_tasks` 通过 `syncRunningVideoToolboxItems` 对账，worker 侧有 `recoverRunningVideoTasks` 回收。
4. **计费/退款用幂等键**（`toolbox:charge:<id>` / `toolbox:refund:<id>`），失败即退款，逻辑自洽。
5. **首尾帧视频**通过 `firstLastFrameMode` 调用主线首尾帧构建链路（这是主线、非工具箱代码）。

代码整体结构清晰、计费严谨，但**生成执行模型（同步阻塞）与产品其余部分（异步耐久任务）不一致**，是后续多数问题的根因。

---

## 2. 图片生成功能 试跑结果

**试跑环境说明（重要）**：当前 Linux 沙箱无法直接拉起该应用做"新发起"实跑——`better-sqlite3`/`esbuild` 都是 macOS 原生二进制（在 Linux 报 `invalid ELF header` / 平台不匹配），真实 provider 密钥目录 `/Users/mark/Documents/key/` 未挂载（只挂了仓库本身），也没有在跑的服务可连。重新编译原生模块会污染你 Mac 上的 `node_modules`，已主动避免。
因此实跑结果来自两条可靠证据：**(A) 完整静态链路追踪**；**(B) 直接读取你真实数据库 `data/qd.sqlite` 中的历史实跑记录**（真实的成功/失败样本与报错）。第 7 节给出在你 Mac 上复现"新发起"实跑的步骤。

**真实历史数据（owner_id=1，2026-05-19 ~ 05-21）**：

| 模式 | 结果 | 证据 |
|---|---|---|
| `text_to_image` | 成功 | 27233b0d，耗时约 65s（05:39:18→05:40:23） |
| `image_to_image` | 成功 | 6a250c1b，耗时约 80s |
| `image_to_image` | **失败** | cf11d9c8，耗时**约 120s 后超时**：`图像生成失败：retry_deadline_exceeded，网络/限流重试超过总预算；最后错误：请求超时（>120s 未返回）`，已自动退款 +30 ✓ |

实跑结论：

- 文生图/图生图**功能本身可用**，结果正确入库（无"已完成但图缺失"的脏数据），失败退款链路有效。
- **单张耗时 60~120s**；图片路由是**串行**生成，`count=4` 最坏 ≈ 4~8 分钟全程压在一个请求里 → 极易触发浏览器/反代超时。已有一例 120s 超时实证。
- 生成期间前端只有一个"生成中"按钮态，**无逐张进度**；`count>1` 时要么全成功要么整体等待，体验差。

---

## 3. 视频生成功能 试跑结果

**真实历史数据**：

| 模式 | 结果 | 证据 |
|---|---|---|
| `image_to_video` | 成功 | 工具箱项 60287aa9 → 视频任务 29318170，provider `doubao-seedance-2-0`，进度 100%，时长 **5.0s** |
| `first_last_frame_video` | **失败** | 工具箱项 629cdf98 → 视频任务 f4b9263e，**约 98ms 前置失败**，`error_message = "Invalid SVG image"`，已自动退款 +150 ✓ |

实跑结论：

- **图生视频可用**，真实经 doubao-seedance 出片，`toolbox_items`↔`video_tasks` 状态对账一致，无残留 `running` 孤儿项；积分账（含已完成后被删除的 09f0d43f/af41740d）核对无误，**无资损**。
- **首尾帧视频实跑失败**：本次失败项的首/尾帧输入是**两张合法 PNG**（`2D 电影.png`、`3D 写实.png`，mime 均 `image/png`），却在提交前 ~98ms 抛出 `Invalid SVG image`。同一时段其它单帧图生视频均成功 → **失败专属于首尾帧路径**，根因在共享的首尾帧构建链路（主线代码），工具箱侧表现为该模式不可用。
- **时长不一致**：工具箱前端写死 `durationSec:4` 并存入 params，但 provider 最小时长把实际产出顶到 **5s**（completed 任务全是 5.0/6.0s）。计费是固定 150 与时长无关，故无资损，但**元数据/展示与实际不符，且 UI 无时长选项**。
- 同步阻塞下视频请求最长可挂 15 分钟，靠前端 6s 轮询 + worker 回收兜底；功能能跑通，但请求层超时风险偏高。

---

## 4. 与原需求目标的偏差

仓库内无独立需求文档；以**首页声明的范围**（图片生成=文生图/图生图、视频生成=图生视频/首尾帧视频；提示词优化/视频拆解/音频生成标注"敬请期待"）为事实基线。对照该 MVP 目标，偏差：

1. **首尾帧视频"功能在售、实跑即失败"**——与"四种模式皆可用"的目标直接冲突（见 B1）。
2. **"重绘高清"语义偏差**：对图片，它实为按原 prompt **重新生成一张新图**（`text_to_image` 还会换一张全新画面），并非"把当前这张放大成高清"；用户直觉与实现不符。
3. **执行模型偏差**：产品其余部分用异步耐久任务，工具箱图片用同步串行、视频用同步阻塞，导致长请求/超时（与"稳定可用"的目标有差）。
4. **视频时长**：目标语义是"4 秒短片"，实际固定被顶成 5s 且无时长控制，存储 4 与实际 5 不一致。
5. **图片清晰度**：首次生成固定 `medium`，仅"重绘高清"才用 `high`，且二者同价（30 积分）——质量档位对用户不透明。

---

## 5. 发现的问题和 Bug（按优先级）

**P0 — 功能性失败**

- **B1 首尾帧视频实跑失败 `Invalid SVG image`**：合法 PNG 输入仍在提交前抛错；根因在共享首尾帧构建链路（主线）。工具箱侧现状：该模式对用户=不可用。

**P1 — 可靠性/体验**

- **B2 图片同步串行阻塞**：单张 60~120s，`count=4` 串行最坏 ~8 分钟单请求，易超时；已实证一次 120s 超时失败；无逐张进度。
- **B3 视频同步阻塞**：未用 `deferProviderPolling`，请求最长挂 15 分钟；虽有轮询/worker 兜底，请求层超时风险高。

**P2 — 正确性/一致性/边界**

- **B4 时长不一致**：存 4s、实出 5s；无时长 UI。
- **B5 运镜哨兵脆弱**：`buildToolboxVideoPrompt` 只过滤中文 `'无'`，对历史/异常的英文 `none`/`static` 不过滤，会把无效运镜注入提示词（真实数据里已出现 `static`/`none`）。
- **B6 "重绘高清"语义**：对图片是重新生成新图而非放大原图，易误导。
- **B7 enhance(图片) 口径不一致**：未走 `assertCanStartAssetGeneration`、未建 asset-library batch（主图片生成两者都有）→ 可绕过资产配额，且增强图不入资产库。
- **B8 删除 running 项的资损边界**：`deleteToolboxItem` 不退款，且删除后 `syncRunningVideoToolboxItems` 再也找不到该项 → 若删除一个仍在跑的视频项、之后失败，则**永不退款**。当前数据未命中，但属潜在资损。
- **B9 图片无并发上限**：视频有 3 并发上限，图片没有；同步阻塞下多发会放大超时与 provider 压力。

---

## 6. 优化方案（结论式，均限定在工具箱层）

> 每条都给出**直接结论**与**改动落点**，不留多选。落点全部在 `app/api/toolbox/*` 与 `public/modules/toolbox.js`，不改 `image-gen.ts`/`video-gen.ts`/`credits.ts`/worker/主线路由。

**针对 B2（图片改为前端并行 + 逐张回显）— 首要**
结论：把"一个请求生成 N 张"改为**前端并行发起 N 个 `count=1` 请求**，每张回来即刷新历史与预览；后端 `image/generate` 维持单张语义即可（已支持 count=1）。同时把工具箱图片请求的客户端超时上调到与单张上限（~150s）匹配。
落点：`toolbox.js` 的 `_generateImage`（拆成并行单张、先渲染 N 个 running 占位）；不改 `generateImage`。
理由：单请求时长从"N×单张"降到"单张"，消除 8 分钟级长请求；天然获得逐张进度与部分成功。

**针对 B3（视频请求即返回）**
结论：工具箱视频路由提交时传 `deferProviderPolling: true`，把分支按 `upstream_pending` 处理（保持 `toolbox_items` 为 `running` 并立即返回——主线 submit 路由对该返回已有成熟处理范式），完成由**现有** worker 轮询 + 前端 6s 轮询驱动。
落点：仅 `app/api/toolbox/video/generate` 与 enhance 视频分支的调用入参；`generateVideo`、worker、`video_tasks` 表结构均不动。
**实施前置校验**：先确认 `startProviderPollingLoop`/`recoverRunningVideoTasks` 会回收工具箱产生的 `deferred` 任务（用一条真实任务验证状态能从 running→completed）。校验不通过则维持现状不切。
理由：消除最长 15 分钟的挂起请求，且只动工具箱入参、复用既有异步基建。

**针对 B1（首尾帧）**
结论：根因修复属主线，**本轮不改主线**。工具箱层只做两件事：(1) 提交前对首/尾帧做**前置校验与必要的栅格化兜底**（在 `toolbox-media`/路由层把 SVG 或异常图判失败并给出明确文案，PNG/JPEG 正常放行）；(2) 失败时把"Invalid SVG image"这类底层报错**翻译成用户可懂的文案**并保留原始信息到日志。同时**另开一张主线工单**跟踪首尾帧构建链路在合法 PNG 下抛 `Invalid SVG image` 的缺陷（不在本轮范围）。
落点：`toolbox-media.ts`/`video/generate` 校验与错误映射；不改首尾帧构建器。

**针对 B4（时长一致性）**
结论：工具箱以"约 5 秒"为准：把存储的 `durationSec` 与展示对齐为实际产出值（用 `video_tasks.duration_sec` 回填展示），UI 文案标注"约 5 秒"。本轮不引入时长选择器（超出 MVP）。
落点：`toolbox-db.ts:serializeToolboxItem` 展示取数 + `toolbox.js` 文案；不改时长决策逻辑。

**针对 B5（运镜哨兵）**
结论：`buildToolboxVideoPrompt` 的"空值哨兵"改为同时忽略 `''`/`无`/`none`/`static` 等无效值（统一归一化后再判断）。
落点：`toolbox-modes.ts:buildToolboxVideoPrompt`（纯函数，工具箱私有）。

**针对 B6（重绘高清语义）**
结论：图片侧把动作文案从"重绘高清"改为"**高清重绘（生成新图）**"并加 tooltip 说明"基于原提示词生成更高清晰度的新版本"，避免误导；行为保持不变。
落点：`toolbox.js` 文案；不改 enhance 逻辑。

**针对 B7（enhance 口径对齐）**
结论：enhance(图片) 补上 `assertCanStartAssetGeneration` 与 asset-library batch，使其与 `image/generate` 完全同口径。
落点：`app/api/toolbox/items/[id]/enhance` 图片分支；复用主线既有函数，不改其实现。

**针对 B8（删除资损边界）**
结论：工具箱删除接口对 `status==='running'` 的项**拒绝删除**（提示"生成中，完成后可删除"），杜绝"删了在跑项→后续失败无法退款"。
落点：`app/api/toolbox/items/[id]` DELETE + `toolbox.js` 删除按钮态。

**针对 B9（图片并发上限）**
结论：图片对齐视频，给一个工具箱级在跑上限（建议 4，与单批最大张数一致），超限返回 409。
落点：`toolbox-limits.ts` 增常量 + `image/generate` 入口校验（仿视频的 `countRunning...` 思路，仅统计工具箱图片项）。

实施顺序建议：B2 → B5 → B6 → B4 → B7 → B8 → B9 →（验证通过后）B3；B1 走独立主线工单。

---

## 7. 不影响主线视频生成逻辑的边界说明

本方案严格遵守"不动主线生成链路"约束：

**绝不修改**（只读、只复用）：
- `lib/video-gen.ts`（含首尾帧构建、provider 提交、任务创建与状态流转、结果回写）、`lib/image-gen.ts`、`lib/credits.ts`、`lib/asset-library.ts`、`scripts/origin-worker.ts` 及 `lib/init-executors` 系列、主线路由 `app/api/video/*`、`video_tasks` 表结构、提示词主链路与镜头计划。
- 不改任何主线的文本/图片/视频生成、镜头计划、提示词拼接、参数传递、模型调用、任务创建、任务状态流转、结果回写。

**只在工具箱边界内动**：`app/api/toolbox/**`、`lib/toolbox-*.ts`、`public/modules/toolbox.js`。所有改动要么是工具箱**调用主线时的入参选择**（如 B3 的 `deferProviderPolling`——`generateVideo` 既有的受支持入参，不改其实现）、要么是工具箱**私有的校验/文案/计费口径对齐/前端交互**。

**两个边界声明**：
1. B3 复用的是**既有**的异步轮询/回收基建（worker + 前端轮询），不新增、不修改该基建；落地前用真实任务验证其能回收工具箱 deferred 任务，验证不过则不切换，保持现状。
2. B1 的根因（合法 PNG 触发 `Invalid SVG image`）位于共享首尾帧构建链路（主线），**本轮不修复**，仅在工具箱层做前置校验与错误文案，并以独立工单跟踪主线修复。
